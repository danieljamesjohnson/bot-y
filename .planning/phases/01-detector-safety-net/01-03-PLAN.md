---
phase: 01-detector-safety-net
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - boty/parse.py
  - boty/retailers.py
  - boty/monitor.py
  - boty/notify.py
  - boty/status.py
  - boty/config.py
  - boty/cli.py
  - boty/fixtures.py
  - boty/fetch.py
  - boty/models.py
  - pyproject.toml
autonomous: true
requirements: [REQ-03]
# Moved to wave 2 after plan-check: this plan annotates boty/fixtures.py and
# boty/cli.py, both of which plan 01-01 creates or modifies. Running them in
# parallel would mean annotating a file that does not exist yet, and two agents
# editing cli.py concurrently.
user_setup: []

must_haves:
  truths:
    - "mypy runs over boty/ and exits 0"
    - "Public functions in boty/ carry parameter and return annotations"
    - "mypy configuration is committed so the check is reproducible, not dependent on invocation flags"
  artifacts:
    - pyproject.toml
  key_links:
    - "pyproject.toml contains a [tool.mypy] section covering the boty package"
---

<objective>
Add type annotations across `boty/` and a committed mypy configuration.

Purpose: the extraction layer passes dicts and optionals between modules —
`Offer | None`, `float | None`, `list[Offer] | None` — where a None-handling
mistake produces a wrong stock verdict rather than a crash. That is exactly the
class of bug that is invisible in production. Types catch it at author time,
and with outside contributors coming in Phase 4 they catch it in review too.

Output: annotated modules and a `[tool.mypy]` section that makes the check
reproducible.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-detector-safety-net/01-CONTEXT.md
@boty/parse.py
@boty/retailers.py
@boty/monitor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: mypy configuration</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>
Add a `[tool.mypy]` section to pyproject.toml targeting `python_version = "3.10"`, with `files = ["boty"]`, `warn_unused_ignores = true`, `warn_redundant_casts = true`, and `no_implicit_optional = true`.

Deliberately do NOT enable `strict = true`. The goal is catching real
None-handling errors in an 854-line codebase, not winning a strictness contest;
an over-strict config that contributors immediately start `# type: ignore`-ing
is worse than a moderate one they respect.

Add `ignore_missing_imports = true` for the `curl_cffi.*`, `apprise.*` and
`yaml.*` modules via `[[tool.mypy.overrides]]` — these ship without complete
stubs and their absence is not a signal about our code.

Plan 01-02 Task 0 installs the dev group (pytest + mypy). If 01-02 has already
run, mypy is present and this is a no-op; if not, install it here with
`.venv/bin/pip install -e '.[dev]'`. Do not assume either order — verify
`.venv/bin/python -c "import mypy"` succeeds before relying on it.
  </action>
  <verify>.venv/bin/python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); print(d['tool']['mypy'])"</verify>
  <acceptance_criteria>
    - pyproject.toml contains "[tool.mypy]" with files = ["boty"]
    - pyproject.toml does NOT contain "strict = true" under [tool.mypy]
    - Overrides exist for curl_cffi, apprise and yaml
  </acceptance_criteria>
  <done>Running `mypy` with no arguments checks the boty package.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the extraction and retailer modules</name>
  <files>boty/parse.py, boty/retailers.py</files>
  <read_first>boty/parse.py, boty/retailers.py, boty/models.py</read_first>
  <action>
Add annotations to every function in `boty/parse.py` and `boty/retailers.py`.

Pay particular attention to the Optional-returning functions, which are where a
mistake changes a stock verdict rather than raising: `ldjson_offers` and
`nextdata_offers` return `list[Offer] | None`, `_as_float` returns
`float | None`, `_dig` returns `Any | None`, `_pick` returns `Offer | None`.

`_iter_nodes` is a generator — annotate it as `Iterator[dict[str, Any]]` and
import `Iterator` from `collections.abc`.

Fix any genuine errors mypy surfaces rather than silencing them. If a
suppression is truly necessary, use a narrow `# type: ignore[code]` with the
specific error code and a comment explaining why — a bare ignore is how type
checking quietly stops being useful.
  </action>
  <verify>.venv/bin/python -m mypy boty/parse.py boty/retailers.py 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - boty/parse.py contains "list[Offer] | None" or "Optional[list[Offer]]"
    - boty/retailers.py::_pick is annotated "-> Offer | None" (check_html and check_bestbuy_api are ALREADY annotated — do not treat those as evidence of work done)
    - mypy reports no errors for these two files
    - No bare "# type: ignore" without an error code
  </acceptance_criteria>
  <done>Both modules typed and clean.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the remaining modules</name>
  <files>boty/monitor.py, boty/notify.py, boty/status.py, boty/config.py, boty/cli.py, boty/fixtures.py, boty/fetch.py, boty/models.py</files>
  <read_first>boty/monitor.py, boty/notify.py, boty/status.py, boty/config.py, boty/cli.py</read_first>
  <action>
Annotate the remaining modules.

In `boty/monitor.py`, `run_once` ALREADY carries its tuple return annotation —
verify before editing rather than assuming. What is genuinely missing is the
`checker` parameter, which is bare: annotate it `Callable[[Watch], Result]` so a
mis-shaped checker is caught at author time rather than at 3am when a restock
lands.

In `boty/notify.py`, the `results` and `unhealthy` parameters are currently
untyped — annotate as `list[Result]` and `list[Health]`. `_client` returns an
Apprise instance or None; annotate as `Any | None` given apprise ships no stubs.

`boty/status.py::write` takes `(Path, list[Result], list[Health]) -> None`.

`boty/cli.py::main` takes `list[str] | None` and returns `int`.

Also cover `boty/fetch.py` and `boty/models.py`, which fall inside
`files = ["boty"]` but were omitted from the original task list —
`fetch.py::Page.json` is unannotated. Non-strict mypy will not error on them, so
leaving them out would pass the check while quietly not delivering "type hints
throughout".

And `boty/fixtures.py`, created by plan 01-01, which is why this plan now
depends on it.
  </action>
  <verify>.venv/bin/python -m mypy 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - boty/monitor.py types the checker parameter as Callable[[Watch], Result]
    - mypy over the whole boty package reports no errors
  </acceptance_criteria>
  <done>`mypy` exits 0 across the package.</done>
</task>

</tasks>

<verification>
- [ ] `.venv/bin/python -m mypy` exits 0
- [ ] `[tool.mypy]` committed in pyproject.toml
- [ ] No bare `# type: ignore` without an error code
- [ ] `boty check` still behaves identically after annotation
</verification>

<success_criteria>
- All tasks completed
- mypy clean across boty/
- No behavioural change — this plan annotates and fixes real type errors, it does not refactor
</success_criteria>

## Artifacts this phase produces

- `pyproject.toml` — `[tool.mypy]` config and per-module import overrides
- Annotations across all `boty/*.py` modules
