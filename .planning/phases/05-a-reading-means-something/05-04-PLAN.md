---
phase: 05-a-reading-means-something
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - docs/retailer-evidence.md
autonomous: false
requirements: [REQ-14, REQ-15, REQ-16]

must_haves:
  truths:
    - "The real Walmart store number was supplied by Dan and by nobody else — Claude did not derive it, guess it, or read it, and its value appears in no file this plan writes and in no commit message"
    - "The deployed daemon's pre-restart state was captured VERBATIM before the restart destroyed it: the withdrawn 'the detector is probably broken' sentence live on the wire, both paced retailers' refusal counts, and the two Walmart verdicts about an arbitrary store"
    - "`boty.service` runs the new code — a new MainPID, an ActiveEnterTimestamp later than 2026-08-04 17:48:52 CDT, and a published cycle carrying 05-01's `store` and `store_pinned` keys — or, if Task 1's signal was `defer`, Task 2 was not entered at all and the restart is recorded NOT OBTAINED with the date and the reason, the daemon left on 2026-08-04 code and every live row of the record saying so"
    - "Whichever state the deployed Walmart watches were ACTUALLY in was recorded from a probe that prints derived booleans and never prints the store number — either they are no longer UNKNOWN-for-want-of-a-pin, or Walmart refused this pass and the live confirmation for criteria 1 and 2 is marked NOT OBTAINED with the date and the reason. Per the outline's evidence constraint, no acceptance in this plan depends on a live Walmart read succeeding"
    - "`make verify` was run ONCE, live, at close, and its verdict is recorded verbatim — including a FAIL, and including any failure class this phase itself caused"
    - "`make verify-offline` exits 0 and the mutation count is recorded beside the pre-phase baseline of 8/8, so the rise is shown rather than claimed"
    - "Six criterion verdicts stand in the ROADMAP, one per criterion, each carrying a measurement or a reason; any criterion that is unmet is recorded unmet with the date and the reason, and NO criterion text was reworded, shortened, merged or amended"
    - "The ROADMAP's `1, 0, 2, 3, 4, 5` numbering is renumbered `1`-`6` with every criterion body byte-identical — proved by an extraction-and-diff that was confirmed to yield exactly six lines from each side — in the same commit as the outcome table, whose message says it is a renumbering only"
    - "The three findings 05-01, 05-02 and 05-03 flagged for the close reach the closing record: the duplicated `_identity_leaks` drift; 05-02's FOUR argued deviations from the outline's file table — `STORE_SCOPED` into `models.py`, `tests/test_alert_text.py` in place of the outline's expected `tests/test_notify.py`, `tests/test_pacing.py` and `tests/test_monitor.py` — plus the prose-pinned health fix that caused two of them; and the fact that persisting `refusals` alone would have made REQ-16 worse"
    - "`scripts/identity_check.py --all` passes over the tracked tree AFTER the closing record is written and BEFORE it is committed"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "the six-verdict outcome table in Phase 4's three-column shape, and the renumbered criteria list"
      contains: "Outcome, recorded"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-14, REQ-15 and REQ-16 flipped with evidence-carrying traceability cells"
      contains: "REQ-14"
    - path: ".planning/STATE.md"
      provides: "the restart warning that has stood since 2026-08-04 closed, and the phase's real position"
      contains: "pacer-state.json"
    - path: "docs/retailer-evidence.md"
      provides: "## Phase 5 closing record — beside the Phase 3.1 record, not over it"
      contains: "Phase 5 closing record"
  key_links:
    - from: ".planning/ROADMAP.md outcome table"
      to: "docs/retailer-evidence.md § Phase 5 closing record"
      via: "a citation, the way Phase 3.1's and Phase 4's tables cite their working"
      pattern: "retailer-evidence\\.md"
    - from: ".planning/REQUIREMENTS.md traceability rows"
      to: "the measurements in the closing record"
      via: "REQ-14/15/16 cells naming what was measured rather than asserting completion"
      pattern: "REQ-1[456] \\| Phase 5"
---

<objective>
Close the phase: put the real store pin on the daemon, run the daemon on the code that was
written, measure what is true, and write down all six verdicts — including any that is not
met.

**Purpose.** This milestone exists because the system stated things it had not established.
A closing record is the last place that defect can hide, and it is the easiest place to
commit it: a table where every row reads MET is exactly what a phase that quietly reworded
one criterion would also produce. Phase 3.1 was offered a rewrite of its criterion 1 that
would have made it meetable and Dan declined; Phase 4 recorded two criteria UNMET rather
than reword them. **This plan does not get to do what those refused.**

**Output.** A store pin supplied by Dan and read by nobody; a service running the code this
phase wrote, for the first time since 2026-08-04; one live `make verify` verdict recorded
verbatim; a mutation count shown rising; six criterion verdicts in the ROADMAP; three
requirement rows carrying measurements; a `## Phase 5 closing record` in
`docs/retailer-evidence.md`; and a ROADMAP numbering defect fixed **in the open**.

**NOT in this plan, and the executor must not reach for it.**

- *No code changes.* `boty/`, `tests/`, `scripts/`, `config/`, `served/` and `deploy/` are
  finished. If a criterion turns out unmet, it is **recorded unmet** — it is not closed by
  writing code in the closing plan, which would be a phase measuring work it did in the act
  of measuring.
- *No rewording of any success criterion.* Not to make one meetable, not to "clarify" one,
  not to merge two. The only permitted edit to the criteria list is the leading numeral, and
  Task 3 proves mechanically that nothing else moved.
- *No fixing of the live `make verify` failure classes.* No Chrome binary on this host, and
  Walmart/Amazon challenge pages, are pre-existing, recorded in STATE.md, not caused by this
  phase and explicitly out of scope (`05-CONTEXT.md` § Deferred). They are **recorded** at
  close.
- *Nothing from Phase 6.* Delivered-total ceiling, matrix/Rung binding, workflow gates,
  CHANGELOG contents gate, `pyproject.toml` version — none of it appears here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-a-reading-means-something/05-CONTEXT.md
@.planning/phases/05-a-reading-means-something/05-PLAN-OUTLINE.md
@.planning/phases/05-a-reading-means-something/05-01-SUMMARY.md
@.planning/phases/05-a-reading-means-something/05-02-SUMMARY.md
@.planning/phases/05-a-reading-means-something/05-03-SUMMARY.md

**Read all three SUMMARYs before writing a single verdict.** They carry the transcripts this
record cites — the identity rule's red-watch, M9/M10 rising from 8/8, the
`tests/test_alert_text.py` failure listing all four withdrawn fragments, the pushed-once
pair and its permanent negative control, and the three replacement alert strings quoted in
full. **Cite them; do not paraphrase them.** A closing record that summarises a transcript
it could have quoted is a claim about a measurement instead of the measurement.

## Measured on the deployed daemon 2026-08-10, BEFORE this plan runs

These were taken from `served/boty/status.json` at `updated: 1786371776`
(**2026-08-10T09:22:56**) while planning. They are recorded here because **the restart in
Task 2 destroys every one of them**, and a record reconstructed afterwards from memory is
the unmeasured claim this milestone exists to close. Task 2 re-takes them live and prefers
its own reading; these are the floor, and they are already dated and attributed.

- `boty.service`: `MainPID=3059142`, `ActiveEnterTimestamp=Tue 2026-08-04 17:48:52 CDT`.
  **Six days on pre-Phase-4 code**, exactly as STATE.md records.
- `healthy: false`. `duration_seconds: 46.52584583708085`.
- **6 watch results published against 13 configured watches**, because two retailers were
  paced out — `config/products.yaml`'s own comment records GameStop at 5 watches and Amazon
  at 2, and 6 + 5 + 2 = 13. Nothing was dropped; pacing is the third state `status.write`'s
  docstring exists to publish.
- The three live `reason` strings, verbatim:

  - `target` — `'control product is not reading IN_STOCK — the detector is probably broken, so real restocks would be missed silently'`
  - `amazon` — `'backing off after 10 refusal(s) — next attempt in ~34 min'`
  - `gamestop` — `'backing off after 4 refusal(s) — next attempt in ~192 min'`

  **The first of those is REQ-15's counterexample, live, on the wire, on the day this phase
  closes.** It is the single best before-picture this record can have and it is worth more
  than any test transcript, because it is the system saying it to a person.

- The two Walmart watches, both producing **verdicts about an arbitrary store**:
  `Pokémon GO Plus +` → `out_of_stock`, price `null`, detail
  `'1 offer(s) via __NEXT_DATA__, none first-party'`; `CONTROL — Great Value whole milk` →
  `in_stock` at `2.42`. Note that Walmart answered fine at 09:22 — **the challenge-blocking
  STATE.md records is intermittent, not permanent**, which is why a live read is recorded as
  a bonus and never required as proof.
- `amazon` at **10 refusals** and `gamestop` at **4** are precisely the condition STATE.md
  has warned against restarting into since 2026-08-04. Task 2 restarts anyway, and the
  reason it is safe now is Task 2's own subject.

## The environment, measured

- `deploy/boty.service`: `EnvironmentFile=/home/dan/.config/boty/env`,
  `WorkingDirectory=/home/dan/CodeProjects/pokemongoplusplus`,
  `ExecStart=.../.venv/bin/boty watch -c config/products.yaml`,
  `StartLimitIntervalSec=300`, `StartLimitBurst=5`.
- `deploy/boty-secret` writes that file with `read -s` and a mode-600 temp-file swap, "so
  nothing lands in `ps` output or `~/.bash_history` either". It has exactly two
  subcommands — `telegram` and `bestbuy` — and **no store subcommand**. The mechanism is
  right and the tool does not cover this value yet; Task 1 records that as a finding and
  does not grow the tool, because a closing plan adding a shell subcommand would ship code
  with no gate under it.
- `make verify-offline` is the phase gate. `make verify` has read `VERIFY: FAIL (live
  controls)`, exit 2, since 2026-08-06.

## Both prior outcome tables, and which shape goes where

Measured against the shipped documents rather than remembered:

- **`ROADMAP.md` uses three columns** — `| # | Verdict | Measurement or reason |` — in both
  Phase 3.1's table (line ~215) and Phase 4's (line ~289). The criteria are listed
  immediately above in that file, so restating them would duplicate.
- **`docs/retailer-evidence.md` uses four** — `| # | Criterion | Verdict | What settles it |`
  (Phase 3.1 closing record, § *Where the phase landed against the ROADMAP's six criteria*).
  The criteria are not restated nearby there, so the column earns its place.

Follow both. This plan writes a three-column table into the ROADMAP and a four-column one
into the evidence document, which is exactly what Phase 3.1 did.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: The real store pin — the one thing Claude cannot supply</name>
  <files>/home/dan/.config/boty/env — mode 600, outside the repo, written by Dan and never read by Claude. No tracked file is modified by this task.</files>

  <action>
**Claude's part of this task is to present the card below, stop, and wait.** There is nothing
to automate ahead of it: the automation-first rule says a checkpoint verifies *after* Claude
has done everything it can, and here Claude can do nothing at all — the value does not exist
anywhere Claude may reach.

Present `<how-to-verify>` verbatim, including the hidden-read snippet and the three
things-not-to-do. Then **stop and wait for one of the three signals in `<resume-signal>`.**

Four things Claude must not do while waiting, each of which would defeat the point of the
checkpoint:

1. **Do not derive, infer or guess the store number.** Not from a postal code, not from a
   fixture, not from a live Walmart read (challenge-blocked at HTTP 200 anyway), and above
   all not from the pre-redaction capture in commit `95f84a6` — which *does* carry a real
   three-digit store number fourteen times over, and is precisely the value `3bd1663` rewrote
   170 commits of history to remove. The standing rule is *bot-y never guesses where the user
   lives*, and this is the one moment it binds.
2. **Do not read `$HOME/.config/boty/env`.** The only permitted command against that path is
   the `grep -c` in `<verify>`, which prints a count and never a value. If the environment's
   file-access policy blocks even that, record the block and fall back to Task 2's probe —
   do not work around the policy.
3. **Do not offer to set it for him**, or to "just use a placeholder for now". A placeholder
   in the `EnvironmentFile` is a pinned store that is not his, which produces a confident
   mismatch instead of an honest UNKNOWN — strictly worse than no pin at all.
4. **Do not proceed to Task 2 on silence.** `autonomous: false` and `gate="blocking-human"`
   mean this task ends with Dan's answer or it does not end. `workflow.auto_advance` does not
   apply to it.

Route the checkpoint to his phone with `notify-dan "bot-y: store pin needed to close phase 5"
"<one line>"` if he is not at the terminal — the standing autonomous-working contract — then
carry on waiting rather than spinning.

If the signal is `pinned`, continue to Task 2. If it is `restart-anyway`, continue to Task 2
and record that criteria 1 and 3 carry offline evidence with the live confirmation recorded
as *not obtained*. If it is `defer`, skip Task 2 entirely and go to Task 3 with every live
row of the record marked as such.
  </action>

  <what-built>
Everything under this phase is written, tested and green offline, and none of it is deployed:

- **05-01** — `Watch.store_id`, `Result.store`, `parse.nextdata_store`, `store` and
  `store_pinned` in `status.json`, a store tag in `boty check` and on the dashboard, and a
  widened `scripts/identity_check.py` rule that catches `store_id: <digits>` in a config
  key, watched going red before it was trusted.
- **05-02** — an unpinned or unexpected store is `Availability.UNKNOWN` **before any stock
  verdict can form**, watched going red by mutations M9 and M10; and the three sentences
  REQ-15 exists because of are withdrawn, gated by an `ast` scan that was watched failing on
  all four fragments first.
- **05-03** — the backoff and the page-once memory survive a restart, with a permanent
  negative control (the same scenario with the state file deleted pushes twice).

`config/products.yaml` ships `store_id: ${WALMART_STORE_ID}` on **both** Walmart watches —
the GO Plus + product watch and the milk control. That variable is unset on this host, so
**both Walmart watches currently read UNKNOWN for want of a pin.** That is criterion 2
working, not a regression.
  </what-built>

  <how-to-verify>
**What you are being asked for, and why it can only be you.**

bot-y needs your Walmart store number. Claude cannot derive it — Walmart is challenge-blocked
at HTTP 200, so a live read cannot supply it — and **must not guess it**: the standing rule
recorded on 2026-08-10 is *bot-y never guesses where the user lives*. This checkpoint is also
what keeps the value out of every file Claude writes: a store number resolves publicly to one
street address, `config/products.yaml` is tracked and public, and commit `3bd1663` rewrote
170 commits of this repo's history over exactly this class of value.

**What it costs if you defer.** Walmart is one of only four retailers that can alert on the
GO Plus +. Until a store is pinned, both its watches read UNKNOWN and neither is alertable —
so the monitor will not page you about a Walmart restock. That is the one setup step you
accepted deliberately on 2026-08-10 ("costs every user one setup step"); it is stated here
because this is the moment it becomes real rather than hypothetical.

**Where to find your store number.** Pick your store on walmart.com; the number is in the URL
of that store's page. No example is printed here, not even an invented one, for the same
reason `config/products.yaml` carries no digits: the whole point is that numbers do not go in
these files.

**How to set it, without the value touching shell history, scrollback, or this transcript.**
This is `deploy/boty-secret`'s own pattern — hidden read, nothing passed as an argument,
mode-600 temp-file swap — applied to a value that tool does not yet have a subcommand for:

```sh
ENV_FILE="$HOME/.config/boty/env"
mkdir -p "$(dirname "$ENV_FILE")"; touch "$ENV_FILE"; chmod 600 "$ENV_FILE"
read -rsp 'Walmart store number (input hidden): ' STORE; echo
[ -n "$STORE" ] || { echo "empty, nothing written"; }
TMP="$(mktemp)"; chmod 600 "$TMP"
grep -v '^WALMART_STORE_ID=' "$ENV_FILE" > "$TMP" 2>/dev/null || true
printf 'WALMART_STORE_ID=%s\n' "$STORE" >> "$TMP"
mv "$TMP" "$ENV_FILE"; chmod 600 "$ENV_FILE"
unset STORE
grep -c '^WALMART_STORE_ID=' "$ENV_FILE"   # prints 1. Never prints the value.
```

**Three things not to do:**

1. **Do not paste the number into this conversation.** Not to confirm it, not to check it.
   Claude does not need it and will confirm the pin from derived booleans instead.
2. **Do not put it in `config/products.yaml`.** That file is tracked and public, and the
   identity guard 05-01 widened will now refuse the commit if you do — which is the intended
   behaviour, but the file is the wrong place regardless.
3. **Do not pass it as an argument** to `echo`, `export` or anything else — that puts it in
   `ps` output and in `~/.bash_history`.
  </how-to-verify>

  <verify>
    <automated>grep -c '^WALMART_STORE_ID=' "$HOME/.config/boty/env"</automated>
  </verify>

  <resume-signal>
Reply with exactly one word, and **no number**:

- `pinned` — the variable is set; proceed to the restart and the close.
- `restart-anyway` — you are not pinning now, but restart the daemon onto the new code
  regardless. Walmart's two watches will read UNKNOWN until you pin; the other five
  retailers are unaffected. Criteria 1 and 3 will be recorded with their **offline**
  evidence and their live confirmation recorded as *not obtained*.
- `defer` — no pin and no restart. The daemon stays on 2026-08-04 code; the phase closes on
  offline evidence alone and every live line in the record says so explicitly.
  </resume-signal>

  <done>
    - `grep -c '^WALMART_STORE_ID=' "$HOME/.config/boty/env"` prints `1` — a count, never a
      value. **If the environment's file-access policy blocks reading that path at all,
      record the block and do not work around it**: Task 2's probe answers the same question
      from the daemon's own output without touching the file.
    - The store number appears nowhere in the transcript, in any file, or in any command
      Claude ran.
    - **Recorded as a finding, not fixed here:** `deploy/boty-secret` has `telegram` and
      `bestbuy` subcommands and no store subcommand, although the value needs the same three
      protections its docstring names (shell history, scrollback, chat transcript) for an
      identity reason rather than a credential one. Flagged for a later plan; adding a
      subcommand in a closing plan would ship code with nothing gating it.
    - Whichever of the three signals was given is recorded verbatim in the SUMMARY, with the
      date. If it was `restart-anyway` or `defer`, that is the reason a live confirmation is
      missing from the record — and the criteria are verdicted on what was measured, never
      on what the tree could have shown.
  </done>
</task>

<task type="auto">
  <name>Task 2: Put the daemon on the code that was written, and measure what changed</name>
  <files>(no files modified — this task produces measurements the closing record cites)</files>

  <action>
**Capture the pre-restart state FIRST. This is the highest-value instruction in the plan.**

The restart destroys every measurement in `<context>` § *Measured on the deployed daemon*,
and they are not retakeable. Before touching `systemctl`, re-take them live and paste both
the command and its output into the SUMMARY:

- `systemctl show boty.service -p MainPID -p ActiveEnterTimestamp`
- every `reason` string in `served/boty/status.json`'s `retailers` block, verbatim
- both Walmart watches' `availability`, `price` and `detail`, verbatim
- the count of published watch results against the 13 configured

If a cycle has landed since 09:22 and the strings have changed, **record what is there now
and keep the 09:22 reading beside it as the earlier observation.** Do not replace one with
the other: two dated readings of a flapping system are worth more than one, and this project
does not overwrite a record with a fresher one.

**Then restart.** `sudo systemctl restart boty.service`. Passwordless sudo is available on
this host; if it prompts, that is an authentication gate — stop, surface it, and do not work
around it.

**Why this restart is safe for the first time since 2026-08-04, and what it still costs.**
STATE.md has carried *do NOT restart while a retailer is in backoff* since 2026-08-04,
because a restart reset every refusal count to zero. 05-03 closed that. But be precise about
what 05-03 closed and what it did not, because this is exactly the kind of claim this
milestone punishes:

- The old code **never wrote a state file**, so there is nothing on disk to restore from.
  **This first restart still loses the current backoff** — Amazon at 10 refusals and GameStop
  at 4 (or whatever Task 2 re-measures) go to zero, and both retailers get asked at full rate
  again. That is a real, one-time politeness cost, bounded by the `retailer_intervals` floors
  that stay in force (`amazon: 1800`, `gamestop: 900` — 30 and 15 minutes, not 5).
- What 05-03 delivers is that the **next** restart preserves it. That property is proved by
  05-03's restart tests and their permanent negative control, **not** by this restart. The
  closing record must say so in those words. Claiming this restart demonstrated criterion 6
  would be the phase's own defect committed in its own closing record.

**Confirm the new code is running:** a MainPID different from the pre-restart one, an
`ActiveEnterTimestamp` later than `2026-08-04 17:48:52 CDT`, and `systemctl is-active` →
`active`. If the unit fails or enters a restart loop, `StartLimitIntervalSec=300` /
`StartLimitBurst=5` will stop it within five restarts — capture `systemctl status` and
`journalctl -u boty.service -n 80 --no-pager`, **record the failure**, and carry it into the
closing record as what was measured. Do not retry until it looks better.

**Wait for the first post-restart cycle to publish before reading anything.** `updated` in
`served/boty/status.json` must be newer than the pre-restart value. A pass took **46.5 s**
at the last measurement, so allow roughly 90 s. Poll `updated`; do not sleep blindly and do
not read a stale file and call it the new one.

**Read the new cycle through a probe that prints derived booleans and never the store
number.** After Task 1, `served/boty/status.json` contains the real value in `store` and
`store_pinned`, and a mismatch `detail` would contain both sides. Claude has no need for the
value and must not acquire it. Use a probe of this shape and **do not** print raw watch
blocks:

```
.venv/bin/python - <<'PY'
import json, pathlib
d = json.loads(pathlib.Path("served/boty/status.json").read_text())
print("updated", d["updated"], "healthy", d["healthy"], "duration_seconds", d["duration_seconds"])
print("watch results", len(d["watches"]))
for w in d["watches"]:
    line = [w["retailer"], w["name"][:34], w["availability"]]
    if w["retailer"] == "walmart":
        line += ["store_present=%s" % (w.get("store") is not None),
                 "pin_present=%s"   % (w.get("store_pinned") is not None),
                 # THREE-VALUED ON PURPOSE. `None == None` is True, so a two-sided
                 # comparison reports a MATCH for a row where neither side is known —
                 # which is precisely a challenge-blocked, unpinned Walmart row. Measured
                 # against the live pre-05-01 `status.json` on 2026-08-10: it printed
                 # `match=True` for both Walmart watches while `store_present=False` and
                 # `pin_present=False`. That is a gate that cannot go red, which this repo
                 # rejects by standing rule, so absence reports as `n/a`, never as True.
                 "match=%s"         % ("n/a" if w.get("store") is None or w.get("store_pinned") is None
                                       else w.get("store") == w.get("store_pinned")),
                 "alertable=%s"     % w["alertable"],
                 # A refusal, told apart from every other UNKNOWN, as a BOOLEAN.
                 # `check_html`'s `except Blocked` / `except FetchError` arms build
                 # their detail as "blocked: ..." / "fetch failed: ..."
                 # (`retailers.py:299-302`) and 05-01 Task 3 pins `store=None` on
                 # both. Without this the plan cannot tell "Walmart refused" from
                 # "the pin never arrived", which are opposite findings.
                 "refusal_shaped=%s" % str(w.get("detail") or "").startswith(("blocked:", "fetch failed:"))]
    print(*line)
WITHDRAWN = ("we are asking too often", "probably broken", "probably fine", "detector problem")
for r in d["retailers"]:
    hits = [f for f in WITHDRAWN if f in r["reason"]]
    # `refused` is published on the retailers block (`status.py`) and is a bool —
    # it corroborates `refusal_shaped` from the health side.
    print(r["retailer"], "ok=%s" % r["ok"], "checked=%s" % r["checked"],
          "refused=%s" % r["refused"], "withdrawn_fragments=%s" % hits)
PY
```

Adjust the key names to whatever 05-01 actually shipped — read `05-01-SUMMARY.md` for them
rather than assuming this plan guessed right. **Do not widen the probe to print `reason`,
`detail` or `failing_controls` verbatim for a Walmart row**: those can carry the store
number after a mismatch. Non-Walmart `reason` strings are safe to quote and criterion 4's
before/after needs them, so quote those.

**What the probe has to establish, and what each answer means:**

| Observation | What it settles |
|---|---|
| `store` and `store_pinned` keys exist on every watch, **including the non-Walmart ones** | criterion 1 — the store is published — **and the deploy worked.** This is deliberately the deploy proof, because it is the one that does not need Walmart to answer: the keys are on every row |
| Walmart rows: `store_present=True`, `pin_present=True`, `match=True` | a **bonus** live confirmation for criteria 1 and 2, and evidence the pin reached the daemon through the `EnvironmentFile` as 05-01 decided. Record it if it happens; it is not what those criteria rest on |
| `match=False` | **record it as measured.** A mismatch is the guard working, not a bug — it means the store that answered is not the one pinned, which is the 2026-08-09 measurement reproduced. Do not "fix" it by changing the pin, and do not report it as MET |
| `store_present=False` **with** `refusal_shaped=True`, corroborated by `refused=True` on the `walmart` row of the `retailers` block | **Walmart was challenge-blocked this pass** — the state `05-CONTEXT.md` § *Deferred Ideas* and STATE.md have recorded since 2026-08-06, and therefore the expected one. Take the branch below. **Do not re-read** |
| `withdrawn_fragments=[]` for every retailer | criterion 4, live on the deployed system, against the 09:22 reading that contained `probably broken`. **Independent of Walmart** — it is a property of every retailer's health reason |
| the replacement `reason` strings, quoted for non-Walmart retailers | the after-picture the record cites beside the withdrawn sentence |

**When Walmart refuses — the branch this plan must not be missing.**

Walmart is challenge-blocked at HTTP 200 (`05-CONTEXT.md` § *Deferred Ideas*; STATE.md since
2026-08-06). When it refuses, `check_html`'s `except Blocked` arm returns **before**
`_verdict_from_html` is ever reached — 05-01 Task 3 pins `store=None` on that arm in as many
words, and 05-02 Task 1's behaviour list asserts the ordering — so the row is UNKNOWN **for a
refusal**: `store_present=False`, and **05-02's config-gap guard never fires.** That is not a
failure of this phase's work, and it is not evidence that the pin failed to arrive. The two
look identical on `store_present` alone, which is exactly why the probe prints
`refusal_shaped`.

**`match` reads `n/a` here, and that is deliberate.** With no store read and possibly no pin,
a plain `store == store_pinned` is `None == None` — **True** — and a row where nothing is
known would report a match. That is not hypothetical: run against this host's live
`status.json` on 2026-08-10, the two-sided form printed `match=True` for both Walmart watches
with `store_present=False` and `pin_present=False`. Only `match=True` **beside**
`store_present=True` and `pin_present=True` means anything, and only that combination is the
bonus row above.

Because that is the current state of the world, it gets a branch rather than a retry:

1. **Record the refusal as the measured state**, with the date, and with the two booleans that
   established it. A refusal `detail` is the one Walmart `detail` that provably carries no
   store — the arm builds it before any page has been read — so it may be quoted, but only
   **after** `identity_check._identity_leaks` has been run against that exact string and
   returned `[]`. Prove it, do not assert it.

   **Gate it without ever printing an ungated string.** The rule above still stands: the probe
   does not print a Walmart `detail`. So do the check and the printing in **one** process, and
   let the gate decide whether anything is emitted at all:

   ```
   .venv/bin/python - <<'PY'
   import json, pathlib, sys
   sys.path.insert(0, "scripts")
   from identity_check import _identity_leaks
   d = json.loads(pathlib.Path("served/boty/status.json").read_text())
   for w in d["watches"]:
       if w["retailer"] != "walmart":
           continue
       detail = str(w.get("detail") or "")
       # Signature is (name, body) -> list[str] — measured at
       # scripts/identity_check.py:72. The name is only used for reporting.
       leaks = _identity_leaks("served/boty/status.json", detail)
       if leaks:
           # Never emit the string. Class and status code only.
           print(w["name"][:34], "GATED — leaks=%s" % leaks, "class=%s" % detail.split(":")[0])
       else:
           print(w["name"][:34], "CLEAN, quotable:", detail)
   PY
   ```

   Confirm the import and the signature against `scripts/identity_check.py` before running it
   rather than trusting this shape. If any row prints `GATED`, record only the class
   (`blocked:` / `fetch failed:`) and the status code for that row, and nothing else.
2. **Mark the live confirmation for criteria 1 and 2 NOT OBTAINED**, with the date and the
   reason — "Walmart challenge-blocked at HTTP 200, 2026-08-10" — and rest those two criterion
   rows on 05-01's and 05-02's **offline** evidence, which is where the outline put them in the
   first place. Criteria 1 and 2 are **not** weakened by this and their text is not touched:
   05-01's fixture reads and 05-02's M9/M10 red-watch still have to carry them.
3. **Do not re-run the Walmart read to try for a better answer.** Politeness is a hard
   constraint in this project and this close spends exactly one live pass. Re-reading until one
   lands breaks both at once; quietly softening the acceptance line instead is the precise
   defect this milestone exists to close, committed inside its own closing record.

A NOT OBTAINED live confirmation, recorded with its date and its reason, is a stronger artefact
than a line someone had to soften in order to tick.

**Confirm `pacer-state.json` appears.** Resolve its path from `config/products.yaml`'s
`settings.pacer_state_path` (05-03 added it) relative to the unit's
`WorkingDirectory=/home/dan/CodeProjects/pokemongoplusplus`. It is written once per cycle, so
it should exist after the first cycle; its `retailers` map is empty until a refusal is
recorded, and 05-03 omits zero-refusal entries by design, so **an empty map is correct and
is not evidence of failure.** Record what is in it. Then check `git status --porcelain` is
clean — the `.gitignore` line 05-03 added is what keeps it out, and a runtime artifact
offered for commit here would be the first thing to prove that line missing.

**If Task 1's signal was `defer`:** skip this entire task, record that the daemon was left on
2026-08-04 code with the reason and the date, and carry that into every live row of the
closing record. Every criterion is then verdicted on offline evidence alone, and the record
says which live confirmations were not obtained rather than implying they were.
  </action>

  <verify>
    <automated>systemctl is-active boty.service && systemctl show boty.service -p MainPID -p ActiveEnterTimestamp && test -s served/boty/status.json && git status --porcelain</automated>
  </verify>

  <done>
    - **On `defer` this task is not entered at all, and none of the bullets below can be
      satisfied — which is the correct outcome, not a shortfall.** Task 1 goes straight to
      Task 3, and this task's whole acceptance is then one line: the daemon was left on
      2026-08-04 code, recorded with the date and with Task 1's verbatim signal, and every
      live row of the closing record reads NOT OBTAINED with that reason. **The bullets
      below are the acceptance for `pinned` and `restart-anyway` only** — on `defer` they
      are not softened, they are not applicable.
    - The pre-restart state was captured **before** the restart, verbatim, with commands and
      outputs pasted into the SUMMARY — including the withdrawn `probably broken` sentence
      live on the wire, or a dated note that it had already cycled away with the 09:22
      reading retained beside it.
    - `boty.service` is `active` with a new MainPID and an `ActiveEnterTimestamp` later than
      `2026-08-04 17:48:52 CDT`.
    - A **new** cycle was published (`updated` strictly greater than the pre-restart value)
      and read; a stale file was not mistaken for a fresh one.
    - The Walmart rows' **actual** state was recorded through derived booleans — either
      `store_present=True` / `match=True`, or a mismatch, or `refusal_shaped=True` with the
      live confirmation for criteria 1 and 2 marked **NOT OBTAINED** with its date and reason
      and those rows rested on 05-01's and 05-02's offline evidence. Whichever of the three it
      was is what the record says, and no live Walmart read was re-attempted to improve it.
      **The store number was never printed, never read, and appears nowhere in the
      transcript.**
    - Every retailer's `withdrawn_fragments` list is empty, measured on the deployed daemon.
    - `pacer-state.json` exists at the configured path (an empty `retailers` map is correct),
      and `git status --porcelain` is clean.
    - The honest limit is written down: **this first restart still lost the pre-restart
      backoff**, because the old code wrote no state file; criterion 6 rests on 05-03's
      tests, not on this restart.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the gate, write six verdicts, and fix a numbering defect in the open</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, docs/retailer-evidence.md</files>

  <action>
### 3a — The gates, offline first

**`make verify-offline` first.** It is the phase gate, it needs no network, and if it is red
there is no reason to spend a live pass. Record: the exit code, the verdict line verbatim,
the test count, and the mutation `caught/total`. Put the **pre-phase baseline of 531 passed
and 8/8 mutations** beside it, plus each SUMMARY's intermediate count, so the rise is shown
rather than claimed. 05-01 was planned to leave mutations at 8/8, 05-02 to reach 10/10 and
05-03 to add three; **read the SUMMARYs for what actually landed** and record the real
numbers, not the planned ones.

**Then `make verify`, once, live.** Start it only after Task 2's post-restart cycle has
finished publishing, so two full retailer passes are not in flight at once — politeness is a
hard constraint and this close spends exactly one extra pass. Record the verdict **verbatim,
including a FAIL**, with `control_check.py`'s own classification of each failing control.

**Expect a FAIL, and expect this phase to have added a class to it. Both go in the record.**

- **Not caused by this phase, pre-existing since 2026-08-06:** Best Buy and Target cannot run
  at all (no Chrome/Chromium binary on this host — though STATE.md's 2026-08-10 entry records
  that Playwright's Chromium at `~/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome`
  works with `BOTY_BROWSER_PATH`), and Amazon/Walmart challenge pages at HTTP 200.
- **Possibly caused by this phase, and if so correct — recorded if it occurs, never
  required:** `make verify` runs in a shell that has **no** `WALMART_STORE_ID`, because that
  variable lives in the systemd `EnvironmentFile` which only the daemon loads. **If Walmart
  answers**, its controls read UNKNOWN through 05-02's **config-gap guard**, naming `store_id`
  and `config/products.yaml`; record that as caused-by-this-phase-and-correct, do **not** trim
  it, and do not present it as pre-existing. **If Walmart refuses** — the state STATE.md has
  recorded since 2026-08-06 — the `except Blocked` arm returns before `_verdict_from_html`, the
  config-gap guard **never fires**, and the Walmart line is a refusal in the pre-existing
  class. Record whichever actually happened, and do not re-run to get the other.
  **Criterion 2 does not rest on this run either way:** its proof is 05-01's config half and
  05-02's verdict-and-health half, both offline and both watched going red. This is a bonus
  observation, which is all the outline's evidence constraint permits a live Walmart read to
  be.

**Do not re-run `make verify` under the service's `EnvironmentFile`,** the way Phase 3.1's
close did with `systemd-run --property=EnvironmentFile=...`. That recipe is still in
`deploy/boty.service`'s comment and it is still right for a browser-path question — but here
it would pull the real store number into a process Claude launched and whose output Claude
reads, which is the one thing Task 1's checkpoint exists to prevent. The daemon's own
`status.json` already supplies the pinned-store confirmation, at zero extra requests and zero
exposure. **Record that as a decision with its reason**, so the departure from Phase 3.1's
method is visible rather than looking like an omission.

A consequence worth stating plainly: the plainly-run `make verify` output is **safe to paste
verbatim** precisely because that shell has no pin — the config-gap detail names a key, not a
number. That is why it is the run that gets quoted.

### 3b — The six verdicts

Use **`05-CONTEXT.md`'s numbering, 1–6** (every plan in this phase does). One row per
criterion. Each row carries a **measurement or a reason** — not an impression, not a
paraphrase of a test name.

| # | Criterion (CONTEXT numbering) | Where the evidence comes from |
|---|---|---|
| 1 | Every Walmart `Result` records its store; published in `status.json` | **Rests on** `05-01-SUMMARY` (fixture reads, `nextdata_store`, producer+consumer assertions) plus Task 2's `store`/`store_pinned` keys present on every published watch. *Bonus if obtained:* `store_present=True` on the Walmart rows — and if Walmart refused, the row reads **live confirmation NOT OBTAINED**, with the date and the reason |
| 2 | Store pinning is required config with no default; unset ⇒ UNKNOWN + a health message | **Rests on** `05-01-SUMMARY` (the config half, `${WALMART_STORE_ID}`, no default) + `05-02-SUMMARY` (the verdict half, the health arm, and M9/M10 watched going red). *Bonus if obtained:* the pinless `make verify` shell driving the config-gap guard in 3a — which cannot fire at all if Walmart refuses, so its absence is recorded as **not obtained** and never as a shortfall against the criterion |
| 3 | Unpinned/unexpected store ⇒ UNKNOWN, never a verdict — **watched going red** | `05-02-SUMMARY`'s M9/M10 transcripts and the mutation count rising from 8/8. Quote the harness's own `CAUGHT` lines |
| 4 | No alert names an unestablished cause; where unknown, it says so | `05-02-SUMMARY`'s `tests/test_alert_text.py` red transcript listing all four fragments, plus the `CAUSE_UNKNOWN` partition **and** the live before/after: the 2026-08-10 09:22 `probably broken` reading against Task 2's `withdrawn_fragments=[]` |
| 5 | Handled refusal recorded not pushed; one past the cap pushed once | `05-03-SUMMARY`'s four restart tests, and the permanent negative control that deletes the state file and asserts two pushes |
| 6 | Page-once state survives a restart | `05-03-SUMMARY`'s restart tests and its three mutations — **and explicitly NOT this phase's restart**, which lost the pre-restart backoff because the old code wrote no file. Say that in the row |

**The rule that binds every row, and it is the hardest constraint in this project.** A
criterion that is not met is recorded **UNMET, with the date and the reason**, and its text
is not touched. Phase 3.1 declined a rewrite of its criterion 1 that would have made it
meetable; Phase 4 recorded criteria 3 and 5 UNMET rather than reword them, and its own record
says *"this plan does not get to do what that one refused."* Neither does this one. If a
criterion is partly met, say which part and which part is not — do not round it up. If the
evidence for a row is offline-only because Task 1 was deferred, the row says *offline
evidence only, live confirmation not obtained*, with the date.

**Write the table into `.planning/ROADMAP.md`** immediately beneath the Phase 5 criteria
list, in Phase 4's three-column shape — `| # | Verdict | Measurement or reason |` — under a
bolded lead line in the established form: `**Outcome, recorded 2026-08-10 by 05-04 — N of six
MET, M UNMET...**`. Cite `docs/retailer-evidence.md § Phase 5 closing record` for the full
working, as both prior tables cite theirs.

### 3c — The numbering defect, fixed in the open

`ROADMAP.md`'s Phase 5 criteria list is numbered **`1, 0, 2, 3, 4, 5`** — the second entry
reads `0.` (currently lines 387–392). `05-CONTEXT.md` renumbers the same six as 1–6 and every
plan in this phase follows CONTEXT. Renumber the ROADMAP list to `1`–`6`.

**This is a typo fix, not an amendment, and the difference is made mechanical rather than
asserted.** The only bytes that may change are the leading numerals. Prove it — this
extraction was run on this tree during planning and yields exactly six lines:

```sh
S="$SCRATCH"   # any scratch directory OUTSIDE the repo
crit() { awk '/^### Phase 5: A Reading Means Something/{f=1} f&&/^\*\*Why this first/{exit} f&&/^  [0-9]+\. /{sub(/^  [0-9]+\. /,""); print}' "$1"; }
git show HEAD:.planning/ROADMAP.md > "$S/roadmap-before.md"
crit "$S/roadmap-before.md" > "$S/crit-before.txt"
crit .planning/ROADMAP.md      > "$S/crit-after.txt"
wc -l < "$S/crit-before.txt"; wc -l < "$S/crit-after.txt"    # BOTH must read 6
diff "$S/crit-before.txt" "$S/crit-after.txt" && echo "RENUMBER ONLY"
```

**Confirm both counts read 6 before trusting the diff.** An extraction that silently yields
zero lines makes the diff pass over nothing — a gate that cannot go red, which this repo
rejects by standing rule. **If the diff shows any difference at all, revert the renumbering
entirely** and record the `0.` as a flagged defect for a later plan instead. Flagging it is
acceptable; changing a criterion's content is not, and the diff is what tells the two apart.

Add one line beneath the renumbered list recording that the list was renumbered on
2026-08-10, that the entry now reading `2.` previously read `0.`, and that no criterion's
text changed. A reader who remembers the `0.` must be able to see it was a typo fix rather
than an amendment — which is the whole difference between this edit and the one Phase 3.1
declined.

### 3d — `REQUIREMENTS.md`

Flip `- [ ]` → `- [x]` on REQ-14, REQ-15, REQ-16 and set their traceability rows to
Complete. **Leave every requirement's text unedited** — REQ-14's rejected-alternatives
paragraph and REQ-15's two quoted counterexamples are the record of why the work was done and
must survive it.

Write the traceability cells in this file's established evidence-carrying style (REQ-10's and
REQ-07a's cells are the models — they state what was measured, and REQ-11's states what was
*not*). Each cell names the measurement that closes it, not the plan that claimed it:

- **REQ-14** — the guard, its two mutations, the pin reaching the daemon through the
  `EnvironmentFile`, and the live confirmation from `status.json` (property, never value).
  Name the 2026-08-09 measurement it answers.
- **REQ-15** — the three withdrawn strings, their replacements quoted from
  `05-02-SUMMARY`, the `ast` gate and its red transcript, and the live before/after off the
  deployed daemon.
- **REQ-16** — the three clauses, the restart tests, the permanent negative control, and the
  honest note that the *first* restart still lost the backoff because the old code wrote no
  state file.

If any is not fully closed, it stays `[ ]` / Pending with the reason — REQ-11's cell is the
worked example of a requirement whose row records what was not achieved.

### 3e — `STATE.md`

Update the frontmatter (`status`, `stopped_at`, `last_updated`, `last_activity`, `progress`)
and the Status block: Phase 5 complete with its verdict split, Phase 6 next, `/gsd-plan-phase
6` as the next command.

**Close the two standing carried-forward items this phase actually closed, by rewriting them
rather than deleting them:**

- *"`Pacer._state` is in-memory only… do NOT restart while a retailer is in backoff"* — closed
  by 05-03. Replace with what is now true, including the limit: the state persists from here
  on, the first restart still lost the pre-restart backoff, and `pacer-state.json` lives at
  the configured path and is gitignored.
- *"`boty.service` has been running pre-Phase-4 code since 2026-08-04 17:48"* — closed by Task
  2. Replace with the new `ActiveEnterTimestamp` and what the restarted daemon published.

Add the new `make verify` live verdict, replacing the 2026-08-06 line, **with the new
this-phase-caused class named separately from the two pre-existing ones**. Add the phase's
decisions to the Decisions list in the established `- [Phase 5]: <decision> — <reason>` form,
including the three findings in 3f. Leave `QUESTIONS.md` § 0e open — it is untouched by this
phase.

### 3f — `docs/retailer-evidence.md` — the closing record

Append a `## Phase 5 closing record (2026-08-10) — …` section **beside** the Phase 3.1 record,
not over it, following that file's own stated convention ("This one sits beside it rather than
over it"). It must carry:

1. **The live before/after of the alert text.** The 2026-08-10 09:22 reading — `'control
   product is not reading IN_STOCK — the detector is probably broken, so real restocks would
   be missed silently'`, published by the deployed daemon for `target` — beside the
   replacement Task 2 measured after the restart. This is the strongest artefact in the phase
   because it is the system saying it to a person, not a test asserting about it.
2. **What the store pin changed**, stated as properties: the two Walmart watches went from
   verdicts about an arbitrary store (`out_of_stock` on the GO Plus + at 09:22, `in_stock` at
   `2.42` on the control) to readings tied to a pinned store. **No store number appears in
   this document.**
3. **The six criteria in Phase 3.1's four-column shape** — `| # | Criterion | Verdict | What
   settles it |`.
4. **The `make verify` verdict, verbatim**, with the three failure classes separated and the
   one this phase caused named as such.
5. **The three findings waves 1–3 flagged for the close.** Each gets its own short subsection,
   because each is a lesson rather than a status line:
   - **05-01 — the identity rule exists twice and has already drifted.**
     `_identity_leaks` lives in `scripts/identity_check.py` (the copy the pre-commit hook and
     `make verify` run) and again in `tests/test_fetch.py`, and the two differ. Only the
     shipped script was widened; the drift is recorded and **deliberately not reconciled**,
     because reconciling it would redden a grid cell and is a decision of its own.
   - **05-02 — four deviations from the outline's file table, argued rather than silent.**
     Record all four. For each, read `05-02-SUMMARY.md` and **quote the argument it made at
     the site**; do not reconstruct one here.
     - **`STORE_SCOPED` landed in `boty/models.py`, not `retailers.py`** — argued at the
       site: two modules read the predicate, and `monitor.py` importing `retailers` would drag
       `curl_cffi` and the browser stack into a module that keeps even `Pacer` behind
       `TYPE_CHECKING`.
     - **`tests/test_alert_text.py` landed in place of the outline's expected
       `tests/test_notify.py`** — argued at the site: REQ-15 is one requirement spread over
       two modules (`monitor.py` and `notify.py`), a `test_notify.py` holding an `ast` scan
       of `monitor.py` would be misfiled, and the positive and negative halves of one
       requirement belong in one module — the same reasoning that gives `test_dashboard.py`
       one module for one surface.
     - **`tests/test_pacing.py`** — absent from the outline's file-contention table entirely,
       and listed in its per-plan sketch under **05-03**, not 05-02. 05-02 entered it because
       three of its assertions pinned the withdrawn claims by their **prose**, so withdrawing
       one sentence broke a suite the outline had assigned to a later plan. The assertions
       were rewritten to the property, not deleted.
     - **`tests/test_monitor.py`** — in 05-02's own per-plan file list but absent from the
       contention table, so that table understated the plan's real footprint.
     And beside all four, the lesson they share: **the 2026-08-04 health fix had been pinned
     by its prose rather than by its property.** That is why withdrawing one sentence reached
     into files nobody had planned to open — a lesson about how this project writes tests, and
     the reason these belong in the closing record rather than only in a diff.
   - **05-03 — persisting `refusals` alone would have made the bug worse.** `warned` is the
     once-per-episode gate and is a `watch_loop` local, so restoring the refusal count without
     it would have restored a deep backoff into a process with no memory of having already
     paged — REQ-16's "once" turning into "once per process" while the backoff looked fixed.
     Both are persisted.
6. **What this phase did not establish**, in its own short subsection. At minimum: the first
   restart did not demonstrate criterion 6; the live `make verify` failure classes are
   recorded and not diagnosed; and any criterion whose live confirmation was not obtained.

### 3g — Commit, once

`git config user.email` **before committing** — author identity has silently gone wrong in
this repo before. Then one commit covering all four files, because the binding constraint is
that the renumbering lands with the outcome table:

```
git add .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md docs/retailer-evidence.md
```

The message states the verdict split **and** says the renumbering changed no criterion text —
e.g. subject `docs(05): close phase 5 — six verdicts, and a renumbering that touched no
criterion text`, with a body recording that the criteria list read `1, 0, 2, 3, 4, 5`, now
reads `1`–`6`, that every criterion body is byte-identical (proved by the 3c diff), and that
no criterion was reworded, shortened, merged or amended.

**Run `.venv/bin/python scripts/identity_check.py --all` after the record is written and
before the commit.** This is the mechanical guard against the one way this task can leak: the
store number reaching a tracked file through a pasted status line. The rule 05-01 widened is
what makes it bite. If it fails, **find and remove the value — do not add anything to
`allowed`**, which is the mutation `test_the_allow_list_cannot_absorb_a_real_value` exists to
catch. The pre-commit hook runs the same check, so a leak blocks the commit either way; run it
first anyway, so the failure is diagnosed rather than merely refused.
  </action>

  <verify>
    <automated>make verify-offline && .venv/bin/python scripts/identity_check.py --all && test $(awk '/^### Phase 5: A Reading Means Something/{f=1} f&&/^\*\*Why this first/{exit} f&&/^  [0-9]+\. /{print}' .planning/ROADMAP.md | grep -c '^  0\.') -eq 0</automated>
  </verify>

  <done>
    - `make verify-offline` exits 0, with the verdict line, the test count and the mutation
      `caught/total` recorded verbatim beside the pre-phase baseline of **531 passed, 8/8**.
    - `make verify` was run **once**, live, and its verdict is in the record **verbatim
      including the FAIL**, with the this-phase-caused Walmart config-gap class named
      separately from the two pre-existing ones.
    - The decision not to re-run it under the service's `EnvironmentFile` is recorded with its
      reason, so the departure from Phase 3.1's method is visible.
    - Six rows, six verdicts, each with a measurement or a reason. **No criterion text was
      reworded, shortened, merged or amended.** Any UNMET carries the date and the reason.
    - The criteria list reads `1`–`6`; the extraction yielded exactly six lines from each
      side; the diff printed `RENUMBER ONLY`; and a note beneath the list records the
      renumbering and that the entry now reading `2.` previously read `0.`.
    - REQ-14/15/16 are flipped with evidence-carrying cells, or left Pending with the reason;
      no requirement text was edited.
    - STATE.md's *do-not-restart* warning and its *running-pre-Phase-4-code* line are both
      **rewritten to what is now true**, not deleted.
    - `docs/retailer-evidence.md` carries `## Phase 5 closing record`, sitting beside the
      Phase 3.1 record, containing the live alert-text before/after, all three wave findings,
      the four-column criteria table, and a *what this phase did not establish* subsection.
    - `identity_check.py --all` PASSED after the record was written and before it was
      committed. **No store number is in any tracked file, any commit message, or the
      transcript.**
    - One commit, four files, `git config user.email` checked first, and a message that says
      the renumbering is a renumbering.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the operator's real store number → the systemd `EnvironmentFile` → the daemon → `served/boty/status.json` → **anything Claude reads** | Identity egress, and this plan is the first in the phase where a *real* value exists to leak. Every prior plan operated on the redaction placeholder `"0"`. |
| the six criterion verdicts → `ROADMAP.md`, `REQUIREMENTS.md`, `docs/retailer-evidence.md` | The project's own audit trail. A record is only worth what its worst row is worth. |
| the running `boty.service` → a restart across six days and four plans of code | Availability. The only monitor Dan has is being moved onto code it has never run. |
| `make verify`'s live control stage → six retailers | Politeness, which this project treats as a hard constraint rather than a preference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Repudiation | the closing record itself | mitigate | A record that cannot be checked is a claim, and this milestone exists because the system made claims. Five controls. **(a)** Every criterion row carries a **measurement or a reason**, never an impression — and where the measurement is a transcript, the transcript is quoted from the SUMMARY rather than paraphrased. **(b)** The live `make verify` verdict is recorded **verbatim including its FAIL**, with the class this phase itself caused named separately from the two it did not; Phase 4's record set that precedent and this one keeps it. **(c)** The pre-restart daemon state is captured **before** the restart destroys it (Task 2's first instruction), because a record reconstructed afterwards from memory is exactly the unmeasured claim being closed — and the 09:22 readings are already in this plan's `<context>`, dated, so the phase cannot lose them even if the daemon cycles first. **(d)** Unmet is recorded unmet, with the date and the reason, and no criterion text is touched — the standing rule Phase 3.1 and Phase 4 both held. **(e)** The one edit that *is* permitted to the criteria list is proved mechanically to be a renumbering (3c's extract-and-diff, confirmed to yield six lines from each side so it cannot pass vacuously), and reverted entirely if the diff shows anything else. Residual, named: the record cites three SUMMARYs written by the plans they describe; the independent checks on those are `make verify-offline`'s exit code and the mutation harness's own count, both re-run here rather than quoted from the SUMMARY. Tasks 2, 3. |
| T-05-18 | Information disclosure | the real store number reaching a tracked file, a commit message or the transcript, via the closing record | mitigate | **New in this plan, and it is the checkpoint's own egress risk.** After Task 1 the real value is live in `served/boty/status.json` (`store`, `store_pinned`), in `boty check` output, and inside any mismatch `detail` — which reaches `Health.failing_controls` and a notification body. 05-04 is the first plan that reads any of those with a real value present, and the whole point of the checkpoint is that Claude never holds it. Four controls. **(a)** Every post-restart confirmation goes through a probe printing **derived booleans** — `store_present`, `pin_present`, `match` — so Claude does not read the number at all rather than reading it and being trusted not to write it. **(b)** No status.json watch block, `boty check` line, Walmart `reason`/`detail`, or `failing_controls` entry is pasted verbatim into any document, SUMMARY or commit message; non-Walmart `reason` strings are safe and are what criterion 4's before/after quotes. **One carve-out, and it is mechanical rather than argued:** a *refusal* `detail` — `blocked: ...` / `fetch failed: ...` from `check_html`'s `except` arms — is built before any page has been read, and 05-01 Task 3 pins `store=None` on those arms, so it cannot carry a store. Task 2's refusal branch may quote it **only after** `identity_check._identity_leaks` has been run against that exact string and returned `[]`; if it returns anything, only the class and the status code are recorded. The carve-out exists because the refusal is the phase's likeliest measured outcome and the record has to be able to state it — and it is gated by the same guard 05-01 widened, not by this plan's confidence. **(c)** `make verify` is run from a shell with **no** `WALMART_STORE_ID`, which is why its output is safe to quote verbatim — and is the reason this close deliberately does **not** repeat Phase 3.1's `systemd-run --property=EnvironmentFile=...` re-run, which would pull the value into a process Claude launched. **(d)** `scripts/identity_check.py --all` runs after the record is written and before the commit, over the whole tracked tree; the config-key rule 05-01 widened and watched going red is what makes it mechanical, and the pre-commit hook runs it again. Residual, accepted: the value is in the daemon's gitignored, loopback-only `status.json`, which 05-01's T-05-02 already accepted deliberately. Tasks 1, 2, 3. |
| T-05-19 | Denial of service | the restart — the only monitor Dan has, moved across six days of code | mitigate | The daemon has run pre-Phase-4 code since **2026-08-04 17:48:52** and this restart crosses all of Phase 4 plus three Phase 5 plans at once. Controls: the pre-restart state is captured first, so a failure is diagnosable rather than merely observed; `StartLimitIntervalSec=300` / `StartLimitBurst=5` bound a crash loop to five attempts in five minutes; and a unit that fails is **recorded with `systemctl status` and `journalctl`** and carried into the closing record as what was measured, rather than retried until it looks better. The tree itself was gated by `make verify-offline` three times before this point. Residual, accepted and one-time: this first restart **loses the current backoff** (Amazon at 10 refusals, GameStop at 4 at the 09:22 reading), because the old code never wrote a state file — bounded by the `retailer_intervals` floors that stay in force (30 min and 15 min, not 5). 05-03 is what makes it the last time. Task 2. |
| T-05-20 | Denial of service | the retailers, via two full passes in flight at once | mitigate | `make verify`'s live control stage and the daemon's own cycle hit the same six retailers, and two concurrent passes double the instantaneous rate against retailers two of which are already refusing us. Mitigation: Task 3 starts `make verify` only **after** Task 2's post-restart cycle has published, so the two are serialised inside one 5-minute window; `make verify-offline` is run **first** so a red tree never costs a live pass at all; and `make verify` is run **once**, not re-run until green. This close spends exactly one extra pass. Tasks 2, 3. |
| T-05-21 | Tampering | a criterion quietly reworded to make the table read MET | mitigate | The specific failure this project keeps catching in itself, and a closing plan is where the pressure to commit it is highest. **The criteria text is out of scope for every edit in this plan except the leading numeral**, and 3c's extract-and-diff is the mechanical proof — with the count assertion in front of it so it cannot pass over an empty extraction, and a written instruction to revert the whole renumbering if the diff shows anything else. The `<verify>` block asserts independently that no criterion line still reads `0.`. Two written precedents are cited in the plan itself so the constraint is not merely stated: Phase 3.1's declined rewrite and Phase 4's two UNMET criteria. Task 3. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | **This plan installs nothing and changes no code.** Every command is `systemctl`, `git`, `make` or an existing script in this repo. Research is disabled for this project, so there is no `## Package Legitimacy Audit` and the fallback policy stands: if the executor finds it needs a package, **stop** — every package is `[ASSUMED]`, and a blocking human-verify checkpoint (gate `blocking-human`) verifying it on `pypi.org/project` must precede the install. Not auto-approvable under `workflow.auto_advance`. |

**Threats owned by earlier plans in this phase, listed so this register is not read as
complete:** T-05-01/02/03/07 (the store number entering a public repo, publishing it,
escaping it into the dashboard, a malformed pin taking down the loader) are 05-01's;
T-05-05/08/09/10/11 (a reading attributed to the wrong store, the alert channel as audit
trail, the store-gap arm as a paging source, the store reaching a notification body,
`STORE_SCOPED` as a kill-switch) are 05-02's; T-05-04/12–17 (the persisted state file, the
overflow bound, staleness, the wall clock, the restored `warned`, the artifact's contents and
its tracking, a local writer) are 05-03's.
</threat_model>

<verification>
Run from the repo root, in this order. **Offline before live, always** — a red tree must
never cost a live retailer pass.

1. `make verify-offline` → exit 0, `VERIFY: PASS (OFFLINE…)`, test count and mutation
   `caught/total` recorded beside the pre-phase baseline **531 passed, 8/8**.
2. `.venv/bin/python scripts/identity_check.py --all` → PASS over the whole tracked tree,
   run **again** after the closing record is written and before the commit.
3. `systemctl is-active boty.service` → `active`, with a MainPID different from the
   pre-restart one and an `ActiveEnterTimestamp` later than `2026-08-04 17:48:52 CDT`.
4. The post-restart `status.json` probe → a fresh `updated`, `store`/`store_pinned` present
   on every watch, and `withdrawn_fragments=[]` for every retailer. The Walmart rows are
   recorded in whichever state they were actually in: `match=True` **beside**
   `store_present=True`/`pin_present=True`, a mismatch, or `refusal_shaped=True` with criteria
   1 and 2's live confirmation marked NOT OBTAINED. A bare `match=True` settles nothing — the
   probe reports `n/a` when either side is absent, for the reason recorded in Task 2.
   **`match=True` is a bonus, never a gate** (outline § *Evidence constraint*), and no read is
   re-attempted to obtain it. **Derived booleans only; the store number is never printed.**
5. `make verify` → run **once**, live, after step 4, and recorded **verbatim including the
   FAIL**.
6. The renumbering proof (3c) → both extractions read `6` and the diff prints
   `RENUMBER ONLY`.
7. `git status --porcelain` → clean of runtime artifacts before the commit.

**What is deliberately NOT done, and why each is a decision rather than an omission:**

- **`make verify` is not re-run under the service's `EnvironmentFile`.** Phase 3.1's close
  did exactly that and the recipe is still in `deploy/boty.service`'s comment — but it would
  pull the real store number into a process Claude launched and reads. The daemon's own
  `status.json` answers the same question at zero extra requests and zero exposure (T-05-18).
- **`make verify` is not re-run until it is green.** It has read FAIL since 2026-08-06 for
  reasons this phase did not cause and is not chartered to fix, and this phase adds a third
  class of its own that is *correct*. Re-running until the number looks better is the
  omission this project keeps catching in itself.
- **A refused Walmart read is not retried.** If Walmart challenge-blocks this pass, that is
  recorded as the measured state and the live confirmation is marked NOT OBTAINED. Re-reading
  until one lands would break the politeness constraint and the one-live-pass budget in the
  same act; softening the acceptance line instead is the defect this milestone exists to
  close.
- **`boty check` is not run.** The daemon's published cycle is strictly better evidence — it
  is the deployed thing — and it costs nothing.
- **No code is written.** A criterion that is unmet at this point is **recorded** unmet. A
  closing plan that implemented its way to a green table would be measuring its own work in
  the act of measuring.
</verification>

<success_criteria>
- [ ] Dan supplied the store pin, or explicitly chose `restart-anyway` / `defer`, and the
      signal is recorded verbatim with the date.
- [ ] **The store number was never read, never printed, and appears in no file, no commit
      message and no transcript.** `identity_check.py --all` PASSED after the record was
      written.
- [ ] The pre-restart daemon state was captured **before** the restart, verbatim — including
      the live withdrawn `probably broken` sentence and both paced retailers' refusal
      counts — **or**, on `defer`, recorded **NOT OBTAINED** with the date and the reason,
      the 09:22 readings already dated in this plan's `<context>` standing as the only
      capture there is.
- [ ] `boty.service` runs the new code: new MainPID, `ActiveEnterTimestamp` past 2026-08-04
      17:48:52, a freshly published cycle carrying `store` and `store_pinned` — **or**, on
      `defer`, the restart is recorded **NOT OBTAINED** with the date and the reason, and
      the record says the daemon is still on 2026-08-04 code.
- [ ] The Walmart watches on the deployed daemon are no longer UNKNOWN-for-want-of-a-pin,
      confirmed through derived booleans — **or** Walmart refused this pass, the refusal is
      recorded as the measured state with its date, and the live confirmation for criteria 1
      and 2 is marked **NOT OBTAINED** with its reason, those two rows resting on 05-01's and
      05-02's offline evidence. No live Walmart read was re-attempted either way.
      `withdrawn_fragments=[]` for every retailer — **or**, on `defer`, that live check is
      recorded **NOT OBTAINED** with the date and the reason, criterion 4 resting on
      05-02's offline evidence with the 09:22 `probably broken` reading standing as a
      before-picture that has no after beside it.
- [ ] `pacer-state.json` exists at the configured path — **or**, on `defer`, recorded **NOT
      OBTAINED** with the date and the reason, because the daemon was never restarted onto
      the code that writes it; `git status --porcelain` is clean either way.
- [ ] **The honest limit is written down:** this first restart still lost the pre-restart
      backoff, and criterion 6 rests on 05-03's tests rather than on this restart —
      **or**, on `defer`, that there was no restart, so nothing was lost and nothing was
      migrated: the backoff is still in memory on 2026-08-04 code, `pacer-state.json` is
      not yet in use, and criterion 6 rests on 05-03's tests exactly as it would have
      anyway.
- [ ] `make verify-offline` exits 0; the mutation count is recorded **beside** the pre-phase
      8/8 so the rise is shown rather than claimed.
- [ ] `make verify` was run **once**, live, and recorded **verbatim including its FAIL**,
      with the this-phase-caused Walmart config-gap class named separately from the two
      pre-existing ones.
- [ ] Six criterion verdicts in `ROADMAP.md`, Phase 4's three-column shape, each with a
      measurement or a reason. **Any unmet criterion is recorded unmet, with the date and the
      reason, and nothing is reworded to pass.**
- [ ] The criteria list reads `1`–`6`; both extractions yielded six lines; the diff printed
      `RENUMBER ONLY`; a note beneath the list records the renumbering in the open; the commit
      message says it is a renumbering only.
- [ ] REQ-14/15/16 flipped with evidence-carrying traceability cells, or left Pending with
      the reason; no requirement text edited.
- [ ] STATE.md's *do-not-restart* warning and its *pre-Phase-4-code* line are rewritten to
      what is now true, not deleted.
- [ ] `docs/retailer-evidence.md` carries `## Phase 5 closing record` beside the Phase 3.1
      record, with the live alert-text before/after, the four-column criteria table, all
      three wave findings, and a *what this phase did not establish* subsection.
- [ ] One commit, four files, `git config user.email` checked first.

**Coverage this plan owns** (the full four-source audit is in `05-PLAN-OUTLINE.md` and is not
restated here):

| Source | Item | Where in this plan |
|---|---|---|
| GOAL | "A Walmart reading is a statement about a known store" — the live confirmation | Tasks 1-2 |
| GOAL | "every alert names only what was measured" — the live before/after | Task 2, Task 3f |
| REQ | REQ-14 — closed and recorded with the measurement that opened it | Task 3d |
| REQ | REQ-15 — closed, with the withdrawn sentence quoted live off the deployed daemon | Task 2, Task 3d/3f |
| REQ | REQ-16 — closed, with the limit of the first restart stated | Task 3d/3f |
| CONTEXT | Never amend a criterion to make it meetable | Task 3b, 3c — and proved mechanically |
| CONTEXT | Every criterion verified by something executable | Task 3a — `make verify-offline`, `make verify`, the mutation harness |
| CONTEXT | Do not put a real store id in a fixture or config without redaction | Task 1 (the checkpoint holds the value), Task 2 (derived booleans), Task 3g (`identity_check --all`) |
| CONTEXT | Politeness is a hard constraint | Task 3a — offline first, one live pass, serialised against the daemon's cycle |
| CONTEXT | bot-y never guesses where the user lives | Task 1 — the checkpoint's whole reason for existing |
| OUTLINE | 05-04 sketch — pin, restart, verify verbatim, mutations rising, six verdicts | Tasks 1-3 |
| OUTLINE | The ROADMAP `1, 0, 2, 3, 4, 5` defect, fixed in the open | Task 3c |
| OUTLINE | Evidence constraint — no acceptance depends on a live Walmart read | Task 2's fourth observation row and its *When Walmart refuses* branch; §3a's demotion of the config-gap run to *recorded if it occurs*; §3b rows 1 and 2 resting on offline evidence with the live line marked NOT OBTAINED; `<verification>` step 4 and its no-retry entry. Every live Walmart line is a bonus and says so |
</success_criteria>

<output>
Create `.planning/phases/05-a-reading-means-something/05-04-SUMMARY.md` when done.

It must carry, beyond the template:

- **The pre-restart capture, verbatim** — commands and outputs — because it is the only copy
  of a state the restart destroyed.
- **The post-restart confirmation**, as derived booleans. **No store number.** If Walmart
  refused this pass, say so with the date, the reason and the two booleans that established
  it, and list criteria 1 and 2's live confirmations as **NOT OBTAINED** — not as met, and not
  as shortfalls against the criteria, which rest on 05-01's and 05-02's offline evidence.
- **The `make verify` verdict, verbatim, including its FAIL**, with the three failure classes
  separated and the one this phase caused named as such.
- **The `make verify-offline` verdict** with its test count and mutation `caught/total`,
  beside the pre-phase 531 / 8/8 and each SUMMARY's intermediate count.
- **The six verdicts as written into the ROADMAP**, reproduced, so the SUMMARY and the
  ROADMAP can be diffed against each other.
- **The renumbering proof output** — both line counts and the diff result.
- **Dan's checkpoint signal, verbatim, with the date**, and if it was `restart-anyway` or
  `defer`, exactly which live confirmations were therefore not obtained.
- **The `deploy/boty-secret` finding** — no store subcommand, flagged not fixed, and why a
  closing plan did not grow it.
- **What this phase did not establish**, as its own section: the first restart did not
  demonstrate criterion 6; the live `make verify` failure classes are recorded and not
  diagnosed; and any criterion resting on offline evidence alone.

Nothing in this summary is reworded to pass. If a criterion is unmet, it is unmet here in the
same words it is unmet in the ROADMAP.
</output>
