---
phase: 03.1-target-and-amazon-supported
verified: 2026-08-03T14:46:48Z
status: gaps_found
score: 12/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
gaps:
  - truth: "No committed fixture in the current tree carries the capturing host's identity — the widened leak guard closes the class"
    status: partial
    reason: >
      The guard WAS widened and it does fire on the Target-shaped JSON leak. But two
      committed fixtures still carry this host's geolocation in a shape the widened guard
      does not match, and the guard returns zero leaks on both. `tests/fixtures/walmart/goplusplus.html`
      contains `Redacted, 00000` six times and `tests/fixtures/walmart/milk-control.html`
      twice — including in rendered, visible markup
      (`aria-label="Redacted, 00000, Change shipping address"`) and in `locationText` /
      `stateOrProvinceCode":"TX"` JSON. The 03.1-02 redaction changed only the
      `"postalCode"` key's value to `00000`; it left the same ZIP in five other places in
      the same file. Executed against the guard's exact patterns lifted out of
      `tests/test_fetch.py:394-475`, both files report `[]`.
      Three phase artifacts state the opposite and are therefore false as written:
      03.1-02-SUMMARY deviation 2 ("All four redacted here"), QUESTIONS.md line 5
      ("the working tree is clean but the pushed public history is not"), and
      QUESTIONS.md § 0e ("every one of these is redacted in the working tree as of
      `a4f2847` … so this class cannot be re-committed silently"). It can — demonstrated.
      This phase's 20 commits are not yet pushed (`origin/main` is at `861b24a`), so the
      current blob would carry the ZIP forward on the next push, not merely leave it in
      history.
    artifacts:
      - path: "tests/fixtures/walmart/goplusplus.html"
        issue: "6 × `Redacted, 00000` (this host's geolocation) surviving the a4f2847 redaction; guard reports no leak"
      - path: "tests/fixtures/walmart/milk-control.html"
        issue: "2 × `Redacted, 00000` surviving the a4f2847 redaction; guard reports no leak"
      - path: "tests/test_fetch.py"
        issue: "`test_no_fixture_leaks_the_capturing_hosts_identity` keys the postal-code rule on the JSON key names `zip|zipCode|postal_code|postalCode`, so a ZIP written as free text (`locationText`, `aria-label`) is invisible to it. No committed test watches the widened guard failing — the only record of it going red is prose in 03.1-02-SUMMARY."
      - path: "QUESTIONS.md"
        issue: "§ 0e and the file preamble assert a clean working tree; the assertion is false, so the escalation Dan is being asked to decide on understates the problem"
    missing:
      - "Redact `Redacted, 00000` (and `stateOrProvinceCode\":\"TX\"` / `\"city\":\"Redacted\"`) out of both Walmart fixtures, then re-run the guard"
      - "Widen the guard so a bare 5-digit ZIP adjacent to a city name, or any occurrence of the capturing host's known ZIP, is a leak — the current rule is keyed on one retailer's JSON spelling, which is the exact defect the widening was written against"
      - "Attach a watched-failing test for the widened guard (a synthetic leaky sample per marker class), matching the discipline every other gate in this repo has"
      - "Correct QUESTIONS.md § 0e and its preamble before Dan decides on it — the choice between 'leave it / filter-repo / recreate' is being offered on a premise that is not true"
deferred: []
---

# Phase 3.1: Target and Amazon, Supported — Verification Report

**Phase Goal:** Target and Amazon reporting trustworthy stock for the GO Plus +, control-verified, on whatever rung reaches them.
**Verified:** 2026-08-03T14:46:48Z
**Status:** gaps_found
**Re-verification:** No — initial verification

Everything in this report was executed, not read. `make verify` was run exactly once,
under the service `EnvironmentFile`, per the politeness constraint; every other number
is read off `served/boty/status.json` or produced by running the gate in this verifier's
own process against synthetic inputs.

## Goal Achievement

### Observable Truths — ROADMAP § Phase 3.1 Success Criteria

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Target reports stock for the GO Plus +, control watch green | **UNMET — correctly recorded, NOT a gap** | Recorded UNMET in `.planning/ROADMAP.md:194` and again in the outcome table at `:205`, with the reason (Target delisted TCIN `88714054`) and the fact that the meetable rewrite was proposed and **Dan declined it**. Verified not tidied away: `config/products.yaml` carries a `retailer: target` CONTROL watch and **no** GO Plus + watch, with the delisting written into the config comment. The control is green — `status.json`: `target / CONTROL — up&up microfiber dust cl / in_stock / $12.59 / rung=browser / extraction=dom / degraded=True`. The half of the criterion that *could* be met is met; the half that cannot, is not, and says so |
| 2 | Amazon reports stock for the GO Plus + if it carries it, control green; or the technical outcome recorded, having actually been attempted | ✓ VERIFIED | `docs/retailer-evidence.md` § Amazon resolves to exactly one section with exactly one anchored verdict, `**Verdict: REACHABLE (rung 1)**` (checked through `evidence_check.sections_for`/`verdict_lines`, not by eye). Amazon carries both a control watch and a real GO Plus + watch. `make verify` live: `in_stock amazon CONTROL — Amazon Basics AA batteri $9.99 add-to-cart control: add-to-cart enabled from Amazon.com` |
| 3 | `boty check` reports five or more retailers with no health warnings — six if Amazon lands | ✓ VERIFIED at six | Live `served/boty/status.json`: `healthy: true`, 13 watches, **6** retailers, all six health entries `{'ok': True, 'reason': '', 'failing_controls': []}`. Zero warnings |
| 4 | Every row of the support matrix states rung, robots.txt position and terms position | ✓ VERIFIED | `README.md:99` header is the seven-cell `\| Retailer \| Rung \| Extraction \| robots.txt \| Terms \| Method \| Status \|`; seven retailer rows follow, each with a judged robots and terms cell and a `⚠ disagree` marker where earned. Machine-checked in both directions by `tests/test_support_matrix.py` (`_extraction_mismatch`, `_undeclared_degraded`, `_misdeclared_disagreement`, `UNREAD_POSITIONS`), all green inside the 377-test run |
| 5 | No regression: four Phase 2 retailers still green, `make verify` exits 0 | ✓ VERIFIED | Run by this verifier under `sudo systemd-run … EnvironmentFile=/home/dan/.config/boty/env`: **bare `VERIFY: PASS`**, exit 0 — not `(INCOMPLETE — …)`, not `(OFFLINE — …)`. 377 tests, mypy clean over 15 files, 11 fixtures ok, **6/6 live controls**, **8/8 mutations caught**. The four Phase 2 controls read with unchanged sources: `ld+json: InStock from GameStop`, `__NEXT_DATA__: IN_STOCK from Walmart.com`, `ld+json: InStock from Best Buy`, `ld+json: InStock from Nintendo of America Inc.` |
| 6 | A single `boty check` still completes in under 2 minutes | ✓ VERIFIED | `status.json` `duration_seconds` = **45.586** against a 120 s budget, read off the payload rather than re-timed. Published by the service's own cycle at `updated` = 1785767875 = 2026-08-03T14:37:55Z |

### Observable Truths — what the phase claims it delivered

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 7 | `Extraction` is a second axis (`structured`\|`dom`), `Result.degraded` fires on browser transport OR dom extraction, published in status.json and the README | ✓ VERIFIED | `[e.value for e in Extraction] == ['structured','dom']`; `Result` field order is `watch, availability, price, detail, url, rung, extraction`. `boty/models.py:168`: `return self.rung is Rung.BROWSER or self.extraction is Extraction.DOM`. `boty/status.py:81-83` publishes `rung`, `extraction`, `degraded` in that order. M6 (whole flag) and M7 (dom disjunct alone) both CAUGHT in the mutation run — M7 by 6 tests including `test_a_dom_reading_on_the_default_transport_is_degraded`. README carries the Extraction column and the two prose blocks explaining it. Dashboard renders `<span class="tag dom">` gated on `w.extraction === 'dom'` |
| 8 | Target registered at rung 3 + dom, control-only | ✓ VERIFIED | `check_target_browser` (`boty/retailers.py:478`) returns `rung=Rung.BROWSER, extraction=Extraction.DOM` on the `Blocked` path, the `FetchError` path, and via `_verdict_from_html(..., allow_dom=True)` on the success path — including the both-readers-empty UNKNOWN (plan-check W-2 closed: `extraction = Extraction.DOM if allow_dom else Extraction.STRUCTURED` is set *before* the no-offers returns). Live payload agrees: `rung=browser, extraction=dom, degraded=True` |
| 9 | Amazon registered at rung 1 + dom, shape C; used-only offer suppressed by the seller filter | ✓ VERIFIED — see the note below | See "The Amazon `dom` reading" |
| 10 | `evidence_check` rule 6 — a refusal must cite an observation | ✓ VERIFIED by execution | See "Behavioural Spot-Checks" |
| 11 | Six retailers, `healthy: true`, a pass under 120 s | ✓ VERIFIED | Same evidence as criteria 3 and 6 |
| 12 | The fixture leak guard is widened, watched failing, and the current tree is clean | ✗ **FAILED** | Guard widened: yes. Watched failing: no committed test. Tree clean: **no** — see the gap |
| 13 | The deployed service publishes six retailers with the `extraction` key | ✓ VERIFIED | `systemctl show boty.service`: `ActiveState=active`, `MainPID=1005982`, `ExecMainStartTimestamp=2026-08-03 09:20:05 CDT` (= 14:20:05Z, after all three feature commits). Its own published cycle at 14:37:55Z carries 13 watches across 6 retailers and **every** watch has both `rung` and `extraction` |

**Score:** 12/13 truths verified (criterion 1 counted as correctly-recorded-UNMET, per Dan's standing decision; it is not scored as a failure and must not be closed).

### The five pressure points, answered

**1. Criterion 1 is deliberately UNMET — was it quietly tidied away?**
No. It stands unmet in three places and each states the reason and whose call it was:
`.planning/ROADMAP.md:194` (the criterion itself, with "**Recorded UNMET, and deliberately
not amended (Dan's call, 2026-08-03)**"), the six-criteria outcome table at `:201-210`, and
`03.1-04-SUMMARY.md`. The wording was not softened to "reports trustworthy stock" — the
rewrite is named in the record as *proposed and declined*, which is stronger than silence.
Downstream artifacts agree rather than paper over it: `config/products.yaml` carries the
delisting as a comment on the Target block, and the README's Target Status cell says
"control-only … Target **delisted** the product … a disproof, not an omission".
**Correctly recorded. Not a gap. Do not close it.**

**2. Is `TARGET_RETAILER_COUNT` still 5, and does the gate still bite?**
`scripts/evidence_check.py:172` — `TARGET_RETAILER_COUNT = 5`. Executed against synthetic
configs derived from the shipped one:

| Configured retailers | rule 3 |
|---|---|
| 6 (shipped) | silent — correct |
| 5 incl. both hard-two | silent — at threshold |
| 4 incl. `target` | **FIRES**, naming target |
| 4 incl. `amazon` | **FIRES**, naming amazon |
| 4 with neither hard-two | silent — the documented allowance |

The gate is live, not permanently satisfied: it fires the moment the count drops below five
while a hard-two member is still shipping. Leaving it at 5 is the documented decision
(a threshold of 6 would fire on the honest five-retailer outcome).
⚠ **Warning:** no test pins the constant at 5. 03.1-04's own hand-off says "Do not raise it
without a commit that says so and a test attached" — that test does not exist, so the value
can drift with nothing going red.

**3. Does rule 6 actually fail a prose-only refusal?**
Yes — run, not read. Four synthetic seven-section evidence documents driven through
`evidence_check.check_phase` against the shipped `config/products.yaml`:

| Case | rule 6 hits |
|---|---|
| REFUSED + `**Refusal observed (rung 1):** Pokemon Center refused us` (prose only) | **2** — one naming the unmeasured line, one for having no measured observation |
| REFUSED with no refusal line at all | **1** |
| REFUSED + `**Refusal observed (rung 1):** HTTP 403, 4,572 B challenge page` | 0 — correctly accepted |
| Hard-two REFUSED with one measured rung-1 observation only | **1** — the higher bar fires (needs ≥2 including one at rung 3) |
| The shipped tree | `[]` — and `scripts/evidence_check.py --phase --strict` prints `evidence check: PASS — phase`, exit 0 |

The plan-check B-3 defect is closed by the *body predicate* (`_MEASURED`: a status code, a
byte count, or a `BLOCK_PHRASES` entry imported from the transport), not by the anchor alone.

§ Target's two historical lines are at `docs/retailer-evidence.md:1960` (rung 1, body
"not a block — **HTTP 200**, 314,757 B and 318,690 B") and `:1966` (rung 2, `41 B`). Both
clear the body predicate — the predicate is *not* what makes them safe. Rule 6 never reads
them because it skips any section whose verdict is not exactly `**Verdict: REFUSED**`, and
§ Target resolves to `**Verdict: REACHABLE (rung 3)**`. That scope is pinned by
`tests/test_evidence_check.py::test_the_shipped_target_non_refusal_line_is_only_harmless_because_of_the_section_scope`,
which asserts both halves — clean in a REACHABLE section, and *counted* if the same line
is moved into a REFUSED one. Run by name: **2 passed**.

**4. The Amazon `dom` reading.**
Amazon has no structured data (`test_the_amazon_fixture_carries_no_structured_data_at_all`)
and the only offer on `B0BX2P43PX` is a used unit at **$219.00** from
`LO Store (We Record Serial Numbers To avoid FRAUD)` against a $54.99 MSRP.

The seller filter fires first: `LO Store …` is not in `FIRST_PARTY['amazon']` (which is the
verbatim buy-box string `Amazon.com`), and `amazon` is in `MARKETPLACES`, which disables
`_pick`'s unattributed fallback — so `_pick` returns `None` and the ceiling is never
consulted. `test_the_amazon_go_plus_plus_reseller_is_rejected_by_the_first_party_filter`
asserts the offer is `available`, priced 219.0, correctly attributed, and then that the
verdict is `OUT_OF_STOCK` with `not r.alertable` and `"none first-party" in r.detail`.
The ceiling is proved independently on the control page by
`test_an_amazon_first_party_offer_over_the_ceiling_is_suppressed_independently`.

**On the reading label:** it *is* `OUT_OF_STOCK`, and that is the correct and deliberate
answer rather than a hedge — the page was read perfectly and there is no first-party offer
on it. The model has three availability states, so "suppressed" is carried by
`alertable=False` plus the detail string, not by a fourth state. Live payload confirms the
shipped behaviour end to end:
`amazon / Pokémon GO Plus + / out_of_stock / rung=tls / extraction=dom / degraded=True / alertable=False / detail: 1 offer(s) via add-to-cart control, none first-party`.
**It is neither an alertable restock nor an UNKNOWN masquerading as stock.** The two
adjacent honest-failure paths are also covered and distinct: a vanished control reads
UNKNOWN (`test_an_amazon_page_whose_control_vanished_is_unknown_never_out_of_stock`) and an
unattributed offer reads UNKNOWN (`test_an_amazon_offer_with_no_seller_recorded_is_unknown_not_a_verdict`).

**5. The fixture leak guard — FAILED.** See the Gaps Summary.

**6. The deployed service.**
Running since 14:20:05Z on the shipped code, MainPID 1005982, and its own cycle at
14:37:55Z publishes 13 watches across 6 retailers with `rung` **and** `extraction` on every
one. The pre-phase four-retailer/no-`extraction` payload 03.1-04 found is gone. Confirmed
against `systemctl show` and the live `status.json`, not against the SUMMARY.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `boty/models.py` | `Extraction` enum, `Result.extraction` last, widened `degraded` | ✓ VERIFIED | Two members in order; field order confirmed by `dataclasses.fields`; one-line two-disjunct `degraded` |
| `boty/status.py` | `extraction` published beside `rung` | ✓ VERIFIED | `:81-83`, and every live watch carries it |
| `boty/retailers.py` | `check_target_browser`, `check_amazon`, `_verdict_from_html(allow_dom=)` | ✓ VERIFIED | Both adapters label every return path including errors; `FIRST_PARTY['amazon']`/`['target']` and `MARKETPLACES` present |
| `boty/parse.py` | one widened `_AddToCartParser` over two page families | ✓ VERIFIED | M1 and M8 both CAUGHT (18 and 12 tests) |
| `boty/cli.py` | `[dom]` tag | ✓ VERIFIED | `:103` |
| `served/boty/index.html` | `dom` tag + rewritten degraded tooltip | ✓ VERIFIED | `:121`, tooltip names both reasons |
| `config/products.yaml` | 6 retailers; target control-only; amazon control + product | ✓ VERIFIED | Read through `Config.load`: gamestop, walmart, nintendo, bestbuy, target, amazon |
| `scripts/evidence_check.py` | rule 6 with anchor + measurement | ✓ VERIFIED | Executed; see spot-checks |
| `scripts/mutation_check.py` | M6 re-anchored, M7, M8 | ✓ VERIFIED | 8/8 caught in the live run |
| `README.md` | 7-column, 7-row matrix; six-retailer count prose | ✓ VERIFIED | Header at `:99`; count prose "Six working retailers" |
| `docs/retailer-evidence.md` | Target REACHABLE (rung 3), Amazon REACHABLE (rung 1), rule-6 lines | ✓ VERIFIED | Section→verdict map produced through the gate's own parser |
| `tests/fixtures/target/control-dust-cloths.html` | leak-scanned | ✓ VERIFIED | 134 `<script>` bodies all replaced with `/* redacted before commit … */`; zero hits for 00000, 33.170, -96.780, Redacted, refreshToken, visitor_id, phone numbers |
| `tests/fixtures/amazon/*` | two fixtures, leaking nothing | ✓ VERIFIED | Zero hits on every marker class |
| `tests/fixtures/walmart/*.html` | redacted of host geolocation (03.1-02 dev. 2) | ✗ **FAILED** | `Redacted, 00000` × 6 and × 2 respectively; guard blind to it |
| `tests/test_fetch.py` leak guard | widened, watched failing | ⚠️ PARTIAL | Widened and it does fire on the Target-shaped leak; but it is keyed on JSON key names and no committed test watches it failing |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `config/products.yaml` `retailer: target` | `check_target_browser` | `cli._make_checker` | WIRED | Live control ran through the browser rung in `make verify` and in the service cycle |
| `config/products.yaml` `retailer: amazon` | `check_amazon` | `cli._make_checker` | WIRED | `test_an_amazon_watch_is_dispatched_to_the_rung_one_dom_path`; live control $9.99 |
| DOM reader | `Result.extraction` | reader-that-ran sets the axis | WIRED | Including the empty path (`extraction` assigned before the no-offer returns) |
| `Result.extraction` | `status.json` → dashboard | `status.write` → `index.html` | WIRED | `extraction=dom` present on 3 live watches; dashboard gates its tag on it |
| `docs/retailer-evidence.md` | `evidence_check` rule 5 | configured retailer may not carry REFUSED | WIRED | Target/Amazon both REACHABLE, gate silent; strict run exits 0 |
| `docs/retailer-evidence.md` | `evidence_check` rule 6 | anchored + measured refusal lines in REFUSED sections | WIRED | Pokémon Center is the only REFUSED section; 4 measured observations across rungs 1 and 3 |
| `tests/fixtures/*` | `test_no_fixture_leaks_the_capturing_hosts_identity` | regex sweep over `*/*.html` | **PARTIAL** | Reaches every file but its postal-code rule cannot see a ZIP that is not a JSON value |

### Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|---|---|---|---|---|
| `served/boty/status.json` | `watches[].extraction` | `Result.extraction` ← the reader that ran | Yes — 3 of 13 read `dom`, 10 read `structured` | ✓ FLOWING |
| `served/boty/status.json` | `duration_seconds` | `cli.main` elapsed | Yes — 45.586, matches the run | ✓ FLOWING |
| `served/boty/status.json` | `retailers[].ok` | `monitor.assess_health` over control results | Yes — 6 entries, all from live control readings | ✓ FLOWING |
| `served/boty/index.html` | `w.extraction` | fetched `status.json` | Yes — key present on every watch | ✓ FLOWING |
| Amazon GO Plus + watch | `offers` | `parse.add_to_cart_offers` over live `/dp/` bytes | Yes — 1 offer found, then suppressed by the seller filter | ✓ FLOWING |

### Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
|---|---|---|---|
| Full gate under the service environment | `sudo systemd-run --pipe … /usr/bin/make verify` | `VERIFY: PASS` (bare), exit 0; 377 tests, mypy 15 files, 11 fixtures, **6/6 live controls**, **8/8 mutations** | ✓ PASS |
| Evidence gate, phase-close bar | `.venv/bin/python scripts/evidence_check.py --phase --strict` | `evidence check: PASS — phase`, exit 0 | ✓ PASS |
| Rule 6 fails a prose-only refusal | `check_phase` over a synthetic REFUSED section | 2 rule-6 problems | ✓ PASS |
| Rule 6 accepts a measured refusal | same, body `HTTP 403, 4,572 B` | 0 problems | ✓ PASS |
| Rule 6 hard-two higher bar | same, Amazon REFUSED with one rung-1 observation | 1 problem naming the rung shortfall | ✓ PASS |
| Rule 3 still bites at `TARGET_RETAILER_COUNT = 5` | `check_phase` over shrunk configs | fires at 4-with-target and 4-with-amazon; silent at 5 and 6 | ✓ PASS |
| Rule-6 scope pinning tests | `pytest -q` on the two named tests | 2 passed | ✓ PASS |
| Leak guard over the Walmart fixtures | guard patterns re-executed verbatim | `[]` on both, while each file contains `Redacted, 00000` | ✗ **FAIL** |
| Leak guard over a Target-shaped synthetic leak | same patterns | 4 leaks (coordinate, postal code, session token, phone) | ✓ PASS |
| Deployed daemon | `systemctl show boty.service` + live `status.json` | active since 14:20:05Z; 13 watches / 6 retailers / `extraction` on all | ✓ PASS |

`make verify` and `boty check` were each run **once** (`make verify` once by this verifier;
`boty check` not at all — the live numbers were read off `served/boty/status.json`), per the
GameStop-403 politeness constraint. No retailer saw more than the six control requests that
`make verify` itself issues.

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention exists in this repository; this project's
executable-criteria mechanism is `make verify`, `scripts/evidence_check.py` and
`scripts/mutation_check.py`, all of which were run above. Step 7c: not applicable.

### Requirements Coverage

| Requirement | Source plan | Description | Status | Evidence |
|---|---|---|---|---|
| REQ-07a | 03.1-01/02/03/05 | Target and Amazon are supported, not dropped; a retailer is dropped only when technically unreachable, and the reason recorded is the observation | ✓ SATISFIED | Both REACHABLE with anchored verdicts; rule 6 makes the requirement mechanical and was watched failing in this verifier's own process. **REQUIREMENTS.md still carries REQ-07a as `Pending` with a long Target-half-done note that predates 03.1-05/02/03** — stale record, see warnings |
| REQ-13 | 03.1-01/02/03/05 | Every matrix row states rung, extraction, robots.txt and terms | ✓ SATISFIED | Seven rows × seven cells, `_extraction_mismatch` tied to the Rung cell in both directions, `UNREAD_POSITIONS` pinned |
| REQ-08 | 03.1-04 | A full pass under two minutes at ~7 retailers | ✓ SATISFIED | 45.586 s at six retailers / 13 watches / two browser rungs, read off `duration_seconds` |
| REQ-06 | 03.1-02/03 | Every configured retailer has a control watch | ✓ SATISFIED | 6/6 controls, all IN_STOCK, zero health warnings |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `tests/fixtures/walmart/goplusplus.html` | 6 sites | Host geolocation `Redacted, 00000` in visible markup and JSON | 🛑 Blocker | See gap |
| `tests/fixtures/walmart/milk-control.html` | 2 sites | Same | 🛑 Blocker | See gap |
| `QUESTIONS.md` | 5, 51-54 | Asserts a clean working tree that is not clean | 🛑 Blocker | An open escalation to Dan rests on a false premise |
| `tests/test_fetch.py` | 394-475 | Guard with no watched-failing test | ⚠️ Warning | The one gate in this repo that is not proved to go red — and it is the one that missed |
| `scripts/evidence_check.py` | 172 | `TARGET_RETAILER_COUNT` unpinned by any test | ⚠️ Warning | The value can be raised or lowered with nothing going red |
| `.planning/REQUIREMENTS.md` | 110 | REQ-07a traceability row still `Pending` with a superseded narrative | ⚠️ Warning | Says "Target stays dropped … Amazon still unprobed"; both are false in the shipped tree |
| `tests/test_support_matrix.py` | 70, 303, 866, 872 | `TBD` literals | ℹ️ Info | Test data / documentation of what the gate rejects, not debt markers |

No `FIXME`, `XXX`, `TODO`, `HACK` or `PLACEHOLDER` in any file this phase touched. No stub
returns, no empty handlers, no hardcoded-empty props.

### Human Verification Required

None that blocks. One decision is already open with Dan and now needs re-stating rather
than re-asking: **`QUESTIONS.md` § 0e** offers a choice between leaving the leak, a
`filter-repo` rewrite, and recreating the repo, on the stated premise that the working tree
is already clean. That premise is false. The options and their costs change once the current
blob is also carrying the ZIP, so § 0e should be corrected before it is answered.

### Gaps Summary

**The phase goal is achieved.** Target and Amazon are both registered, both control-verified,
both correctly labelled on the two axes, and both visible in the live payload the deployed
daemon publishes. All six ROADMAP criteria resolve exactly as the record says they do —
five MET on measurements this verifier reproduced, and criterion 1 standing UNMET with the
reason and the declined rewrite written down. Nothing was tidied away; if anything the
record is harder on itself than it needed to be. Rule 6 is a real gate, not a sentence: it
rejects a prose-only refusal, accepts a measured one, applies a higher bar to the hard two,
and ignores § Target's two historical lines only because of a scope that has its own pinning
test. `TARGET_RETAILER_COUNT` at 5 still bites. The Amazon reseller offer is suppressed by
the seller filter before the ceiling is consulted, and reads OUT_OF_STOCK / not alertable,
which is the correct answer rather than a hedge.

**One claimed deliverable does not survive contact with the tree.** 03.1-02 found the old
fixture-leak guard passing a capture that carried a session token, an OAuth-shaped
`refreshToken`, a vendor key, this host's geolocation and five store addresses. It widened
the guard, redacted the Target capture properly — that one is genuinely clean, every
`<script>` body replaced — and then reported that four already-committed fixtures were
"all redacted here". Two of them were not. `tests/fixtures/walmart/goplusplus.html` and
`milk-control.html` still say `Redacted, 00000` — this host's own ZIP, the same fact the
redaction was for — six and two times each, including in rendered visible markup. The
redaction reached the `"postalCode"` JSON value and stopped.

The reason nobody noticed is the reason the widening happened in the first place, arriving
one turn later: the new rule is keyed on *the JSON key names Target uses*, so a ZIP that
Walmart writes as free text is invisible to it. Executed against the guard's own patterns,
both files report zero leaks. So the sentence in `QUESTIONS.md` § 0e — "this class cannot be
re-committed silently" — is not true, and it can be demonstrated in one command. This
phase's twenty commits are not yet pushed, so the working blob would carry the ZIP forward
on the next push rather than merely leaving it in history.

This does not overturn any ROADMAP success criterion and it does not make a detector wrong.
It is a privacy exposure in a public repo, in a project whose own history includes losing a
repository to exactly this class, and it is the specific claim the phase made about itself.
Fix the two fixtures, widen the guard past one retailer's spelling, attach the
watched-failing test every other gate here has, and correct § 0e before Dan answers it.

---

_Verified: 2026-08-03T14:46:48Z_
_Verifier: Claude (gsd-verifier)_
