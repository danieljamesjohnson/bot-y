---
phase: 03.1-target-and-amazon-supported
verified: 2026-08-03T14:46:48Z
re_verified: 2026-08-03T19:40:00Z
status: gaps_found
score: 12/13 must-haves verified
overrides_applied: 0
re_verification:
  round: 5
  previous_status: gaps_found
  previous_score: 12/13
  rounds:
    - round: 1
      commit_verified: 481d81d
      status: gaps_found
      score: 12/13
      finding: >
        Truth 12 failed on its first clause. The guard existed but had never been
        watched failing, and both Walmart fixtures rendered the host's city and ZIP
        as visible markup — `aria-label="<city>, <zip>, Change shipping address"` —
        which every rule missed because every rule was keyed on a JSON key name or
        a query parameter.
    - round: 2
      commit_verified: f2aa40d
      status: gaps_found
      score: 12/13
      finding: >
        Real fix, same axis missed. City/ZIP/store-name gone and the guard extracted
        and watched failing on six mutations — but the store number survived under
        `pickupStore`/`deliveryStore`/`storeId=` after being redacted under `storeId`,
        `docs/retailer-evidence.md` still republished the values by name, and the new
        synthetic test cases were seeded with the host's REAL city and ZIP.
    - round: 3
      commit_verified: e871377
      status: gaps_found
      score: 12/13
      finding: >
        Named the root cause the two prior fixes shared — they added no leak RULE at
        all, only hand-redaction plus a scope pin. 31 shapes probed against the guard,
        29 passed clean including all four that commit had just removed; 11 of 15
        mutations silent, among them "add the real city and ZIP to `allowed`" (one
        line, nothing red) and "narrow the scan to one retailer".
    - round: 4
      commit_verified: 9d8383c
      status: gaps_found
      score: 12/13
      finding: >
        By far the strongest round to that point, and still not closed. Blind shapes
        29/31 -> 4/31; all 13 keyed rules watched failing; the `allowed` hole and the
        by-directory/by-suffix scope holes closed; the real public IP found in a
        PLANNING file and removed from the working tree. But a real geolocation value
        survived undetected in a shipped fixture (`region_code=` x3 in
        `tests/fixtures/bestbuy/unresolved-sku.html`), a key redacted by hand that
        round still had no rule (`shippingZipcode`), 11 mutations remained silent, and
        9 cheap-disable paths remained green.
    - round: 5
      commit_verified: b645740
      status: gaps_found
      score: 12/13
      finding: >
        Every round-4 finding is closed, and closed by mechanism. 31/31 probe shapes
        caught (0 blind), 87 of 96 mutations RED, the `?state=` case decision pinned in
        both directions, `02-REVIEW.md`'s garbled instruction and hop chain restored,
        fixtures byte-length-exact and parse-identical. But the SAME defect recurs in a
        fifth spelling and in a fifth tracked file: two shipped GameStop fixtures carry
        a live store number as `data-preferred-store-id=` that neither store rule can
        see; the store NAME class has no rule at all (its redaction placeholder
        `REDACTED SUPERCENTER` sits in `allowed` for a rule that does not exist, and 26
        real store names ship in a Best Buy fixture); and the host's real ISP value
        `REDACTED` was scrubbed out of a fixture and out of `02-REVIEW.md` by this commit
        and simultaneously written INTO `tests/test_fetch.py` as a "synthetic" case,
        under a docstring reading "EVERY value here is invented."
  gaps_closed:
    - "`region_code=` is gone from `tests/fixtures/bestbuy/unresolved-sku.html` (-> `XX` x3), and so are `georegion=285` (-> `0`) and `network_type=REDACTED` (-> `REDACTED`), which round 4 had not asked for. The marker tuple now carries all 16 EdgeScape keys"
    - "`shippingZipcode` (and `shipping_zip`) are in the K5/J2 postal alternation — deleting that entry is RED. Round 4's hand-redaction-without-a-rule on this key is closed"
    - "The ZIP+4 rule now has a leak case; deleting the rule and dropping its append are both RED. The one rule carrying a do-not-delete comment is no longer the only undeletable-by-nothing rule"
    - "15 of 16 EdgeScape markers are individually watched (was 0 of 8). `lat`, `long`, `county`, `areacode`, `fips`, `dma`, `region_code`, `georegion`, `network_type`, `pmsa`, `msa`, `asnum`, `timezone`, `continent` and `zip` each turn the suite RED when dropped"
    - "Both previously-unwatched IP headers are watched: dropping `x-forwarded-for` — the spelling that carried the IP x3 in the actual incident — and dropping `client-ip` are both RED"
    - "Scope is pinned per FILE. Round 4's two live holes are shut: one `.html` per directory is RED, and skipping the Walmart pages while keeping the directory via its `.json` is RED"
    - "The free-text rule's ordering defect is closed: `continue` -> `break` is RED, pinned by the case `Redacted, 00000 ... later ... Exampleville, 99999`. The keyed loop's equivalent (`break` unconditionally) is RED too"
    - "All six round-4 regex weakenings are RED: 8-character city name (city rule, free-text rule and `City, ST ZIP` rule), leading-9 ZIP (K5 and json postal), and header-IP-flags-only-203.0.113.x. `test_each_rule_fires_on_a_value_of_the_REAL_shape_not_just_the_synthetic_one` is the mechanism and it works"
    - "The 31-shape probe set is fully caught: 31/31, 0 blind (round 4: 4 blind, 2 of them live in the tree). Both live ones — `region_code=` and `shippingZipcode` — are caught now"
    - "The `?state=` case decision holds and is pinned in BOTH directions. GameStop's `&state=ca` (TrustArc CCPA config) leaves both GameStop fixtures clean; `?state=TX` and `&state=CA` are caught; deleting the rule is RED and widening it to case-insensitive is also RED (it would redden the two GameStop fixtures)"
    - "`02-REVIEW.md` is repaired. `replace 192.0.2.1 with 192.0.2.1` is now `replace the real IP with 192.0.2.1` with a dated note; the `x-forwarded-for` chain reads `<this host's public IP>, <akamai hop>, <akamai hop>`, restoring the two Akamai hops; `network_type=<isp>`, `region_code=<ST>`, `city=<CITY>`, `georegion=<n>`. Round 4's item 4(b) is fully closed and the ISP name is out of that file"
    - "Fixture integrity holds exactly as claimed. Both Best Buy edits are byte-length-identical; `pikachu-control.html` is a pure `\"storeId\":\"7\"` -> `\"0\"` x13 substitution and nothing else; `unresolved-sku.html` is the same x4 plus `region_code`, `georegion` and `network_type`. `ldjson_offers` -> IN_STOCK $59.99 'Best Buy' / None; `nextdata_offers` -> IN_STOCK $229.99 'Clove Brothers LLC' / IN_STOCK $2.42 'Walmart.com'; Amazon `add_to_cart_offers` -> $219.00 'LO Store...' / $9.99 'Amazon.com'. Both Amazon fixtures are byte-identical to the round-1 baseline `481d81d`. `test_an_unresolved_sku_is_diagnosed_as_unresolved_not_as_our_parser` passes"
    - "Running the shipped guard over EVERY tracked file (not just fixtures) trips on nothing but `tests/test_fetch.py`'s own synthetic corpus and the three audit records. No fixture trips it. The classes the guard covers really are clean"
  gaps_remaining:
    - "A live store number ships in TWO fixtures the guard cannot see: `data-preferred-store-id=\"0\"` in `tests/fixtures/gamestop/goplusplus.html` and `=\"0606\"` in `tests/fixtures/gamestop/ps5-control.html`. Both rules are keyed to `\"...store...\":` JSON or `storeId=` URL forms; the `data-` attribute is a third spelling. The suite is green with them in the tree — that is the positive control"
    - "The store NAME class has NO rule at all. `REDACTED SUPERCENTER` sits in the `allowed` set as the placeholder for a rule that does not exist — removing it leaves the suite green, which proves nothing can produce it. 26 real store names ship as `\"storeName\":\"...\"` in `tests/fixtures/bestbuy/pikachu-control.html`"
    - "`?network_type=REDACTED` at `tests/test_fetch.py:623` is the host's REAL ISP. The same commit scrubbed it out of `unresolved-sku.html` and out of `02-REVIEW.md` (-> `<isp>`) and wrote it into a test whose docstring says `EVERY value here is invented.` Fifth round in which the fix puts a real value into a tracked file"
    - "9 of 96 mutations are silent, and 8 of the 9 let a real-shaped value escape: EdgeScape marker `city` is still shadowed by `zip`; K1 store number re-narrowable to `\\d{2,}` (re-admitting the exact `\"storeId\":\"7\"` this commit found); ZIP+4 narrowable to leading-9; K6 short-key coordinate narrowable to 3+ decimals; the json_markers loop still has the ORDERING defect (`break` unconditionally is silent); json_markers' `re.I` is droppable; and the header-IP rule is narrowable two ways (3-digit first octet, gap 12->2)"
    - "45 of 50 newly-probed carriers of this class are blind — cookies, `data-*` attributes, `<meta name=\"geo.*\">`, URL- and base64-encoding, path segments, split `x`/`y` coordinates, JSON-LD `addressLocality`/`addressRegion`, a ZIP in a form value, bare and spaced phone digits. Two of the 45 are LIVE in the tree (the two above)"
    - "QUESTIONS.md §0e is now accurate on the state, the ZIP, the city and the IP — and still wrong on two items. Line 132's list of what fixtures are clean of still asserts `the store name` and `the store id`; the fourth correction retracted only the state. And lines 213/226-228 assert `24 mutations run, zero silent` and `deleting any rule, weakening any pattern, or narrowing the scope started turning the suite red — which is where it is now`; one rule deletion and four pattern weakenings are still green"
  regressions: []
gaps:
  - truth: "The fixture leak guard is widened, watched failing, and the current tree is clean"
    status: partial
    reason: >
      Say the size of this round's fix first, because it is the largest of the five and
      every single round-4 finding is closed by mechanism rather than by inspection.
      The 31-shape probe set is now 31/31 caught with nothing blind — round 4 had four
      blind, two of them live in the tree. 87 of 96 mutations turn the suite RED,
      including all six regex-weakenings round 4 named, both live scope holes, the
      free-text ordering defect, all three IP headers, the whole ZIP+4 rule and 15 of
      16 EdgeScape markers. The `?state=` case decision is not just documented but
      pinned in both directions: delete it and the suite goes red, widen it to
      case-insensitive and the suite goes red because it would redden two GameStop
      fixtures. `02-REVIEW.md` is genuinely repaired — the garbled `replace X with X`
      instruction and the erased Akamai hops are both restored, and the ISP name is
      out of it. The fixtures are byte-length-exact and parse-identical. Running the
      shipped guard over every tracked file in the repo trips on nothing but its own
      synthetic corpus and the three audit records.

      The truth is still not true, on the same axis, for the fifth consecutive round —
      and this time the failing instances predate all five rounds and were never found,
      because every round has probed the spellings it had just been shown.

      (a) A LIVE STORE NUMBER SHIPS IN TWO FIXTURES, IN A THIRD SPELLING.
      `tests/fixtures/gamestop/goplusplus.html` carries
      `data-preferred-store-id="0"` and `tests/fixtures/gamestop/ps5-control.html`
      carries `data-preferred-store-id="0"`. The guard has two store-number rules —
      K1 for the JSON form `"...store...":"N"` and K2 for the URL form `storeId=N` —
      four synthetic cases and three real-shape cases. A `data-` attribute is neither
      form, and both values pass clean. This is the same one-spelling failure as round
      4's `region_code`, and the same as round 2's `pickupStore`: the source comment
      three lines above K1 says *"A store number IS a locator: it resolves publicly to
      one address"*, and the repo ships two. GameStop distinguishes them from its own
      defaults in the same tag — `data-default-store="null"`,
      `data-defaultstore-id="null"` — so `preferred` carries a value that `default`
      does not. The positive control needs no construction: the suite is 383/383 green
      with both of them in the tree right now.

      (b) THE STORE NAME CLASS HAS NO RULE, AND THE ALLOW-LIST PROVES IT.
      `allowed` contains `REDACTED SUPERCENTER` — the placeholder written over
      Walmart's store name in round 2, because a store name "locates this host just as
      well as the ZIP does" (§0e's own words). No rule in `_identity_leaks` can ever
      produce that value: removing it from `allowed` leaves the suite 383/383 green,
      and `"storeName":"<a real store name>"` passes clean under every rule. Meanwhile
      `tests/fixtures/bestbuy/pikachu-control.html` ships 26 real store names as
      `"storeName":"Roseville"`, `"Maplewood"`, `"Oakdale"` and so on. Those 26 read as
      Best Buy's own Minnesota region rather than as this host's neighbourhood — but
      the repo has already ruled on exactly that argument: *"Best Buy's default-store
      ZIPs redacted too — not allow-listed. A ZIP in a fixture is a ZIP; deciding
      case-by-case which are 'theirs' is how this started."* The class §0e lists as
      clean is the one class with no enforcement behind it at all.

      (c) THE FIX PUT A REAL VALUE INTO A TRACKED FILE. AGAIN. FIFTH ROUND.
      `b645740` scrubbed `network_type=REDACTED` out of `tests/fixtures/bestbuy/
      unresolved-sku.html` (-> `REDACTED`, x2) and out of `.planning/phases/
      02-five-retailers-green/02-REVIEW.md` (-> `network_type=<isp>`), correctly
      treating it as a real fact about this host. In the same commit it added
      `"?network_type=REDACTED": "geolocation network_type"` to `tests/test_fetch.py:623`
      — twelve lines below a docstring that reads **"EVERY value here is invented."**
      and eight lines below a source comment that reads "`network_type` names the ISP."
      The commit message says it found and re-invented six such cases (georegion, fips,
      dma, pmsa, msa, asnum). It found six and introduced a seventh.

      (d) NINE MUTATIONS ARE STILL SILENT, AND EIGHT LET A REAL VALUE OUT. §0e claims
      "24 mutations run, zero silent" and that the guard only stopped being weakenable
      "when deleting *any* rule, weakening *any* pattern, or narrowing the scope
      started turning the suite red — which is where it is now." Of 96 mutations run
      here, 9 stay green. Dropping the EdgeScape marker `city` is a whole-rule deletion
      that stays green — it is the one marker of sixteen without its own case, still
      shadowed by `zip` behind `?city=Exampleville&zip=99999`, and it is the marker at
      the centre of the `02-REVIEW.md` record. Re-narrowing K1 to `\d{2,}` stays green
      and re-admits `{"storeId":"7"}` — the precise value this commit's own shape test
      found and redacted. Narrowing ZIP+4 to a leading 9, and the short-key coordinate
      rule to three decimal places, both stay green. The json_markers loop still has
      the ordering defect that was fixed for the other two loops: making it `break`
      unconditionally is silent, and `{"zipCode":"00000"} … {"zipCode":"<real>"}` — the
      exact Walmart shape — then escapes. Its `re.I` is droppable silently. And the
      header-IP rule, the one guarding the most serious class in this whole thread, can
      be narrowed two ways with nothing red: require a three-digit first octet (both
      synthetic IPs have one; a `99.x.x.x` address does not), or shrink the
      header-to-address gap from 12 characters to 2 (both synthetic cases use
      `header: ip`; the form that actually leaked, `"true-client-ip":"<ip>"`, needs 3).

      (e) THE CARRIER SURFACE, LOOKED FOR RATHER THAN RE-CHECKED. Fifty carriers of
      this class that no prior round had probed: 45 are blind. Cookies
      (`Set-Cookie: ...zip=...`, `document.cookie=`), `data-` attributes,
      `<meta name="geo.position|geo.placename|ICBM">`, `og:locality`/`og:postal-code`,
      URL-encoded and base64-encoded payloads, HTML entities, `\uXXXX` escapes, store
      numbers in path segments (`/store/202/`, `/stores/<city>-<st>-<n>`), coordinates
      split into `x`/`y` or an array or a `geo:` href, phones written with spaces or as
      bare digits or in E.164, JSON-LD `addressLocality`/`addressRegion` alone, a ZIP in
      an `<input value=>`. Two of the 45 are live in the tree today (a and b above).
      The rest are not findings on their own — but `document.cookie=` appears in both
      GameStop fixtures and `\uXXXX` runs appear in eight of twelve, so the carriers
      are present even where the values currently are not.

      Scope and history, unchanged and stated again because §0e turns on it. Everything
      above is in UNPUSHED commits (`origin/main` = `861b24a`, 24 behind), so all of it
      is fixable at zero history cost. What is live and public right now is exactly what
      it was: the real public IP x3 and the full EdgeScape record in `02-REVIEW.md`, the
      ZIP in both Walmart fixtures, the store id and state in the Best Buy fixtures.
      That is the decision still sitting with Dan and nothing in this round changes it.
    artifacts:
      - path: "tests/fixtures/gamestop/goplusplus.html"
        issue: "Ships `data-preferred-store-id=\"0\"` — a live store number, undetected. Reproduce: `git grep -n 'data-preferred-store-id' -- tests/fixtures/`; the suite is 383/383 green with it present"
      - path: "tests/fixtures/gamestop/ps5-control.html"
        issue: "Ships `data-preferred-store-id=\"0606\"` — same class, different value, same blindness. Sits beside `data-default-store=\"null\"`, so it is a session value rather than a site constant"
      - path: "tests/fixtures/bestbuy/pikachu-control.html"
        issue: "Ships 26 real store names as `\"storeName\":\"...\"`. The store-name class has no rule; `REDACTED SUPERCENTER` in `allowed` is a placeholder for a rule that does not exist (removing it from `allowed` leaves the suite green)"
      - path: "tests/test_fetch.py"
        issue: "Line 623 `\"?network_type=REDACTED\"` is the host's REAL ISP, scrubbed out of a fixture and out of `02-REVIEW.md` by this same commit, under a docstring at line 585 asserting `EVERY value here is invented.` Separately: 9 of 96 mutations are silent — EdgeScape marker `city` (whole-rule deletion), K1 `\\d+`->`\\d{2,}`, ZIP+4 leading-9, K6 coordinate 3+ decimals, json_markers `break`-unconditionally (the ordering defect, unfixed on the third loop), json_markers `re.I`, header-IP 3-digit-first-octet, header-IP gap 12->2, and the EdgeScape allow-list case-fold (cry-wolf direction only). `REDACTED-CITY` and `NOT-AVAILABLE` are also dead allow-list entries"
      - path: "QUESTIONS.md"
        issue: "§0e line 132 still asserts every `tests/fixtures/` file is clean of `the store name` and `the store id`; the FOURTH CORRECTION retracted only the state. Lines 213 and 226-228 assert `24 mutations run, zero silent` and that weakening any pattern or deleting any rule now turns the suite red — one rule deletion and four pattern weakenings are still green"
      - path: "docs/retailer-evidence.md"
        issue: "Still names the real ZIP once (line ~2275). Unchanged from round 4. §0e discloses it and puts `may the audit trail name what it audits` to Dan — correctly — but this is the only one of the four files that is user-facing documentation rather than an audit record"
    missing:
      - "Add a `data-` attribute form to the store-number rules — e.g. extend K2 to `\\bdata-[a-z-]*store[a-z-]*-?id\\s*=\\s*\"?(\\d+)` — with its own synthetic case AND its own real-shape case, then redact `data-preferred-store-id` in both GameStop fixtures. Rule first, redaction second"
      - "Add a store-NAME rule (`\"store_?[Nn]ame\"\\s*:\\s*\"([^\"]+)\"`, allow-listing only the placeholder) so that `REDACTED SUPERCENTER` in `allowed` becomes load-bearing, and decide explicitly what happens to Best Buy's 26 — redact them, as the repo's own stated policy on Best Buy's ZIPs requires, or write down why store names get the case-by-case treatment ZIPs were denied"
      - "Replace `REDACTED` in `tests/test_fetch.py:623` with an invented ISP token, and make the docstring's claim checkable rather than asserted — the same commit scrubbed this value from two other tracked files"
      - "Give the EdgeScape marker `city` its own single-marker case (`?city=Exampleville` with expected `geolocation city`) so it stops being shadowed by `zip` — 15 of 16 markers already have one"
      - "Add real-shape cases for the four unpinned patterns: a one-digit JSON store number (`{\"storeId\":\"7\"}` — the value this commit found), a ZIP+4 not starting with 9, a two-decimal short-key coordinate, and an IP with a two-digit first octet written as `\"true-client-ip\":\"<ip>\"` (the quoted form, which needs a gap of 3)"
      - "Fix the json_markers loop the way the other two loops were fixed: only `break` on a REPORTED leak, and add an ordering case (`{\"zipCode\":\"00000\"} … {\"zipCode\":\"<invented>\"}`). Add a mixed-case key case so `re.I` is load-bearing"
      - "Correct §0e line 132 on the store name and the store id, and soften lines 213/226-228 from `zero silent` to the count actually measured — the sentence `which is where it is now` has now been wrong in four consecutive corrections"
      - "Consider one structural change instead of a sixth spelling-by-spelling round: assert that the guard's rule table covers a declared list of CLASSES x CARRIERS (JSON key, query param, `data-` attribute, path segment, free text, cookie), so a missing cell is visible without anyone having to think of the spelling first"
deferred: []
---

# Phase 3.1: Target and Amazon, Supported — Verification Report

**Phase Goal:** Target and Amazon reporting trustworthy stock for the GO Plus +, control-verified, on whatever rung reaches them.
**Verified:** 2026-08-03T14:46:48Z
**Re-verified:** 2026-08-03T18:20:00Z — round 4, after gap closure in `9d8383c`
**Re-verified:** 2026-08-03T19:40:00Z — round 5, after gap closure in `b645740`
**Status:** gaps_found (unchanged)
**Score:** 12/13 (unchanged)
**Rounds:** initial (`481d81d`) → `f2aa40d` → `e871377` → `9d8383c` → `b645740`. Truth 12 has
failed in all five, on the same axis each time. Round 5 closes every round-4 finding and fails
on two instances that predate all five rounds.

Everything in this report was executed, not read. `make verify` was run exactly once,
under the service `EnvironmentFile`, per the politeness constraint; every other number
is read off `served/boty/status.json` or produced by running the gate in this verifier's
own process against synthetic inputs. **The re-verification pass ran no live retailer
requests at all** — only `pytest tests/ -q` (offline, 379 tests, 2.6 s) and mutations of it.
**Round 4 likewise ran nothing live** — no `make verify`, no `boty check`, per the standing
politeness constraint. Every round-4 number came from `.venv/bin/python -B -m pytest tests/ -q
-p no:cacheprovider` (382 passed, 3.0 s) and from executing the shipped `_identity_leaks` in
this verifier's own process. `-B` and `-p no:cacheprovider` throughout, because a stale `.pyc`
produced a false RED in round 3.

---

## Round 5 — truth 12 only, after `b645740`

Scope: truth 12 and nothing else. Truths 1-11 and 13 are unchanged and were not re-run;
`b645740` touches no production module (`git diff b645740^ HEAD --name-only` = two planning
docs, `QUESTIONS.md`, two Best Buy fixtures, `tests/test_fetch.py`).

**No live retailer requests, no `make verify`, no `boty check`** — those were run by the
executor after the fix (bare `VERIFY: PASS`, 383 tests, 6/6 live controls, 8/8 mutations) and
re-running them costs six retailers a round of traffic for no new information. Every number
below came from `.venv/bin/python -B -m pytest tests/ -q -p no:cacheprovider` (383 passed,
3.8 s) or from executing the shipped `_identity_leaks` in this verifier's own process.
`-B` and `-p no:cacheprovider` throughout. Working tree `git status --porcelain` empty before
and after; the mutation harness asserts the source is restored byte-exact after every run.

**This is the strongest of the five rounds and every round-4 finding is closed.** Said first
and without hedging, because the pattern of these reports has been to lead with the failure.

### 1. The 31-shape probe set, re-run: 4 blind → **0 blind**

Same 31 shapes as rounds 3 and 4, executed against the shipped `_identity_leaks`.

| Round | Caught | Blind |
|---|---|---|
| 3 (`e871377`) | 2/31 | **29** |
| 4 (`9d8383c`) | 27/31 | 4 |
| **5 (`b645740`)** | **31/31** | **0** |

The four that were blind are all caught now, with the classification the rule intends:

| # | Shape | Now |
|---|---|---|
| 12 | `region_code=<ST>` — EdgeScape query form of the state | `geolocation region_code=XX` |
| 21 | `"shippingZipcode":"<zip>"` | `postal code 00000` |
| 8 | `"store_id":"<n>"` — snake_case store number | `store number 202` |
| 13 | `?state=<ST>` — query form of the state | `geolocation state=TX` |

**Nothing on the list I was given still passes clean.** Item 1 of the brief is answered:
zero.

### 2. Mutation sweep — 96 mutations, 9 silent

Every keyed rule, every `json_markers` entry, every EdgeScape marker individually, every IP
header individually, both scope functions, every allow-list entry, all five loop bodies
(append removal, `break`/`continue` semantics, outer-loop `break`), and a 26-strong
regex-weakening battery of exactly the kind that passed round 4. Each mutation applied to
`tests/test_fetch.py`, full offline suite, source restored from the in-memory original.

**87 RED. All of round 4's eleven silent mutations and all nine of its cheap-disable paths
are closed:**

| Round-4 finding | Round-5 result |
|---|---|
| ZIP+4 rule deletable silently | **RED** (and dropping its append is RED) |
| 8 EdgeScape markers individually unwatched | **RED x14 of 15 now-tested markers** (`city` is the exception — below) |
| `x-forwarded-for` and `client-ip` unwatched | **RED, both** |
| `_fixture_files` → one `.html` per directory | **RED** |
| `_fixture_files` → skip the Walmart pages | **RED** |
| Free-text carve-out `continue` → `break` | **RED** — the ordering case works |
| 8-char city name (city rule / free-text rule / `City, ST ZIP` rule) | **RED x3** |
| Leading-9 ZIP (K5 and json postal) | **RED x2** |
| Header IP flags only `203.0.113.x` | **RED** |

And the new axes hold too: `KL3` breaking out of the outer keyed loop is RED, `E17`/`E18` on
the EdgeScape loop body are RED, `L1`-`L4` on the allow-list are RED (real city+ZIP, real
state, real store number, Best Buy's ZIPs), all four `json_markers` deletions are RED, all
four phone spellings are RED, `S1`-`S6` on the scope are RED.

**Nine mutations stay green. Eight of the nine let a value of the real shape out** — proved
by loading the mutated module and probing it, not inferred from the test result:

| Mutation | Result | Real value that then escapes |
|---|---|---|
| Drop EdgeScape marker **`city`** | GREEN | `?city=Redacted&zip=00000` → `[]`. The one marker of 16 with no case of its own; still shadowed by `zip` behind `?city=Exampleville&zip=99999`, whose `expected` is the bare string `"geolocation"` |
| K1 store number `\d+` → `\d{2,}` | GREEN | `{"storeId":"7"}` and `{"pickupStore":"3"}` → `[]`. This re-admits **the exact value this commit's own shape test found and redacted 17 times** |
| ZIP+4 → require a leading 9 | GREEN | `00000-1234`, `55423-0117` → `[]`. There is no ZIP+4 entry in the shape test |
| K6 short-key coordinate → 3+ decimals | GREEN | `{"lat":"33.15","lng":"-96.82"}` → `[]`. The shape test's short-key case has six decimals |
| `json_markers` loop → `break` unconditionally | GREEN | `{"zipCode":"00000"} … {"zipCode":"00000"}` → `[]`. **This is the ordering defect the commit fixed for the free-text loop and the keyed loop, on the third loop it did not fix** — and the placeholder-before-real-value shape is precisely the Walmart one |
| `json_markers` → drop `re.I` | GREEN | `{"ZipCode":"00000"}`, `{"POSTALCODE":"00000"}`, `{"Latitude":"33.15"}` → `[]` |
| Header IP → 3-digit first octet | GREEN | `true-client-ip: 192.0.2.1` → `[]`. Both synthetic IPs (`203.0.113.7`, `198.51.100.7`) have a 3-digit first octet |
| Header IP → gap `{0,12}` → `{0,2}` | GREEN | `"true-client-ip":"192.0.2.1"` → `[]`. Both synthetic cases are `header: ip` (2 chars); **the form that actually leaked is the JSON-quoted one, which needs 3** |
| EdgeScape → case-sensitive `allowed` | GREEN | *no leak escapes* — cry-wolf direction only (`?city=Redacted` starts firing). Cosmetic; listed for completeness |

Two of these matter more than their count suggests. The store-number one reverts, silently,
the single live miss this commit's headline mechanism caught. And the two header-IP ones sit
on the class that caused this repo to be deleted and recreated — the rule guarding the public
IP is pinned only against values shaped like its own two synthetic constants.

**Also found: three dead allow-list entries.** Removing `REDACTED SUPERCENTER`,
`REDACTED-CITY` or `NOT-AVAILABLE` from `allowed` leaves the suite green — no rule can
produce any of them. (`REDACTED`, `00000`, `XX`, `0.0000`/`0.0` are all load-bearing: RED.)
`REDACTED SUPERCENTER` being dead is not a curiosity; it is §3 below.

### 3. A shape of this class still unenforced? Yes — three, two of them live

Fifty carriers probed that no prior round had looked at. **45 are blind.** Most are
theoretical for the six retailers currently captured. Three are not.

#### (a) A live store number, in two shipped fixtures, in a third spelling

```
tests/fixtures/gamestop/goplusplus.html   data-preferred-store-id="0"
tests/fixtures/gamestop/ps5-control.html  data-preferred-store-id="0"
```

Both pass the guard clean. K1 is keyed to the JSON form `"…store…":"N"`; K2 to the URL form
`storeId=N` / `storeId%3DN`. A `data-` attribute is neither. The suite is **383/383 green with
both values in the tree right now** — that is the positive control, no construction needed.

This is not an edge case the rules merely failed to reach. The comment three lines above K1
reads *"A store number IS a locator: it resolves publicly to one address."* The rule set has
two store-number rules, four synthetic cases and three real-shape cases for it. And two ship.

It is also unlikely to be a site constant: GameStop writes `data-default-store="null"` and
`data-defaultstore-id="null"` in the same tag, and the two captures — taken minutes apart in
one session — carry **different** preferred values. Whatever set them, it was set for this
session, not baked into the page.

These values predate all five rounds. `tests/fixtures/gamestop/*` is byte-identical to the
round-1 baseline `481d81d`. Nobody found them because every round probed the spellings it had
just been shown.

#### (b) The store-name class has no rule at all — and the allow-list proves it

`allowed` contains `REDACTED SUPERCENTER`. That token exists because round 2 wrote it over
Walmart's store name, on the reasoning §0e still states: a store name *"locates this host just
as well as the ZIP does."*

**No rule in `_identity_leaks` can produce that value.** Removing it from `allowed` leaves the
suite 383/383 green. `"storeName":"Redacted Supercenter"` passes clean. So does
the bare free-text form. The placeholder is a monument to a rule that was never written.

Meanwhile `tests/fixtures/bestbuy/pikachu-control.html` ships **26** real store names:

```
Ames, Baxter, Bloomington Warehouse, Burnsville, Coon Rapids, Dubuque, Duluth, Eagan,
Eau Claire, Eden Prairie, Eden Prairie Outlet, Fargo, La Crosse, Mankato, Maple Grove,
Maplewood, Minnetonka, Oakdale, Richfield, Rochester, Roseville, Sioux Falls, St Cloud,
Stevens Point, Waterloo, Wausau
```

These read as Best Buy's own upper-Midwest region rather than as this host's neighbourhood,
and that is a fair reading. But the repo has already ruled on exactly this argument, in §0e,
about exactly this fixture: *"Best Buy's default-store ZIPs redacted too — not allow-listed. A
ZIP in a fixture is a ZIP; deciding case-by-case which are 'theirs' is how this started."*

The class §0e lists as clean is the one class with nothing enforcing it.

#### (c) The fix put a real value into a tracked file — fifth round running

`b645740` scrubbed `network_type=REDACTED` out of `tests/fixtures/bestbuy/unresolved-sku.html`
(→ `network_type=REDACTED`, x2) and out of `02-REVIEW.md` (→ `network_type=<isp>`), correctly
treating the ISP as a real fact about this host.

In the same commit it added, at `tests/test_fetch.py:623`:

```python
"?network_type=REDACTED": "geolocation network_type",
```

Twelve lines below a docstring reading **"EVERY value here is invented."** and eight lines
below a source comment reading "`network_type` names the ISP." The commit message says it
found six synthetic cases carrying real leak values (georegion, fips, dma, pmsa, msa, asnum)
and re-invented them all. It found six and introduced a seventh, in the same edit.

`REDACTED` is coarse — an ISP brand, not a city or an address. The finding is not its
sensitivity. It is that the repo cannot simultaneously hold *"this value must be scrubbed from
two tracked files"* and *"this value is a fine test constant."* That contradiction is the same
shape as round 4's (`state is a leak class` asserted while shipping a state), one round later.

#### The rest of the 45, for the record

Blind but not currently carrying anything: cookies (`Set-Cookie: …zip=…`, `document.cookie=`
— present in both GameStop fixtures), `data-zip`/`data-city`/`data-latitude`,
`<meta name="geo.position|geo.placename|ICBM">`, `og:locality`/`og:postal-code`, URL-encoded
and base64-encoded payloads, HTML entities, `\uXXXX` escapes (present in 8 of 12 fixtures),
store numbers in path segments (`/store/202/`, `/stores/<city>-<st>-<n>`,
`/store-locator/<zip>`), coordinates split into `x`/`y`, as an array, as a `geo:` href or in a
maps URL, phones written with spaces / as ten bare digits / in E.164, JSON-LD
`addressLocality` and `addressRegion` alone, a ZIP in an `<input value=>`.

Caught, pleasingly: all three punctuated `tel:` forms (`tel:+1-972-555-0100`,
`tel:+1.972.555.0100`, `tel:+1 (972) 555-0100`), JSON-LD `PostalAddress`, and `network_type`
in prose.

### 4. Repo-wide, case-insensitive — and whether §0e is accurate this time

Two passes over every tracked file: the shipped guard run against each, and targeted
case-insensitive greps for the known real values and the blind carriers.

**Pass A — the shipped guard over every tracked file.** Four files trip it:

| File | Class | Verdict |
|---|---|---|
| `QUESTIONS.md` | city, ZIP, state, store number | **acknowledged audit trail** (§0e) |
| `03.1-VERIFICATION.md` | state, store number, ISP | **acknowledged audit trail** (this file) |
| `03.1-02-SUMMARY.md` | ZIP, state, coordinate | **acknowledged audit trail** |
| `tests/test_fetch.py` | its own synthetic corpus | by construction — **except `REDACTED`**, §3(c) |

**No fixture trips it.** For the classes the guard covers, the tree really is clean.

**Pass B — targeted, case-insensitive.**

| Value | Where it survives |
|---|---|
| The real public IP | **0 hits**, tree-wide. Still gone |
| EdgeScape city / county / FIPS / DMA / areacode / lat / long | **0 hits** with real values; `unresolved-sku.html` carries 38 markers, all `=REDACTED`/`0` |
| `region_code` | `XX` only. Round 4's finding is closed |
| City `Redacted` | `QUESTIONS.md` only (x4) — audit trail |
| ZIP `7503x` | `QUESTIONS.md`, `03.1-VERIFICATION.md` (`75039`, a 1worldsync path), `03.1-02-SUMMARY.md` — audit trail — **plus `docs/retailer-evidence.md` x1**, disclosed in §0e, unchanged from round 4 |
| Best Buy ZIPs `55113`/`55423` | audit records only |
| `REDACTED` | `03.1-VERIFICATION.md` (audit) **and `tests/test_fetch.py` (not audit)** |
| Store number in a `data-` attribute | **`gamestop/goplusplus.html`, `gamestop/ps5-control.html` — not audit** |
| `"storeName"` real values | **`bestbuy/pikachu-control.html` x26 — not audit** |
| Any global IPv4 literal in a fixture | 36 distinct, **all false positives** — SVG path coordinates, jQuery version strings, two Akamai hop addresses. Checked by hand |

That last row is worth recording rather than reporting: `02-REVIEW.md`'s Fix step 2 prescribes
scanning fixtures "for any public IPv4 literal." Implemented literally, it would fire 36 times
on `<path d="M3.38 91.9 …">`. The header-anchored rule the repo actually built is the right
call, and the prescription should be marked as satisfied-in-spirit rather than read as
outstanding.

**Is §0e accurate now?** Partly — more accurate than in any prior round, and wrong on two
things.

**Accurate, verified by execution:**
- The four-file grep claim (line 126-129) — `Redacted|7503[0-9]` hits exactly `QUESTIONS.md`,
  `03.1-VERIFICATION.md`, `03.1-02-SUMMARY.md`, `docs/retailer-evidence.md`. I ran it.
- The FOURTH CORRECTION on the state — `region_code` is genuinely gone from the fixture.
- The public IP, the city, the ZIP and the fine-grained EdgeScape record: all genuinely absent
  from the working tree.
- The unpushed/pushed distinction, and that the pushed exposure is unchanged.
- The `02-REVIEW.md` garbling account, and that the restored instruction is the one still not
  carried out.

**Wrong, item 1 — line 132.** *"Every `tests/fixtures/` file is clean of the city, the ZIP, the
store name, the store id and the state."* The FOURTH CORRECTION retracts the **state** and
only the state. The **store id** is not clean: `data-preferred-store-id` x2, §3(a). The
**store name** has no rule at all and 26 real ones ship, §3(b). Five rounds, five status
claims, and this sentence has now been wrong about three of its five nouns at different times.

**Wrong, item 2 — lines 213 and 226-228.** *"24 mutations run, zero silent"* and *"It only
stopped being true when deleting **any** rule, weakening **any** pattern, or narrowing the
scope started turning the suite red — which is where it is now."* Of 96 mutations, 9 stay
green: one whole-rule deletion (EdgeScape `city`) and four pattern weakenings, §2. The 24 the
executor ran were presumably all red; the generalisation from 24 to *any* is what fails.

**Wrong, item 3 — `tests/test_fetch.py:585`,** not §0e but the same claim in the same voice:
*"EVERY value here is invented."* `REDACTED` is not.

### 5. Fixture integrity after the `storeId` and `region_code` edits — clean

| Check | Result |
|---|---|
| `pikachu-control.html` byte length | **1 138 146 → 1 138 146**, unchanged |
| `pikachu-control.html` edit content | **pure substitution**: `old.replace('"storeId":"7"','"storeId":"0"') == new` is exactly True. x13. No other byte moved |
| `unresolved-sku.html` byte length | **921 811 → 921 811**, unchanged |
| `unresolved-sku.html` edit content | `"storeId":"7"`→`"0"` x4, `region_code=XX`→`XX` x3, **and two the commit message did not enumerate**: `georegion=285`→`0` x3 and `network_type=REDACTED`→`REDACTED` x2. All length-preserving. Nothing else changed |
| `ldjson_offers(bestbuy/pikachu-control)` | `[Offer(available=True, price=59.99, seller='Best Buy', raw_availability='InStock')]` — **IN_STOCK $59.99 'Best Buy'** |
| `ldjson_offers(bestbuy/unresolved-sku)` | **`None`** — the unresolved-SKU path intact |
| `ldjson_offers(walmart/goplusplus, walmart/milk-control)` | `None` / `None` — correct; Walmart is a `__NEXT_DATA__` shape |
| `nextdata_offers(walmart/goplusplus)` | **IN_STOCK $229.99 `Clove Brothers LLC`** |
| `nextdata_offers(walmart/milk-control)` | **IN_STOCK $2.42 `Walmart.com`** |
| `ldjson_offers(amazon/goplusplus, amazon/control-aa-batteries)` | `None` / `None` — correct; Amazon is shape C, `dom` extraction |
| `add_to_cart_offers(amazon/goplusplus)` | **$219.00 `LO Store (We Record Serial Numbers To avoid FRAUD)`** |
| `add_to_cart_offers(amazon/control-aa-batteries)` | **$9.99 `Amazon.com`** |
| Both Amazon fixtures vs round-1 baseline `481d81d` | **byte-identical** (sha256) |
| `unresolved-sku.html` still exercises the unresolved-SKU path | **yes** — `test_an_unresolved_sku_is_diagnosed_as_unresolved_not_as_our_parser` passes; the fixture is served through `_serve_rendered` at `test_retailers.py:698` |
| Consuming tests changed? | **none** — `git diff b645740^ HEAD -- tests/` touches only `test_fetch.py` |

All five checks the brief asks for hold. This part of the commit is clean.

### 6. The `?state=` case-sensitivity decision — it holds, and it is pinned both ways

| Check | Result |
|---|---|
| Where `state=` appears in fixtures | `gamestop/goplusplus.html` and `gamestop/ps5-control.html`, once each: `…topus.com&country=us&state=ca&c=teconsent…` — TrustArc's CCPA config, as described |
| Both GameStop `.html` fixtures under the guard | **clean** |
| Both GameStop `.json` notes under the guard | **clean** |
| `?state=TX` / `&state=TX` / `?state=TX&zip=00000` / `&state=TX,` | **caught** — `geolocation state=TX` |
| `&state=CA` (uppercase) | **caught** — the rule is about case, not about California |
| `&state=ca` (GameStop's own) | not caught — **as designed** |
| Delete the rule | **RED** |
| Widen it to case-insensitive | **RED** — it would redden the two GameStop fixtures |

So the decision is not merely documented in a comment, it is enforced from both sides: it
cannot be removed and it cannot be widened. That is better than any other single decision in
this file, and it is the model the rest of the guard should follow.

One narrow note, not a gap: `&state=tx`, `&state=Tx` and `&STATE=TX` all pass. The stated
rationale — a lowercase value distinguishes GameStop's consent config from a geolocation —
covers the first two. It does not cover an uppercase *key*, which no consent config would use.
One character in the pattern (`[?&;,][Ss][Tt][Aa][Tt][Ee]=([A-Z]{2})`) closes it without
touching the decision.

### 7. Baseline

`383 passed in 3.88 s`, offline, `-B -p no:cacheprovider`. Six guard tests all pass
(`…catches_every_leak_class`, `…fires_on_a_value_of_the_REAL_shape…`,
`…allow_list_cannot_absorb_a_real_value`, `…scans_every_fixture_directory…`,
`…scans_the_json_provenance_notes…`, `…no_fixture_leaks_the_capturing_hosts_identity`).
No `TBD`/`FIXME`/`XXX` in any file `b645740` touched. `git status --porcelain` empty before,
during teardown and after — the harness asserts byte-exact restoration after each of the 96
mutations and it held every time.

### Verdict on truth 12

**Not closed — fifth round, same axis.**

The truth has three clauses. *Widened*: yes, decisively — 0 blind of 31 probe shapes.
*Watched failing*: yes for 87 of 96 mutations, including every one round 4 named; no for nine,
one of which is a whole rule and four of which re-open real misses. *The current tree is
clean*: **no** — two GameStop fixtures ship a live store number the guard cannot see, a Best
Buy fixture ships 26 store names for a class that has no rule, and `tests/test_fetch.py`
carries the host's real ISP under a docstring claiming otherwise.

What makes this the fifth round rather than a new one is that (a) and (b) are the same defect
as rounds 2, 3 and 4 — a rule keyed to the spellings someone had been shown, and a class
declared covered while an instance of it ships. What makes it different is that both instances
predate all five rounds. They were never introduced by a fix; they were simply never looked
for, because each round's search was scoped to the list it had been handed.

That is the argument for stopping the spelling-by-spelling loop. A sixth round that adds a
`data-` rule and a `storeName` rule will close (a) and (b) and will not tell anyone whether a
seventh spelling exists. The mechanism that would is a coverage assertion over
**classes x carriers** — store number, store name, city, state, ZIP, coordinate, phone,
address, IP against JSON key, query parameter, `data-` attribute, path segment, free text,
cookie — so that a missing cell is visible without anyone having to think of the spelling
first. Six of the nine silent mutations and both live findings are single cells in that grid.

Truth 12 stays **FAILED**; status stays **`gaps_found`** at **12/13**.

**The pushed-vs-tree distinction, unchanged.** `origin/main` is `861b24a`, 24 commits behind.
Everything in this round is unpushed and fixable at zero history cost. What is live and public
right now is exactly what it has been since Phase 1: the real public IP x3 and the full
EdgeScape record in `02-REVIEW.md`, the ZIP in both Walmart fixtures, the store id and state in
the Best Buy fixtures. That is Dan's decision and nothing here moves it.

---

## Round 4 — truth 12 only, after `9d8383c`

Scope: truth 12 and nothing else. Truths 1-11 and 13 are unchanged and were not re-run;
`9d8383c` touches no production module (`git diff 9d8383c^ HEAD` = two Phase-2 planning
docs, `QUESTIONS.md`, two Best Buy fixtures, `tests/test_fetch.py`).

**Say the good part first, because it is most of the commit.** Round 3 found the guard was
redaction wearing a gate's clothes: 29 of 31 probe shapes passed clean and 11 of 15
mutations were silent. `9d8383c` is the round that turned it into an actual rule. It also
found, and removed from the working tree, the leak that matters more than everything this
thread had been about: this host's real public IP, sitting in a *planning* file that every
fixture-aimed scan had missed. That finding is correct, it is properly escalated, and it
was not asked for.

It still does not close the truth, on the same axis, for the fourth round running.

### 1. The 31-shape probe set, re-run: 29 blind → 4 blind

Executed against the shipped `_identity_leaks` in this verifier's own process. A shape that
returns `[]` is one the guard cannot see.

**27 of 31 now caught (was 2 of 31).** Every shape round 3 named is closed, including all
four `e871377` had removed by hand: `"pickupStore"`, `"deliveryStore"`, `storeId=` in a
query string (both `=` and `%3D` forms), and the WIC agency list. City in four spellings and
in upper case, ZIP in five keys, coordinates in both key styles and the EdgeScape query
form, county/FIPS/DMA, street address, `City, ZIP` free text, `City, ST ZIP`, phone in four
spellings, and the client-IP echo — all flagged.

**Four shapes still pass clean, and two of them are live in the tree:**

| # | Shape | Live in the tree? |
|---|---|---|
| 12 | `region_code=<state>` — the EdgeScape **query** form of the state | **YES — ×3 in `tests/fixtures/bestbuy/unresolved-sku.html`**, and ×1 in `02-REVIEW.md` |
| 21 | `"shippingZipcode":"<zip>"` | **YES as a key** — hand-redacted to `00000` in `pikachu-control.html` by this very commit, with no rule added |
| 8 | `"store_id":"<n>"` — snake_case store number | no |
| 13 | `?state=<ST>` — query form of the state | no |

**Positive control, to rule out any doubt that these ship.** Each payload appended to a real
fixture, full offline suite run, fixture restored:

| Payload appended to `walmart/milk-control.html` | Result |
|---|---|
| `aria-label="<city>, <zip>, Change shipping address"` | **RED — caught** (the guard works) |
| `x-akamai-edgescape: region_code=<state>,city=REDACTED` | **GREEN — 382/382, ships undetected** |
| `"shippingZipcode":"<real zip>"` | **GREEN — 382/382, ships undetected** |
| `"store_id":"<n>"` | **GREEN — 382/382, ships undetected** |

The state one is not hypothetical. `git grep -c 'region_code=' -- tests/fixtures/` returns
`tests/fixtures/bestbuy/unresolved-sku.html:1` (three occurrences on that line). The repo's
own rule K3 requires `{"regionCode":"XX"}`, `{"stateCode":"XX"}` and
`{"stateOrProvinceCode":"XX"}` to be caught — the synthetic case set fails if any of them
slips. So the repo asserts that the state is a leak class *and* ships the state in a
fixture, because K3 is keyed to the JSON spelling and the EdgeScape marker tuple was never
given `region_code`. That is the same one-spelling failure as rounds 1-3, in a fourth
spelling.

And `shippingZipcode` is the pattern itself recurring inside the commit that declares it
over. Verified by diff: `pikachu-control.html` went `"shippingZipcode":"<real>"` →
`"shippingZipcode":"00000"` by hand. No rule covers that key. The commit message's own
sentence — *"take the same capture tomorrow and every one ships again"* — is still true for
it.

### 2. Is every rule watched? 34 mutations, 11 silent

Each rule deleted or neutralised one at a time, full offline suite each time, source
restored from the in-memory original after every run. `.venv/bin/python -B … -p
no:cacheprovider` throughout.

**The 13 class-keyed rules are all genuinely watched — this is the headline claim and it
holds.** K1 store number (JSON), K2 store number (URL), K3 state/region, K4 city, K5
`destinationZipCode`/`postCode`, K6 short-key `lat`/`lng`, K7 DMA/FIPS/CBSA/county, K8
street address, K9 WIC agency, the `City, ST ZIP` rule, all three phone spellings
individually, and the free-text `City, ZIP` rule: **13/13 RED when deleted.** The four
`json_markers` entries and the hyphenated-phone rule are RED too. The shadowed
street-address rule the commit says it found is confirmed fixed by diff — the old
`(r'"(address_line1|address_line2)"…', …)` captured the *key* into `group(1)`, not the
value, which is why it never fired; it is now merged into a working alternation.

**11 mutations are silent:**

| Mutation | Result | Why it matters |
|---|---|---|
| Delete the **ZIP+4 rule** entirely | **GREEN 382/382** | There is no ZIP+4 case in the synthetic set. This is the one rule carrying an explicit comment asking maintainers *"narrow it deliberately and say so here rather than deleting it"* — and it is the only whole rule nothing stops them deleting |
| Drop EdgeScape `city` | GREEN | The single case `?city=Exampleville&zip=99999` covers `city` **and** `zip`, so the two shadow each other — either can go silently |
| Drop EdgeScape `zip` | GREEN | as above |
| Drop EdgeScape `lat` / `long` / `county` / `areacode` / `fips` / `dma` (6 markers) | GREEN ×6 | No synthetic case exercises any of them at all |
| Drop header `x-forwarded-for` | GREEN | **This is the spelling that carried the IP ×3 in the actual incident** — `02-REVIEW.md` names it explicitly. Only `true-client-ip` has a case |
| Drop header `client-ip` | GREEN | as above |

So the block that handles the EdgeScape record — the exact artefact at the centre of the
`02-REVIEW.md` finding — is the least-defended block in the function: eight markers, one
case, and that case covers two of them redundantly. The commit found *one* shadowed rule and
merged it; there are eight more of the same shape, plus a whole unwatched rule.

### 3. Can the gate still be disabled cheaply? 9 of 19 attempts stay green

**Closed — round 3's findings are genuinely shut:**

| Attempt | Result |
|---|---|
| Add the real city + ZIP to `allowed` | **RED** |
| Add the real state to `allowed` | **RED** (2 failures) |
| Add the store number to `allowed` | **RED** (2 failures) |
| `_fixture_files` → `[]` | **RED** |
| `_fixture_files` → one retailer only | **RED** |
| `_fixture_files` → drop the `.json` half | **RED** |
| `_fixture_files` → drop the `.html` half | **RED** |
| `break` out of the outer keyed-rule loop | **RED** |
| Remove the `leaks.append` from the keyed loop | **RED** |
| EdgeScape loop: `break` before the append | **RED** |
| Store number: require 4+ digits | **RED** — the synthetic case is a 3-digit number, so the shape is pinned |

**Still open — 9 ways to turn the guard off with nothing going red:**

| Attempt | Result | What it costs |
|---|---|---|
| `_fixture_files` → **one `.html` per directory** (+ all `.json`) | **GREEN** | The scope test asserts on the set of *directory names*, so half the pages can be dropped — including one of the two Walmart fixtures this whole thread is about — while every directory still appears "scanned" |
| `_fixture_files` → skip the Walmart pages, keep the directory via its `.json` | **GREEN** | same hole, aimed |
| Free-text rule: benign carve-out `continue` → `break` | **GREEN** | Disables the free-text rule for any page where a redacted `Redacted, 00000` appears **before** the real value. That is precisely the shape of the Walmart fixtures, which carry `"locationText":"Redacted, 00000"` |
| City rule: require an 8-character city name | **GREEN** | `Exampleville` (12) passes the case; a 6-letter real city does not match |
| Free-text rule: require an 8-character place name | **GREEN** | same |
| `City, ST ZIP` rule: require an 8-character place name | **GREEN** | same |
| K5 postal rule: require a leading `9` | **GREEN** | `99999` passes the case; a real ZIP does not match |
| `json_markers` postal rule: require a leading `9` | **GREEN** | same |
| Header-IP rule: flag only `203.0.113.x` | **GREEN** | `203.0.113.7` is the synthetic case; the real IP prefix stops being flagged |

Six of those nine are one mechanism, and it is the deepest finding in this round. Round 3
correctly required the synthetic cases to stop carrying real values, and `9d8383c` correctly
invented them — `Exampleville`, `99999`, `203.0.113.7`, `555-01xx`. The side effect is that
**no test now ties any rule to the shape of the value it exists to catch**, so every rule can
be narrowed until it matches only its own test case. The fix is not to put real values back:
it is to assert each pattern against a value of the *real shape* — a six-letter city, a ZIP
not starting with 9, a three-digit store number. The store-number rule already works this way
and is the only weakening mutation that turns red.

### 4. The `02-REVIEW.md` remediation — the IP is gone, two things are not

**Searched case-insensitively, repo-wide, over tracked files** — that is how the original
hid, since the file spells the city in capitals.

| Value | Working tree | On `origin/main` (861b24a) |
|---|---|---|
| Real public IP | **0 hits — gone** | **3 hits, live and public right now**, in `02-REVIEW.md` |
| EdgeScape city / county / FIPS / lat / long | **0 hits — gone** | live in `02-REVIEW.md` |
| ZIP | 12 hits, all narrative: `QUESTIONS.md` §0e (7), this file (2), `03.1-02-SUMMARY.md` (2), `docs/retailer-evidence.md:2275` (1) | live in both Walmart fixtures |
| **State (`region_code=`)** | **4 hits — ×3 in a shipped fixture, ×1 in `02-REVIEW.md`** | live in both |

The IP and the fine-grained geolocation are genuinely gone from the working tree. Two things
survive:

**(a) A real geolocation value outside the audit trail.** `region_code=<state>` in
`tests/fixtures/bestbuy/unresolved-sku.html` is a fixture, not an audit record, and the guard
cannot see it (§1 above). `georegion=` survives in the same blobs, and `02-REVIEW.md:191`
still names the host's ISP in prose as `network_type=REDACTED`. State + ISP is coarse next to a
public IP — but it is a real fact about where the host sits, in a tracked file, and §0e
asserts it is not there.

**(b) The remediation garbled its own instruction.** `02-REVIEW.md` is one of the two files
the blunt regex was deliberately *kept* on, and it rewrote values inside sentences:

- The Fix step now reads: *"1. Scrub the fixture now: replace `192.0.2.1` with `192.0.2.1`
  (RFC 5737 documentation range)"* — replace X with X. This is the single instruction whose
  non-execution is the whole of §0e, and it is now unreadable as an instruction.
- The header chain now reads `"x-forwarded-for":"192.0.2.1, 192.0.2.1, 192.0.2.1"`. The two
  Akamai hop addresses were replaced with the client placeholder, so the record now claims
  the client IP appeared three times in one header. Not a leak — but the evidence of what the
  header actually contained is no longer reconstructible from the record that exists to
  preserve it.

That is exactly the class item 6 asks about, surviving in the one file the scrub was kept on.

**`docs/retailer-evidence.md:2275`** still names the ZIP. §0e discloses it and explicitly
puts "may the audit trail name what it audits" to Dan, which is the right call — but it is
worth being precise that this file is the only one of the four that is **user-facing
documentation** rather than an audit record, and §0e's phrasing ("this record and its
neighbours") groups it with three planning files.

### 5. Fixture integrity — clean, and byte-exact

| Check | Result |
|---|---|
| `pikachu-control.html` vs `9d8383c^` | `old.replace('<real zip>','00000') == new` is **exactly True**; identical byte length. **No other byte changed** |
| `unresolved-sku.html` vs `9d8383c^` | same — exactly True, identical length |
| `data-app-version` mangling from the reverted blunt regex | **fully reverted** — ruled out categorically by the byte-exact result above |
| `ldjson_offers(pikachu-control)` | `[Offer(available=True, price=59.99, seller='Best Buy', raw_availability='InStock')]` — **IN_STOCK $59.99 'Best Buy'**, as claimed |
| `ldjson_offers(unresolved-sku)` | `None` — the unresolved-SKU path intact; `test_an_unresolved_sku_is_diagnosed_as_unresolved_not_as_our_parser` **passes** |
| Walmart `goplusplus.html` / `milk-control.html` | **byte-identical** to `9d8383c^`; `nextdata_offers` → IN_STOCK $229.99 `Clove Brothers LLC` and IN_STOCK $2.42 `Walmart.com` |
| Amazon `goplusplus.html` | **byte-identical** to `9d8383c^` |
| Consuming tests | **none changed** — `git diff 9d8383c^ HEAD -- tests/` touches only `test_fetch.py`. `conftest.py`, `test_parse.py`, `test_retailers.py` untouched, so they assert exactly what they asserted before |

All five claims in item 5 hold. This part of the commit is clean.

### 6. Did the revert break anything? No, apart from 4(b)

`git diff 9d8383c^ HEAD` is six files: the two Phase-2 planning docs, `QUESTIONS.md`, the two
Best Buy fixtures, `tests/test_fetch.py`. Every other tracked file is untouched, so the
narrative records, the other fixtures and the rest of `tests/` are byte-identical to their
pre-scrub state by construction.

`tests/test_fetch.py`'s only deletions are the two deliberate docstring edits (both removed
lines that named the city and ZIP), the old `allowed` set, and the shadowed street-address
rule — confirmed by reading every `-` line in the diff. Nothing else was removed.

The only garbling that survived is in `02-REVIEW.md`, in the one place the scrub was kept
deliberately — §4(b) above. No debt markers (`TBD`/`FIXME`/`XXX`) in any changed file.

### 7. Baseline

`382 passed in 3.0 s`, offline, `-B -p no:cacheprovider`. The five guard tests
(`…catches_every_leak_class`, `…allow_list_cannot_absorb_a_real_value`,
`…scans_every_fixture_directory…`, `…scans_the_json_provenance_notes…`,
`…no_fixture_leaks_the_capturing_hosts_identity`) all pass. Working tree left clean —
`git status --porcelain` empty after every mutation sweep.

### Verdict on truth 12

**Not closed.** The direction is right and the distance covered this round is larger than the
previous three combined — 29 blind shapes down to 4, all 13 keyed rules watched, the
allow-list and coarse-scope holes shut, and the real public IP found and removed. But the
truth as written has two clauses and both are still false: a shipped fixture carries a real
geolocation value the guard cannot see, and a key redacted by hand this round has no rule
behind it. Truth 12 stays **FAILED**; status stays **`gaps_found`** at **12/13**.

**The pushed-vs-tree distinction, stated for §0e.** Everything found in this round is in
**unpushed** commits — `origin/main` is at `861b24a`, 23 behind — so all of it is fixable at
zero history cost. What is on `origin/main` right now, public: the real public IP ×3 and the
full EdgeScape record in `02-REVIEW.md`, the ZIP in both Walmart fixtures, and the state in
the Best Buy fixture. That exposure is unchanged by this commit and is the decision sitting
with Dan.

---

## Round 2 (historical): what `f2aa40d` closed and what it did not

`f2aa40d` is a real fix, not a paper one. Most of the blocker is genuinely gone and every
warning is genuinely addressed. It also repeats, for the third time in this phase, the one
mistake it was written to correct — and it introduces a fresh instance of the leak in the
test that exists to prevent it. So the status does not move.

### Closed — verified by execution

| # | Claim in `f2aa40d` | How it was checked | Result |
|---|---|---|---|
| 1 | City, ZIP and store name gone from both Walmart fixtures | `grep -rn` over all 12 files under `tests/fixtures/` for the city, both ZIPs and the store name | **0 hits.** The store name is now `Redacted Supercenter` (2× / 6× per file, same occurrence counts as before), `"city":"Redacted"`, `"locationText":"Redacted, 00000"`, `"locationAddress":null`. No street address or ZIP+4 anywhere |
| 2 | The redaction did not break what the fixtures are for | Parsed the pre-fix blob (`f2aa40d^`) and the current blob through `boty.parse.nextdata_offers` / `ldjson_offers` and compared | **Byte-identical readings.** `goplusplus`: `Offer(available=True, price=229.99, seller='Clove Brothers LLC', raw_availability='IN_STOCK')` both sides — the reseller-at-4×-MSRP case is intact. `milk-control`: `Offer(available=True, price=2.42, seller='Walmart.com', raw_availability='IN_STOCK')` both sides — the first-party control is intact. `ldjson_offers` `None` both sides. Deltas of −16 B and −82 B, all inside redacted values. The commit touched no test that reads them (`test_parse.py`, `test_retailers.py`, `conftest.py` unmodified), so they assert exactly what they asserted before, and 379/379 pass |
| 3 | Amazon `.json` notes describe the redaction without naming it | Read both notes in full | **Confirmed, and still auditable.** They keep the class ("the city it placed us in (×3) and the ZIP (×3)"), the replacement token (`REDACTED`), the enumerated redaction procedure, the raw byte figure vs the stored one, and an explicit statement of *why* the values are withheld. A reviewer can count `REDACTED` in the sibling `.html` (402 and 459) and reconstruct the audit without the values. This is the right shape |
| 4 | `_identity_leaks` extracted and **watched failing** | Five independent mutations of the shipped rule, each run through `pytest tests/test_fetch.py -q` and reverted | **The gate is not decorative.** Delete the free-text `City, 12345` rule → RED. Delete the postal-code JSON rule → RED. Delete the coordinate rule → RED. Delete the phone-number rule → RED. And the anti-cry-wolf half bites in the other direction: remove the `allowed` carve-out from the free-text rule → RED (2 tests); make the JSON rules fire on `00000` → RED (2 tests) |
| 5 | `TARGET_RETAILER_COUNT` pinned at 5 | Mutated `scripts/evidence_check.py:172` to 4 and to 6, full offline suite each time, reverted | **Bites both ways.** At **4**: `test_the_retailer_count_target_is_pinned_at_five_in_both_directions` + `test_a_short_count_with_a_hard_two_retailer_configured_fails`. At **6**: the pin + `test_five_retailers_including_a_hard_two_one_passes`. Two failures each direction, so the behaviour is pinned as well as the constant. **Warning closed** |
| 6 | REQ-07a Pending → Complete | Cross-read the new cell against the tree | **Justified, not a status edit ahead of the facts.** Every claim in it was independently verified in the initial pass: Target at rung 3 + `dom`, control-only, delisted TCIN; Amazon at rung 1 + `dom`, shape C; six retailers, 6/6 control-verified; rule 6 watched failing on a prose-only body. § 0d is `ANSWERED 2026-08-03 (Dan)`, so the robots decision it leans on is real. The old narrative was replaced rather than appended to, which is correct for a status cell — the mid-phase story survives in the SUMMARYs. **Warning closed** |
| 7 | QUESTIONS.md § 0e corrected in place | Read § 0e in full against the tree | **Structurally right, one item wrong.** The correction is dated, names the verifier, quotes the false sentence rather than deleting it, and separates "what had actually happened" from "what is true now". The pushed-history problem is explicitly stated as unchanged; the three options and their costs are word-for-word as before; the recommendation (option 2) is unchanged and still reasoned. The cited grep (`grep -rE '\bRedacted\b\|\b7503[0-9]\b' tests/fixtures/`) genuinely returns nothing — I ran it, exit 1. The Best Buy exculpations check out: `55423` appears only as `destinationZipCode` in Best Buy's own Minnesota region, and `75039` in `unresolved-sku.html` is `afe75039` inside a `ws.cs.1worldsync.com` URL path, not a ZIP. **Dan is being presented the decision fairly.** The one false item is carried below |

### Still open

**(a) The store number survives — the same failure mode, third occurrence.**

`f2aa40d`'s own commit message: *"plus the city alone, the named store (…) and storeId 202,
which locate this host as well as the ZIP does."* It then redacted the store number under
the literal keys `storeId` / `storeIds` and left it standing everywhere else:

| Spelling | `goplusplus.html` | `milk-control.html` | Redacted? |
|---|---|---|---|
| `"storeId":"…"` | 10 | 14 | ✓ → `"0"` |
| `"storeIds":["…"]` | 1 | 1 | ✓ → `["0"]` |
| `"pickupStore":"…"` | **3** | **3** | ✗ |
| `"deliveryStore":"…"` | **3** | **3** | ✗ |
| `storeId=` in a URL query string | **3** | **3** | ✗ |
| `"stateCode"` / `"stateOrProvinceCode"` | **5** | **5** | ✗ |

Three of the six survivors per file are inside `&amp;`-escaped `href` attributes — rendered,
visible markup, exactly the shape the free-text rule was added for. A Walmart store number
resolves publicly to that store's street address in one lookup, which is the same locator the
store *name* was removed for; the state code narrows it before you even look. The previous
verification's `missing:` list named `stateOrProvinceCode` explicitly and it was not actioned.
Executed against the shipped `_identity_leaks`: `'"pickupStore":"202","deliveryStore":"202"'`
→ quiet, `'&storeId=202&specificity='` → quiet, `'"stateOrProvinceCode":"TX"'` → quiet.

**(b) The `.json` defect's twin, in the docs tree, unfixed.**

The commit's sharpest finding was that a redaction note "recorded its own redaction by NAMING
the values it removed … The file documenting the removal was republishing the thing removed."
That construct — same values, same per-class counts, same sentence shape — is still live at
`docs/retailer-evidence.md:436`, and the Walmart ZIP is named again at `:2272`. Both predate
`f2aa40d` (blame: `7caeaf2`, `a4f2847`) but both are inside this phase, and neither is covered
because the guard globs `tests/fixtures/*/*.{html,json}` and nothing else.

**(c) A new occurrence, introduced by the closure commit.**

`tests/test_fetch.py:488`, `:490` and `:494` seed the guard's synthetic leak cases with the
host's **real** city and ZIP:

```
"?city=<city>&zip=<zip>":                                  "geolocation",
'{"zipCode":"<zip>"}':                                     "postal code",
'aria-label="<city>, <zip>, Change shipping address"':      "rendered destination",
```

`git blame` puts all three on `f2aa40d`. The test written to keep the host's ZIP out of a
public repo hardcodes the host's ZIP into a public repo, three times, where any documentation
value would prove the identical regexes. This is a **regression**, not a residual.

**Scope of the exposure, stated precisely.** All of (a), (b) and (c) sit in **unpushed**
commits — `origin/main` is at `861b24a`, 21 commits behind `HEAD` — so every one of them is
fixable at zero history cost before the next push. Only the Phase-1 fixture blobs in `58e38ef`
are actually public, and those are the § 0e decision already with Dan. That makes this
actionable rather than academic, and it makes the next push the deadline.

### Guard quality, measured

Every leak rule inside `_identity_leaks` is pinned. The **caller** is not, and the rules are
still keyed on spellings rather than semantics:

| Mutation / probe | Expected | Observed |
|---|---|---|
| Delete free-text `City, 12345` rule | RED | ✓ RED |
| Delete postal-code JSON rule | RED | ✓ RED |
| Delete coordinate JSON rule | RED | ✓ RED |
| Delete phone-number rule | RED | ✓ RED |
| Remove `allowed` carve-out, free-text rule | RED (cry-wolf half) | ✓ RED, 2 tests |
| Make JSON rules fire on `00000` | RED (cry-wolf half) | ✓ RED, 2 tests |
| **Delete the `*/*.json` glob from the tree scan** | RED | ✗ **379/379 GREEN — unpinned** |
| Probe: `"destinationZipCode":"<zip>"` | caught | ✗ MISSED |
| Probe: `"postCode"` / `"shippingPostalCode"` | caught | ✗ MISSED |
| Probe: `<City>, TX <zip>` (the commonest US rendering) | caught | ✗ MISSED |
| Probe: `"city":"<city>"` alone, no ZIP | caught | ✗ MISSED |
| Probe: the store name as free text | caught | ✗ MISSED |
| Probe: `aria-label="<city>, <zip>, Change shipping address"` | caught | ✓ CAUGHT |

The `.json` glob line is the one the commit added and described as "watched red on an injected
note leak before being trusted" — that was a manual in-process observation, and no committed
test records it. With the notes now clean there is nothing keeping the glob honest.

### Does the guard cry wolf?

Not on the current tree (379/379 green, 0 leaks) and not on the cases the task named — version
strings, SVG path data, UPC/GTIN, prices, ISO dates, offer ids and item weights are all quiet.
It **does** fire on three classes of legitimate retailer content:

| Legitimate content | Result |
|---|---|
| `Customer service: 1-800-925-6278` | ✗ fires — "phone number" |
| `"Roasted in <City>, <zip> since 1971"` (a city genuinely part of a listing) | ✗ fires — "rendered destination" |
| `"sku":"12345-6789"` | ✗ fires — "ZIP+4" |

This is fail-closed, so it is a WARNING not a blocker: the cost is a red build a human resolves,
not a leak that ships. But the benign set in `test_the_fixture_identity_guard_catches_every_leak_class`
contains no case of this kind — its seven entries are all redaction output, and two of them
(`"city":"Redacted"`, `jQuery 3.3.6.4 loaded`) match no rule at all, so they constrain nothing.
The test's own stated purpose ("a guard that fires on redacted output is one nobody keeps")
argues for adding a first-party support number and a listing-city case to that set.

---

## Goal Achievement

### Observable Truths — ROADMAP § Phase 3.1 Success Criteria

Truths 1-11 and 13 were confirmed in the initial pass and are **out of scope for this
re-verification** per the politeness constraint (`make verify` and `boty check` were not
re-run; GameStop 403'd this host earlier today). They are reproduced unchanged. Regression
sanity: the offline suite is green at 379 tests, up 2 from 377, and the closure commit touched
only `tests/`, `.planning/`, `QUESTIONS.md` and fixture data — no `boty/` module, no
`scripts/`, no `config/`.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Target reports stock for the GO Plus +, control watch green | **UNMET — correctly recorded, NOT a gap** | Recorded UNMET in `.planning/ROADMAP.md:194` and again in the outcome table at `:205`, with the reason (Target delisted TCIN `88714054`) and the fact that the meetable rewrite was proposed and **Dan declined it**. Verified not tidied away: `config/products.yaml` carries a `retailer: target` CONTROL watch and **no** GO Plus + watch, with the delisting written into the config comment. The control is green — `status.json`: `target / CONTROL — up&up microfiber dust cl / in_stock / $12.59 / rung=browser / extraction=dom / degraded=True`. The half of the criterion that *could* be met is met; the half that cannot, is not, and says so |
| 2 | Amazon reports stock for the GO Plus + if it carries it, control green; or the technical outcome recorded, having actually been attempted | ✓ VERIFIED | `docs/retailer-evidence.md` § Amazon resolves to exactly one section with exactly one anchored verdict, `**Verdict: REACHABLE (rung 1)**` (checked through `evidence_check.sections_for`/`verdict_lines`, not by eye). Amazon carries both a control watch and a real GO Plus + watch. `make verify` live: `in_stock amazon CONTROL — Amazon Basics AA batteri $9.99 add-to-cart control: add-to-cart enabled from Amazon.com` |
| 3 | `boty check` reports five or more retailers with no health warnings — six if Amazon lands | ✓ VERIFIED at six | Live `served/boty/status.json`: `healthy: true`, 13 watches, **6** retailers, all six health entries `{'ok': True, 'reason': '', 'failing_controls': []}`. Zero warnings |
| 4 | Every row of the support matrix states rung, robots.txt position and terms position | ✓ VERIFIED | `README.md:99` header is the seven-cell `\| Retailer \| Rung \| Extraction \| robots.txt \| Terms \| Method \| Status \|`; seven retailer rows follow, each with a judged robots and terms cell and a `⚠ disagree` marker where earned. Machine-checked in both directions by `tests/test_support_matrix.py` (`_extraction_mismatch`, `_undeclared_degraded`, `_misdeclared_disagreement`, `UNREAD_POSITIONS`) |
| 5 | No regression: four Phase 2 retailers still green, `make verify` exits 0 | ✓ VERIFIED | Run by this verifier under `sudo systemd-run … EnvironmentFile=/home/dan/.config/boty/env`: **bare `VERIFY: PASS`**, exit 0. 377 tests, mypy clean over 15 files, 11 fixtures ok, **6/6 live controls**, **8/8 mutations caught**. The four Phase 2 controls read with unchanged sources. Re-verification: offline suite now 379/379 with the same fixture set, and the closure commit changed no production module |
| 6 | A single `boty check` still completes in under 2 minutes | ✓ VERIFIED | `status.json` `duration_seconds` = **45.586** against a 120 s budget |

### Observable Truths — what the phase claims it delivered

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 7 | `Extraction` is a second axis, `Result.degraded` fires on browser transport OR dom extraction, published in status.json and the README | ✓ VERIFIED | Unchanged from the initial pass |
| 8 | Target registered at rung 3 + dom, control-only | ✓ VERIFIED | Unchanged from the initial pass |
| 9 | Amazon registered at rung 1 + dom, shape C; used-only offer suppressed by the seller filter | ✓ VERIFIED | Unchanged from the initial pass |
| 10 | `evidence_check` rule 6 — a refusal must cite an observation | ✓ VERIFIED by execution | Unchanged from the initial pass |
| 11 | Six retailers, `healthy: true`, a pass under 120 s | ✓ VERIFIED | Unchanged from the initial pass |
| 12 | The fixture leak guard is widened, watched failing, and the current tree is clean | ✗ **FAILED — round 5; the strongest round of the five, still not closed** | **Widened: fully.** The 31-shape probe set is **31/31 caught, 0 blind** (round 4: 4 blind, 2 live). **Watched failing: 87 of 96 mutations RED**, including every one of round 4's eleven silent mutations and all nine of its cheap-disable paths; the `?state=` case decision is pinned in both directions; `02-REVIEW.md` is repaired; the fixtures are byte-length-exact and parse-identical. **Tree clean: no.** `data-preferred-store-id="0"`/`"0606"` ship live in both GameStop fixtures and neither store rule can see them (suite 383/383 green with them present). The store-NAME class has **no rule at all** — `REDACTED SUPERCENTER` sits in `allowed` for a rule that does not exist, and 26 real store names ship in a Best Buy fixture. And `?network_type=REDACTED` at `test_fetch.py:623` is the host's real ISP, scrubbed from two other tracked files by this same commit, under a docstring reading "EVERY value here is invented." 9 mutations remain silent; 8 let a real-shaped value out |
| 13 | The deployed service publishes six retailers with the `extraction` key | ✓ VERIFIED | Unchanged from the initial pass |

**Score:** 12/13 truths verified — unchanged (criterion 1 counted as correctly-recorded-UNMET,
per Dan's standing decision; it is not scored as a failure and must not be closed).

### The five pressure points, answered

Sections 1-4 and 6 of the initial report stand unchanged and are not restated here — criterion
1 is deliberately UNMET and correctly recorded in three places; rule 6 fails a prose-only
refusal and accepts a measured one; the Amazon `dom` reading is `OUT_OF_STOCK` /
`alertable=False` and that is the correct answer rather than a hedge; the deployed daemon
publishes six retailers with `extraction` on every watch.

**2. Is `TARGET_RETAILER_COUNT` still 5, and does the gate still bite?** — updated.
`scripts/evidence_check.py:172` — `TARGET_RETAILER_COUNT = 5`. Executed against synthetic
configs: fires at 4-with-target and 4-with-amazon, silent at 5 and 6, silent at 4 with neither
hard-two member. **The warning is now closed:**
`test_the_retailer_count_target_is_pinned_at_five_in_both_directions` pins the constant, and
mutating it produces two failures in each direction (the pin plus a behavioural test), so both
the number and what it does are held.

**5. The fixture leak guard — still FAILED, for different and smaller reasons.** See the
re-verification section above.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `boty/models.py` | `Extraction` enum, `Result.extraction` last, widened `degraded` | ✓ VERIFIED | Unchanged; not touched by `f2aa40d` |
| `boty/status.py` | `extraction` published beside `rung` | ✓ VERIFIED | Unchanged |
| `boty/retailers.py` | `check_target_browser`, `check_amazon`, `_verdict_from_html(allow_dom=)` | ✓ VERIFIED | Unchanged |
| `boty/parse.py` | one widened `_AddToCartParser` over two page families | ✓ VERIFIED | Unchanged |
| `boty/cli.py` | `[dom]` tag | ✓ VERIFIED | Unchanged |
| `served/boty/index.html` | `dom` tag + rewritten degraded tooltip | ✓ VERIFIED | Unchanged |
| `config/products.yaml` | 6 retailers; target control-only; amazon control + product | ✓ VERIFIED | Unchanged |
| `scripts/evidence_check.py` | rule 6 with anchor + measurement; `TARGET_RETAILER_COUNT` | ✓ VERIFIED | Now pinned in both directions by a committed test |
| `scripts/mutation_check.py` | M6 re-anchored, M7, M8 | ✓ VERIFIED | 8/8 caught in the live run |
| `README.md` | 7-column, 7-row matrix; six-retailer count prose | ✓ VERIFIED | Unchanged |
| `docs/retailer-evidence.md` | Target REACHABLE (rung 3), Amazon REACHABLE (rung 1), rule-6 lines | ⚠️ **PARTIAL** | Verdicts and rule-6 lines correct. **`:436` republishes the redacted city and ZIP by name with per-class counts; `:2272` names the Walmart ZIP.** Outside the guard's glob |
| `tests/fixtures/target/control-dust-cloths.html` | leak-scanned | ✓ VERIFIED | 134 `<script>` bodies replaced; zero hits on every marker class |
| `tests/fixtures/amazon/*` | two fixtures + notes, leaking nothing | ✓ VERIFIED | Zero hits. Both `.json` notes rewritten to describe the redaction by class without naming values, and remain auditable |
| `tests/fixtures/walmart/*.html` | redacted of host geolocation | ✗ **PARTIAL** | City, ZIP, store name, `storeId`/`storeIds` gone; readings byte-identical. **Store number survives ×6 per file under `pickupStore` / `deliveryStore` / `storeId=`; state code ×5** |
| `tests/fixtures/walmart/*.json` | provenance notes | ℹ️ Info | Neither note records any redaction at all — the only Walmart provenance is the commit message. `bytes` fields (483194, 471690) no longer match the files (483024, 471561); the drift predates `f2aa40d` but widened. Amazon and Target notes both do this correctly |
| `tests/test_fetch.py` leak guard | widened, watched failing | ⚠️ **PARTIAL** | **Watched failing: closed** — four rule deletions and two cry-wolf mutations all go red. Remaining: the `*/*.json` glob is unpinned; the rules are still spelling-keyed; and lines 488/490/494 hardcode the host's real city and ZIP |
| `tests/test_evidence_check.py` | `TARGET_RETAILER_COUNT` pin | ✓ VERIFIED | Fails at 4 and at 6, two tests each |
| `.planning/REQUIREMENTS.md` | REQ-07a status | ✓ VERIFIED | `Complete`, and the new narrative is true of the tree. Line 38's `[x]` and line 110 now agree |
| `QUESTIONS.md` § 0e | corrected before Dan answers | ⚠️ **PARTIAL** | Correction present, dated, non-destructive, decision unchanged and fairly put. One false item: "the store id [is] gone from both Walmart fixtures" |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `config/products.yaml` `retailer: target` | `check_target_browser` | `cli._make_checker` | WIRED | Unchanged |
| `config/products.yaml` `retailer: amazon` | `check_amazon` | `cli._make_checker` | WIRED | Unchanged |
| DOM reader | `Result.extraction` | reader-that-ran sets the axis | WIRED | Unchanged |
| `Result.extraction` | `status.json` → dashboard | `status.write` → `index.html` | WIRED | Unchanged |
| `docs/retailer-evidence.md` | `evidence_check` rules 5 & 6 | anchored + measured refusal lines | WIRED | Unchanged |
| `evidence_check.TARGET_RETAILER_COUNT` | a committed test | `assert == 5` + two behavioural tests | **WIRED** (new) | Mutating to 4 or 6 produces 2 failures each |
| `_identity_leaks` | `test_the_fixture_identity_guard_catches_every_leak_class` | one synthetic body per class + benign set | **WIRED** (new) | Six mutations, six reds |
| `tests/fixtures/*` | `_identity_leaks` | `root.glob("*/*.html")` + `root.glob("*/*.json")` | **PARTIAL** | The `.json` half of the glob is unpinned — deleting it leaves 379/379 green |
| `docs/**`, `.planning/**`, `QUESTIONS.md` | any leak guard | — | **NOT WIRED** | Nothing scans the prose tree; 28 occurrences of the host's city/ZIP live there across 5 tracked files, all unpushed |

### Behavioural Spot-Checks (re-verification pass — offline only)

| Behaviour | Command | Result | Status |
|---|---|---|---|
| Offline suite baseline | `.venv/bin/python -m pytest tests/ -q` | 379 passed in 2.59 s | ✓ PASS |
| Fixtures clean of city/ZIP/store name | `grep -rn` over `tests/fixtures/` | 0 hits | ✓ PASS |
| § 0e's own cited grep | `grep -rE '\bRedacted\b\|\b7503[0-9]\b' tests/fixtures/` | exit 1, no output | ✓ PASS |
| Redaction is semantically neutral | `nextdata_offers` over `f2aa40d^` blob vs current blob | identical `Offer(...)` on both files | ✓ PASS |
| Guard mutation: free-text rule deleted | `pytest tests/test_fetch.py -q` | 1 failed, 15 passed | ✓ PASS (goes red) |
| Guard mutation: postal-code rule deleted | same | 1 failed, 15 passed | ✓ PASS (goes red) |
| Guard mutation: coordinate rule deleted | same | 1 failed, 15 passed | ✓ PASS (goes red) |
| Guard mutation: phone rule deleted | same | 1 failed, 15 passed | ✓ PASS (goes red) |
| Guard mutation: `allowed` carve-out removed | same | 2 failed, 14 passed | ✓ PASS (cry-wolf half bites) |
| Guard mutation: JSON rules fire on `00000` | same | 2 failed, 14 passed | ✓ PASS (cry-wolf half bites) |
| **Guard mutation: `*/*.json` glob deleted** | same | **16 passed — silent** | ✗ **FAIL (unpinned)** |
| `TARGET_RETAILER_COUNT` → 4 | `pytest tests/ -q` | 2 failed, 377 passed | ✓ PASS (goes red) |
| `TARGET_RETAILER_COUNT` → 6 | `pytest tests/ -q` | 2 failed, 377 passed | ✓ PASS (goes red) |
| Store number / state code visible to the guard | `_identity_leaks` over the surviving strings | quiet on all three spellings | ✗ **FAIL** |
| Cry-wolf probe, 17 samples | `_identity_leaks` over legitimate content | quiet on 12, fires on 3 (toll-free number, listing city, ZIP+4-shaped SKU) | ⚠️ WARNING (fail-closed) |

No live retailer request was issued in this pass. `make verify` and `boty check` were not
re-run — the initial pass ran `make verify` once (bare `VERIFY: PASS`, 377 tests, 6/6 live
controls, 8/8 mutations) and the executor ran it once after the fix (bare `VERIFY: PASS`,
379 tests, 6/6 live controls, 8/8 mutations). Both figures are consistent with the offline
379 observed here.

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention exists in this repository; this project's
executable-criteria mechanism is `make verify`, `scripts/evidence_check.py` and
`scripts/mutation_check.py`. Step 7c: not applicable.

### Requirements Coverage

| Requirement | Source plan | Description | Status | Evidence |
|---|---|---|---|---|
| REQ-07a | 03.1-01/02/03/05 | Target and Amazon are supported, not dropped; a retailer is dropped only when technically unreachable, and the reason recorded is the observation | ✓ SATISFIED | Both REACHABLE with anchored verdicts; rule 6 makes the requirement mechanical and was watched failing. **`.planning/REQUIREMENTS.md:110` now reads `Complete` with a narrative that matches the tree — warning closed** |
| REQ-13 | 03.1-01/02/03/05 | Every matrix row states rung, extraction, robots.txt and terms | ✓ SATISFIED | Seven rows × seven cells, `_extraction_mismatch` tied to the Rung cell in both directions |
| REQ-08 | 03.1-04 | A full pass under two minutes at ~7 retailers | ✓ SATISFIED | 45.586 s at six retailers / 13 watches / two browser rungs |
| REQ-06 | 03.1-02/03 | Every configured retailer has a control watch | ✓ SATISFIED | 6/6 controls, all IN_STOCK, zero health warnings |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `tests/fixtures/walmart/goplusplus.html` | 6 sites | Host store number under `pickupStore` / `deliveryStore` / `storeId=` query, 3 of them in visible `href` markup; state code ×5 | 🛑 Blocker | Publicly resolvable to the store whose name was removed |
| `tests/fixtures/walmart/milk-control.html` | 6 sites | Same | 🛑 Blocker | Same |
| `docs/retailer-evidence.md` | 436, 2272 | A redaction note that names the values it removed, with per-class counts | 🛑 Blocker | The exact construct `f2aa40d` removed from `amazon/goplusplus.json`, surviving in a public doc outside the guard's glob |
| `tests/test_fetch.py` | 488, 490, 494 | Guard's synthetic leak cases hardcode the host's real city and ZIP | 🛑 Blocker — **regression, introduced by `f2aa40d`** | The test that exists to keep the value out of the repo puts it in |
| `tests/test_fetch.py` | 545 | `*/*.json` glob unpinned by any test | ⚠️ Warning | The second hole the fix closed can silently reopen |
| `tests/test_fetch.py` | 394-476 | Rules still keyed on spellings — `<City>, TX <zip>`, `destinationZipCode`, bare `"city"`, the store name all pass clean | ⚠️ Warning | Same failure mode as the two it was widened against |
| `tests/test_fetch.py` | 505-517 | Benign set constrains nothing realistic; two of seven entries match no rule at all | ⚠️ Warning | The anti-cry-wolf half is thinner than the leak half |
| `QUESTIONS.md` | 78 | "the store name and the store id are gone from both Walmart fixtures" — the store id is not | ⚠️ Warning | A correction that is itself wrong on one of four items, in the document Dan decides from |
| `tests/fixtures/walmart/*.json` | note, `bytes` | No provenance record of either redaction; `bytes` no longer matches the file | ℹ️ Info | Amazon and Target notes do this correctly; Walmart's do not |
| `tests/test_support_matrix.py` | 70, 303, 866, 872 | `TBD` literals | ℹ️ Info | Test data documenting what the gate rejects, not debt markers |

No `FIXME`, `XXX`, `TODO`, `HACK` or `PLACEHOLDER` in any file this phase touched. No stub
returns, no empty handlers, no hardcoded-empty props.

### Human Verification Required

None that blocks the phase. One decision remains open with Dan and now needs one line
corrected rather than re-asking:

**`QUESTIONS.md` § 0e.** The correction is the right shape and the decision — leave it /
`filter-repo` / recreate — is unchanged and fairly put; the pushed-history problem is exactly
as described and the recommendation (option 2) is still reasoned. Two amendments before it is
answered:

1. **"the store id [is] gone from both Walmart fixtures" is false** — it survives six times
   per file under three other spellings. Either fix the fixtures first or say so.
2. **"the working tree is clean *now*" is true only of `tests/fixtures/`.** 28 occurrences of
   the host's city and ZIP live in five other tracked files, including three that `f2aa40d`
   itself added to `tests/test_fetch.py`. All are unpushed, so they can still be removed for
   free — which is a materially better position than § 0e currently describes, and worth
   telling Dan.

### Gaps Summary

**The phase goal is achieved and nothing in this re-verification touches it.** Target and
Amazon are both registered, both control-verified, both correctly labelled on the two axes,
both in the live payload. All six ROADMAP criteria stand as recorded, five MET on reproduced
measurements and criterion 1 UNMET with the reason and the declined rewrite written down.

**`f2aa40d` is a good-faith fix that got most of the way.** The loud values are genuinely gone
from the fixtures, the redaction is provably neutral to what the fixtures are for, the Amazon
notes now describe the removal without performing it again, the guard is a real gate rather
than a read-only rule — six mutations, six reds, and the anti-cry-wolf half bites too — and
both remaining warnings, `TARGET_RETAILER_COUNT` and REQ-07a, are properly closed.

**It fails on the same axis it was written to fix.** The commit named the store number as a
locator "as good as the ZIP" and then redacted it under one key while leaving it under three
others; the sentence it deleted from a `.json` note for republishing the redacted values
survives verbatim in `docs/retailer-evidence.md`; and the new watched-failing test seeds its
synthetic cases with the host's real city and ZIP. Three occurrences, one root cause, and it
is the same root cause as the two before it: the fix is applied to the instance that was
found, not to the class. That is exactly what the truth being verified asserts had been done.

**The good news is that it is cheap.** Every occurrence found in this pass is in an unpushed
commit — `origin/main` is 21 behind — so all of it can be corrected before it becomes a
history problem, unlike the Phase-1 blobs in § 0e. Four edits close it: redact the store
number and state code by class in both Walmart fixtures; strip the named values out of
`docs/retailer-evidence.md:436` and `:2272`; swap documentation values into
`tests/test_fetch.py`'s synthetic cases; and pin the `.json` glob so the second hole cannot
silently reopen. Then correct the one false line in § 0e and hand it back to Dan.

**Status stays `gaps_found` at 12/13.** Not because the fix was bad — most of it holds under
mutation, which is more than the original could say — but because the truth it was closing is
still not true, and a second false green here would cost more than the first.

**Round 5 (`b645740`) — the fix is now doing real work, and it is time to change the
method.** Every round-4 finding is closed by mechanism: 0 blind probe shapes, 87 of 96
mutations red, the ordering defect and both scope holes shut, the `?state=` case decision
pinned from both sides, `02-REVIEW.md` repaired, the fixtures byte-exact. What fails is two
instances that were in the tree before round 1 and that no round looked for — a store number
in a `data-` attribute in both GameStop fixtures, and a store-name class whose redaction
placeholder outlived the rule it was written for — plus the fifth instance of a real value
landing in a tracked file *by way of the fix* (`REDACTED` scrubbed from two files and written
into a third in one commit). Five rounds of adding the spelling in front of us has produced a
good guard and has not produced a bounded one. The next step that would is a coverage
assertion over **classes x carriers**: nine leak classes against six carriers (JSON key, query
parameter, `data-` attribute, path segment, free text, cookie), so that a missing cell is
visible without anyone having to think of the spelling first. Both live findings and six of
the nine silent mutations are single empty cells in that grid.

---

_Verified: 2026-08-03T14:46:48Z_
_Re-verified: 2026-08-03T16:31:00Z — round 2, targeted, after `f2aa40d`_
_Re-verified: 2026-08-03T18:20:00Z — round 4, truth 12 only, after `9d8383c`_
_Re-verified: 2026-08-03T19:40:00Z — round 5, truth 12 only, after `b645740`_
_Verifier: Claude (gsd-verifier)_
_Verifier: Claude (gsd-verifier)_
