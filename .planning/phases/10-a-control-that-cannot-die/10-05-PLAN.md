---
phase: 10-a-control-that-cannot-die
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - scripts/mutation_check.py
  - CLAUDE.md
autonomous: true
requirements: [REQ-24]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Criterion 5: at least one new mutation is registered as M44, observed CAUGHT, and anchored on BEHAVIOUR — a line whose removal changes WHICH HEALTH ARM a dead control reaches — never on message text or on a prose comment"
    - "Two candidate anchors are broken in the sandbox, both kill sets recorded, the more informative one registered, and the rejected one written into the prose block as evidence rather than discarded"
    - "M44's kill set is compared against the existing registry and the comparison is RECORDED. A subset result is written down as a subset result — it buys localisation and not detection — on M42's precedent, rather than rounded up into coverage it does not provide"
    - "M21-M24 stay empty and the intentional-gap marker count does not fall below the 9 it stands at today, checked by command"
    - "M30 was re-anchored by `10-01` in wave 1 and that is not a new ident: the range, the total and the next free ident all move by exactly one here, for M44 alone"
    - "`CLAUDE.md`'s registry counts are ADVANCED in the same commit that registers M44 — the ident range, the ident total, the next free ident and the gap-marker count. Nothing in the suite gates them, which is why they were stale for three days once"
    - "`make verify-offline` exits 0 AND its verdict line is read and quoted verbatim — PASS, PASS (OFFLINE) and PASS (INCOMPLETE) are three different outcomes and the exit code alone distinguishes none of them"
    - "A five-criterion verdict table is written, every verdict citing where it was measured, MET IN PART used wherever it is the honest answer, and no criterion reworded"
    - "Criterion 3's row carries the OFFLINE premise correction from `10-DECISIONS.md` Collision 10 — that Best Buy's control has never been shown dead, was last shown alive in early August, and had been unmeasured for roughly four weeks — WHATEVER read 1 returned. It was established without the wire and a live result cannot unestablish it"
  prohibitions:
    - "A mutation is never anchored on message text or on a prose comment. M44 must rebuild a BEHAVIOUR — a dead control reaching the wrong health arm — so that a refactor preserving the behaviour and rewriting every word around it leaves the mutation still caught."
    - "An ident is never registered that raises the denominator without defending anything. If every break is already caught by a second independent test, that is recorded and the ident is not taken."
    - "M21-M24 are never filled. `apply_mutation` cannot add a file, so the defect they would have covered is outside the harness by construction, and the marker count must not fall."
    - "A criterion is never reworded so that it passes. MET IN PART and `not measured` are shippable outcomes here."
    - "No live retailer request, no `boty check`, no write to the three live documents, and no `systemctl restart boty`."
  artifacts:
    - path: "scripts/mutation_check.py"
      provides: "M44, anchored on the dead-control behaviour, with its kill-set comparison recorded, its rejected anchor kept as evidence, and the intentional-gap rule restated rather than weakened"
      contains: "M44"
    - path: "CLAUDE.md"
      provides: "registry counts advanced in the same commit that moves the thing they describe"
      contains: "M44"
  key_links:
    - from: "scripts/mutation_check.py::M44"
      to: "the dead-control state in boty/monitor.py and boty/retailers.py"
      via: "a mutation that rebuilds the defect — a dead control reported as a broken detector — killed by criterion 1's three-state tests"
      pattern: "M44"
---

<objective>
Close the phase: register the mutation that would notice if a dead control started being reported as
somebody else's fault again, read the gate's verdict line rather than its exit code, and write a
verdict table that rounds nothing up.

**Purpose.** Criterion 5 asks for a mutation observed CAUGHT. A gate nobody has watched fail is not a
gate, and a mutation anchored on prose is a gate that tests the prose. M44 must rebuild **this
phase's defect** — a control whose target no longer resolves being reported as a probably-broken
detector — so that a future refactor which preserves the three states and rewrites every word around
them leaves it still caught.

And it must be honest about what it buys. M42 was registered and only afterwards measured to have a
kill set that is a proper subset of M33's, so it buys localisation and not detection, which is
written into its own comment block rather than smoothed over. M43's was measured disjoint from all
thirty-eight before it. The same measurement is owed here, and a subset result is written down as
one.

**Output.** M44 caught and honestly priced, `CLAUDE.md`'s counts moved in the same commit, a green
`make verify-offline` whose verdict line is quoted, and a five-criterion table.

## The five criteria this phase is judged on (verbatim from ROADMAP.md, unedited)

  1. Three states are **distinct** and asserted separately: *dead control* (our SKU no longer resolves), *refused* (the retailer served a challenge), and *detector broken* (the page arrived and the extractor could not read it). Today the first is reported as the third
  2. A dead control does **not** describe the retailer as refusing us, and does not silence a real refusal if both are true at once
  3. Best Buy's control is repaired — measured against a real reading, not a fixture — or replaced with one that reads, with the replacement's durability argued
  4. **The rule for what makes a control durable is written down and applied to every existing control**, with any control failing it named. Best Buy's died because it was a specific game SKU; a control that can be discontinued eventually will be
  5. `make verify-offline` exits 0, with at least one new mutation registered and observed CAUGHT
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-a-control-that-cannot-die/10-PLAN-OUTLINE.md
@.planning/phases/10-a-control-that-cannot-die/10-DECISIONS.md
@.planning/phases/10-a-control-that-cannot-die/10-01-SUMMARY.md
@.planning/phases/10-a-control-that-cannot-die/10-02-SUMMARY.md
@.planning/phases/10-a-control-that-cannot-die/10-03-SUMMARY.md
@.planning/phases/10-a-control-that-cannot-die/10-04-SUMMARY.md
@CLAUDE.md

**Reproduced so `.planning/STATE.md` need not be referenced:** never invoke a `gsd-tools` state or
phase WRITE subcommand, and this plan does not edit `STATE.md` or `ROADMAP.md`. Recording the phase's
close in those files is the phase-completion step's business, not this plan's.
</context>

<tasks>

<task type="auto">
  <name>Task 1: M44 — a mutation that rebuilds the misattribution, observed CAUGHT and honestly priced</name>
  <files>scripts/mutation_check.py</files>
  <precondition>`10-04` is committed and `make verify-offline` was last seen green with the mutation registry at its pre-M44 tally. Read that tally out of `10-04-SUMMARY.md` or from a run before starting; the new run must show exactly one more mutation and the same number of survivors. Note that `10-01` **re-anchored M30** in wave 1 — the ident count did not change, but the file did, and its SUMMARY records the re-point.</precondition>
  <read_first>
    - `scripts/mutation_check.py` — M43's prose block and its `Mutation(...)` entry: the form for a registration, including the what-it-rebuilds paragraph, the why-it-earned-an-ident paragraph, the recorded kill-set comparison, the rejected-anchor paragraph and the `IF IT EVER SURVIVES:` paragraph
    - `scripts/mutation_check.py` — `SANDBOX_CONTENTS` and the note beside it, so the target is known to be inside the sandbox before anything is written
    - `boty/monitor.py` and `boty/retailers.py` — the two lines that together produce the dead-control state: the field set at the producer, and the arm that reads it
    - `.planning/phases/10-a-control-that-cannot-die/10-01-SUMMARY.md` and `10-02-SUMMARY.md` — the tests that exist to kill this mutation, so the expected kill set is known before it is measured
  </read_first>
  <action>
Register **M44**, the next free ident, on the dead-control **behaviour**.

**Choose the anchor by measurement, not by taste.** There are two candidate lines and they are not
equivalent. One is the producer — the field set when a page was read and named no matching product.
The other is the arm in `assess_health` that reads it. Break each in the sandbox, record which tests
die for each, and register the one whose kill set is the more informative. Write the comparison into
the prose block either way: **the loser is evidence about what the suite covers, not a discarded
draft**, and 09-05 recorded its rejected anchor for exactly this reason.

**Then measure M44's kill set against the existing registry**, before the SUMMARY is written and
before the ident is priced:

- if M44 kills tests no other mutation kills, say so with the list;
- if M44's kill set is a subset of another's, **say that instead**, name the mutation it sits under,
  and record that the honest remedy if a future reader judges the localisation insufficient is to
  delete the ident rather than to reword the paragraph until it sounds like coverage.

Registering an ident that raises the denominator without defending anything is the failure this
registry's own rules name, and the registry records that reasoning too.

**Restate the intentional-gap rule rather than weakening it.** M21–M24 stay empty because
`apply_mutation` cannot add a file, so the defect they would have covered is outside the harness by
construction. M44 must reach its target without anything being added to the sandbox — confirm that
before writing the block, the way M41 and M43 did.

**Then run the harness and observe it CAUGHT.** Record the full tally: mutations run, caught,
survivors. A survivor is a finding and stops this task. Record M44's own kill count.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/mutation_check.py 2>&1 | tail -20 && grep -c "INTENTIONAL GAP" scripts/mutation_check.py</automated>
  </verify>
  <done>M44 is registered on behaviour, observed CAUGHT with its kill count recorded, both candidate anchors broken and both kill sets written into the prose block, its kill set compared against the registry with the result recorded whichever way it falls, and the intentional-gap rule restated with its marker count not fallen.</done>
</task>

<task type="auto">
  <name>Task 2: The registry counts, advanced in the same commit that moves them</name>
  <files>CLAUDE.md</files>
  <precondition>Task 1 is committed and the new tally is known from an actual run, not predicted.</precondition>
  <read_first>
    - `CLAUDE.md` § *Registries and invariants* — the ident range, the ident total, the caught/survivor line, the gap-marker count, the next-free-ident sentence, and the parenthetical that records how long these were stale last time
    - `.planning/phases/09-out-of-lockstep/09-05-SUMMARY.md` — how 09-05 advanced them alongside M43, which is the precedent this follows
  </read_first>
  <action>
Advance `CLAUDE.md`'s registry counts in the same commit that registers M44. They are **correct
today** — M1–M20 and M25–M43, thirty-nine idents, all caught, survivors zero, nine gap markers, next
free M44 — because 09-05 moved them when it moved the thing they describe.

Move: the ident range, the ident total, the caught/survivor line, the gap-marker count as measured by
command rather than assumed, and the next-free-ident sentence. Nothing in the suite gates these
numbers; a count that is only ever corrected by a code review is a count that will be wrong again by
the time anybody looks, and the parenthetical in that section already records the three days they
were stale and the collision that would have caused.

**Add a sentence about what M44 defends**, in the section's own voice — one or two lines, naming the
behaviour rather than the phase, so a reader who never opens `.planning/` still learns what the ident
is for.

**And check the rest of that section against this phase.** If anything else in `CLAUDE.md` went false
here — a claim about how a control failure is reported, or about what the health arms say — correct
it as part of this work. A stale `CLAUDE.md` misleads every future agent, and several of its rules
exist only because somebody learned them the expensive way.
  </action>
  <verify>
    <automated>grep -n "M44\|M45\|INTENTIONAL GAP" CLAUDE.md | head -20 && .venv/bin/python scripts/identity_check.py --all</automated>
  </verify>
  <done>The ident range, total, caught/survivor line, gap-marker count and next-free ident are all advanced in the same commit as M44, with the gap count taken from a command rather than assumed; a sentence names what M44 defends; and anything else in `CLAUDE.md` this phase falsified is corrected.</done>
</task>

<task type="auto">
  <name>Task 3: The gate's verdict line, the five-criterion table, and what this phase does not claim</name>
  <files>CLAUDE.md</files>
  <precondition>Tasks 1 and 2 are committed.</precondition>
  <read_first>
    - `Makefile` — which verdict lines `verify` and `verify-offline` can print and what each means
    - `.planning/phases/09-out-of-lockstep/09-05-SUMMARY.md` — the verdict-table form: one row per criterion, the verdict, where it was measured, and the half that is not met where there is one
    - `10-01-SUMMARY.md` … `10-04-SUMMARY.md` — the citations every row of the table must point at
  </read_first>
  <action>
Run `export NVM_DIR=$HOME/.nvm; . $NVM_DIR/nvm.sh; make verify-offline` and **read the verdict line**.
`PASS`, `PASS (OFFLINE — live controls were NOT run)` and `PASS (INCOMPLETE — some controls could not
run on this host)` are three different outcomes and exit 0 distinguishes none of them. Quote the line
verbatim in the SUMMARY with the test count, the skip summary, the mypy result and the mutation tally
beside it.

**Check that nothing silently skipped.** A JS-executing gate has skipped inside this target before
because `make` does not inherit nvm; source it first and confirm the dashboard test bound rather than
skipped.

Write the **five-criterion verdict table** into the SUMMARY. One row per criterion, in the ROADMAP's
order, each with the verdict — **MET AS WRITTEN**, **MET IN PART** or **NOT MET** — where it was
measured by file and test name, and, where it is MET IN PART, the half that is not met stated in the
same sentence as the half that is.

**Criterion 3's row has two halves and the first one is not conditional.** `10-DECISIONS.md`
Collision 10 established **offline, from the evidence log with line citations**, that Best Buy's
control has never been shown dead, was last shown alive in early August, and had been unmeasured for
roughly four weeks — absent-then-stale evidence, not contrary. **That finding goes in the row
whatever read 1 returned**: a refusal leaves it standing, a successful read confirms it, an
unresolved result dates the death inside the window it bounds. Then the second half — what the wire
actually said. A row that reports only the live result would throw away the more durable of the two
findings, and a row that reported the premise as settled *by* the read would misattribute it.

**Do not reword a criterion to make a row read better.** Criterion 3 is the one that depends on a
third party answering us, and `10-04`'s branch table was written before the first request precisely
so that its verdict could be stated without renegotiating the criterion. Criterion 4's row must name
the controls the rule condemned; a row that says the rule was applied without naming what it caught
is the summary rounding up what the table below it says.

Then write § *What this phase does not claim*, and make it concrete rather than modest:

- **Nothing is on the wire.** `boty` is an editable install, so this phase reaches the daemon at the
  next restart — Dan's call, deferred, and now carrying Phases 8, 9 and 10 together. Say what it will
  do on the day it happens: the new health state becomes reachable in production and, if a control
  changed, the health gate starts vouching for a different page.
- **A reading is not a distribution.** Whatever `10-04` measured, it measured once.
- **The controls this phase named and did not repair are still named and not repaired**, and the
  legibility gap at rung 3 is still open, with the invent-a-status remedy still refused.
  </action>
  <verify>
    <automated>export NVM_DIR=$HOME/.nvm; . $NVM_DIR/nvm.sh; make verify-offline 2>&1 | tail -30</automated>
  </verify>
  <done>`make verify-offline` exits 0 with its verdict line quoted verbatim beside the test, skip, mypy and mutation tallies and a confirmation that the JS-executing gate bound rather than skipped; a five-criterion verdict table cites where each verdict was measured and rounds nothing up; and § *What this phase does not claim* names the deferred restart, the single reading and the controls left named and unrepaired.</done>
</task>

</tasks>

<threat_model>
This plan adds **no production code and no network surface**: it registers a mutation, corrects a
guide, and runs the gate.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the mutation harness -> a sandbox copy of the tree | `apply_mutation` deliberately breaks source in a sandbox; nothing it does may reach the working tree |
| a verdict table -> a future reader | a row that rounds up is what a later phase will cite as settled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Repudiation | the five-criterion verdict table | high | mitigate | a rounded-up row is a record of a check nobody passed. Every row cites a file and a test name, MET IN PART names its missing half in the same sentence, and criterion 4's row names the controls the rule condemned rather than reporting that a rule exists |
| T-10-15 | Tampering | `scripts/mutation_check.py`'s sandbox | low | accept | the harness copies the tree and mutates the copy; the sandbox contents are already argued at their definition. M44 must reach its target without adding anything to that list, which Task 1 confirms |
| T-10-16 | Repudiation | `CLAUDE.md`'s registry counts | medium | mitigate | nothing in the suite gates them and they were stale for three days once, which would have handed the next agent an ident collision. They move in the same commit as M44 and the gap count is taken from a command rather than assumed |

**There is no `T-10-SC` row:** this plan installs no package.

Rows `T-10-01` through `T-10-13` are carried from `10-01` … `10-04` and mitigated there. This plan
introduces none of them and adds no new production surface.
</threat_model>

<verification>
Every command from the repository root, `.venv/bin/python` and never bare `python`; source nvm before
anything invoking `make`.

1. **M44 was observed CAUGHT**, with the full tally recorded — mutations run, caught, survivors 0 —
   and its own kill count stated.
2. **Both candidate anchors were broken and both kill sets recorded**, with the reason the registered
   one was chosen.
3. **M44's kill set was compared against the existing registry** and the result written into its
   prose block, whichever way it fell.
4. **M21–M24 are still empty** and the gap-marker count has not fallen below 9, checked by command.
5. **`CLAUDE.md`'s registry counts moved in the same commit as M44**, with a sentence naming what M44
   defends.
6. **`make verify-offline` exits 0 and its verdict line is quoted verbatim**, with confirmation that
   the JS-executing gate bound rather than skipped.
7. **The verdict table cites a file and a test name per row**, uses MET IN PART where that is honest,
   names the condemned controls in criterion 4's row, carries the offline premise correction in
   criterion 3's row whatever the wire said, and reworded no criterion.
8. **No live retailer request**, no `boty check`, no write to the three live documents, no restart.
</verification>

<success_criteria>
- [ ] M44 registered on behaviour, observed CAUGHT, survivors 0
- [ ] Both candidate anchors measured and the comparison recorded, the rejected one kept as evidence
- [ ] M44's kill set compared against the registry, a subset result recorded as a subset result
- [ ] M21–M24 still empty; the gap-marker count has not fallen, checked by command
- [ ] `CLAUDE.md`'s range, total, caught/survivor line, gap count and next free ident all advanced
- [ ] A sentence in `CLAUDE.md` naming what M44 defends, and anything this phase falsified corrected
- [ ] `make verify-offline` exits 0 with its verdict line quoted verbatim
- [ ] The JS-executing gate confirmed bound rather than skipped
- [ ] A five-criterion verdict table with per-row citations and nothing rounded up
- [ ] Criterion 4's row names the controls the rule condemned
- [ ] Criterion 3's row carries the offline premise correction unconditionally, beside what the wire said
- [ ] § *What this phase does not claim* names the deferred restart, the single reading, and the gaps left open
</success_criteria>

<output>
Create `.planning/phases/10-a-control-that-cannot-die/10-05-SUMMARY.md` when done.
</output>
