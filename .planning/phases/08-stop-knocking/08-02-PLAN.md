---
phase: 08-stop-knocking
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - boty/pacing.py
  - tests/test_pacing.py
  - tests/test_cli_watch.py
  - scripts/mutation_check.py
autonomous: true
requirements: [REQ-22]

estimate:
  tokens: 160000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - >-
      REQ-22 / C1 — BOUNDARY PROBE, authored explicitly. At 29 consecutive refusals
      `Pacer.current_interval` returns exactly 21600.0 on a 300.0 standing interval and exactly
      21600.0 on an 1800.0 one; at 30 it returns exactly 259200.0 on both; at 31 it returns
      exactly 259200.0 on both. Hand-written literal seconds, asserted AT the step and never
      re-derived through `current_interval`, `BACKOFF_FACTOR`, `MAX_BACKOFF_SECONDS` or
      `COOLOFF_SECONDS`. The threshold is pinned at the step, not near it.
    - >-
      REQ-22 / C1 — PRECISION PROBE, authored explicitly. The cool-off wait is an exact literal
      reached WITHOUT exponentiation: at `st.refusals >= REFUSALS_BEFORE_COOLOFF` the
      `st.interval * BACKOFF_FACTOR ** st.refusals` sub-expression is never evaluated at all, so
      no float rounding contract is entered and `==` rather than `pytest.approx` is the correct
      assertion. `skipped_reason`'s format specifiers are presentation and never feed `record`'s
      `due_at`. `MAX_PERSISTED_REFUSALS = 64` keeps the pre-threshold path at `2.0 ** 64`, far
      below the `2.0 ** 1024` cliff measured on 2026-08-10.
    - >-
      REQ-22 / C2. Over a simulated sequence of 300 s cycles spanning a whole cool-off window, a
      retailer at or past the threshold is requested exactly once — a stated integer counted by
      the repo's own `if p.due(...): count += 1; p.record(...)` idiom over a plain float `now`,
      never inferred from the interval and never with a clock library.
    - >-
      REQ-22 / C3-after. The number of requests made to a never-recovering retailer over a
      simulated 30 days under the new rule is a STATED literal, recorded beside 08-01's
      before-number, and it states its zero-restart assumption in the same breath and names the
      per-restart cost as exactly one request.
    - >-
      REQ-22 / C4. A retailer that answers at cool-off depth is back on its standing interval on
      the very next read of `current_interval` and its `refusals` is 0 — asserted AT depth
      (at or past `REFUSALS_BEFORE_COOLOFF`), never one refusal in.
    - >-
      The cool-off reaches the published number and the fetch schedule as ONE expression. The
      same 259200.0 appears in `record`'s `due_at`, in `cli._current_intervals` and in
      `status.write`'s `current_interval_seconds`, proved end-to-end off the written bytes rather
      than off a returned object.
    - >-
      `MAX_BACKOFF_SECONDS` is still `6 * 60 * 60`. What this plan replaces is that ceiling being
      applied INDEFINITELY, not its value — and the surviving assertion that a cap beyond a few
      hours is not a monitor stays alive to keep the distinction.
    - >-
      Nothing about `Pacer.load`, `Pacer.save`, `_RetailerState`'s fields or `STATE_VERSION`
      moves in this plan. The days-long cool-off is therefore discarded by a six-hour staleness
      window until 08-03 re-derives it. That is a RECORDED ONE-WAVE GAP, stated out loud, not a
      shipped defect: nothing is deployed mid-phase and a restart is Dan's call.
  artifacts:
    - "boty/pacing.py — REFUSALS_BEFORE_COOLOFF = 30"
    - "boty/pacing.py — COOLOFF_SECONDS = 3 * 24 * 60 * 60"
    - "boty/pacing.py — a cool-off branch inside the existing max(st.interval, ...) in Pacer.current_interval"
    - "boty/pacing.py — a cool-off arm in Pacer.skipped_reason"
    - "boty/pacing.py — dated reversal on the MAX_BACKOFF_SECONDS comment and on the module docstring's cap paragraph"
    - "tests/test_pacing.py — _CADENCE_ACROSS_THE_COOLOFF_THRESHOLD and its parametrized test"
    - "tests/test_pacing.py — a single-probe simulated-cycles test, a recovery-at-depth test, a 30-day after-count test, a skipped_reason arm test"
    - "tests/test_pacing.py — test_the_backoff_is_capped_so_a_monitor_does_not_quietly_stop_monitoring, rewritten with the dated reversal"
    - "tests/test_cli_watch.py — the end-to-end tracer test"
    - "scripts/mutation_check.py — M33 and M38 anchors re-pointed and both kill sets re-measured"
  key_links:
    - "Pacer.current_interval -> Pacer.record's due_at: record computes its wait THROUGH the accessor, so the cool-off reaches the fetch schedule with no second site"
    - "Pacer.current_interval -> cli._current_intervals -> status.write's current_interval_seconds: the published cadence and the fetch schedule are one expression"
    - "Pacer.due's 150 s tolerance vs a 259200 s cool-off: four orders of magnitude apart, so W1 cannot make a cool-off leak — and the cool-off must not be written in a way that depends on that tolerance being correct"
    - "REFUSALS_BEFORE_COOLOFF = 30 vs MAX_PERSISTED_REFUSALS = 64: the threshold sits under the clamp, so a restored count can cross it"
    - "The cool-off branch's source text vs scripts/mutation_check.py M33 and M38: both anchor on the verbatim return max(...) block, so editing it drifts both anchors"
  prohibitions:
    - statement: "A cool-off never becomes never-ask-again."
      why: >-
        A retailer left alone for days is still on the schedule, still counted, and still
        published with a cadence. A wait that never expires is a silently dropped retailer with a
        row on the dashboard — this project's core defect one level up.
    - statement: "A cool-off is never used to silence a failure that is ours."
      why: >-
        If the refusal is really a dead control or a broken detector, a days-long silence hides
        it for days. That is Phase 10's problem to fix and this phase's not to make worse.
---

<objective>
REQ-22, the middle four of six criteria: a retailer that has refused 30 times in a row stops being
asked every six hours and is left alone for three days, then probed once — and the days-scale
number reaches the fetch schedule and the published `current_interval_seconds` as ONE expression.

Purpose: 08-01 recorded what the current rule costs against a retailer that never recovers. This
plan changes the rule, records what the new one costs, and pins the boundary with hand-written
literal seconds so the threshold is asserted at the step rather than near it.

Output: two constants and one branch in `boty/pacing.py`; four new gates and one dated-reversal
rewrite in `tests/test_pacing.py`; one end-to-end test in `tests/test_cli_watch.py`; two
re-pointed mutation anchors in `scripts/mutation_check.py`.

**Criteria covered here:** C1 (literal days-scale wait at the boundary), C2 (exactly one probe
over simulated cycles), C3-after (the stated 30-day count under the new rule), C4 (recovery
clears the cool-off at depth).
**Criteria NOT covered here:** C5 (restart survival — `08-03`), C6 (M42 and the verdict line —
`08-04`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-stop-knocking/08-PLAN-OUTLINE.md
@.planning/phases/08-stop-knocking/08-PATTERNS.md
@.planning/phases/08-stop-knocking/08-CONTEXT.md
@.planning/phases/08-stop-knocking/08-DECISIONS.md
@boty/pacing.py
@tests/test_pacing.py
</context>

---

## The literals, fixed here so 08-03 and 08-04 do not have to guess

`REFUSALS_BEFORE_COOLOFF = 30`

- The phase goal says *"30-odd times"*, and 30 is the number that makes collision A **bind**: the
  existing `test_the_backoff_is_capped_so_a_monitor_does_not_quietly_stop_monitoring`
  (`tests/test_pacing.py:176`) drives exactly `range(30)` and asserts the old ceiling. Choosing 31
  or more would let that test survive untouched, which is cheaper and worse — the sentence REQ-22
  exists to overrule would still be standing, unremarked, at the exact boundary criterion 1 is
  about.
- It sits comfortably under `MAX_PERSISTED_REFUSALS = 64`, so a count restored from disk can
  actually cross it. A threshold at or above the clamp would be a threshold that can never be
  reached from a restart.
- **The outline's third constraint is now vacuous and that is recorded rather than quietly
  satisfied.** The outline asks for a threshold above `cli.REFUSALS_BEFORE_PAGING = 5`. That
  constant **no longer exists**: `boty/cli.py:432-451` records that it and
  `_refusal_is_entrenched` were deleted on 2026-08-12 under Dan's no-paging rule, and
  `test_the_clamp_never_restores_a_shallower_wait_than_the_cap`
  (`tests/test_pacing.py:884`) was re-anchored that day for the same reason. 30 is above 5 anyway,
  so the constraint is satisfied — but it is satisfied against a deleted symbol, and the honest
  form of that is to say so. (`boty/pacing.py:175` still names `cli.REFUSALS_BEFORE_PAGING` in the
  `MAX_PERSISTED_REFUSALS` comment. That is a pre-existing stale cross-reference, it is **not**
  this plan's to fix, and no task below touches it.)

`COOLOFF_SECONDS = 3 * 24 * 60 * 60` — that is **259200.0** seconds.

- REQ-22 says *"a period measured in days"*. Three days is unambiguously days-plural; two is the
  smallest thing that can be called days and reads as a rounding-up of "a couple".
- Written as `3 * 24 * 60 * 60` in the source, on `MAX_BACKOFF_SECONDS`' own precedent
  (`6 * 60 * 60`), so the units are legible at the definition site. It is a **literal**: no
  exponentiation, no `BACKOFF_FACTOR`, no derivation from the cap. That is what makes the
  precision probe in `must_haves.truths` true rather than hoped for.
- Three days rather than seven because `COOLOFF_SECONDS` is what `08-03` will derive
  `STATE_MAX_AGE_SECONDS` from. A week-long staleness window would hold a refusal record as
  evidence for a week, which weakens the stale-file answer the module docstring gives at length
  for a marginal reduction — measured below, 3 days buys 37 requests and 7 days buys 33.

---

## The numbers, and how they were reached

Projected 2026-08-28 by the plan writer, by replicating the scheduling arithmetic
(`now + interval * 0.5 >= due_at`, then `max(interval, min(interval * 2.0 ** refusals, 21600))`)
in a scratch script over 8640 cycles of 300 s — 30 days — against a retailer that never recovers
and never restarts:

| Rule | 30-day request count |
|---|---|
| current (fixed 6 h ceiling, applied indefinitely) | **125** |
| threshold 30, cool-off 2 days | 41 |
| **threshold 30, cool-off 3 days (chosen)** | **37** |
| threshold 30, cool-off 5 days | 34 |
| threshold 30, cool-off 7 days | 33 |

**These are a projection, not a measurement, and the distinction is load-bearing.** They were
computed against a re-implementation of the arithmetic, not against the real `Pacer`. The
executor must run the real simulation against the real class and record what it actually returns.
**If the measured number differs from 37, the measured number wins**, it goes into the test as the
literal, and the difference is recorded in the SUMMARY with the reason. Rewriting 37 into the test
without running it is the one unforgivable move wearing a measurement's clothes.

`08-01` owns the before-number independently. If `08-01` recorded something other than 125, **use
`08-01`'s number** — it was measured against the unmodified tree, which this plan can no longer
do — and record the discrepancy rather than reconciling it silently.

---

## Collisions this plan must clear (three, one of which the outline did not name)

**Collision A — `tests/test_pacing.py:176` asserts the ceiling this plan stops applying.**
Named by the outline. `test_the_backoff_is_capped_so_a_monitor_does_not_quietly_stop_monitoring`
runs `for _ in range(30)` and asserts `due_at - now == MAX_BACKOFF_SECONDS`. At a threshold of 30
it goes red the moment the branch lands, and **that red is the measurement that the threshold
binds at exactly 30**. Handled in Task 1.

**Collision B — the dashboard's `fmtDur` tops out in hours.** `served/boty/index.html:118` renders
259200 as `72h`. Legible, and it is a presentation decision `08-04` must make deliberately. **This
plan does not touch `index.html`** and does not pre-empt that decision; it records that the number
now reaching the page is 259200.0 so `08-04` decides against a fact.

**Collision C — NOT IN THE OUTLINE, found by reading `scripts/mutation_check.py` on 2026-08-28.**
`M33` (line 960) and `M38` (line 1235) both carry a `search` that is the **verbatim seven-line
source** of `current_interval`'s `return max(st.interval, min(st.interval * BACKOFF_FACTOR **
st.refusals, MAX_BACKOFF_SECONDS))` block. `apply_mutation` (line 1573) raises `HarnessError` when
its anchor is not found, and `main` turns that into **exit 2 with "This is not a result"** — so
the instant the branch lands, `make verify-offline`'s mutation pass stops being a result at all.
The registry has been here before and left a protocol: M33's own comment block (lines 942-951)
records the 2026-08-17 re-anchor as *"the fifth anchor in this registry to drift"* and states that
its kill set was **re-measured against the new anchor rather than carried over**. Task 1 follows
that protocol for both idents.

**Why the branch goes inside the `max(...)` anyway, knowing it costs two anchors.** A guard clause
returning `max(st.interval, COOLOFF_SECONDS)` before the existing `return` would preserve both
anchors verbatim and is the cheaper edit. It is the wrong one: it creates a **second**
`max(st.interval, ...)` site, and "a backoff may only ever widen the wait" is a rule this module
keeps in exactly one place on purpose (`boty/pacing.py:380-415`). Two copies of a rule are two
things to edit and they only have to disagree once. The anchor drift is the price, it is a price
this registry has paid five times with a written protocol for paying it, and paying it is cheaper
than a second copy of the module's central invariant.

---

## The recorded one-wave gap (state this in the SUMMARY, do not close it here)

Between this plan and `08-03` the tree holds a **three-day cool-off that a restart discards after
six hours.** `STATE_MAX_AGE_SECONDS` is still `MAX_BACKOFF_SECONDS` (`boty/pacing.py:151`), so
`Pacer.load`'s two-sided window throws away any refusal record stamped more than 6 h ago — which,
once a retailer is in a three-day cool-off, is every record it will ever have.

This is deliberate and it is the outline's decision, not an oversight. Collision 1's
`STATE_MAX_AGE_SECONDS` re-derivation lands in `08-03` **so that `08-03`'s tests are genuinely red
before its one-line fix rather than red against a synthetic revert.** Nothing is deployed
mid-phase, no `systemctl restart boty` is a deliverable of any plan in this phase, and a restart
is Dan's call. It is a recorded gap, not a shipped defect, and the difference is that it is
written down here before it exists.

A second thing is handed to `08-03` rather than decided here: after this plan `refusals` past 30
means something it did not mean before, which is exactly the kind of policy-units change
`STATE_VERSION`'s comment (`boty/pacing.py:96-140`) says a bump is owed for. The outline assigns
that argument — and the argument for **no** new `_RetailerState` field — to `08-03`. This plan
adds no persisted structure, so it does not pre-empt it, and it names the question rather than
leaving `08-03` to find it.

---

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One retailer, past the threshold, end to end — with every behavioural gate watched red first</name>

  <read_first>
    - `boty/pacing.py` — the module docstring's cap argument (lines 16-27), `MAX_BACKOFF_SECONDS`
      and its comment (lines 91-94), `MAX_PERSISTED_REFUSALS`' overflow argument (lines 153-179),
      `Pacer.record` (273-307), `Pacer.current_interval` in full including its comment block
      (309-422), `Pacer.due` (262-271)
    - `tests/test_pacing.py` — the imports at line 51,
      `test_the_backoff_is_capped_so_a_monitor_does_not_quietly_stop_monitoring` (176-182),
      the simulated-cycles idiom at 275-277, `_CADENCE_AFTER_N_REFUSALS` and the literals argument
      above it (364-382), the parametrized reader at 442-460, and
      `test_a_retailer_that_answers_is_back_on_its_standing_interval_at_once` (463-476)
    - `tests/test_cli_watch.py` — `_check_config` (398), `_checker` (84), `_offline` (438),
      `_published_cadence` (657-666), and `test_both_surfaces_publish_one_cadence_from_one_document`
      (669-739) as the shape to copy
    - `boty/cli.py` — `_current_intervals` (225-250)
    - `scripts/mutation_check.py` — `Mutation` (174), `apply_mutation` (1573-1586),
      `check_mutation`'s CAUGHT print (1597-1602), `main` (1620-1650), the M33 comment block and
      entry (942-963), and the M38 comment block and entry (1228-1240)
    - `.planning/phases/08-stop-knocking/08-01-SUMMARY.md` — for the before-number to record beside
  </read_first>

  <files>boty/pacing.py, tests/test_pacing.py, tests/test_cli_watch.py, scripts/mutation_check.py</files>

  <reversibility rating="reversible">
    Both constants and the branch are a handful of lines under version control; the two prose
    reversals are additive by house rule (superseded text is recorded beside, never edited away),
    so nothing here destroys a prior statement. Nothing persisted changes shape.
  </reversibility>

  <behavior>
    - At 29 consecutive refusals, `current_interval` is 21600.0 at a 300.0 standing interval and
      21600.0 at an 1800.0 one — unchanged from today.
    - At 30, it is 259200.0 at both. At 31, it is 259200.0 at both.
    - Driven to 30 refusals, then simulated forward in 300 s cycles across a whole cool-off window,
      the retailer is requested exactly once.
    - Driven to 30 refusals and then answered once, `refusals` is 0 and `current_interval` is the
      standing interval on the very next read.
    - A daemon cycle over a retailer at 30 refusals publishes `current_interval_seconds` of
      259200.0 into `status.json`, on the paced branch, with the retailer reported as not checked.
  </behavior>

  <action>
Do these steps **in this order**. The order is the whole of this task's validity: the cool-off is
one branch, so once it lands every test of its behaviour is green from birth. This is the phase's
only natural red window and all four gates open inside it. Record the ACTUAL failure counts and
the ACTUAL observed values at every red step — a predicted count is not a measurement.

**Step 1 — the constants alone, and prove they change nothing.** Add `REFUSALS_BEFORE_COOLOFF = 30`
and `COOLOFF_SECONDS = 3 * 24 * 60 * 60` to `boty/pacing.py`, next to `MAX_BACKOFF_SECONDS`, each
with a `#:` comment block carrying the reasoning from the *"The literals"* section above — the
"30-odd" goal, why 30 makes the superseded test bind, the position under `MAX_PERSISTED_REFUSALS`
and the recorded fact that `cli.REFUSALS_BEFORE_PAGING` no longer exists; and for the duration,
why days, why a literal rather than a derivation, and that `08-03` will derive the staleness
window from it. Add nothing else. Run `.venv/bin/python -m pytest tests/test_pacing.py -q` and
record that it is still fully green: two unreferenced constants are not a behaviour change, and
saying so is what makes the next step's red attributable to the branch.

**Step 2 — the tracer test, written and watched red on its own.** In `tests/test_cli_watch.py`,
under the REQ-21 section that already owns the cross-surface join, add
`test_a_retailer_in_cooloff_publishes_the_days_scale_cadence_it_is_actually_on`. Build it on
`test_both_surfaces_publish_one_cadence_from_one_document`'s shape: `_check_config(tmp_path)`,
`Config.load`, a `Pacer` constructed exactly as `watch_loop` constructs one (`default_interval`,
`overrides`, `state_path`), then `for _ in range(REFUSALS_BEFORE_COOLOFF): pacer.record("gamestop",
refused=True, now=0.0)`. Drive the loop off the constant so a later threshold edit cannot leave the
tracer testing an off-threshold depth. Then call `cli.watch_cycle` with that pacer at `now=0.0`
and assert, off the written bytes via `_published_cadence(cfg)`, that
`current_interval_seconds == 259200.0` — the **literal**, never `COOLOFF_SECONDS`, so the assertion
is not a re-derivation of the constant it is checking. Assert in the same test that the row's
`checked` is `False` (this is the paced branch, which is the branch a cool-off always lands on),
and that `pacer._for("gamestop").due_at == 259200.0` so `record`'s schedule and the published
number are pinned to each other rather than separately. Give it a docstring saying what it is: the
one path proving the cool-off reaches the fetch schedule and the published number as one
expression, which is why the branch goes inside the accessor. Run
`.venv/bin/python -m pytest tests/test_cli_watch.py -q`. **Record the failure count and the value
actually published** — it will be the old ceiling.

**Step 3 — the three pacing-level gates, in the same red window.** In `tests/test_pacing.py`:

  (a) *Criterion 1, the boundary.* Add `_CADENCE_ACROSS_THE_COOLOFF_THRESHOLD` as a sibling of
  `_CADENCE_AFTER_N_REFUSALS` — not an extension of it, because that list is indexed by N from 0
  and running it to 31 would bury the boundary in twenty-two flat entries. Shape it as
  `(standing_interval, refusals, expected_seconds)` triples with six rows: 300.0 at 29 → 21600.0,
  at 30 → 259200.0, at 31 → 259200.0; and 1800.0 at 29 → 21600.0, at 30 → 259200.0, at 31 →
  259200.0. Every number **hand-written**. Carry a `#:` comment block above it restating
  `_CADENCE_AFTER_N_REFUSALS`' own argument in this table's terms — computing these through
  `current_interval`, `BACKOFF_FACTOR`, `MAX_BACKOFF_SECONDS` or `COOLOFF_SECONDS` would make every
  assertion a re-derivation of the code it checks, which is a test that cannot fail — and stating
  that the rows are N-1, N and N+1 on purpose, so the threshold is asserted **at** the step rather
  than near it. Add `test_the_cadence_across_the_cooloff_threshold_is_the_literal_it_is`,
  parametrized over the table, driving `p.record(retailer, refused=True, now=0.0)` the stated
  number of times against a fresh `Pacer(default_interval=interval)` and asserting
  `p.current_interval(...) == expected` with `==` and never `pytest.approx` — the precision probe
  in `must_haves.truths` is exactly the claim that equality is the right operator here.

  (b) *Criterion 2, exactly one probe.* Add
  `test_a_retailer_in_cooloff_is_probed_exactly_once_when_it_expires`. Use the repo's idiom
  verbatim in shape: `now` is a plain float, `for _ in range(N): if p.due(r, now): count += 1;
  p.record(r, refused=True, now=now); now += 300`. No `freezegun`, no fixture, no monkeypatched
  clock. Drive to `REFUSALS_BEFORE_COOLOFF` refusals first, note the cycle count at which that
  happened, then simulate a window comfortably longer than one cool-off (at least
  `259200 / 300 = 864` further cycles, and state the count in the test rather than computing it
  from `COOLOFF_SECONDS`) and assert the probe count over that window is exactly 1 — a stated
  integer. Give it a docstring saying why this is a simulation and not an inference: an interval of
  259200 is consistent with one probe and with none, and only driving `due` every cycle
  distinguishes them.

  (c) *Criterion 4, recovery at depth.* Add
  `test_a_retailer_that_answers_during_its_probe_is_back_on_its_standing_interval_at_once`,
  modelled on `test_a_retailer_that_answers_is_back_on_its_standing_interval_at_once` (line 463)
  and asserted at **cool-off** depth rather than at the cap, for the same stated reason that one
  is asserted at the cap rather than one refusal in — the interesting direction is a retailer
  coming back from three days. Drive `REFUSALS_BEFORE_COOLOFF` refusals against
  `Pacer(default_interval=300, overrides={"amazon": 1800})`, assert `current_interval` is
  259200.0, record one `refused=False`, then assert `refusals == 0`, `current_interval == 1800.0`
  and `due_at == 1800.0` — the field, the accessor and the schedule, because a reset that cleared
  the count without reaching the schedule would pass on the first assertion alone.

  Run `.venv/bin/python -m pytest tests/test_pacing.py -q`. **Record the failure count and, for
  each of the three, the value actually returned.** Expect the boundary table red on its N=30 and
  N=31 rows and green on N=29; expect the probe count to be far above 1; expect the recovery
  test's at-depth setup assertion to fail on the old ceiling. If any of the three does **not** go
  red, stop: that is a finding about the gate, not a formality to skip, and it goes in the SUMMARY.

**Step 4 — land the branch.** In `Pacer.current_interval`, replace the second argument of the
existing `max(...)` with a conditional expression: `COOLOFF_SECONDS` when
`st.refusals >= REFUSALS_BEFORE_COOLOFF`, otherwise the existing
`min(st.interval * BACKOFF_FACTOR ** st.refusals, MAX_BACKOFF_SECONDS)` **unchanged**. There is
still exactly one `return`, still exactly one `max(st.interval, ...)`, and the `.get`-not-`_for`
read at the top of the method is untouched. Extend the method's comment block with a paragraph
recording three things: that the branch sits inside the `max` so the widen-only rule applies to
the cool-off too and is stated in one place rather than two; that `COOLOFF_SECONDS` is a literal
reached without evaluating `BACKOFF_FACTOR ** st.refusals` at all, so no rounding contract is
entered past the threshold and the overflow cliff `MAX_PERSISTED_REFUSALS` guards is unreachable
from this arm; and that `record` computes its wait through this accessor, which is why the
cool-off reaches `due_at` and `status.json` with no second site. Re-run both test modules and
record all five gates green.

**Step 5 — collision A, and its red is a measurement.** Run
`.venv/bin/python -m pytest tests/test_pacing.py -q`.
`test_the_backoff_is_capped_so_a_monitor_does_not_quietly_stop_monitoring` is now red. **Record
that failure and the value it saw** — it is the direct measurement that the threshold binds at
exactly 30 and nowhere else, and it is the only place in this phase that fact is observed rather
than asserted. Then rewrite the test in the house dated-reversal form: quote the withdrawn
assertion in full inside the docstring, dated; state the two measured facts that overruled it
(REQ-22 for a **persistently** refused retailer, and the measured 30-day count going from the
before-number to the after-number); state what survives. What survives is the second assertion,
`MAX_BACKOFF_SECONDS <= 6 * 60 * 60`, and it stays **alive and unmodified** — `MAX_BACKOFF_SECONDS`
is still six hours, because what this phase replaces is that ceiling being applied *indefinitely*
and not its value. Repoint the first assertion at the boundary the test now guards: at
`REFUSALS_BEFORE_COOLOFF - 1` refusals the wait is still the six-hour cap. Rename it only if the
old name has become false; prefer keeping the name so `git log -S` still finds its history.

**Step 6 — collision C, both anchors, protocol from M33's own comment.** Run
`.venv/bin/python scripts/mutation_check.py` and **record what it printed**: a `HARNESS ERROR` for
M33 and exit 2, not a mutation result. Note that `main` aborts at the first miss, so M38's
identical miss is invisible until M33's is repaired — repair M33, re-run, then repair M38. For
each: re-point `search` to the new verbatim source of the block (leading whitespace included), and
choose `replace` so the **meaning** of each ident is unchanged — M33 still makes the accessor
ignore the backoff entirely and answer with the standing interval; M38 still strips the outer
`max` so a refusal can shorten the wait above the cap. Then **re-measure each kill set from
scratch** by reading the `CAUGHT` line's failed-test count and names off the run; do **not** carry
the previous counts over — M33's comment says that in as many words. Extend each ident's comment
block with a dated anchor-drift note in M33's own form: that the anchor drifted because the CODE
changed shape rather than for a prose or version reason, what the new shape is, that the gate's
meaning is unchanged, and the re-measured kill set. State the drift ordinal honestly by counting
the re-anchor notes already in the file rather than assuming this plan's is the sixth. Re-run the
whole script and record `41/41 mutations caught`, or the actual figures if they differ.

Do not touch `served/boty/index.html`, `boty/monitor.py`, `boty/status.py`, `boty/cli.py`,
`Pacer.load`, `Pacer.save`, `_RetailerState` or `STATE_VERSION` in this task.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pacing.py tests/test_cli_watch.py -q</automated>
    <automated>.venv/bin/python scripts/mutation_check.py</automated>
    <automated>grep -c "REFUSALS_BEFORE_COOLOFF\|COOLOFF_SECONDS" boty/pacing.py</automated>
  </verify>

  <acceptance_criteria>
    - **Source:** `grep -n "^REFUSALS_BEFORE_COOLOFF = 30$" boty/pacing.py` and
      `grep -n "^COOLOFF_SECONDS = 3 \* 24 \* 60 \* 60$" boty/pacing.py` each return exactly one
      line.
    - **Source:** `grep -n "MAX_BACKOFF_SECONDS = 6 \* 60 \* 60" boty/pacing.py` still returns one
      line — the cap's VALUE is untouched.
    - **Source:** `boty/pacing.py`'s `current_interval` still contains exactly one `return max(`
      and exactly one `min(` — verified by reading the method, not by a count that a comment could
      inflate.
    - **Source:** `grep -c "259200" tests/test_pacing.py` is at least 4 (the boundary table's four
      cool-off rows), and `grep -c "259200" tests/test_cli_watch.py` is at least 1.
    - **Behaviour:** `.venv/bin/python -m pytest tests/test_pacing.py tests/test_cli_watch.py -q`
      exits 0 with no test skipped in either module.
    - **Behaviour:** `.venv/bin/python -c "from boty.pacing import Pacer; p = Pacer(default_interval=300.0);
      [p.record('x', refused=True, now=0.0) for _ in range(29)]; print(p.current_interval('x'))"`
      prints `21600.0`; the same with `range(30)` prints `259200.0`.
    - **Behaviour:** `.venv/bin/python scripts/mutation_check.py` exits **0** and prints a
      `mutation check: N/N mutations caught` line with no survivors and no `HARNESS ERROR`.
    - **Recorded:** the SUMMARY carries, as five separate measured numbers, the failure count from
      step 1 (zero), step 2, step 3, step 5 (collision A) and step 6 (the harness error), each with
      the value actually observed alongside the value expected.
    - **Recorded:** the SUMMARY carries the re-measured kill sets for M33 and M38 as counts read
      off the run, and states explicitly that they were re-measured rather than carried over.
  </acceptance_criteria>

  <done>
    `REFUSALS_BEFORE_COOLOFF = 30` and `COOLOFF_SECONDS = 259200` exist with their reasoning; the
    cool-off branch is inside `current_interval`'s single `max(...)`; a retailer driven to 30
    refusals publishes `current_interval_seconds` of 259200.0 into `status.json` off a real
    `watch_cycle`; the boundary table pins 29/30/31 at two standing intervals with hand-written
    literals; one probe per cool-off window and immediate recovery at depth are both asserted over
    simulated cycles; `test_the_backoff_is_capped_so_a_monitor_does_not_quietly_stop_monitoring`
    carries the dated reversal and its six-hour assertion still runs; M33 and M38 are re-anchored
    with re-measured kill sets and the mutation script is a result again. Every red was watched and
    counted before it was fixed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The prose surface, and the two sentences the branch overruled</name>

  <read_first>
    - `boty/pacing.py` — the module docstring's cap paragraph (lines 16-27) and its existing
      dated-reversal precedent immediately below it (29-75), `MAX_BACKOFF_SECONDS`' comment
      (91-94), the `STATE_VERSION` comment's reversal form (105-139), `Pacer.skipped_reason`
      (424-445)
    - `tests/test_pacing.py` — the `skipped_reason` assertions at 332-337 and the truthiness
      assertion at 969, so neither is broken by the new arm
    - `tests/test_status.py:437-457` — `test_the_current_cadence_is_published_for_a_paced_out_retailer_too`,
      which hand-supplies a `paced` string and is therefore unaffected; confirm rather than assume
    - `.planning/phases/08-stop-knocking/08-PATTERNS.md` § *The reversal convention* — the exact
      house form
  </read_first>

  <files>boty/pacing.py, tests/test_pacing.py</files>

  <precondition>
    Task 1 is committed and `.venv/bin/python -m pytest tests/test_pacing.py -q` exits 0. If it
    does not, the red observed in step 1 below is not attributable to the missing arm and this task
    cannot start.
  </precondition>

  <behavior>
    - At fewer than 30 consecutive refusals, `skipped_reason` returns the existing backing-off
      prose, byte-unchanged. The existing assertion at three refusals still passes.
    - At 30 or more, it returns prose naming the cool-off, the refusal count, and the remaining
      wait expressed in **days** — because a true wait of "~4320 min" is a number no reader of the
      dashboard can act on.
    - The number `skipped_reason` formats never reaches `record` or `due_at`.
  </behavior>

  <action>
**Step 1 — write the gate and watch it red.** In `tests/test_pacing.py`, beside the existing
`skipped_reason` assertions, add `test_a_retailer_in_cooloff_says_so_rather_than_reporting_a_minute_count`.
Drive `REFUSALS_BEFORE_COOLOFF` refusals at `now=0.0` against `Pacer(default_interval=300)`, then
assert the string returned by `skipped_reason(retailer, 0.0)` names the cool-off, carries the
refusal count, and expresses the remaining wait in days rather than minutes. Add a second
assertion in the same test that at three refusals the prose is **still** the backing-off form, so
the arm is proved to be a new branch and not a replacement of the old one. Run
`.venv/bin/python -m pytest tests/test_pacing.py -q` and **record the failure count and the string
actually returned** — it will be the minutes form.

**Step 2 — add the arm.** In `Pacer.skipped_reason`, insert a branch ahead of the existing
`if st.refusals:` arm, guarded on `st.refusals >= REFUSALS_BEFORE_COOLOFF`, returning prose built
from the same `max(0.0, st.due_at - now)` quantity the method already computes, divided by 86400
and formatted to one decimal place. One decimal, not zero: a partial day formatted to zero
decimals renders a live wait as "0 days", which is the confident lie this module's own
`skipped_reason` docstring exists to prevent. Extend the method's comment block with a paragraph
stating that the format specifiers here are **presentation only** and never feed the schedule —
`record` computes `due_at` from `current_interval`'s float and never from this string — and that
this is why the boundary table asserts exact equality while this method is allowed to round.
Re-run and record green, including the two pre-existing `skipped_reason` assertions.

**Step 3 — the module docstring's cap paragraph.** The sentence at `boty/pacing.py:24-27` — that
the backoff *"is capped because a monitor that has backed off to once a day has quietly stopped
being a monitor"* — is the argument REQ-22 overrules, and it must be withdrawn in the house form
the file already uses twice (lines 29-75 and 105-139): quote the withdrawn sentence **in full**,
dated 2026-08-28; then the measured facts that overruled it — the before-number and after-number
from this phase's simulations, and the 2026-08-20 daemon measurement of gamestop at 14400 s and
walmart at 21600 s, both on backoff and neither recovering; then what survives. What survives is
the paragraph's premise and not its conclusion: a monitor that quietly stops monitoring is still
the failure to avoid, and the answer is now that the retailer is still on the schedule, still
counted, still published with a cadence, and probed once every three days — left alone, not
dropped. Say explicitly that a cool-off is not a never-ask-again, because that is the prohibition
this plan carries and a docstring is where a future reader will look for it.

**Step 4 — `MAX_BACKOFF_SECONDS`' comment.** Same form, shorter. The comment at lines 91-94 opens
*"Never wait longer than this between attempts, however many refusals"*, and that clause is now
false: past `REFUSALS_BEFORE_COOLOFF` the wait is `COOLOFF_SECONDS`. Quote it, date it, and state
what survives — **the value is unchanged at six hours**, the cap still governs every refusal count
below the threshold, and what moved is that it is no longer applied indefinitely. Point the reader
at `REFUSALS_BEFORE_COOLOFF` for where it stops governing, and note that
`test_the_backoff_is_capped_so_a_monitor_does_not_quietly_stop_monitoring` still asserts the
six-hour bound so the distinction between the value and its indefinite application is held by a
test rather than by this comment.

Add no new constant, no new field, and no new persisted structure in this task.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pacing.py tests/test_status.py -q</automated>
    <automated>grep -c "WITHDRAWN ON 2026-08-28" boty/pacing.py</automated>
  </verify>

  <acceptance_criteria>
    - **Source:** `grep -c "WITHDRAWN ON 2026-08-28" boty/pacing.py` returns at least 2 — one for
      the module docstring's cap paragraph, one for the `MAX_BACKOFF_SECONDS` comment.
    - **Source:** `grep -n "MAX_BACKOFF_SECONDS = 6 \* 60 \* 60" boty/pacing.py` still returns one
      line. The reversal is a rewrite of the argument, never an edit to the value.
    - **Behaviour:** `.venv/bin/python -c "from boty.pacing import Pacer, REFUSALS_BEFORE_COOLOFF as N;
      p = Pacer(default_interval=300.0); [p.record('x', refused=True, now=0.0) for _ in range(N)];
      print(p.skipped_reason('x', 0.0))"` prints prose naming the cool-off and a wait in days, and
      the same script with `range(3)` prints the existing backing-off-in-minutes prose.
    - **Behaviour:** `.venv/bin/python -m pytest tests/test_pacing.py tests/test_status.py -q`
      exits 0; the pre-existing assertion `"backing off after 3 refusal(s)"` still passes,
      unmodified.
    - **Recorded:** the SUMMARY carries step 1's failure count and the exact string the method
      returned before the arm existed.
  </acceptance_criteria>

  <done>
    A retailer at or past the threshold is described on the status page as cooling off, with its
    remaining wait in days; below the threshold the prose is byte-unchanged. The module docstring's
    cap argument and the `MAX_BACKOFF_SECONDS` comment each carry a dated reversal quoting the
    withdrawn text in full and stating what survives, and neither reversal moved a value.
  </done>
</task>

<task type="auto">
  <name>Task 3: The 30-day number under the new rule, stated beside the old one, and the gap said out loud</name>

  <read_first>
    - `tests/test_pacing.py` — the simulated-cycles idiom at 275-277, and the 30-day baseline test
      `08-01` added (read it before writing this one; the two must be the same simulation with one
      rule changed, or the comparison is between two different questions)
    - `.planning/phases/08-stop-knocking/08-01-SUMMARY.md` and
      `.planning/phases/08-stop-knocking/08-DECISIONS.md` — the recorded before-number, and
      collision 2's decision that a restart costs exactly one probe
    - `boty/pacing.py` — the module docstring's concession (b), lines 62-75, for the restart
      wording to cite rather than paraphrase
    - `CLAUDE.md` — the three verdict lines `make verify` can print, and the nvm note under
      *Commands*
    - `Makefile` — the `verify-offline` target, to know what it runs before running it
  </read_first>

  <files>tests/test_pacing.py</files>

  <precondition>
    `make` does not inherit nvm, and `tests/test_dashboard.py` needs a JS runtime to parse-check
    `served/boty/index.html` — it has silently SKIPPED inside `make verify-offline` before for
    exactly this reason. Run `export NVM_DIR=$HOME/.nvm; . $NVM_DIR/nvm.sh` in the shell first and
    confirm `node --version` answers. If the dashboard tests report skipped rather than passed, the
    verdict line is not a pass: halt and record it.
  </precondition>

  <action>
**Step 1 — the after-number, measured.** In `tests/test_pacing.py`, immediately beside `08-01`'s
baseline test so a reader sees both without scrolling, add
`test_the_thirty_day_request_count_under_the_cooloff_is_a_stated_number`. It must be the **same
simulation** `08-01` ran with exactly one thing different — the rule — so read that test first and
match its cycle count, its standing interval, its refusal behaviour and its counting idiom rather
than re-deriving them: 8640 cycles of 300 s against a retailer that refuses every time it is
asked, counted by `if p.due(r, now): count += 1; p.record(r, refused=True, now=now); now += 300`.
Run it, read the count it produces, and write **that** number into the assertion as a literal with
`==`. The plan writer's projection is 37; if the measured number differs, the measured number wins
and the difference goes in the SUMMARY with the reason. Assert in the same test that the new count
is strictly smaller than `08-01`'s before-number, written as a literal beside it.

**Step 2 — the assumption, stated in the test and not left to a reader.** The docstring of that
test must state, in its own words, three things. First: **the simulation contains zero restarts**,
and the count is only the count under that assumption. Second: **a restart costs exactly one
request**, because `due_at` is deliberately never persisted — cite the module docstring's
concession (b) rather than paraphrasing it, and state that what a restart inherits is the depth
the penalty resumes at, never the position on the schedule. Third: therefore the honest form of
the claim is *"N requests over 30 days, plus one per restart"*, and a reader who needs the number
for a flapping service must add the restarts themselves. This is the outline's explicit
instruction and it is the difference between a stated number and a number that quietly depends on
an assumption nobody wrote down.

**Step 3 — the recorded gap.** Do not change any code for this step. In the SUMMARY, record the
one-wave gap in the terms the *"The recorded one-wave gap"* section of this plan states it: the
tree now holds a three-day cool-off that a restart discards after six hours, because
`STATE_MAX_AGE_SECONDS` is still derived from `MAX_BACKOFF_SECONDS` and collision 1's
re-derivation deliberately lands in `08-03` so that plan's tests are genuinely red before its fix
rather than red against a synthetic revert. State that nothing is deployed mid-phase, that no plan
in this phase restarts the daemon, and that this is a recorded gap and not a shipped defect.
Record the second hand-off in the same paragraph: `refusals` past 30 now carries a meaning it did
not carry before, which is the kind of policy-units change `STATE_VERSION`'s comment says a bump
may be owed for, and `08-03` owes that argument — including the argument for **no** bump and
**no** new `_RetailerState` field.

**Step 4 — the whole gate, and read the verdict line.** With the nvm precondition satisfied, run
`make verify-offline`. Record the **exit code and the verdict line separately**: `PASS`,
`PASS (OFFLINE — live controls were NOT run)` and `PASS (INCOMPLETE — some controls could not run
on this host)` are three different outcomes and only the line distinguishes them. Confirm in the
output that the dashboard tests **bound** rather than skipped. If the identity checker trips on
prose in any file this plan wrote, fix the prose and not the gate — that has happened here four
times and the rule is in `CLAUDE.md`.

Do not run `boty check`. Do not make any live retailer request. Do not read or write `state.json`,
`pacer-state.json` or `served/boty/status.json`; every test above builds its own `Pacer` in
`tmp_path`. Do not restart the daemon.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pacing.py -q</automated>
    <automated>make verify-offline</automated>
  </verify>

  <acceptance_criteria>
    - **Source:** `grep -c "restart" tests/test_pacing.py` increases relative to `08-01`'s tree,
      and the new 30-day test's docstring contains the zero-restart assumption and the
      one-request-per-restart cost as separate sentences.
    - **Behaviour:** `.venv/bin/python -m pytest tests/test_pacing.py::test_the_thirty_day_request_count_under_the_cooloff_is_a_stated_number -q`
      exits 0, and the asserted count is a literal integer, not an expression over
      `COOLOFF_SECONDS` or `REFUSALS_BEFORE_COOLOFF`.
    - **CLI output:** `make verify-offline` exits 0 **and** its verdict line is transcribed into
      the SUMMARY verbatim, with a statement of which of the three passes it was.
    - **CLI output:** the `make verify-offline` transcript shows `tests/test_dashboard.py` tests
      passing rather than skipped, confirming the JS runtime bound.
    - **Recorded:** the SUMMARY carries both 30-day numbers side by side — `08-01`'s before and
      this plan's after — each labelled as measured, with the reduction stated as a ratio and the
      zero-restart assumption named beside it.
    - **Recorded:** the SUMMARY carries the one-wave gap paragraph and the `STATE_VERSION` hand-off
      to `08-03`.
  </acceptance_criteria>

  <done>
    The 30-day request count under the cool-off rule is a measured literal in a test, sitting
    beside `08-01`'s before-number, with its zero-restart assumption and the one-request-per-restart
    cost stated rather than implied. The one-wave staleness gap and the `STATE_VERSION` question
    are both recorded as hand-offs to `08-03`. `make verify-offline` exits 0 and its verdict line
    has been read and written down.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pacer-state.json` → `Pacer.load` | A file on disk that the daemon writes and a person can hand-edit, parsed into scheduling state. It is the only untrusted input anywhere near this plan. |
| (no network boundary) | This plan adds **no network surface**. It edits local scheduling arithmetic and makes no request — no retailer is contacted by any task above, by design and by `08-CONTEXT.md`'s in-scope statement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Denial of Service | the new cool-off wait arithmetic inside `Pacer.current_interval` | medium | mitigate | The wait must not produce `inf`, a negative, or an absurd value. Three mitigations, each already load-bearing: (1) `COOLOFF_SECONDS` is a **literal** — `3 * 24 * 60 * 60` — reached without evaluating `st.interval * BACKOFF_FACTOR ** st.refusals` at all, because Python's conditional expression does not evaluate the branch it does not take, so past the threshold the overflow path is not merely clamped but unreachable; (2) below the threshold the pre-existing path is bounded by `MAX_PERSISTED_REFUSALS = 64`, i.e. `2.0 ** 64`, far below the `2.0 ** 1024` cliff measured on CPython 3.12.3 on 2026-08-10; (3) the outer `max(st.interval, ...)` survives untouched, so the wait can only ever widen and can never be driven below a configured standing interval. Gated by `_CADENCE_ACROSS_THE_COOLOFF_THRESHOLD`'s exact-equality assertions at 29, 30 and 31 refusals at two standing intervals (Task 1), and by `test_the_clamp_never_restores_a_shallower_wait_than_the_cap`, which is re-run unchanged. |

**No `T-08-SC` row, and that is stated rather than omitted.** This plan installs no npm, pip or
cargo package — it adds two module-level constants and one branch to a file already in the tree —
so there is nothing for the package-legitimacy gate to audit and no `## Package Legitimacy Audit`
table is owed by `08-RESEARCH.md`.

**`T-08-01` (Tampering — `Pacer.load` over `pacer-state.json`, medium, mitigate) is carried by
`08-03`, not dropped.** That plan is the one that touches `load`'s staleness window; this plan adds
no persisted structure, no `_RetailerState` field and no `STATE_VERSION` change, so it adds nothing
for T-08-01 to cover. `08-03` must confirm that in writing rather than assume it.
</threat_model>

<verification>
1. `.venv/bin/python -m pytest tests/test_pacing.py tests/test_cli_watch.py tests/test_status.py -q`
   exits 0, with no test skipped in any of the three modules.
2. `.venv/bin/python scripts/mutation_check.py` exits **0** — not 1 (a survivor) and not 2 (a
   harness error, which is not a result at all) — and every ident including the re-anchored M33 and
   M38 is reported CAUGHT.
3. `make verify-offline` exits 0 **and** its verdict line is read and recorded. The exit code alone
   is not the verdict; `PASS`, `PASS (OFFLINE — live controls were NOT run)` and `PASS (INCOMPLETE
   — some controls could not run on this host)` are three different results.
4. Every gate this plan adds was observed **red before it was trusted**, with the actual failure
   count and the actual observed value recorded — Task 1 steps 1, 2, 3, 5 and 6, and Task 2 step 1.
   The 30-day after-count in Task 3 is the one exception and it must say so in its own words: it is
   a **measurement, not a gate**, exactly as `08-01`'s baseline is, it cannot be made to fail, and
   that is the finding rather than a skipped formality.
5. `git diff` shows no change to `served/boty/index.html`, `boty/monitor.py`, `boty/status.py`,
   `boty/cli.py`, or to `Pacer.load`, `Pacer.save`, `_RetailerState` or `STATE_VERSION` in
   `boty/pacing.py`.
6. `MAX_BACKOFF_SECONDS` is still `6 * 60 * 60`, and the surviving assertion that a cap beyond a
   few hours is not a monitor still runs.
7. No live retailer request was made, `boty check` was never run, and `state.json`,
   `pacer-state.json` and `served/boty/status.json` were neither read nor written.
</verification>

<success_criteria>
- **C1 met:** `current_interval` returns exactly 21600.0 at 29 refusals and exactly 259200.0 at 30
  and 31, at both configured standing intervals, asserted against hand-written literal seconds.
- **C2 met:** exactly one probe over a simulated cool-off window, counted over cycles by the
  repo's own idiom, stated as an integer.
- **C3-after met:** the 30-day request count under the new rule is a measured literal in a test,
  recorded beside `08-01`'s before-number, strictly smaller than it, with the zero-restart
  assumption and the one-request-per-restart cost both stated.
- **C4 met:** a retailer answering at cool-off depth is back on its standing interval on the next
  read, with `refusals`, the accessor and `due_at` all asserted.
- **C5 and C6 are not claimed by this plan.** C5 is `08-03`'s and is explicitly *not met* while
  this plan's wave stands — the recorded one-wave gap. C6 is `08-04`'s. Claiming either here would
  be rounding a claim up.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**`boty/pacing.py`**
- `REFUSALS_BEFORE_COOLOFF = 30` — with its reasoning, including the recorded fact that
  `cli.REFUSALS_BEFORE_PAGING` no longer exists
- `COOLOFF_SECONDS = 3 * 24 * 60 * 60` (259200)
- a cool-off branch **inside** the existing `max(st.interval, ...)` in `Pacer.current_interval` —
  one `return`, one `max`, no second site
- a cool-off arm in `Pacer.skipped_reason`, expressing the remaining wait in days
- dated reversals on the module docstring's cap paragraph and on the `MAX_BACKOFF_SECONDS` comment
- **deliberately absent:** no `_RetailerState` field, no `STATE_VERSION` bump, no change to
  `STATE_MAX_AGE_SECONDS`, `Pacer.load` or `Pacer.save` — all argued in `08-03`

**`tests/test_pacing.py`**
- `_CADENCE_ACROSS_THE_COOLOFF_THRESHOLD` — six hand-written `(interval, refusals, seconds)` rows
  at N-1, N and N+1 for two standing intervals — and its parametrized reader
- `test_a_retailer_in_cooloff_is_probed_exactly_once_when_it_expires`
- `test_a_retailer_that_answers_during_its_probe_is_back_on_its_standing_interval_at_once`
- `test_a_retailer_in_cooloff_says_so_rather_than_reporting_a_minute_count`
- `test_the_thirty_day_request_count_under_the_cooloff_is_a_stated_number`
- `test_the_backoff_is_capped_so_a_monitor_does_not_quietly_stop_monitoring` — **rewritten** with
  the dated reversal, its `MAX_BACKOFF_SECONDS <= 6 * 60 * 60` assertion alive and unmodified

**`tests/test_cli_watch.py`**
- `test_a_retailer_in_cooloff_publishes_the_days_scale_cadence_it_is_actually_on` — the tracer

**`scripts/mutation_check.py`**
- M33 and M38 `search` anchors re-pointed to the new shape of `current_interval`'s return, both
  kill sets **re-measured** rather than carried over, each with a dated anchor-drift note in M33's
  own house form
- **no new ident.** M42 belongs to `08-04`; M21–M24 remain the intentional gap and are never
  filled.

**`served/boty/index.html`** — untouched. The `fmtDur` day band is `08-04`'s decision, and this
plan hands it the fact that the number now reaching the page is 259200.

## Notes for the executor

- **The estimate's confidence is `low` and it is derived, not self-rated.** `estimate-calibration`
  could not be run on this host: `gsd-tools` resolves through `/usr/bin/env node`, and `node` is
  nvm-only and absent from a non-login shell. Zero calibration samples means `low`, and inflating
  it would be the same move as rounding a claim up.
- **This plan does not touch `.planning/ROADMAP.md`.** It is one chunk of a chunked single-plan
  invocation; writing a partial `Plans:` list would clobber its siblings. The orchestrator owns
  that edit once every chunk has landed.
- `boty/pacing.py:175`'s reference to `cli.REFUSALS_BEFORE_PAGING` is a pre-existing stale
  cross-reference to a symbol deleted on 2026-08-12. It is recorded here and is **not** this plan's
  to fix — no task above edits it.

<output>
Create `.planning/phases/08-stop-knocking/08-02-SUMMARY.md` when done.
</output>
