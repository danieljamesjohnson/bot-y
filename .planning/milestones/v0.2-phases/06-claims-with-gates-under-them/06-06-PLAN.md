---
phase: 06-claims-with-gates-under-them
plan: 06
type: execute
wave: 6
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - docs/retailer-evidence.md
autonomous: false
requirements: [REQ-17, REQ-18, REQ-19, REQ-20]

must_haves:
  truths:
    - "The alertability cost of criterion 1 was put to Dan as a MEASURED TABLE — four GO Plus + watches, alertable-before and alertable-after — and his answer is recorded verbatim, including a deferral. Nintendo and Amazon stopping being alertable is stated as a cost he accepted, not as a consequence Claude absorbed"
    - "No lenient rule was offered as a live option. Falling back to the item price reopens the exact hole REQ-17 closes, and the card says so rather than listing it"
    - "Five criterion verdicts stand in the ROADMAP in Phase 3.1's / 4's / 5's three-column format, each carrying a measurement or a reason; any criterion that is unmet is recorded UNMET with the date and the reason, and NO criterion text anywhere was reworded, shortened, merged or amended"
    - "`make verify-offline` exits 0, and its verdict line, test count and mutation ratio are recorded verbatim beside Phase 5's closing baseline of exit 0, 667 passed, 16/16 — so the rise is shown rather than claimed"
    - "The mutation idents actually present in the tree were READ, not assumed, and the deliberate gap at M21-M24 is recorded explicitly — 06-03 and 06-04 each register no mutation by design, and a future reader must not read four lost mutations"
    - "`make verify` was run ONCE, live, and its verdict is recorded VERBATIM including a FAIL, with any class this phase caused named separately from the three recorded on 2026-08-10 (2/6 cannot run — no Chrome binary; the intermittent Walmart/Amazon challenge class; 1/6 Walmart through Phase 5's config-gap guard in a pinless shell)"
    - "Every correction waves 1-5 made to `06-CONTEXT.md`, to `06-PLAN-OUTLINE.md`, to `06-PATTERNS.md` and to REQ-18's own text reaches the closing record, QUOTED from the SUMMARY that made it rather than reconstructed here"
    - "REQ-18's stale `131` and REQ-18's stale claim that Extraction is already bound to the code are FLAGGED as measurement notes and NOT edited — the requirement's text is the record of why the work was done"
    - "The REQ-19 defect's recurrence inside this milestone's own planning is recorded with the evidence for each instance, and the leaked-markup sweep was RE-RUN at close and its actual output recorded rather than the outline's figure repeated"
    - "REQ-17, REQ-18, REQ-19 and REQ-20 carry evidence-bearing traceability cells; the ROADMAP `Plans:` list for Phase 6 exists for the first time; STATE.md's `milestone:` line is NOT touched, and the version binding that now reads it was re-run after STATE.md was edited"
    - "No code was written. `boty/`, `tests/`, `scripts/`, `config/`, `.github/`, `pyproject.toml`, `README.md` and `CHANGELOG.md` are untouched by this plan"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "the five-verdict outcome table in Phase 4's three-column shape, the live `make verify` verdict, and the Phase 6 `Plans:` list no earlier planner wrote"
      contains: "Outcome, recorded"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-17 through REQ-20 flipped with evidence-carrying traceability cells, and REQ-18's two stale claims flagged rather than edited"
      contains: "REQ-17"
    - path: ".planning/STATE.md"
      provides: "the phase's real position, the mutation ident gap, and the corrections waves 1-5 made — with the `milestone:` frontmatter line untouched because a gate now reads it"
      contains: "M21"
    - path: "docs/retailer-evidence.md"
      provides: "## Phase 6 closing record — beside the Phase 5 record, not over it"
      contains: "Phase 6 closing record"
  key_links:
    - from: ".planning/ROADMAP.md outcome table"
      to: "docs/retailer-evidence.md § Phase 6 closing record"
      via: "a citation, the way Phase 3.1's, Phase 4's and Phase 5's tables cite their working"
      pattern: "retailer-evidence\\.md"
    - from: ".planning/REQUIREMENTS.md traceability rows"
      to: "the measurements in the five SUMMARYs"
      via: "REQ-17/18/19/20 cells naming what was measured rather than asserting completion"
      pattern: "REQ-1[789] \\| Phase 6|REQ-20 \\| Phase 6"
    - from: ".planning/STATE.md frontmatter milestone key"
      to: "tests/test_packaging_metadata.py"
      via: "06-05's version binding, which makes any edit to that line a gate-visible act"
      pattern: "milestone"
---

<objective>
Close Phase 6: measure what the five plans actually landed, put the one user-visible cost of
criterion 1 to Dan as a measurement rather than a sentence, and write down all five verdicts —
including any that is not met.

**Purpose.** This milestone exists because the system stated things it had not established. A
closing record is the last place that defect can hide and the easiest place to commit it: a table
where every row reads MET is exactly what a phase that quietly reworded one criterion would also
produce. Phase 3.1 was offered a rewrite of its criterion 1 that would have made it meetable and
Dan declined. Phase 4 recorded two criteria UNMET rather than reword them, and its own record says
*"this plan does not get to do what that one refused."* Phase 5 held the line again and closed with
every live row marked NOT OBTAINED. **This plan does not get to do what those three refused.**

And this phase carries a second obligation the others did not. Criterion 1 **costs something
measurable**: 06-01's strict delivered-total rule stops Nintendo's and Amazon's GO Plus + watches
being alertable. The design is settled — the tiebreaker was already recorded — but the *cost* is
Dan's to accept, not Claude's to absorb.

**Output.** One blocking checkpoint carrying a four-row measured table and his verbatim answer; one
`make verify-offline` verdict with the mutation ratio shown rising from 16; one live `make verify`
verdict recorded verbatim including its FAIL; five criterion verdicts in the ROADMAP; four
requirement rows carrying measurements; the Phase 6 `Plans:` list that no earlier planner wrote; and
a `## Phase 6 closing record` in `docs/retailer-evidence.md`.

**NOT in this plan, and the executor must not reach for it.**

- *No code.* `boty/`, `tests/`, `scripts/`, `config/`, `.github/`, `pyproject.toml`, `README.md`,
  `CHANGELOG.md`, `MANIFEST.in` and `Makefile` are finished. 05-04's recorded decision governs:
  *"a closing plan that implemented its way to a green table would be a phase measuring work it did
  in the act of measuring."* If a criterion turns out unmet, it is **recorded** unmet.
- *No rewording of any success criterion, anywhere in the ROADMAP.* Not Phase 6's five, not any
  earlier phase's. Not to make one meetable, not to "clarify" one, not to merge two.
- *No edit to any requirement's text*, including REQ-18's two stale claims. They are flagged in the
  traceability cell, which is the cell's job.
- *No touch to Phase 5's closing table* or to any part of the Phase 5 section.
- *No fixing of the live `make verify` failure classes.* No Chrome binary on this host, and the
  intermittent Walmart/Amazon challenge class, are pre-existing since 2026-08-06; the Walmart
  config-gap class is Phase 5's and is *correct*. All three are recorded, none is diagnosed
  (`06-CONTEXT.md` § *Deferred Ideas*).
- *No deployment.* Dan answered `defer` on 2026-08-10; the daemon still runs 2026-08-04 code
  (`MainPID=3059142`). That is **recorded**, not pending here. `QUESTIONS.md` § 0f holds it.
- *Nothing beyond this phase.* No `boty/config.py` shipping declaration, no `deploy/boty-secret`
  store subcommand, no `identity_check` prose rule, no `_identity_leaks` reconciliation, no fixture
  re-capture. Each is already flagged for a later plan and stays flagged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-claims-with-gates-under-them/06-CONTEXT.md
@.planning/phases/06-claims-with-gates-under-them/06-PLAN-OUTLINE.md
@.planning/phases/06-claims-with-gates-under-them/06-01-SUMMARY.md
@.planning/phases/06-claims-with-gates-under-them/06-02-SUMMARY.md
@.planning/phases/06-claims-with-gates-under-them/06-03-SUMMARY.md
@.planning/phases/06-claims-with-gates-under-them/06-04-SUMMARY.md
@.planning/phases/06-claims-with-gates-under-them/06-05-SUMMARY.md

**Read all five SUMMARYs before writing a single verdict, and cite them rather than paraphrasing
them.** A closing record that summarises a transcript it could have quoted is a claim about a
measurement instead of the measurement. Each SUMMARY carries the transcript its verdict rests on:
06-01's four-watch alertability re-measurement and its M4 re-anchor; 06-02's re-measured `131` and
its `CAUGHT M19` line; 06-03's exit-0-before / exit-1-after pair on an identical file and command;
06-04's byte-for-byte recovery of the shipped Phase 4 `CHANGELOG.md` and the same pair on it;
06-05's STATE binding observed RED against the real tree before the roll and GREEN after.

**Where the SUMMARY disagrees with anything written below, the SUMMARY wins** — it was written
against code that exists, and this plan was written before it did. Record the disagreement rather
than resolving it silently.

## The three prior outcome tables, and which shape goes where

Measured against the shipped documents rather than remembered:

- **`ROADMAP.md` uses three columns** — `| # | Verdict | Measurement or reason |` — in Phase 3.1's
  table, Phase 4's and Phase 5's. The criteria are listed immediately above in that file, so
  restating them would duplicate.
- **`docs/retailer-evidence.md` uses four** — `| # | Criterion | Verdict | What settles it |`. The
  criteria are not restated nearby there, so the column earns its place.

Follow both, exactly as 05-04 did.

## The baselines this record measures against

- **Phase 5's close, 2026-08-10:** `make verify-offline` exit 0, **667 passed**, **16/16
  mutations**. Pre-milestone (Phase 4's close): 531 passed, 8/8.
- **The planned mutation arithmetic**, which Task 1 must *verify against the tree* rather than
  assume: 06-01 registers M17-M18, 06-02 registers M19-M20, **06-03 registers none by design and
  left M21-M22 unallocated**, **06-04 registers none by design and left M23-M24 unallocated**,
  06-05 registers M25-M26. Expected total **22**, with a **deliberate ident gap at M21-M24**.
- **`make verify` has read `VERIFY: FAIL (live controls)`, exit 2, since 2026-08-06**, re-measured
  at Phase 5's close in three classes (STATE.md): (1) `2/6 control(s) could not run on THIS HOST` —
  Best Buy and Target, no Chrome/Chromium binary, which the tool itself says *"says nothing about
  the DETECTOR"*; (2) the Walmart/Amazon challenge class, which **did not manifest** on 2026-08-10
  and is therefore intermittent rather than permanent; (3) `1/6 control(s) not reading IN_STOCK` —
  Walmart through Phase 5's config-gap guard, because `make verify` runs with no
  `WALMART_STORE_ID`.
- **06-01 F1 pre-measured the blast radius of criterion 1 on that live run:** every `max_price` in
  `config/products.yaml` is on a GO Plus + product watch, and **every control watch carries none**,
  so `alertable` short-circuits at `if self.watch.max_price is None: return True` and no control's
  verdict can change under any ceiling edit. The live run either confirms that or falsifies it.
  Whichever it does is what the record says.

## What the five plans corrected, and where each correction was first written

Each of these must reach the closing record, **quoted from the SUMMARY that measured it**. The
pointers below exist so nothing is lost if a SUMMARY is terse — they are not a substitute for
reading the SUMMARY.

| Correction | First written | Corrects |
|---|---|---|
| `06-CONTEXT.md` claims `test_support_matrix.py` *"already binds the README's Routing and Extraction cells to the code in both directions"*. It binds cell **to cell** — both directions are inside the table. No test anywhere compared a matrix cell to a `Rung` member or to `boty/retailers.py`. **The code side was new construction in 06-02** | `06-PLAN-OUTLINE.md` § *CONTEXT-1*, re-confirmed by 06-02 | `06-CONTEXT.md` |
| **REQ-18's own text carries the same stale claim** — *"Routing and Extraction are already pinned; Rung is the gap"* (`REQUIREMENTS.md:77`). **Flag it in the traceability cell. Do not edit it** | this plan | `REQUIREMENTS.md` |
| `06-CONTEXT.md` frames unresolvable shipping as the edge case. **On Walmart it is the common path** — every numeric shipping field is `null` in both captures and no payload this repo has ever captured shows Walmart's paid-marketplace shipping shape | `06-PLAN-OUTLINE.md` § *CONTEXT-2*, honoured by 06-01 | `06-CONTEXT.md` |
| **REQ-18's `131`** (tests green under the `check_amazon` Rung mutation) is a pre-Phase-5 figure against a 667-test suite. 06-02 **re-measured** it through the harness's own sandbox | 06-02 Task 1 | a stale figure, **not** the criterion |
| The outline's Walmart free-shipping rule (two agreeing signals) resolves **one** of the two fixtures, not both — `goplusplus` → `0.0`, `milk-control` → **unresolved**, which is the correct fail-safe answer and costs nothing because milk carries no ceiling | 06-01 § F2 | `06-PLAN-OUTLINE.md`, `06-PATTERNS.md` |
| `fulfillmentOptions[type=DELIVERY].speedDetails.fulfillmentPrice` carries a real `7.95` on `milk-control` — a **from-store delivery fee on a pickup/delivery item**, not shipping. Reading it would have **invented a $7.95 shipping charge out of a real field**. It is read nowhere | 06-01 § F2 | `06-PATTERNS.md`'s *"every numeric shipping field null"* |
| 06-03 **proved the workflow gap by executing it**: a third workflow with one violation per rule family written into the real `.github/workflows/`, `pytest tests/` → **exit 0, 667 passed**. Not one of the 667 tests noticed | 06-03 § F1 | nothing — it is the criterion, measured |
| **06-03 registers no mutation, by design.** **06-04 registers no mutation, by design.** So **M21-M24 are an intentional ident gap** | 06-03 § F8, 06-04 § *No mutation* | a future reader who would otherwise see four lost mutations |
| The outline's proposed 06-05 pairing (`pyproject` ↔ `CHANGELOG`) would have left **both** version rules skipping under `make mutation`, because `CHANGELOG.md` is absent from `SANDBOX_CONTENTS` — the exact defect the pairing exists to prevent. Re-anchored on **`README.md`**, which *is* in `SANDBOX_CONTENTS` and states the version in exactly one line | 06-05 § F3 | `06-PLAN-OUTLINE.md` § *Finding 7* |
| `06-PATTERNS.md` and the outline both name the exit-code rule `_flattened_exit_codes`. **No such function exists.** It is `_flattening` | 06-03 § F2 | two planning documents, **not** the criterion or REQ-19 |

## The REQ-19 defect recurring inside this milestone's own planning

This is the strongest evidence the gate was needed and it belongs in the record — but it is a
**claim about counts**, in a phase about claims, so it is measured rather than repeated.

What is on the record today, with its provenance:

- **`05-02-PLAN.md`** was written with a stray tool-call tag pair at its end and **caught by a
  planning agent before commit** (`06-CONTEXT.md` § *Specific Ideas*). Being caught pre-commit means
  it leaves no trace in the tree — the evidence is the note, not a sweep hit.
- **`06-PATTERNS.md`** became a sweep hit **in the act of measuring the sweep** — the document that
  measured the defect committed it (`06-PLAN-OUTLINE.md` § *Finding 3*).
- 06-04 § F4 re-measured the sweep and recorded the opposite finding for the later plans:
  ***"`06-01-PLAN.md`, `06-02-PLAN.md` and `06-03-PLAN.md` were written since that measurement and
  none became a hit"*** — the discipline of naming the tag shapes without reproducing them worked.

**So a third committed instance is not established by the tree.** Task 3 re-runs the sweep, records
its actual output, and states the recurrence with each instance's evidence — naming the pre-commit
catch as a near-miss rather than a hit. **If the count in a SUMMARY and the count from the sweep
disagree, record both with their dates.** Repeating a number nobody re-measured is the defect this
milestone exists to close, and a closing record is the last place it can hide.

*(This plan deliberately names the tag shapes without reproducing them, exactly as the outline and
the five plans did. It must not become the next file in that table.)*

## Two hazards this phase created for its own closing plan

1. **`.planning/STATE.md`'s `milestone:` frontmatter line is now machine-read** by 06-05's version
   binding. Editing STATE.md is therefore a gate-visible act, and the version rule must be re-run
   **after** the edit. `milestone: v0.2` itself must not change — v0.2 is the milestone Phase 6
   completes, not a milestone Phase 6 supersedes.
2. **`docs/retailer-evidence.md` is read by `scripts/evidence_check.py`**, which `make test`
   exercises through `tests/test_evidence_check.py`. Appending a closing record to that file can
   trip a rule — rule 6 in particular, which requires a `REFUSED` verdict to cite a measured
   observation. Re-run the suite after writing it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure what landed — the gate, the mutations, the ident gap, and the cost</name>
  <files>(no files modified — this task produces the measurements the record and the checkpoint cite)</files>

  <action>
**Read all five SUMMARYs first.** Everything below either confirms them against the tree or extracts
a figure the record needs. Where this plan and a SUMMARY disagree, the SUMMARY wins and the
disagreement is recorded.

### 1a — The phase gate, offline

Run `make verify-offline` **once**. Record, verbatim: the exit code, the verdict line, the
`N passed` line, and the mutation `caught/total` ratio. Put Phase 5's closing baseline — **exit 0,
667 passed, 16/16** — and the pre-milestone **531 passed, 8/8** beside them, plus each SUMMARY's
intermediate count, so the rise is **shown** rather than claimed.

**It is slow now** — 600+ tests and 18+ mutation sandboxes, each a `copytree` plus `git init` plus
`git add -A`. Allow it minutes. **Do not kill it and do not reduce it to `make test`.** A timeout is
not a verdict; if it does time out, raise the timeout and re-run, and record that you did.

If it is red, **stop**. Do not spend a live pass, do not write a verdict table, and do not fix the
tree — report which stage failed and what it said. A closing plan that repaired its own phase would
be measuring work it did in the act of measuring.

### 1b — The mutation idents, READ rather than assumed

Do not trust this plan's arithmetic. Read the registry:
`grep -v '^[[:space:]]*#' scripts/mutation_check.py | grep -o 'ident="M[0-9]*"' | sort -V -u`.
The comment filter matters — `grep -c` over an unfiltered file counts comment prose, and this
project has been bitten by exactly that class of self-invalidating count.

Record the full ident list and, from `make verify-offline`'s mutation stage output, each new ident
observed **CAUGHT** by name. Expected: **M17, M18, M19, M20, M25, M26** present, **22 total**, and
**M21, M22, M23, M24 absent**.

**Write the gap down explicitly, and write down why**, quoting 06-03's and 06-04's SUMMARYs: each
registers **no mutation by design**, and each recorded its reasons at the time. A reader meeting
`… M20, M25 …` with no note reads four lost mutations and starts looking for them. If the idents
present differ from the expected set, **record what is there** and reconcile it against the
SUMMARYs — the tree wins.

### 1c — The four-watch alertability table, re-measured

06-01 Task 3 produced this table against the built tree and 06-01-SUMMARY.md carries it. **Quote it
from the SUMMARY.** Its expected shape, from 06-01's `<decision>` block at today's `max_price: 80`:

| GO Plus + watch | Delivered total establishable? | Alertable before 06-01 | Alertable after 06-01 |
|---|---|---|---|
| GameStop | Yes — 54.99 + 6.99 = 61.98, under 80 | yes | **yes** |
| Walmart | Yes — 229.99 + 0.00, over 80 (and the seller filter suppresses it first) | yes, when a first-party offer under the ceiling appears | **yes, same condition** |
| Nintendo | **No** — `shippingDetails` is prose | yes | **NO — this watch stops being alertable** |
| Amazon | **No** — the reader is a button | yes | **NO — this watch stops being alertable** |

**If 06-01-SUMMARY's measured table differs from this, the SUMMARY's table is the one that goes to
Dan**, and the difference is recorded. If for any reason the SUMMARY does not carry it, re-measure
it **offline** through the shipped captures with `retailers.get` monkeypatched, exactly as 06-01
Task 3 specifies. **Do not probe a retailer to obtain it.**

Also extract, for the card: Nintendo is the one entry in `config/products.yaml` listing the GO
Plus + at its **$54.99 MSRP with no marketplace attached** (`README.md`'s Nintendo row) — so the
honest rule silences the watch most likely to be a genuine restock. That sentence is the cost, and
it is why this goes to a person.

### 1d — The live gate, once

Start `make verify` only **after** checking `served/boty/status.json`'s `updated` so a daemon cycle
and this pass are not in flight against the same six retailers at once. Politeness is a hard
constraint here and this close spends exactly **one** live pass.

Record the verdict **verbatim, including a FAIL**, with `control_check.py`'s own classification of
every failing control. Then **separate the classes**:

- **Pre-existing, not this phase's:** Best Buy and Target unable to run (no Chrome/Chromium binary
  on this host — STATE.md records that Playwright's Chromium at
  `~/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome` works when `BOTY_BROWSER_PATH` points
  at it, and that is a later plan's work); the **intermittent** Walmart/Amazon challenge class,
  which did not manifest on 2026-08-10 — record whether it manifests now, either way;
  **Walmart reading UNKNOWN through Phase 5's config-gap guard**, because `make verify` runs in a
  shell with no `WALMART_STORE_ID`. That third one is Phase 5's, and it is **correct**.
- **Anything else is new and is this phase's until proven otherwise.** 06-01 F1's pre-measurement
  says criterion 1 cannot reach the control stage, because no control watch carries a `max_price`.
  If a control's verdict moved anyway, **that is a finding and it outranks the prediction** — record
  it as caused by this phase, name it separately, and do not fold it in with the three above.

**Do not re-run `make verify` until it is green.** It has read FAIL since 2026-08-06 for reasons
this phase did not cause and is not chartered to fix. Re-running until the number looks better is
the omission this project keeps catching in itself. **Do not run it under the service's
`EnvironmentFile`** — 05-04 recorded that departure from Phase 3.1's method and its reason, and the
reason still holds: it would pull a real store number into a process Claude launched and reads. The
pinless run's output is safe to quote verbatim precisely because that shell has no pin.

**The mutation stage does not run inside a failing live invocation** — `make verify` exits at the
control stage — so mutations are evidenced by 1a's `make verify-offline`, never by this run.

### 1e — The sweep, and the two stale REQ-18 figures

1. **Re-run the leaked-markup sweep** over the tracked tree for the tool-call shapes, and record its
   **actual** output — matching lines and files. The outline and 06-04 § F4 both recorded **9 lines
   in 4 files**; a sweep run while writing this plan returned a slightly different count under a
   slightly different pattern. **Record the pattern you used and the number you got.** Do not repeat
   9 without measuring it, and do not adjust the pattern until the number matches a remembered one.
2. **Quote 06-02-SUMMARY's re-measured figure** for REQ-18's `131` — the `N passed` line from the
   criterion's own mutation applied to a tree with no Rung binding in it. It is a **measurement
   note**. It is not an amendment to the criterion and it is not an edit to REQ-18.
3. **Confirm REQ-18's second stale claim** by reading `REQUIREMENTS.md:77`: *"Routing and Extraction
   are already pinned; Rung is the gap."* Extraction was pinned **cell-to-cell**, not to the code
   (§ *CONTEXT-1*). Note it for Task 3's traceability cell. **Do not edit the requirement.**

Record everything in this task in the SUMMARY as it was observed, before anything is written into
any planning document.
  </action>

  <verify>
    <automated>make verify-offline</automated>
    <automated>grep -v '^[[:space:]]*#' scripts/mutation_check.py | grep -o 'ident="M[0-9]*"' | sort -V -u | tr '\n' ' '; echo; git status --porcelain</automated>
    <automated>.venv/bin/python -c '
import subprocess
lt, gt = chr(60), chr(62)
shapes = [lt + "/invoke" + gt, lt + "/content" + gt, lt + "function_calls" + gt, lt + "parameter", "ant" + "ml:"]
pat = "|".join(shapes)
r = subprocess.run(["git", "grep", "-n", "-E", pat], capture_output=True, text=True)
lines = [l for l in r.stdout.splitlines() if l.strip()]
files = sorted({l.split(":", 1)[0] for l in lines})
print("PATTERN USED:", pat)
print("MEASURED:", len(lines), "matching lines in", len(files), "files")
for f in files: print("   ", f, sum(1 for l in lines if l.startswith(f + ":")))
print("RECORD-ONLY - this command asserts nothing. Copy both numbers into the SUMMARY beside the")
print("outline 9-in-4, state any difference, and do NOT adjust the pattern until it matches that.")
'</automated>
  </verify>

  <done>
    - `make verify-offline` exits 0, and its verdict line, `N passed` line and mutation ratio are
      recorded **verbatim** beside Phase 5's **667 passed, 16/16** and the pre-milestone
      **531 passed, 8/8**. It was allowed to finish; it was not killed and it was not narrowed.
    - The mutation idents were **read from the registry with comment lines filtered out**, recorded
      in full, and each new one observed **CAUGHT** by name. The **deliberate gap at M21-M24** is
      written down with 06-03's and 06-04's own reasons quoted, so nobody reads four lost mutations.
    - The four-watch alertability table is quoted from 06-01-SUMMARY (or re-measured **offline** if
      it is absent), with the two watches that stop being alertable named. **No retailer was probed
      to obtain it.**
    - `make verify` was run **once**, live, after a daemon-cycle check, and its verdict is recorded
      **verbatim including the FAIL**, with any class this phase caused named separately from the
      three of 2026-08-10. It was not re-run and it was not run under the service's
      `EnvironmentFile`.
    - The leaked-markup sweep was re-run; the pattern used and the count obtained are both recorded,
      whether or not they match the outline's 9-in-4.
    - REQ-18's re-measured `131` figure is quoted from 06-02-SUMMARY as a **measurement note**, and
      REQ-18's stale Extraction claim is confirmed at `REQUIREMENTS.md:77` and noted. **Neither is
      edited.**
    - `git status --porcelain` is clean. **No code was written.**
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The cost of criterion 1, put to Dan as a measurement</name>
  <files>(no files modified — this task produces a recorded answer)</files>

  <action>
**Claude's part of this task is to present the card below, stop, and wait.** Task 1 has already done
everything that can be automated: the table is measured, the gate is green offline, the live verdict
is recorded. This checkpoint verifies **after** the automation, which is the only place a checkpoint
belongs.

Fill the card's table from **Task 1c's measured figures**, not from this plan's expected shape. If
they differ, the measured one goes in and the difference is stated on the card.

Route it to his phone with `notify-dan "bot-y: phase 6 close — two GO Plus + watches stop being
alertable" "<one line>"` if he is not at the terminal — the standing autonomous-working contract —
then keep waiting rather than spinning.

**Four things Claude must not do while waiting:**

1. **Do not present a lenient rule as a live option.** Falling back to the item price when a
   retailer publishes no shipping reopens the exact hole REQ-17 closes: Walmart's paid-marketplace
   payload is **unobserved**, so *"publishes nothing"* and *"publishes something we did not read"*
   are indistinguishable on the very retailer this criterion exists for. It is named on the card as
   **rejected, with its reason** — that is not the same as offering it.
2. **Do not present this as a request for permission to have shipped it.** The design is settled and
   the tiebreaker was recorded before this phase began: `REQUIREMENTS.md` § *Non-Functional* —
   *"**Trustworthiness over coverage.** Where they conflict, correctness wins. … This is the
   tiebreaker for every scoping decision."* What is being put to him is the **cost**, which this
   milestone exists so that consequences are stated rather than absorbed.
3. **Do not soften the table.** Two of four product watches stop being able to page him, and one of
   them is the watch most likely to be a genuine MSRP restock. Say it in those words.
4. **Do not proceed on silence.** `autonomous: false` and `gate="blocking"` mean this task ends with
   Dan's answer or it does not end. `workflow.auto_advance` does not apply to it.

**Whatever comes back is recorded verbatim, with the date, including a deferral** — `defer` is one
of the offered answers and 05-04's precedent is that a deferral is recorded rather than worked
around.

**What each answer does, and none of them writes code in this plan:**

- `accept` — the strict rule stands as shipped. Task 3 records the cost as accepted, with his word
  and the date, in the criterion 1 verdict row and in the closing record.
- `allowance` — he wants a per-watch operator shipping declaration **added on top of** the strict
  rule, in Phase 5's `store_id` shape: required config, no default, unset means unresolved, bot-y
  never guesses. **It is not built here.** 06-01 recorded why it was not taken in wave 1 — it needs
  `boty/config.py`, which the outline's coverage audit puts out of scope for every plan in this
  phase — so Task 3 writes it into `QUESTIONS.md` and STATE.md as a decided, scoped item for a later
  plan, with his answer quoted. **It never replaces the strict rule**, and Task 3's record says so.
- `defer` — no decision now. Task 3 records the cost as **stated and not yet accepted**, with the
  date, and the criterion 1 verdict row says which watches stopped being alertable regardless.

Under every answer, criterion 1's verdict is decided by what 06-01 **measured**, never by the
answer. The answer is a record of a cost being seen, not a vote on a verdict.
  </action>

  <what-built>
Waves 1-5 are complete, green offline, and none of it is deployed:

- **06-01** — the price ceiling now measures the **delivered total**. Shipping is read from each
  retailer's own payload where it is really there (GameStop's `OfferShippingDetails` object → 6.99)
  and **refused where it is not**: Nintendo publishes a sentence, Amazon publishes a button, and
  Walmart publishes only free-shipping signals. A ceiling that cannot be evaluated does not
  authorise an alert. `Availability` was deliberately **not** touched — a pricing question does not
  get to erase a page's own stock statement.
- **06-02** — the README's **Rung** cell is bound to the code across both joins (retailer → adapter
  in `cli._make_checker`, adapter → `Rung` in `boty/retailers.py`), two-directionally. The mutation
  REQ-18 names now turns a test red.
- **06-03** — the pin, exit-code, timeout and runner rules are keyed to the
  **`.github/workflows/` directory** rather than to `ci.yml`. Proven by executing the criterion: a
  third, deliberately broken workflow written to disk left the suite at **exit 0, 667 passed**
  before the gate existed, and turns it red naming the file after.
- **06-04** — `CHANGELOG.md` is gated on its **contents**. The exact document that shipped in Phase
  4 with leaked tool-call markup was recovered byte-for-byte from git and watched going from green
  to red against it.
- **06-05** — `pyproject.toml` reads **0.2.0**, bound to the README's publication instruction, the
  changelog's top released heading and this project's own milestone version, watched red against the
  real tree before the roll and green after.

**What none of it changed:** the running daemon. `boty.service` has published 2026-08-04 code since
before Phase 5, because you answered `defer` on 2026-08-10. Everything above is in the tree.
  </what-built>

  <how-to-verify>
**Criterion 1 works, and it costs you two watches. Here is the measurement.**

The price ceiling used to compare a reseller's *item price* against your ceiling, so a $54.99
listing with $45 shipping walked straight through it — one of only two defences against a reseller
alert. It now compares the **delivered total**. Where a retailer publishes a shipping cost we can
actually read, that works. Where it does not, bot-y **refuses to guess**, and a ceiling it cannot
evaluate does not authorise an alert.

**The four GO Plus + watches, measured against the built tree** (ceiling `max_price: 80`):

*(Insert Task 1c's measured table here — retailer, delivered-total establishable, alertable before,
alertable after. If it differs from what this plan expected, say so on the card in one line.)*

**What that means in one sentence: Nintendo and Amazon can no longer page you about a GO Plus +
restock.** And Nintendo is the sharp one — it is the only entry in your config listing the product
at its **$54.99 MSRP with no marketplace attached**. The rule that stops bot-y inventing a shipping
cost is the same rule that silences the watch most likely to be a real restock.

**Why it was built this way, so you can push back on the reasoning rather than just the outcome.**
Your recorded tiebreaker settled it before the phase started — *"Trustworthiness over coverage.
Where they conflict, correctness wins."* Two concrete things it ruled out:

- **Nintendo's page says the shipping in a sentence** — *"Standard UPS Ground Shipping: $6.99 …
  Free UPS Ground Shipping on orders over $50."* The item is $54.99, so the true shipping cost is
  **zero**. A regex over that sentence pulls out $6.99 and produces a delivered total of **$61.98 for
  an item that ships free** — which could suppress a genuine MSRP restock. So bot-y reads no
  shipping figure out of prose anywhere.
- **Walmart's `milk-control` capture carries a real `7.95`** in a delivery-fee field. It is a
  from-store delivery fee on a pickup item, not shipping. Reading it would have invented a $7.95
  shipping charge **out of a real field**. So that field is read nowhere.

**One option was rejected and is not on the menu, so you can see it was considered:** falling back
to the item price when a retailer publishes no shipping at all. That reopens exactly the hole this
requirement closes — no capture this project has ever taken shows Walmart's *paid* marketplace
shipping, so "publishes nothing" and "publishes something we did not read" look identical on the one
retailer that carries marketplace sellers.

**What is genuinely open is whether you want a way back.** The shape with a precedent is the one you
took for the Walmart store pin: a per-watch operator declaration — required config, no default,
unset means unresolved, bot-y never guesses. It would let you tell bot-y what Nintendo's shipping
actually costs, at the price of one config key and a docs paragraph. **It would sit on top of the
strict rule, never in place of it.** It is not built, and nothing is built in this plan.
  </how-to-verify>

  <verify>
    <automated>git status --porcelain; test -z "$(git status --porcelain)" &amp;&amp; echo "no code written — correct for a closing plan"</automated>
  </verify>

  <resume-signal>
Reply with exactly one word:

- `accept` — the strict rule stands. Nintendo and Amazon stop being alertable on the GO Plus +, and
  that is recorded as a cost you accepted, with today's date.
- `allowance` — keep the strict rule and add a per-watch operator shipping declaration on top of it.
  Nothing is built now; it is written down as a decided, scoped item for a later plan, with your
  answer quoted.
- `defer` — no decision now. The cost is recorded as stated and not yet accepted, and the verdict
  table says which watches stopped being alertable either way.
  </resume-signal>

  <done>
    - The card was presented with **Task 1c's measured table**, not this plan's expected one, and
      any difference between them was stated on the card.
    - **No lenient rule was offered as an option.** The item-price fallback appears only as a named
      rejection with its reason.
    - Dan's answer is recorded **verbatim with the date**, including a `defer`.
    - `git status --porcelain` is clean. On `allowance`, nothing was built — it was written down.
    - Criterion 1's verdict is decided by 06-01's measurements, not by the answer, and the SUMMARY
      says so.
  </done>
</task>

<task type="auto">
  <name>Task 3: Five verdicts, four requirements, a Plans list nobody wrote, and one commit</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, docs/retailer-evidence.md</files>

  <action>
### 3a — The five verdicts, into `.planning/ROADMAP.md`

Use the ROADMAP's own numbering **1-5**, verbatim from the Phase 6 criteria list. One row per
criterion. Each row carries a **measurement or a reason** — never an impression, never a paraphrase
of a test name. Follow Phase 4's and Phase 5's three-column shape: `| # | Verdict | Measurement or
reason |`, under a bolded lead line in the established form
`**Outcome, recorded <date> by 06-06 — N of five MET, M UNMET…**`.

Place it **immediately beneath the Phase 6 criteria list**, matching where Phase 5's table sits.
Cite `docs/retailer-evidence.md § Phase 6 closing record` for the full working, as all three prior
tables cite theirs.

Where each row's evidence comes from — read the SUMMARY and **quote it**:

| # | Criterion | Where the evidence comes from |
|---|---|---|
| 1 | Ceiling applies to the delivered total; unresolvable shipping is UNKNOWN, not a pass | 06-01-SUMMARY: the GameStop fixture reading 6.99 and a delivered total of 61.98; Nintendo's prose yielding **no** number; Walmart's two agreeing free signals resolving one fixture and not the other; `alertable` refusing an unestablished total; **`Availability` deliberately untouched**; M4 re-anchored and M17/M18 observed CAUGHT. **The row must name the two watches that stopped being alertable** and carry Task 2's answer with its date |
| 2 | Mutating an adapter's `Rung` against a contradicting README row turns a test red — today it leaves 131 green | 06-02-SUMMARY: `CAUGHT M19` with its killing tests; the proof that M19 moves `check_amazon` and **only** `check_amazon`; both directions and the rung-4 case; and the **re-measured** figure standing beside `131` as a measurement note. The row states that the code-side binding was **new construction**, not a copy — `06-CONTEXT.md` said otherwise and was measured wrong |
| 3 | A workflow file added under `.github/workflows/` is covered by the pin, exit-code, timeout and runner rules | 06-03-SUMMARY: the criterion **executed** — the same non-compliant file and the same command at **exit 0, 667 passed** before the gate and exit 1 naming the file after; all four rule families reporting it; every one of the 67 pre-existing tests still present and still biting |
| 4 | `CHANGELOG.md` is gated on its contents, not its existence — the leaked-markup class cannot ship again | 06-04-SUMMARY: the Phase 4 document recovered **byte-for-byte from git**, green before and red after on identical bytes; the prohibitions paired with presence rules so a gutted document cannot pass; the real `build_sandbox()` run showing the gate is not inert where the file is absent |
| 5 | `pyproject.toml` reads `0.2.0`, agrees with the project's milestone version, and cannot silently diverge | 06-05-SUMMARY: the STATE binding observed **RED against the real tree** before the roll and GREEN after, same command both sides; the four safety facts re-measured at execution (`git tag -l`, `git ls-remote --tags origin`, PyPI 404 twice); the always-on README binding observed **passing inside a real sandbox**; M25/M26 CAUGHT; the `Development Status` classifier re-decided and argued in place |

**The rule that binds every row, and it is the hardest constraint in this project.** A criterion
that is not met is recorded **UNMET, with the date and the reason**, and its text is not touched.
Phase 3.1 declined a rewrite of its criterion 1 that would have made it meetable. Phase 4 recorded
criteria 3 and 5 UNMET rather than reword them, and its own record says *"this plan does not get to
do what that one refused."* Phase 5 held the line a third time. Neither does this one. If a
criterion is partly met, say which part is and which part is not — do not round it up.

**Then the phase gate paragraph**, in Phase 4's and Phase 5's established form, immediately beneath
the table: `make verify-offline` exit 0 with its counts and the rise from 16/16 shown; and
`make verify`'s live verdict **verbatim including its FAIL**, with the classes separated and any
class this phase caused named as ours. **Do not trim it and do not present a self-caused class as
pre-existing.**

**Also in the ROADMAP, and nowhere else:**

- Tick Phase 6 in the *Milestone v0.2* checklist — `- [x] **Phase 6: Claims With Gates Under
  Them** … (completed <date> — <verdict split>)` — in Phase 5's form. Use the real date from
  `date -I`, not a date copied from this plan.
- **Write the `Plans:` list for Phase 6.** No earlier planner wrote it, deliberately: it was left to
  be written once rather than raced between five concurrent planners. Add `**Plans**: 6 plans, in 6
  waves` and a list in Phases 1-4's established form, one line per plan with its objective and its
  wave, `- [x]` only where that plan's SUMMARY exists on disk. Mark 06-06 as the close and note
  `autonomous: false`. Follow the outline's § *Why every wave is serial* for one sentence on the
  mechanism: **file ownership is not what serialised this phase; a single whole-tree gate is**, and
  06-03, 06-04 and 06-05 are the first plans in this project that could have run concurrently if the
  gate were per-plan. That is forward-useful and it is measured, so it belongs in the roadmap.

**Do not touch anything else in this file.** Not Phase 5's closing table, not any phase's criteria
list, not a single criterion's text anywhere in the document. Prove it in the commit: the diff must
show additions to the Phase 6 section, one checklist line, and nothing else.

### 3b — `.planning/REQUIREMENTS.md`

Flip `- [ ]` → `- [x]` on REQ-17, REQ-18, REQ-19 and REQ-20, and set their traceability rows.
**Leave every requirement's text unedited.** Write the cells in this file's established
evidence-carrying style — REQ-10's, REQ-14's and REQ-16's cells are the models; REQ-11's is the
worked example of a row recording what was *not* achieved.

- **REQ-17** — the delivered total, the three readers and what each refuses to read, the two
  inventions avoided (Nintendo's $61.98-for-a-free-ship, Walmart's $7.95 delivery fee), M4's
  re-anchor and M17/M18. **State the cost in the cell**: two of four product watches stopped being
  alertable, and Task 2's answer with its date.
- **REQ-18** — the two joins, the two directions, M19 uniquely anchored and observed CAUGHT.
  **Flag both stale claims in the cell without editing the requirement:** the `131` figure, which is
  pre-Phase-5 and was re-measured; and *"Routing and Extraction are already pinned"*, which is false
  for Extraction in the sense that matters — it was pinned **cell-to-cell**, so the code side was
  new construction in 06-02. The requirement's own text is the record of why the work was done and
  must survive it.
- **REQ-19** — both halves, because REQ-19's own text names both: the workflow directory rules
  (06-03) and the CHANGELOG contents gate (06-04). Both red-watches were the criterion **executed**,
  not simulated. Record that **neither plan registers a mutation, by design**, and that M21-M24 are
  therefore a deliberate ident gap. Record the recurrence of the defect inside this milestone's own
  planning with the evidence for each instance, per § *The REQ-19 defect recurring* — the pre-commit
  catch named as a near-miss, `06-PATTERNS.md` named as a committed hit, and 06-04 § F4's
  counter-measurement for the later plans quoted rather than smoothed over.
- **REQ-20** — the roll as a **correction rather than a bump**, the four safety facts re-measured at
  execution, the four bound statements with `pyproject.toml` named authoritative, the red-then-green
  pair against the real tree, and the classifier re-decided.

If any is not fully closed, it stays `- [ ]` / Pending **with the reason**.

### 3c — `.planning/STATE.md`

**Read the two hazards in `<context>` first.** This file's `milestone:` frontmatter line is now
machine-read by 06-05's version binding.

- Update the frontmatter `status`, `stopped_at`, `last_updated`, `last_activity` and `progress`
  (Phase 6 of 6 in v0.2 → `completed_phases: 2`, `total_plans: 10`, `completed_plans: 10`,
  `percent: 100` — but **read the block and compute it from what is there**, and keep the existing
  comment explaining why percent is phase-based).
- **Do not change `milestone: v0.2`.** v0.2 is the milestone Phase 6 completes, not one it
  supersedes, and moving it would turn 06-05's binding red for no reason.
- Rewrite the Status block: Phase 6 complete with its verdict split, the next command, and the fact
  that **the daemon still runs 2026-08-04 code** — that entry is closed by a restart, not by this
  phase, and it stays until one happens.
- Add the phase's decisions to the Decisions list in the established `- [Phase 6]: <decision> —
  <reason>` form. At minimum: 06-01's strict rule and its recorded cost; where the UNKNOWN goes
  (`alertable`, not `Availability`) and why; no shipping figure parsed out of prose; the static-AST
  Rung binding and its stated cost; the directory-keyed workflow rules; the CHANGELOG scope decision
  and its structural argument; the version roll as a correction; and **the deliberate M21-M24 ident
  gap**, which a future reader needs more than any of the others.
- Replace the `make verify` live line with Task 1d's verdict, keeping the classes separated.
- Add a carried-forward entry recording that **`.planning/STATE.md` is now machine-read by
  `tests/test_packaging_metadata.py`** — say it where an editor of this file will see it, because an
  edit to that line is now a gate-visible act.
- On Task 2's `allowance` answer, add the per-watch shipping declaration as a decided, scoped item
  for a later plan with Dan's answer quoted, and open a `QUESTIONS.md` entry for it. **Do not build
  it.**
- **Leave `QUESTIONS.md` § 0e and § 0f open.** Neither is touched by this phase.

**Then re-run the version binding**, because you have just edited a file a gate reads:
`.venv/bin/python -m pytest tests/test_packaging_metadata.py -q`. If it is red, your STATE.md edit
moved something it should not have — fix the edit, not the gate.

### 3d — `docs/retailer-evidence.md` — the closing record

Append a `## Phase 6 closing record (<date>) — …` section **beside** the Phase 5 record, not over
it, following that file's own stated convention (*"This one sits beside it rather than over it"*).
It must carry:

1. **The five criteria in Phase 3.1's four-column shape** — `| # | Criterion | Verdict | What
   settles it |`.
2. **The gates:** `make verify-offline` verbatim with the rise from 531/8-8 through 667/16-16 to
   what it reads now; the full mutation ident list; and the **M21-M24 gap with 06-03's and 06-04's
   own reasons quoted**.
3. **The live `make verify` verdict, verbatim**, with its classes separated and any class this
   phase caused named as ours.
4. **The four-watch alertability table** and Dan's verbatim answer with its date. This is the
   phase's user-visible consequence and it is the one thing in this record a person would notice
   before any test did.
5. **Every correction in the `<context>` table, one short subsection each**, because each is a
   lesson rather than a status line. Quote the SUMMARY that made it. At minimum:
   - **`06-CONTEXT.md` was wrong twice, and measurement caught both** — the matrix binding it
     claimed existed (cell-to-cell, not to code) and the framing of unresolvable shipping as the
     edge case (on Walmart it is the common path). CONTEXT was auto-generated under
     `workflow.skip_discuss`, so these are drafting errors rather than withdrawn decisions — say
     that, so nobody reads them as Dan changing his mind.
   - **REQ-18 carries the same stale claim, and it is flagged rather than edited.**
   - **06-01's two near-inventions** — $61.98 out of Nintendo's sentence, $7.95 out of a real
     Walmart delivery-fee field. Both are the same class as 05-01's rejected `"0"` sentinel:
     reading presentation data as a fact.
   - **The outline's Walmart free-shipping rule resolves one fixture, not two** — and that being
     the fail-safe answer is why it cost nothing.
   - **06-03 proved the gap by executing it**, and the naming correction: two planning documents
     call the exit-code rule `_flattened_exit_codes`; the function is `_flattening`.
   - **06-05's correction to the outline's pairing** — both rules would have skipped under
     `make mutation`; re-anchored on `README.md` because it is in `SANDBOX_CONTENTS`.
   - **The REQ-19 defect inside this milestone's own planning**, with the sweep re-run at close and
     its actual output recorded — including 06-04 § F4's finding that the three plans written after
     the outline became hits in neither direction.
6. **What this phase did not establish**, in its own short subsection. At minimum: **not one of
   these five gates is running on the deployed daemon**, which still publishes 2026-08-04 code; the
   live `make verify` failure classes are recorded and not diagnosed; no retailer was probed;
   Walmart's paid-marketplace shipping shape remains **unobserved**, so the criterion's central case
   is guarded by a refusal rather than demonstrated against a capture; and anything else the five
   SUMMARYs flagged as not established.

### 3e — Commit, once

Check `git config user.email` **before committing** — author identity has silently gone wrong in
this repo before. Then run `.venv/bin/python scripts/identity_check.py --all` over the whole tracked
tree after the record is written and before the commit; the pre-commit hook runs it again, but
running it first means the failure is diagnosed rather than merely refused. If it fails, **find and
remove the value — do not add anything to `allowed`.**

Then re-run `make verify-offline` **once more**, because 3c and 3d both edited files that gates now
read (`.planning/STATE.md` through the version binding, `docs/retailer-evidence.md` through
`scripts/evidence_check.py` under `make test`). A closing record that reddened its own tree and
committed anyway would be this milestone's defect in its purest form.

One commit, four files:
`git add .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md docs/retailer-evidence.md`.
The message states the verdict split and says in its body that **no criterion text was reworded,
shortened, merged or amended**, and that the mutation ident gap at M21-M24 is deliberate.
  </action>

  <verify>
    <automated>make verify-offline &amp;&amp; .venv/bin/python scripts/identity_check.py --all</automated>
    <automated>git diff --name-only HEAD | sort | tr '\n' ' '</automated>
    <automated>.venv/bin/python -c '
import re, subprocess
def show(ref):
    return subprocess.run(["git", "show", ref + ":.planning/ROADMAP.md"], capture_output=True, text=True, check=True).stdout
CRIT = re.compile(r"^\s+\d+\.\s+(.*\S)\s*$")
def crits(src):
    return {m.group(1) for m in (CRIT.match(l) for l in src.splitlines()) if m}
new = open(".planning/ROADMAP.md", encoding="utf-8").read()
base = "HEAD" if show("HEAD") != new else "HEAD~1"
o, n = crits(show(base)), crits(new)
print("baseline:", base, "| criterion lines at baseline:", len(o), "| now:", len(n))
for g in sorted(o - n): print("REWORDED, SHORTENED, MERGED OR DELETED:", g)
assert not (o - n), "a criterion text no longer appears verbatim - this plan may not touch one"
print("every criterion text at " + base + " is still present verbatim; additions:", len(n - o))
'</automated>
    <automated>.venv/bin/python -c '
import subprocess
def show(ref):
    return subprocess.run(["git", "show", ref + ":.planning/ROADMAP.md"], capture_output=True, text=True, check=True).stdout
def table(src):
    sec = src.split("### Phase 5:")[1].split("### Phase 6:")[0]
    rows = [l for l in sec.splitlines() if l.startswith("|")]
    assert len(rows) == 8, "expected 8 table lines in the Phase 5 section, got " + str(len(rows))
    return chr(10).join(rows)
new = open(".planning/ROADMAP.md", encoding="utf-8").read()
base = "HEAD" if show("HEAD") != new else "HEAD~1"
a, b = table(show(base)), table(new)
print("baseline:", base, "| Phase 5 closing table:", len(b), "bytes, 8 lines")
assert a == b, "Phase 5 closing table is NOT byte-identical to " + base
print("Phase 5 closing table byte-identical")
'</automated>
    <automated>grep -c 'Phase 6 closing record' docs/retailer-evidence.md; grep -c '^milestone: v0.2$' .planning/STATE.md; grep -c '06-06' .planning/ROADMAP.md</automated>
  </verify>

  <done>
    - Five rows, five verdicts, each with a measurement or a reason, in the ROADMAP's three-column
      shape beneath the Phase 6 criteria. **Any UNMET carries the date and the reason. No criterion
      text anywhere in the ROADMAP was reworded, shortened, merged or amended** — the diff shows
      additions to the Phase 6 section plus one checklist line and nothing else, and **Phase 5's
      closing table is byte-identical**. Both of those are asserted by the two extract-and-compare
      commands in this task's `<verify>` block, which exit non-zero naming what moved; neither is
      left to the eye.
    - The phase gate paragraph carries `make verify-offline` verbatim with the rise from 16/16
      shown, and `make verify`'s live verdict **verbatim including its FAIL**, classes separated,
      any self-caused class named as ours.
    - The **Phase 6 `Plans:` list exists** for the first time, six lines, waves named, `- [x]` only
      where a SUMMARY exists, with 06-06 marked the close and `autonomous: false`, and one sentence
      recording that a whole-tree gate — not file ownership — is what serialised the phase.
    - REQ-17/18/19/20 flipped with evidence-carrying cells, or left Pending with the reason.
      **No requirement text edited**, and REQ-18's two stale claims are flagged in its cell.
    - STATE.md carries the phase's real position, the decisions including the **deliberate M21-M24
      gap**, the new live `make verify` verdict, and a note that this file is now machine-read.
      **`milestone: v0.2` is unchanged**, and `tests/test_packaging_metadata.py` was re-run green
      after the edit.
    - `docs/retailer-evidence.md` carries `## Phase 6 closing record` beside the Phase 5 record,
      with the four-column table, the gates, the alertability table and Dan's verbatim answer, every
      correction as its own subsection, and a *what this phase did not establish* subsection naming
      that **none of these five gates is running on the deployed daemon**.
    - `identity_check.py --all` PASSED after the record was written; `make verify-offline` exits 0
      **after** the record was written, not only before.
    - One commit, four files, `git config user.email` checked first, and a message that says no
      criterion was reworded and that the ident gap is deliberate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the five criterion verdicts → `ROADMAP.md`, `REQUIREMENTS.md`, `docs/retailer-evidence.md` | The project's own audit trail. **A verdict table is a claim like any other, and this is the phase that put gates under claims.** A record is worth what its worst row is worth. |
| a measured cost (two watches stop alerting) → a decision only Dan can take | Consequence egress. The failure mode is Claude absorbing a cost the user never saw. |
| `.planning/STATE.md`'s `milestone` line → `tests/test_packaging_metadata.py` | **New as of 06-05.** A planning file is now an input to an executable gate; the closing plan is the first writer to meet that. |
| `docs/retailer-evidence.md` → `scripts/evidence_check.py` → `make test` | The closing record is itself parsed by a gate this project ships. |
| `make verify`'s live control stage → six retailers | Politeness, treated here as a hard constraint rather than a preference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-60 | Repudiation | **the closing record itself — a verdict table is a claim, and this phase exists to put gates under claims** | mitigate | Six controls, and the fact that this is a *gates* phase raises the bar rather than lowering it. **(a)** Every row carries a **measurement or a reason**, and where the measurement is a transcript the transcript is **quoted from the SUMMARY**, never paraphrased — a record that summarises a transcript it could have quoted is a claim about a measurement instead of the measurement. **(b)** The mutation idents are **read from the registry with comment lines filtered** rather than counted or assumed, because `grep -c` over an unfiltered file counts prose and this project has been bitten by that self-invalidating class. **(c)** The live `make verify` verdict is recorded **verbatim including its FAIL**, with any self-caused class named as ours — Phase 4 and Phase 5 both set that precedent. **(d)** Unmet is recorded unmet with the date and the reason, and **no criterion text is touched anywhere in the ROADMAP**; the diff is the proof and Phase 5's table must come out byte-identical. **(e)** Stale figures are **re-measured, not repeated** — REQ-18's `131`, and the leaked-markup sweep, whose count this plan deliberately declines to assert. **(f)** `make verify-offline` is re-run **after** the record is written, so the record cannot redden the tree it certifies. Residual, named: the record cites five SUMMARYs written by the plans they describe; the independent checks are `make verify-offline`'s exit code and the mutation harness's own CAUGHT lines, both re-run here rather than quoted. Tasks 1, 3. |
| T-06-61 | Repudiation | a criterion quietly reworded so the table reads five-for-five | mitigate | The specific failure this project keeps catching in itself, and a closing plan is where the pressure to commit it is highest. Three written precedents are cited in the plan rather than merely invoked: Phase 3.1's declined rewrite of its criterion 1, Phase 4's two UNMET criteria, Phase 5's six-row table with every live confirmation marked NOT OBTAINED. **The criteria text is out of scope for every edit in this plan**, including earlier phases'. Two `<verify>` commands in Task 3 **assert** rather than report, in 06-03 Task 2's extract-and-compare idiom. The first pulls every criterion line out of the baseline `ROADMAP.md` and out of the working copy, strips the leading numeral, and exits **non-zero naming any criterion whose text no longer appears verbatim** — additions are allowed, rewordings, shortenings, merges and deletions are not, and stripping the numeral is what lets a renumbering like 05-04's pass while a reword cannot. The second slices the eight lines of Phase 5's closing table out of both and asserts them **byte-identical**. Both were run while writing this plan: exit 0 on the clean tree, exit 1 naming what moved against a tree with criterion 1 reworded in place and one Phase 5 row edited. The baseline is `HEAD`, falling back to `HEAD~1` when the working copy already matches `HEAD`, so neither command can pass vacuously if it is run after this plan's own commit. Task 3. |
| T-06-62 | Repudiation | a user-visible cost absorbed by the agent instead of shown to the user | mitigate | **The reason this plan is `autonomous: false`.** Criterion 1 stops two of four GO Plus + product watches being able to page Dan, and one of them is the retailer listing the product at MSRP with no marketplace. The design was settled by a **recorded** tiebreaker, so the checkpoint puts the **cost**, not the design. Controls: it is presented as the **measured four-row table**, taken from 06-01's re-measurement against the built tree rather than from this plan's expectation; the lenient option is named as a **rejection with its reason** rather than offered, because offering it would reopen the hole REQ-17 closes; the answer is recorded **verbatim including a deferral**; and the criterion's verdict is decided by the measurement under every answer, so the checkpoint cannot become a vote on a verdict. Task 2. |
| T-06-63 | Tampering | the closing plan reddening a gate the phase just built | mitigate | Two gates now read files this plan writes: 06-05's version binding reads `.planning/STATE.md`'s `milestone` line, and `scripts/evidence_check.py` reads `docs/retailer-evidence.md` under `make test`. Both are named in `<context>` as hazards this phase created for its own closing plan. Controls: `milestone: v0.2` is explicitly not edited; `tests/test_packaging_metadata.py` is re-run immediately after the STATE.md edit; and `make verify-offline` is re-run in full **after** the record is written and before the commit. Task 3. |
| T-06-64 | Information disclosure | the closing record becoming the next leaked-markup hit | mitigate | This phase's own subject, pointed at its own document. `06-PATTERNS.md` became a sweep hit **in the act of measuring the sweep**, and the outline avoided the same fate by naming the tag shapes without reproducing them. **This plan and the record it produces name the shapes and never reproduce them**, and Task 1e re-runs the sweep at close so the record's claim about the sweep is measured rather than inherited. Separately, `scripts/identity_check.py --all` runs over the whole tracked tree after the record is written and before the commit; nothing in this phase handles a store number, but the guard is mechanical and is run regardless. Tasks 1, 3. |
| T-06-65 | Denial of service | six retailers, via a live pass overlapping the daemon's own cycle | mitigate | `make verify`'s live control stage and `boty.service`'s cycle hit the same six retailers, two of which have refused us before. Controls: `make verify-offline` runs **first**, so a red tree never costs a live pass; the live pass is started only after checking `served/boty/status.json`'s `updated`, so the two are not in flight together; and it is run **once**, never re-run until green. Tasks 1a, 1d. |
| T-06-66 | Tampering | the closing plan writing code to make a row read MET | mitigate | 05-04's recorded decision governs — *"a closing plan that implemented its way to a green table would be a phase measuring work it did in the act of measuring."* **No code is written.** The `<objective>` names every directory that is off limits; Task 2's `<verify>` asserts `git status --porcelain` is clean at the checkpoint; on the `allowance` answer the per-watch declaration is **written down for a later plan and not built**; and Task 1's instruction on a red offline gate is to **stop and report**, not to repair. Tasks 1-3. |
| T-06-67 | Repudiation | four mutation idents read as lost | mitigate | 06-03 and 06-04 each register **no mutation by design**, leaving a real gap at **M21-M24** in an otherwise dense sequence. A future reader meeting `… M20, M25 …` with no note goes looking for four deleted gates and concludes the harness was weakened. Controls: the idents are read from the registry rather than assumed; the gap is recorded explicitly in the SUMMARY, in STATE.md's decisions and in the closing record, each time with 06-03's and 06-04's **own reasons quoted**. Tasks 1b, 3c, 3d. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | **This plan installs nothing and changes no code.** Every command is `make`, `git`, `grep`, `date` or an existing script in this repo. Research is disabled for this project, so there is no `## Package Legitimacy Audit` and the fallback policy stands: if the executor finds it needs a package, **stop** — every package is `[ASSUMED]`, and a blocking human-verify checkpoint (gate `blocking-human`) verifying it on `pypi.org/project` must precede any install. Not auto-approvable; `workflow.auto_advance` does not apply. |

**Threats owned by earlier plans in this phase, listed so this register is not read as complete:**
T-06-01/02/10 (a hostile shipping value reaching `alertable`, a reseller defeating the ceiling with
shipping, a delivery fee read as shipping) are 06-01's; T-06-06 (the matrix claiming a rung the code
does not take) is 06-02's; T-06-03/34 (a new workflow escaping the rules, a red observed against a
tree that was never green) are 06-03's; T-06-41/43 and 06-04's half of T-06-08 (a gate scoped wide
enough to invalidate itself, the red-watch pair, the sandbox absence) are 06-04's;
T-06-05/50/52/06b (a published artifact disagreeing with the project's record, a criterion met by a
skip line, a binding satisfied by deletion, a version claim no offline gate reads) are 06-05's.
T-06-07 (the mutation harness's runtime cost) was accepted at outline time with its reason.
</threat_model>

<verification>
Run from the repo root, in this order. **Offline before live, always** — a red tree must never cost
a live retailer pass.

1. `make verify-offline` → **exit 0**, verdict line, `N passed` and mutation `caught/total` recorded
   verbatim beside Phase 5's **667 passed, 16/16** and the pre-milestone **531 passed, 8/8**. Allow
   it minutes; do not kill it.
2. The mutation idents, read with comment lines filtered → the full list recorded, each new ident
   observed **CAUGHT** by name, and the **M21-M24 gap** recorded as deliberate with 06-03's and
   06-04's reasons quoted.
3. The four-watch alertability table → quoted from 06-01-SUMMARY, or re-measured **offline** through
   the shipped captures. **No retailer probed.**
4. `make verify` → run **once**, live, after checking the daemon's `updated`, and recorded
   **verbatim including its FAIL** with the classes separated.
5. The leaked-markup sweep → re-run, pattern and count both recorded as measured.
6. The record written; `.venv/bin/python -m pytest tests/test_packaging_metadata.py -q` green after
   the STATE.md edit.
7. `.venv/bin/python scripts/identity_check.py --all` → PASS over the whole tracked tree, after the
   record is written and before the commit.
8. `make verify-offline` → **exit 0 again**, after the record is written. The record must not redden
   the tree it certifies.
9. `git status --porcelain` → clean of anything outside the four planning/doc files before the
   commit.

**What is deliberately NOT done, and why each is a decision rather than an omission:**

- **No code is written.** A criterion unmet at this point is **recorded** unmet. 05-04's decision
  governs and is quoted in the objective.
- **`make verify` is not re-run until it is green.** It has read FAIL since 2026-08-06 for reasons
  this phase did not cause and is not chartered to fix, and one of its three classes is Phase 5's
  guard **working**. Re-running until the number looks better is the omission this project keeps
  catching in itself.
- **`make verify` is not re-run under the service's `EnvironmentFile`.** 05-04 recorded that
  departure from Phase 3.1's method and its reason, and the reason still binds: it would pull a real
  store number into a process Claude launched and reads. The pinless run is what gets quoted,
  precisely because its output names a key rather than a number.
- **No retailer is probed and no fixture is re-captured.** Both are recorded as needing their own
  plan (`06-CONTEXT.md` § *Deferred Ideas*), and every gate in this phase was watched going red
  offline by design.
- **The daemon is not restarted and no store is pinned.** Dan answered `defer` on 2026-08-10;
  `QUESTIONS.md` § 0f holds it. The closing record states that **none of this phase's five gates is
  running on the deployed daemon** rather than implying they are.
- **No criterion is amended and no requirement text is edited** — including REQ-18's two stale
  claims, which are flagged in a traceability cell because that is the cell's job.
- **Phase 5's section is not touched.** Its missing `Plans:` list is noted as an observation for a
  later plan; fixing it here would be an edit to a closed phase in the plan that closes another.
</verification>

<success_criteria>
- [ ] `make verify-offline` exits 0, and its verdict line, test count and mutation ratio are
      recorded **verbatim** beside **667 passed, 16/16** and **531 passed, 8/8**, so the rise is
      shown rather than claimed. It was allowed to finish.
- [ ] The mutation idents were **read** from the registry with comment lines filtered, every new one
      was observed **CAUGHT** by name, and the **deliberate gap at M21-M24** is recorded with
      06-03's and 06-04's own reasons quoted — in the SUMMARY, in STATE.md and in the closing
      record.
- [ ] The four-watch alertability table was measured (or quoted from 06-01-SUMMARY) **offline**, and
      it names the two watches that stop being alertable.
- [ ] Dan was shown that table at a **blocking** checkpoint, the lenient item-price rule appeared
      only as a named rejection with its reason, and his answer is recorded **verbatim with the
      date** — including a `defer`. Nothing was built on any answer.
- [ ] `make verify` was run **once**, live, and recorded **verbatim including its FAIL**, with the
      three 2026-08-10 classes named as pre-existing/Phase 5's and any new class named as this
      phase's.
- [ ] Five criterion verdicts stand in `ROADMAP.md` in Phase 4's three-column shape, each with a
      measurement or a reason. **Any unmet criterion is recorded UNMET with the date and the reason,
      and nothing anywhere was reworded to pass.** Phase 5's closing table is byte-identical.
- [ ] The **Phase 6 `Plans:` list** exists for the first time, six lines with waves, plus the one
      sentence recording that a whole-tree gate rather than file ownership is what serialised the
      phase.
- [ ] REQ-17/18/19/20 are flipped with evidence-carrying traceability cells, or left Pending with
      the reason. **No requirement text was edited**, and REQ-18's stale `131` and its stale
      *"Extraction is already pinned"* claim are both flagged in its cell.
- [ ] STATE.md carries the real position, the phase's decisions, the new live `make verify` verdict
      and a note that this file is now machine-read — with **`milestone: v0.2` unchanged** and
      `tests/test_packaging_metadata.py` re-run green after the edit.
- [ ] `docs/retailer-evidence.md` carries `## Phase 6 closing record` beside the Phase 5 record,
      with the four-column table, the gates, the alertability table, Dan's answer, **every
      correction from waves 1-5 as its own subsection quoting the SUMMARY that made it**, and a
      *what this phase did not establish* subsection stating that none of these gates is running on
      the deployed daemon.
- [ ] The leaked-markup sweep was **re-run at close** and its actual pattern and count recorded; the
      REQ-19 recurrence is stated with the evidence for each instance, including 06-04 § F4's
      counter-measurement.
- [ ] `identity_check.py --all` PASSED after the record was written, and `make verify-offline` exits
      0 **again** after it was written.
- [ ] One commit, four files, `git config user.email` checked first, and a message saying no
      criterion was reworded and that the M21-M24 gap is deliberate.
- [ ] **No code was written.** `git status --porcelain` shows nothing outside the four planning and
      documentation files.

**Coverage this plan owns** (the full four-source audit is in `06-PLAN-OUTLINE.md` and is not
restated here):

| Source | Item | Where in this plan |
|---|---|---|
| GOAL | *"…has a gate under it that has been watched going red"* — recorded for all five claims | Task 1b, Task 3a, Task 3d |
| REQ | REQ-17 — closed, with the alertability cost measured and put to Dan | Tasks 1c, 2, 3b |
| REQ | REQ-18 — closed, with **both** stale claims flagged and neither edited | Tasks 1e, 3b |
| REQ | REQ-19 — closed on both halves, with the M21-M24 gap and the recurrence recorded | Tasks 1b, 1e, 3b |
| REQ | REQ-20 — closed, with the roll recorded as a correction rather than a bump | Task 3b |
| CONTEXT | Never amend a success criterion to make it meetable | Task 3a — and the ROADMAP diff is the proof |
| CONTEXT | A gate must be watched going red before it is trusted — with double force this phase | Task 1b — every new mutation observed CAUGHT by ident, and the red-watch transcripts quoted from the SUMMARYs |
| CONTEXT | Every criterion verified by something executable | Task 1a/1d — `make verify-offline`, `make verify`, the mutation harness |
| CONTEXT | UNKNOWN is never a verdict, and never OUT_OF_STOCK | Task 3a row 1 — records that `alertable` returns False and `Availability` was deliberately untouched |
| CONTEXT | Never write a real store number or host identity into a tracked file | Task 3e — `identity_check.py --all` before the commit; no store number is handled anywhere in this phase |
| CONTEXT | Out of scope: the live `make verify` failure classes and the deployment | `<objective>`, `<verification>` — recorded, not fixed |
| OUTLINE | 06-06 sketch — the blocking checkpoint, the gates measured, the five verdicts | Tasks 1-3 |
| OUTLINE | Evidence constraint — no acceptance depends on a live retailer read | Task 1a is the gate; Task 1d's live run is recorded, never required to be green |
</success_criteria>

<output>
Create `.planning/phases/06-claims-with-gates-under-them/06-06-SUMMARY.md` when done.

It must carry, beyond the template:

- **The `make verify-offline` verdict**, verbatim, with its test count and mutation `caught/total`
  beside **667 / 16/16** and **531 / 8/8**, and each wave's intermediate count.
- **The full mutation ident list as read from the registry**, with each new ident's CAUGHT line, and
  the **M21-M24 gap stated as deliberate** with 06-03's and 06-04's reasons quoted.
- **The four-watch alertability table**, and **Dan's checkpoint answer verbatim with the date** —
  including a `defer`, and including exactly what was therefore not decided.
- **The live `make verify` verdict, verbatim including its FAIL**, with the classes separated and
  any class this phase caused named as ours.
- **The leaked-markup sweep's pattern and count as measured at close**, beside the outline's 9-in-4,
  with any difference stated rather than reconciled away.
- **The five verdicts as written into the ROADMAP**, reproduced, so the SUMMARY and the ROADMAP can
  be diffed against each other.
- **Every correction from waves 1-5**, quoted from the SUMMARY that made it — including that
  `06-CONTEXT.md` was wrong twice and that REQ-18's own text carries one of the same stale claims.
- **What this phase did not establish**, as its own section: none of the five gates is running on
  the deployed daemon; the live `make verify` failure classes are recorded and not diagnosed;
  Walmart's paid-marketplace shipping shape remains unobserved; and anything the five SUMMARYs
  flagged.

Nothing in this summary is reworded to pass. If a criterion is unmet, it is unmet here in the same
words it is unmet in the ROADMAP.
</output>
