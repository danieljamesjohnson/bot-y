---
phase: 07-a-reading-has-an-age
plan: 06
type: execute
wave: 6
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - docs/retailer-evidence.md
  - QUESTIONS.md
autonomous: false
requirements: [REQ-21]

must_haves:
  truths:
    - "Five criterion verdicts stand in `.planning/ROADMAP.md` in Phase 3.1's / 4's / 5's / 6's three-column format, each carrying a MEASUREMENT or a REASON; any criterion not fully met is recorded UNMET or MET IN PART with its date and its reason, and NO criterion text anywhere in the document was reworded, shortened, merged or amended — asserted by command, not by eye"
    - "The mutation arithmetic is stated ONCE and correctly: the registry was **26** at phase start (`M1`-`M20`, `M25`-`M30`), `M21`-`M24` are an INTENTIONAL GAP from Phase 6, this phase adds **SEVEN** (M31 07-01, M32 07-02, M33 07-03, M34/M35 07-04, M36/M37 07-05), and it ends at **33** — read from the registry with comment lines filtered, never counted with a bare `grep -c`, and the rise recorded FROM 26"
    - "Every new ident was observed CAUGHT by ident in the mutation stage's own output, and the gap at M21-M24 is written down with its reason so no reader sees four lost mutations"
    - "Both things only Dan can settle went to him at a BLOCKING checkpoint as measurements — the dashboard moving from a varying 3-8 rows to a constant 13, and `state.json`'s frozen Walmart entry that will publish `out_of_stock · age ? · not checked` indefinitely until a store is pinned — and his answers are recorded VERBATIM with the date, including a third `defer`"
    - "NO store number was read, derived, inferred or printed anywhere in this plan. Presence was measured as a COUNT and never as a value, and `identity_check.py --all` passed over the whole tracked tree before the commit"
    - "`make verify-offline` exits 0 and its verdict line, pass count and mutation ratio are recorded VERBATIM beside the phase-start baseline of exit 0, 778 passed, 26/26 — so the rise is SHOWN rather than claimed — and it exits 0 AGAIN after the closing record is written, because `scripts/evidence_check.py` parses that file"
    - "`make verify` was run ONCE, live, and its verdict recorded VERBATIM including its FAIL, with any class this phase caused named separately from the pre-existing ones (2/6 no Chrome binary; the intermittent challenge class; 1/6 Walmart through Phase 5's config-gap guard)"
    - "Every correction waves 1-5 made reaches the record, QUOTED from the SUMMARY that measured it: `07-CONTEXT.md`'s wrong registry set, `07-PATTERNS.md`'s miscount of that corrected set, the 13-not-14 denominator, the ~97-minute cadence that is a remaining-time figure against a 21 600 s cap, the absent-row finding that reframed REQ-21, the three near-miss mutation anchors measured before they were walked into, and the fourth instance of the leaked tool-call markup class"
    - "`07-CONTEXT.md`, `07-PATTERNS.md`, `07-PLAN-OUTLINE.md` and REQ-21's own text are FLAGGED where they are wrong and EDITED nowhere — a planning document is the record of what was believed when the work was scoped"
    - "REQ-21 carries an evidence-bearing traceability cell; the Phase 7 `Plans:` list is ticked against SUMMARYs that exist on disk; the Phase 7 checkbox is set; and `docs/retailer-evidence.md` carries a `## Phase 7 closing record` BESIDE the Phase 6 record, not over it"
    - "`.planning/STATE.md`'s `milestone: v0.3` frontmatter line is NOT touched — it is machine-read against `pyproject.toml`'s `0.3.0` — and `tests/test_packaging_metadata.py` was re-run green after the file was edited"
    - "No code was written. `boty/`, `tests/`, `scripts/`, `config/`, `served/`, `.github/`, `pyproject.toml`, `README.md`, `CHANGELOG.md` and `Makefile` are untouched by this plan, and `.planning/milestones/` is byte-identical"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "the five-verdict outcome table in Phase 4's / 5's / 6's three-column shape, the two phase-gate verdicts, the ticked Phase 7 `Plans:` list and the Phase 7 checkbox"
      contains: "Outcome, recorded"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-21 flipped with an evidence-carrying traceability cell, and its two measured discrepancies recorded BELOW the traceability table rather than edited into the requirement"
      contains: "REQ-21"
    - path: ".planning/STATE.md"
      provides: "the phase's real position, its decisions, the ident arithmetic including the M21-M24 gap, and Dan's two answers — with `milestone: v0.3` untouched"
      contains: "M21"
    - path: "docs/retailer-evidence.md"
      provides: "## Phase 7 closing record — beside the Phase 6 record, not over it"
      contains: "Phase 7 closing record"
    - path: "QUESTIONS.md"
      provides: "§ 0f carrying Dan's third answer on the Walmart store pin, dated, with no store number anywhere in it"
      contains: "0f"
  key_links:
    - from: ".planning/ROADMAP.md Phase 7 outcome table"
      to: "docs/retailer-evidence.md § Phase 7 closing record"
      via: "a citation, the way Phase 3.1's, Phase 4's, Phase 5's and Phase 6's tables cite their working"
      pattern: "retailer-evidence\\.md"
    - from: ".planning/REQUIREMENTS.md REQ-21 traceability row"
      to: "the measurements in the five SUMMARYs"
      via: "a cell naming what was measured rather than asserting completion"
      pattern: "REQ-21 \\| Phase 7"
    - from: ".planning/STATE.md frontmatter milestone key"
      to: "tests/test_packaging_metadata.py"
      via: "06-05's version binding, which makes any edit to that line a gate-visible act"
      pattern: "milestone: v0\\.3"
    - from: "docs/retailer-evidence.md § Phase 7 closing record"
      to: "scripts/evidence_check.py"
      via: "the closing record is itself parsed by a gate this project ships under `make test`"
      pattern: "Phase 7 closing record"
---

<objective>
Close Phase 7: measure what the five plans actually landed, put the two consequences only Dan can
settle to him as measurements rather than as sentences, and write down all five verdicts —
including any that is not met.

**Purpose.** This milestone exists because a reading was published without the moment it was taken.
A closing record is the last place that defect can hide and the easiest place to commit it: a table
where every row reads MET is exactly what a phase that quietly reworded one criterion would also
produce, and a record that repeats a number nobody re-measured is the same error one level up.
Phase 3.1 was offered a rewrite of its criterion 1 that would have made it meetable and Dan
declined. Phase 4 recorded two criteria UNMET rather than reword them. Phase 5 closed with every
live row marked NOT OBTAINED. Phase 6 recorded criterion 1 MET IN PART rather than round it up.
**This plan does not get to do what those four refused.**

And this phase carries an obligation none of them did: **it changes what Dan looks at every day, on
a daemon that is actually running this tree.** v0.2 was deployed on 2026-08-12, `boty` is an
editable install, so unlike Phases 5 and 6 this phase's code reaches the wire at the next restart.
Two of its consequences are his to accept, not Claude's to absorb.

**Output.** One blocking checkpoint carrying two measured consequences and his verbatim answers;
one `make verify-offline` verdict with the mutation ratio shown rising **from 26** to **33**; one
live `make verify` verdict recorded verbatim including its FAIL; five criterion verdicts in the
ROADMAP; one requirement row carrying measurements; the Phase 7 `Plans:` list ticked; and a
`## Phase 7 closing record` in `docs/retailer-evidence.md`.

**NOT in this plan, and the executor must not reach for it.**

- *No code.* `boty/`, `tests/`, `scripts/`, `config/`, `served/`, `.github/`, `pyproject.toml`,
  `README.md`, `CHANGELOG.md`, `MANIFEST.in` and `Makefile` are finished. 05-04's recorded decision
  governs: *"a closing plan that implemented its way to a green table would be a phase measuring
  work it did in the act of measuring."* If a criterion turns out unmet, it is **recorded** unmet.
- *No rewording of any success criterion, anywhere in the ROADMAP.* Not Phase 7's five, not any
  earlier phase's, not v1.0.0's. Not to make one meetable, not to "clarify" one, not to merge two.
- *No edit to REQ-21's text*, including the sentence this phase measured one direction off. It is
  flagged below the traceability table, which is that note's job.
- *No edit to `07-CONTEXT.md`, `07-PATTERNS.md` or `07-PLAN-OUTLINE.md`*, all three of which are
  wrong about something this phase measured. A planning document is the record of what was believed
  when the work was scoped; corrections go **beside** it.
- *No touch to `.planning/milestones/`.* v0.2's archive is closed and its two outcome tables are the
  format this plan writes in, not a document it edits.
- *No milestone close, no tag, no version roll.* v0.3 has one phase in it so far. `pyproject.toml`
  stays at `0.3.0` and STATE.md's `milestone: v0.3` stays exactly as it is.
- *No store number, in any form.* Not read, not derived, not inferred from a capture or a live page,
  not printed, not written. Presence is measured as a **count** and never as a value. This is the
  hardest constraint in the plan because the checkpoint asks Dan a question whose answer is that
  number, and the answer is his to enter, not Claude's to handle.
- *No restart and no deploy.* This phase's code reaches the daemon at the next
  `sudo systemctl restart boty`, and that is **recorded** here, not performed.
- *No fixing of the live `make verify` failure classes.* No Chrome binary on this host, and the
  intermittent challenge class, are pre-existing since 2026-08-06; the Walmart config-gap class is
  Phase 5's and is *correct*. All are recorded, none is diagnosed (`07-CONTEXT.md` § *Deferred*).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-a-reading-has-an-age/07-CONTEXT.md
@.planning/phases/07-a-reading-has-an-age/07-PLAN-OUTLINE.md
@.planning/phases/07-a-reading-has-an-age/07-01-SUMMARY.md
@.planning/phases/07-a-reading-has-an-age/07-02-SUMMARY.md
@.planning/phases/07-a-reading-has-an-age/07-03-SUMMARY.md
@.planning/phases/07-a-reading-has-an-age/07-04-SUMMARY.md
@.planning/phases/07-a-reading-has-an-age/07-05-SUMMARY.md

**Read all five SUMMARYs before writing a single verdict, and cite them rather than paraphrasing
them.** A closing record that summarises a transcript it could have quoted is a claim about a
measurement instead of the measurement. Each SUMMARY carries the transcript its verdict rests on:
07-01's twenty stated `Result(` sites and its AST completeness gate; 07-02's real pre-07 document
loaded without exception and with alert behaviour unchanged; 07-03's accessor observed widening
under backoff with the fetch schedule asserted unmoved against literal expected seconds; 07-04's row
count before and after with the fetched count in the same cycle; 07-05's four rendered forms on both
surfaces and the join test that makes `status.json` criterion 3's first surface.

**Where a SUMMARY disagrees with anything written below, the SUMMARY wins** — it was written against
code that exists, and this plan was written before it did. Record the disagreement rather than
resolving it silently.

## The two prior outcome-table shapes, and which goes where

Measured against the shipped documents rather than remembered:

- **`ROADMAP.md` uses three columns** — `| # | Verdict | Measurement or reason |` — in Phase 3.1's
  table, Phase 4's, Phase 5's and Phase 6's. The criteria are listed immediately above in that file,
  so restating them would duplicate. Phases 5 and 6 are in
  `.planning/milestones/v0.2-ROADMAP.md`; **read them for the shape and edit nothing there.**
- **`docs/retailer-evidence.md` uses four** — `| # | Criterion | Verdict | What settles it |`. The
  criteria are not restated nearby there, so the column earns its place.

Follow both, exactly as 05-04 and 06-06 did.

## The mutation arithmetic, stated once, correctly — several documents got it wrong

This is the single number a closing plan for *this* phase is most likely to get wrong, because three
separate planning documents carry three different figures. **Read the registry; do not inherit any
of them.**

- **The registry was 26 at phase start:** `M1`-`M20` and `M25`-`M30`, measured on this tree at
  `dbc9d49` with comment lines filtered.
- **`M21`-`M24` are an INTENTIONAL GAP** left by Phase 6: 06-03 and 06-04 each register no mutation
  by design, each with its reasons recorded at the time, and the code says so at
  `scripts/mutation_check.py:669`. **Write the gap down.** A reader meeting `… M20, M25 …` with no
  note reads four lost mutations and starts looking for them.
- **This phase adds SEVEN:** M31 (07-01), M32 (07-02), M33 (07-03), **M34 and M35** (07-04),
  **M36 and M37** (07-05).
- **So it ends at 33**, and 26 + 7 = 33 is the only arithmetic that appears in the record.

Three documents to flag and edit nowhere:

| Document | What it says | What is true |
|---|---|---|
| `07-CONTEXT.md` § *Existing Code Insights* | *"26 mutations at M1–M20, M25–M28 … New idents start at **M29**"* | The set is wrong **and counts to 24**; M29 and M30 already existed (Phase 6's paging pair at `mutation_check.py:680-693`), so new idents started at **M31** |
| `07-PATTERNS.md` § 6 | corrects the set to `M1-M20, M25-M30` and calls it *"**28** mutations"* | Twenty plus six is **26**. PATTERNS got the set right and the count wrong, in the same sentence |
| `07-PLAN-OUTLINE.md` | six new idents, M35/M36 assigned to 07-05 | **Seven** landed; 07-04 took M34 **and** M35, so 07-05's pair is M36/M37 (07-04-SUMMARY and 07-05-SUMMARY both record the correction) |

`make verify-offline` printing `26/26` at phase start was *caught over the whole registry*, not 26
of a larger 28. **07-06 records the count rising from 26.** A closing plan that recorded it rising
from 28 would be this milestone's own defect in the plan that closes it.

## The baselines this record measures against

- **Phase start, re-measured for the outline on 2026-08-13 at `dbc9d49`, run to completion:**
  `make verify-offline` **exit 0, 778 passed, 26/26 mutations**, identity PASS over 208 files.
- **Phase 6's close (2026-08-11):** exit 0, **768 passed, 24/24** — **769** after the post-close
  correction commit. **Phase 5's close:** 667 passed, 16/16. **Pre-milestone (Phase 4's close):**
  531 passed, 8/8. Each SUMMARY in this phase carries its own intermediate count; put them in a row
  so the rise is **shown**.
- **`make verify` has read `VERIFY: FAIL (live controls)`, exit 2, since 2026-08-06**, in three
  classes: (1) `2/6 control(s) could not run on THIS HOST` — Best Buy and Target, no Chrome/Chromium
  binary, which the tool itself says *"says nothing about the DETECTOR"*; (2) the Walmart/Amazon
  challenge class, **absent on both 2026-08-10 and 2026-08-11**, so two consecutive passes support
  *intermittent* rather than *permanent* — record whether it manifests now, either way; (3)
  `1/6 control(s) not reading IN_STOCK` — Walmart through Phase 5's config-gap guard, because
  `make verify` runs in a shell with no `WALMART_STORE_ID`. That third one is Phase 5's, and it is
  **correct**.

## What waves 1-5 corrected, and where each was first written

Each of these must reach the closing record, **quoted from the SUMMARY that measured it**. The
pointers exist so nothing is lost if a SUMMARY is terse; they are not a substitute for reading it.

| Correction | First written | Corrects |
|---|---|---|
| The registry's **set** — M29/M30 already existed, so new idents start at M31 | `07-PATTERNS.md` § 6, carried by `07-PLAN-OUTLINE.md` § *CORRECTION 1* | `07-CONTEXT.md` |
| The registry's **count** — the corrected set is **26**, not 28 | `07-PLAN-OUTLINE.md` § *CORRECTION 1* | `07-PATTERNS.md` |
| The phase adds **seven** idents, not six; 07-04 took M34/M35 and 07-05 took M36/M37 | 07-04-SUMMARY, re-stated by 07-05-SUMMARY | `07-PLAN-OUTLINE.md` |
| **The denominator is 13 configured watches, not 14.** Every planning document says 14, from a `grep -c "retailer:" config/products.yaml`; the fourteenth match is a **comment** at `config/products.yaml:309`. `Config.load` returns **13** | 07-04 § *AND THE DENOMINATOR IS 13, NOT 14* | every planning document in this phase, **including this plan's own checkpoint** |
| **"A retailer at seven refusals is on a ~97-minute cadence" is wrong arithmetic.** `min(300 · 2^7, 21600)` = **21 600 s** — target and walmart are on the **6-hour cap**. The ~97 and ~53 minute figures are `skipped_reason`'s time **remaining**, which is a different quantity | 07-03 § *Measurement note 5*; used by 07-05, whose M37 `breaks=` rests on the corrected number | `07-PLAN-OUTLINE.md` § *Finding 4* and § *Finding 9*, and `07-PATTERNS.md` |
| **The absent-row finding, which reframed the phase.** A paced-out watch has **no row**, not a stale one: 3 of 13 at 08:25:10 and **8 of 13 at 09:24:54 the same morning**, from an unchanged config — so the row count is a function of pacing, which is the sharper statement | `07-PLAN-OUTLINE.md` § *Finding 4*, re-measured twice by 07-04 and once by 07-05 at 09:41:39 | **REQ-21's own sentence** — *"the page presents both as current"* is one direction off; the page presents the stale one **not at all**. Recorded beside the unedited text, on Phase 3.1's precedent |
| **Three near-miss mutation anchors, each measured before it was walked into.** M33's obvious single-line anchor occurs twice in `boty/pacing.py` — line 243 and line 128 **inside a comment** — and `apply_mutation` replaces the FIRST, so it would have mutated prose, changed no behaviour and **SURVIVED**. M34's `"checked":` occurs twice today and four times after 07-04 Task 1, and the first is a **retailer** row, so it would have gated a different rule. M36/M37 were pre-counted for the same reason | 07-03 § T-07-09, 07-04 § T-07-11, 07-05's pre-counts | nothing — it is the discipline, measured. **Record every count** |
| **The leaked tool-call markup class recurred a fourth time**, while `07-01-PLAN.md` was being written on 2026-08-13, **caught before commit** — in `.planning/`, which **no gate this repo ships covers** | this plan's brief | a class v0.2's audit logged as unbuilt three candidates deep |

## The leaked-markup class, with each instance's own evidence

This is a **claim about counts**, in a milestone about measured claims, so it is measured rather
than repeated. What is on the record, with provenance:

1. **`05-02-PLAN.md`** — a stray tool-call tag pair at its end, **caught by a planning agent before
   commit**. A near-miss: it leaves no trace in the tree, and the evidence is the note.
2. **`06-PATTERNS.md`** — a committed sweep hit, produced **in the act of measuring the sweep**.
3. **`06-07-SUMMARY.md` at `a71e79b`** — committed, two whole-line closing tags after its metadata
   line, **one day after the gate for that exact byte-shape landed**. Removed at `7355034` and
   recorded rather than quietly fixed; the evidence is permanent at `a71e79b`.
4. **`07-01-PLAN.md`, 2026-08-13** — caught before commit while the file was being written. The
   **fourth** instance of the class, the **second** produced by an agent writing a PLAN into
   `.planning/`, and the second near-miss.

**No gate covers `.planning/`**: `leaked_markup` is deliberately scoped to `CHANGELOG.md`, and the
only `.planning/` file any test or script reads is `STATE.md`, for one frontmatter key. Task 1
re-runs the sweep, records the pattern used and the count obtained, and states each instance with
its own evidence. **If a count in a SUMMARY and the count from the sweep disagree, record both with
their dates.** Repeating a number nobody re-measured is the defect this milestone exists to close.

*(This plan deliberately names the tag shapes without reproducing them, exactly as the outline and
the five plans did. It must not become the fifth file in that list, and neither must its SUMMARY.)*

## Three hazards this phase created for its own closing plan

1. **`.planning/STATE.md`'s `milestone:` frontmatter line is machine-read** by 06-05's version
   binding against `pyproject.toml`, component-wise. `milestone: v0.3` agrees with `0.3.0`. Editing
   STATE.md is a gate-visible act; **that line must not move**, and the binding is re-run after the
   edit.
2. **`docs/retailer-evidence.md` is read by `scripts/evidence_check.py`**, which `make test`
   exercises through `tests/test_evidence_check.py`. Sections are split on `## ` headings and
   attributed to a retailer by **heading prefix**, so a `## Phase 7 closing record …` heading is
   attributed to nobody — but the verdict vocabulary is matched on whole lines
   (`**Verdict: REFUSED**`, `**Verdict: REACHABLE (rung N)**`, `**Verdict: UNPROBED (scoped …)**`),
   so **do not write a bare verdict-shaped line into the new section**, and re-run the suite after
   writing it.
3. **REQ-21's body is asserted unchanged by this plan's own `<verify>`**, extracted between
   `**REQ-21**` and `### Traceability`. So the measurement notes recorded beside it go **BELOW the
   traceability table under their own `###` heading**, not between the requirement and the table.
   The gate shapes the layout on purpose.

## The daemon, and why this phase's checkpoint is different from Phases 5 and 6

v0.2 was **deployed on 2026-08-12** (STATE.md § *Deferred Items*), and `boty` is an **editable
install** — the unit runs this tree's venv from this tree's directory. So unlike Phases 5 and 6,
whose closing records both had to say *none of this is on the wire*, **this phase's code reaches
Dan's dashboard at the next `sudo systemctl restart boty`**. Measure the running process
(`systemctl show boty` for `MainPID` and `ActiveEnterTimestamp`) rather than assuming either way,
and say plainly on the card which side of the restart he is on. **Nothing in this plan restarts it.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure what landed — the gate, the seven idents, the corrections, and the two consequences</name>
  <files>(no files modified — this task produces the measurements the record and the checkpoint cite)</files>

  <action>
**Read all five SUMMARYs first.** Everything below either confirms them against the tree or extracts
a figure the record needs. Where this plan and a SUMMARY disagree, the SUMMARY wins and the
disagreement is recorded.

### 1a — The phase gate, offline

Run `make verify-offline` **once**. Record, verbatim: the exit code, the identity-check line, the
`N passed` line, the mutation `caught/total` ratio and the `VERIFY:` line. Put the phase-start
baseline — **exit 0, 778 passed, 26/26** — beside it, plus Phase 6's close (**768 → 769 passed,
24/24**), Phase 5's (**667, 16/16**) and the pre-milestone (**531, 8/8**), plus each SUMMARY's
intermediate count. The rise is **shown**, never claimed, and every ratio is written as numerator
over denominator rather than as the word "green".

**It is slow** — 700+ tests and 33 mutation sandboxes, each a `copytree` plus `git init` plus
`git add -A`. Allow it minutes. **Do not kill it and do not reduce it to `make test`.** A timeout is
not a verdict; if it does time out, raise the timeout, re-run, and record that you did.

If it is red, **stop**. Do not spend a live pass, do not write a verdict table, and do not fix the
tree — report which stage failed and what it said. A closing plan that repaired its own phase would
be measuring work it did in the act of measuring.

### 1b — The seven idents, READ rather than assumed

Do not trust this plan's arithmetic. Read the registry with comment lines filtered — a bare
`grep -c` over an unfiltered file counts comment prose, and this project has been bitten by exactly
that self-invalidating class, twice this phase alone (the registry count and the watch denominator).

Record the full ident list in file order. Expected: **M1-M20 and M25-M37, 33 total, M21-M24 absent.**
From `make verify-offline`'s mutation stage output, record each of **M31, M32, M33, M34, M35, M36,
M37** observed **CAUGHT by ident**, with the killing-test count each one reports, and record that the
survivor list is empty.

Then state the arithmetic once: **26 at phase start + 7 this phase = 33**, with M21-M24 written down
as the deliberate Phase 6 gap and 06-03's and 06-04's reasons quoted. If the idents present differ
from the expected set, **record what is there** and reconcile against the SUMMARYs — the tree wins.

### 1c — The two consequences, measured, because the checkpoint quotes numbers

**The configured watch count.** Get it from `Config.load`, not from a grep:
`.venv/bin/python -c "from boty.config import Config; print(len(Config.load('config/products.yaml').watches))"`
(adjust to the real signature — read `boty/config.py` rather than guessing it). Expect **13**. Record
it beside the `grep -c "retailer:" config/products.yaml` figure of 14 and name
`config/products.yaml:309`'s comment as the fourteenth match. **A checkpoint quoting an unmeasured
number would be this milestone's own defect in the plan that closes it.**

**The row count as it stands right now.** Read `served/boty/status.json` (gitignored, read-only) and
record its `updated` stamp and its `watches` array length. 07-04 measured 3 rows at 08:25:10 and 8 at
09:24:54, and 07-05 measured 8 at 09:41:39, all against 13 configured watches and an unchanged
config. Whatever it reads now is a third data point for the same statement: **the row count is a
function of pacing**, and that is why the dashboard varies today and will not after this phase.

**Walmart's frozen entry, confirmed without reading a store number.** Confirm from the SUMMARYs and
from `boty/monitor.py` that `transitioned_to_stock` returns on `Availability.UNKNOWN` **before** it
writes, and that the entry `walmart:Pokémon GO Plus + -> out_of_stock` therefore cannot be updated
by anything while `WALMART_STORE_ID` is unset. Confirm the pin is unset **as a count and never as a
value** — a `grep -c` on the deployment env file's key name is the only permitted form, and if that
file is unreadable by policy, record that instead and take the fact from STATE.md and from
`QUESTIONS.md` § 0f. **Do not read, derive, infer or print the number under any circumstances, and
do not run anything under the service's `EnvironmentFile`.**

**Which side of the restart Dan is on.** Record `systemctl show boty` for `MainPID` and
`ActiveEnterTimestamp`, and state whether the running process predates this phase's commits. Do not
restart it.

### 1d — The corrections roll-up, quoted from the SUMMARY that made each

Assemble, from the SUMMARYs and not from this plan's table: the registry set and count corrections
against `07-CONTEXT.md` and `07-PATTERNS.md`; the seven-not-six ident correction against the
outline; the 13-not-14 denominator; the ~97-minute figure that is `skipped_reason`'s time remaining
against a **21 600 s** cap; the absent-row finding and the one direction REQ-21's own sentence is
off; and **every measured anchor pre-count** for M33, M34, M35, M36 and M37 with the competing count
recorded beside each. Each of the three near-miss anchors is a gate that would have stood in the
registry gating nothing — record them as measurements, with their numbers.

### 1e — The sweep, re-run

Re-run the leaked-markup sweep over the tracked tree for the tool-call tag shapes and record its
**actual** output — matching lines and files — together with **the pattern you used**. Do not repeat
a remembered number, and **do not adjust the pattern until the number matches one**. Then state the
four instances from `<context>` with each one's own evidence, naming the two pre-commit catches as
near-misses rather than hits.

### 1f — The live gate, once

Start `make verify` only **after** checking `served/boty/status.json`'s `updated`, so a daemon cycle
and this pass are not in flight against the same six retailers at once. Politeness is a hard
constraint and this close spends exactly **one** live pass.

Record the verdict **verbatim, including a FAIL**, with `control_check.py`'s own classification of
every failing control. Then **separate the classes**: the two that cannot run on this host; the
intermittent challenge class, recording whether it manifests now either way; and Walmart through
Phase 5's config-gap guard, which is Phase 5's and is correct. **Anything else is new and is this
phase's until proven otherwise** — this phase touches `status.write`, `cli._report` and the check
path, so a control whose *verdict* moved is a finding that outranks any prediction. Name it
separately; do not fold it in.

**Do not re-run `make verify` until it is green.** It has read FAIL since 2026-08-06 for reasons this
phase did not cause and is not chartered to fix. **Do not run it under the service's
`EnvironmentFile`** — 05-04 recorded that departure and its reason still holds: it would pull a real
store number into a process Claude launched and reads. The pinless run's output is safe to quote
verbatim precisely because that shell has no pin. The mutation stage does not run inside a failing
live invocation, so mutations are evidenced by 1a and never by this run.

Record everything in this task in the SUMMARY as it was observed, before anything is written into
any planning document.
  </action>

  <verify>
    <automated>make verify-offline</automated>
    <automated>.venv/bin/python -c '
import re
q = chr(34)
src = open("scripts/mutation_check.py", encoding="utf-8").read()
lines = [l for l in src.splitlines() if not l.lstrip().startswith("#")]
idents = re.findall("ident=" + q + "(M[0-9]+)" + q, chr(10).join(lines))
nums = [int(i[1:]) for i in idents]
expected = list(range(1, 21)) + list(range(25, 38))
print("idents, file order:", " ".join(idents))
print("count:", len(nums), "| M21-M24 absent:", not (set(range(21, 25)) & set(nums)))
assert len(nums) == len(set(nums)), "duplicate ident in the registry"
assert sorted(nums) == expected, "registry is not M1-M20 + M25-M37: got " + str(len(nums)) + " idents"
print("registry = 33 = 26 at phase start + 7 this phase (M31 M32 M33 M34 M35 M36 M37)")
print("M21-M24 deliberately unfilled by 06-03 and 06-04 - NOT four lost mutations")
'</automated>
    <automated>git status --porcelain; test -z "$(git status --porcelain)" &amp;&amp; echo "no code written - correct for a closing plan"</automated>
    <automated>.venv/bin/python -c '
import subprocess
lt, gt = chr(60), chr(62)
shapes = [lt + "/invoke" + gt, lt + "/content" + gt, lt + "function_calls" + gt, lt + "parameter", "ant" + "ml:"]
pat = "|".join(shapes)
r = subprocess.run(["git", "grep", "-n", "-E", pat], capture_output=True, text=True)
lines = [l for l in r.stdout.splitlines() if l.strip()]
files = sorted({l.split(":", 1)[0] for l in lines})
print("PATTERN USED:", pat)
print("MEASURED:", len(lines), "matching lines in", len(files), "files")
for f in files: print("   ", f, sum(1 for l in lines if l.startswith(f + ":")))
print("RECORD-ONLY - this command asserts nothing. Copy the pattern AND the number into the SUMMARY,")
print("state any difference from a remembered figure, and do NOT adjust the pattern to match one.")
'</automated>
  </verify>

  <done>
    - `make verify-offline` exits 0, and its identity line, `N passed` line, mutation ratio and
      `VERIFY:` line are recorded **verbatim** beside **778 / 26-26** at phase start, **768→769 /
      24-24**, **667 / 16-16** and **531 / 8-8**. It was allowed to finish; it was not killed and it
      was not narrowed.
    - The idents were **read from the registry with comment lines filtered**, recorded in full, and
      **M31 through M37 were each observed CAUGHT by ident** with an empty survivor list. The
      arithmetic is written down once: **26 + 7 = 33**, with **M21-M24** recorded as Phase 6's
      deliberate gap and 06-03's and 06-04's own reasons quoted.
    - The configured watch count came from **`Config.load` and reads 13**, with the `grep` figure of
      14 and `config/products.yaml:309`'s comment recorded beside it as the reason the planning
      documents say 14.
    - The live `status.json` row count and `updated` stamp were read and recorded as a third data
      point beside 3-at-08:25:10, 8-at-09:24:54 and 8-at-09:41:39.
    - Walmart's frozen entry is confirmed from the code path, and the pin's absence is recorded **as
      a count, never as a value**. **No store number was read, derived, inferred or printed**, and
      nothing was run under the service's `EnvironmentFile`.
    - `systemctl show boty`'s `MainPID` and `ActiveEnterTimestamp` are recorded, and whether the
      running process predates this phase is stated rather than assumed. Nothing was restarted.
    - Every correction from waves 1-5 is assembled **quoted from the SUMMARY that made it**,
      including all five mutation anchor pre-counts with their competing counts.
    - The leaked-markup sweep was re-run; the pattern used and the count obtained are both recorded,
      and the four instances are stated with their individual evidence.
    - `make verify` was run **once**, live, after a daemon-cycle check, and recorded **verbatim
      including its FAIL**, with any class this phase caused named separately from the pre-existing
      ones. It was not re-run and it was not run under the service's `EnvironmentFile`.
    - `git status --porcelain` is clean. **No code was written.**
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The two consequences, put to Dan as measurements</name>
  <files>(no files modified — this task produces two recorded answers)</files>

  <action>
**Claude's part of this task is to present the card below, stop, and wait.** Task 1 has already done
everything that can be automated: the numbers are measured, the gate is green offline, the live
verdict is recorded. This checkpoint verifies **after** the automation, which is the only place a
checkpoint belongs.

Fill every number on the card from **Task 1's measurements**, not from this plan's expected figures.
If they differ, the measured one goes on the card and the difference is stated in one line.

Route it to his phone with `notify-dan "bot-y: phase 7 close — your dashboard changes, and Walmart's
row is frozen" "<one line>"` if he is not at the terminal — the standing autonomous-working contract
— then keep waiting rather than spinning.

**Six things Claude must not do while waiting:**

1. **Do not read, derive, infer or print the Walmart store number.** Not from the deployment env
   file, not from `systemctl show`, not from a capture, not from a live page, not as a "for
   example". Presence is a count. If he answers `pin`, **he sets it**, through the existing
   hidden-prompt secret tool — Claude does not handle the value, and nothing in this plan writes it
   anywhere. `identity_check.py` has rejected a commit in this repo over a four-digit literal in a
   test comment, and that guard is the floor, not the ceiling.
2. **Do not offer to keep the dashboard at 3-8 rows.** Every configured watch having a row is what
   criterion 3 needs in order to bind at all — a staleness rule applied only to rows that are fresh
   by construction is a bound that cannot bind. What is genuinely open is whether he wants an
   *affordance* later, not whether the rows exist.
3. **Do not soften the Walmart row.** It will say `out_of_stock`, `age ?`, `not checked`
   indefinitely. That is the honest output and it is a consequence of an open question, not of this
   phase — say it in those words.
4. **Do not present this as a request for permission to have shipped it.** The design is settled by
   the criteria and by `07-CONTEXT.md`'s locked decisions. What is being put to him is the
   **consequence**, which is what this milestone exists for.
5. **Do not build anything on any answer**, and do not restart the daemon.
6. **Do not proceed on silence.** `autonomous: false` and `gate="blocking"` mean this task ends with
   Dan's answer or it does not end. `workflow.auto_advance` does not apply to it.

**Whatever comes back is recorded verbatim, with the date, including a deferral** — `defer` is one of
the offered answers, it would be the **third** time on this same question, and 05-04's precedent is
that a deferral is recorded rather than worked around.

**What each answer does:**

- **`keep`** — the constant 13 rows stand as shipped. Task 3 records it as seen and accepted with the
  date.
- **`note-it`** — he wants a way to collapse or filter remembered rows on the dashboard. **It is not
  built here.** `served/boty/index.html` is 07-05's file and this phase is closed; Task 3 writes it
  into STATE.md and `QUESTIONS.md` as a decided, scoped item for a later plan, with his words quoted.
  It never removes rows from `status.json`.
- **`pin`** — he will set `WALMART_STORE_ID` himself. Task 3 records the answer and the date, updates
  `QUESTIONS.md` § 0f, and states what changes as a consequence: after he sets it **and** restarts,
  Walmart's readings stop being UNKNOWN, `transitioned_to_stock` starts writing again, and the frozen
  entry gets a real availability and a real age. **Claude does not set it, does not ask for the
  number, and does not print it.**
- **`defer`** — no pin now. Task 3 records the third deferral verbatim with the date, § 0f stays
  open, and the closing record states that Walmart's GO Plus + row publishes `out_of_stock` with an
  UNKNOWN age indefinitely as the honest output.

Under every answer, the five criterion verdicts are decided by what waves 1-5 **measured**, never by
the answer. The answer is a record of a consequence being seen, not a vote on a verdict.
  </action>

  <what-built>
Waves 1-5 are complete and green offline. Five plans, seven new gates, no criterion reworded:

- **07-01** — every `Result` now carries `read_at`, the wall-clock moment the page was read, stated
  at all **20** construction sites in `boty/retailers.py` and proved complete by a static gate over
  the source rather than by covering arms one at a time. An arm that read nothing carries **no**
  moment — `null`, never `0`, because epoch 1970 renders as maximally stale. Two Best Buy arms that
  the obvious rule would have mis-stamped (`bad api json` and `sku not found`) are stamped as reads,
  because Best Buy answered.
- **07-02** — the age survives a restart. `state.json` becomes a dated per-watch ledger, and the
  **real** pre-07 document on your disk — 13 bare strings — loads as *availability with an unknown
  age*, with alert behaviour byte-for-byte unchanged. A stamp read back that is in the future, or a
  string, or a `True`, is discarded rather than believed.
- **07-03** — a retailer's current cadence is now one readable number, computed in one place and used
  by the scheduler itself, so the number shown and the schedule kept cannot drift apart. `boty check`
  reads it from the same file the daemon writes, **load-only** — it never writes to it.
- **07-04** — every configured watch has a row, every cycle. A row nobody read this cycle is visibly
  a memory: `checked: false`, the remembered availability, the stamp the ledger holds for it, and
  `alertable: false` **stated** — a two-day-old memory does not get to page you.
- **07-05** — all three surfaces say the age out loud. `boty check` prints one of four forms on
  every row, including `[age ?]` for a reading nobody dated. The dashboard renders it at the two
  established tag weights, judged against **that retailer's own current cadence** — never against
  the page's existing 30-minute banner constant, which asks a different question and was left
  exactly where it is.

**What none of this is yet:** running. It reaches your dashboard at the next
`sudo systemctl restart boty`. Nothing in this plan restarts anything.
  </what-built>

  <how-to-verify>
**Two things change on your screen, and both are the criteria working. I need you to see them before
the phase closes.**

**1. Your dashboard goes from a varying number of rows to a constant 13.**

Today it shows however many watches happened to be checked in the last cycle. Measured on your own
file this morning: **3 rows at 08:25:10, and 8 rows at 09:24:54** — from a config that did not
change. A watch whose retailer is backing off simply **has no row**. That is the defect that opened
this phase: you asked when Amazon and Walmart last read `out_of_stock`, and the answer was not
*hard to find*, it was *absent* — the rows were not there to be old.

After this phase there are **13 rows, always** — one per configured watch. *(Not 14. Every planning
document in this phase said 14, from a grep that counted a comment line; `Config.load` returns 13,
and that correction is in the record.)* The ones nobody read this cycle say so: `not checked`, with
the age of the last real reading beside them, or `age ?` if nobody ever recorded one.

**What that costs you in daily use, plainly:** on a normal cycle five to ten of those thirteen rows
will be **memories rather than fresh readings**. The page will look busier. It is more information,
not less, and none of it is invented — but it is a different page than the one you look at now, and
you should say so if it is not what you want.

*(Insert Task 1c's measured figures here — the current row count and `updated` stamp, and the
configured count from `Config.load`. If any differs from the numbers above, say so in one line.)*

**2. `state.json` holds a fossil, and after this phase it will say so out loud, forever.**

Your state file contains this entry: `walmart:Pokémon GO Plus + -> out_of_stock`.

**That string can no longer be updated by anything.** `WALMART_STORE_ID` is unset, so every Walmart
reading comes back UNKNOWN through Phase 5's store-gap guard — and `transitioned_to_stock` returns on
UNKNOWN **before** it writes. Nothing has been able to move that value since the guard was deployed.
It is not merely undated; it is frozen.

So after this phase, that row publishes `out_of_stock · age ? · not checked` — **indefinitely, until
a store is pinned.** That is the honest output. The age genuinely is not established, and inventing
one would be the exact defect this phase exists to remove. But it is the **third** time this
milestone has put the same open question in front of you (`QUESTIONS.md` § 0f, answered `defer` on
2026-08-10), and a checkpoint that shows it is better than a dashboard that surprises you with it.

**What pinning costs:** one environment variable in the mode-600 file systemd loads, set through the
existing tool that prompts hidden and verifies before writing, plus a restart. **You enter it — I do
not ask for it, do not read it, do not print it, and nothing in this plan writes it anywhere.** Once
it is set, Walmart's readings stop being UNKNOWN, that entry starts moving again, and the row gets a
real availability with a real age.

**What deferring costs:** nothing breaks. The row stays frozen and honestly labelled, the phase
closes on that as the recorded output, and § 0f stays open for a fourth time.

*(Insert Task 1c's `systemctl show boty` measurement here — whether the running daemon predates this
phase, i.e. whether a restart is what stands between you and both changes above.)*
  </how-to-verify>

  <verify>
    <automated>git status --porcelain; test -z "$(git status --porcelain)" &amp;&amp; echo "clean at the checkpoint - nothing was built while waiting"</automated>
    <automated>.venv/bin/python scripts/identity_check.py --all</automated>
  </verify>

  <resume-signal>
Reply with **two words, one from each line**:

**The dashboard** — `keep` or `note-it`

- `keep` — 13 rows always, remembered ones labelled as memories. Recorded as seen and accepted.
- `note-it` — keep the 13 rows, and log a later plan for a way to collapse or filter the remembered
  ones on the page. Nothing is built now; it is written down with your words quoted.

**The Walmart store pin** — `pin` or `defer`

- `pin` — you will set `WALMART_STORE_ID` yourself and restart. I record the decision and § 0f; I
  never see, ask for or print the number.
- `defer` — no pin now. The third deferral is recorded verbatim with the date, § 0f stays open, and
  the closing record states that Walmart's GO Plus + row publishes `out_of_stock` with an UNKNOWN age
  indefinitely.

Example: `keep defer`.
  </resume-signal>

  <done>
    - The card was presented with **Task 1's measured figures**, not this plan's expected ones, and
      any difference was stated in one line on the card.
    - **No store number was read, derived, inferred, requested or printed**, and
      `identity_check.py --all` passed at the checkpoint.
    - **No option was offered that would remove rows from `status.json`** — the dashboard question is
      about an affordance, never about the criterion.
    - Both answers are recorded **verbatim with the date**, including a `defer`, and a `defer` is
      recorded as the **third** on this question.
    - `git status --porcelain` is clean. Nothing was built on any answer and nothing was restarted.
    - The five criterion verdicts are decided by waves 1-5's measurements, not by either answer, and
      the SUMMARY says so.
  </done>
</task>

<task type="auto">
  <name>Task 3: Five verdicts, one requirement, the closing record, and one commit</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, docs/retailer-evidence.md, QUESTIONS.md</files>

  <action>
### 3a — The five verdicts, into `.planning/ROADMAP.md`

Use the ROADMAP's own numbering **1-5**, verbatim from the Phase 7 criteria list. One row per
criterion, each carrying a **measurement or a reason** — never an impression, never a paraphrase of a
test name. Follow the established three-column shape `| # | Verdict | Measurement or reason |` under
a bolded lead line in the form `**Outcome, recorded <date> by 07-06 — N of five MET…**`. Use the real
date from `date -I`, not a date copied from this plan.

Place it **immediately beneath the Phase 7 criteria list**, matching where Phases 5 and 6 put theirs.
Cite `docs/retailer-evidence.md § Phase 7 closing record` for the full working.

Where each row's evidence comes from — read the SUMMARY and **quote it**:

| # | Criterion | Where the evidence comes from |
|---|---|---|
| 1 | Every `Result` records when it was read, published per watch in `status.json` | 07-01-SUMMARY: the 20 stated construction sites with the 11/9 read/non-read partition; the two Best Buy arms the naive rule gets wrong; the **static AST completeness** proof; `read_at` published as `null` never `0`; `tests/test_status.py`'s exact-keyset assertion updated by enumeration rather than loosened; M31 CAUGHT |
| 2 | A reading with no recorded time is shown as UNKNOWN age, never as current — watched going red | 07-01-SUMMARY (the datum is `None`, M31 red-watched) **and** 07-05-SUMMARY (the rendering: `[age ?]` on the CLI and the warn weight on the page, M36 red-watched). The row must state that this criterion spans two plans and why |
| 3 | A reading older than its retailer's current interval is presented as stale in `status.json`, `boty check` and the dashboard, derived from the retailer's own pacing rather than a fixed clock | 07-03-SUMMARY (the accessor, the schedule asserted unmoved against literal seconds, M33), 07-04-SUMMARY (a row that can be old — 13 rows where the file had 8, `checked: false`, `alertable` stated, M34/M35), 07-05-SUMMARY (both renderings against the published per-retailer cadence, `index.html`'s 30-minute banner constant untouched and unreused, M37). **See the paragraph below — this is the row that must not be rounded up** |
| 4 | The age survives a service restart | 07-02-SUMMARY: the real pre-07 13-bare-string document loaded without exception, every availability preserved, every age unknown, **alert behaviour byte-for-byte unchanged**; the both-ended bound validation; the age observed surviving a real two-process restart; M32 CAUGHT |
| 5 | `make verify-offline` exits 0, and every gate this phase adds has been watched going red | Task 1a's verdict and Task 1b's seven CAUGHT idents. **"Every gate" is broader than "every mutation"** — the phase also adds AST completeness gates, producer/consumer pairs, the join test and the cross-surface equality test. Enumerate the gates each SUMMARY records and say, per gate, whether it was observed failing first. Any gate that was never observed failing is named as such; do not let seven mutations stand in for a claim about all of them |

**The row that must not be rounded up: criterion 3's `status.json` third.** 07-05 deliberately added
**no staleness flag and no new key** to `status.json` — a `stale` boolean computed at write time is
the bound that cannot bind (`pacing.py:196-199`'s recorded lesson, one file over), so the file
publishes the three raw facts (`read_at`, `checked`, `current_interval_seconds`) and a join test
proves them **jointly sufficient** to derive the verdict with nothing else. State that measurement
plainly, state the argument that it satisfies *"presented as stale in `status.json`"* and the
argument that it does not, and then **record the verdict you actually reach**. If you cannot settle
it, it is **MET IN PART** with the reason and the date. It is not MET because four other rows are.

**The rule that binds every row.** A criterion that is not met is recorded **UNMET, with the date and
the reason**, and its text is not touched. Phase 3.1 declined a rewrite that would have made its
criterion 1 meetable. Phase 4 recorded two criteria UNMET. Phase 5 closed with every live row NOT
OBTAINED. Phase 6 recorded criterion 1 MET IN PART. **This plan does not get to do what those four
refused.** If a criterion is partly met, say which part is and which part is not.

**Then the phase gate paragraph**, immediately beneath the table, in the established form:
`make verify-offline` exit 0 with its counts and the rise **from 26/26 to 33/33** shown; the full
ident list; **M21-M24 stated as Phase 6's deliberate gap**; and `make verify`'s live verdict
**verbatim including its FAIL**, classes separated, any class this phase caused named as ours. Do not
trim it and do not present a self-caused class as pre-existing.

**Also in the ROADMAP, and nowhere else:**

- Tick Phase 7 in the *Milestone v0.3* checklist at the top of the file —
  `- [x] **Phase 7: A Reading Has an Age** … (completed <date> — <verdict split>)` — in Phase 5's and
  Phase 6's form.
- **Tick the Phase 7 `Plans:` list**, which already exists: `- [x]` only where that plan's SUMMARY is
  on disk, leaving `- [ ]` for any that is not. **Do not rewrite the objective text on those lines.**
- Add **one** line immediately beneath that list recording the corrected denominator: the list's
  07-04 entry and the paragraph below it both say *"3 rows for 14 configured watches"*, and the
  configured count measured through `Config.load` is **13**, the fourteenth `grep` match being a
  comment at `config/products.yaml:309`. **Record it beside; do not edit the lines.** That is Phase
  3.1's precedent and it is the same rule that keeps REQ-21's text intact.

**Do not touch anything else in this file.** Not any phase's criteria list, not v1.0.0's material,
not a single criterion's text anywhere. The diff must show additions to the Phase 7 section, one
checklist line, and the `Plans:` list's checkboxes — nothing else.

### 3b — `.planning/REQUIREMENTS.md`

Flip `- [ ]` → `- [x]` on REQ-21 and set its traceability row from Pending to a cell carrying
**evidence**, in this file's established style (REQ-10's, REQ-14's and REQ-16's cells in the v0.2
archive are the models; REQ-11's is the worked example of a row recording what was *not* achieved).
**Leave the requirement's text unedited** — this task's `<verify>` asserts it byte-for-byte after
whitespace normalisation.

The cell names what was measured: the stamp at 20 sites proved complete statically; the age surviving
a real restart with the pre-07 document loading unchanged; the cadence as one number both surfaces
read; 13 rows where the file had 3-8; the four rendered forms; and the ratio **26/26 → 33/33** with
all seven idents CAUGHT. If REQ-21 is not fully closed, it stays `- [ ]` / Pending **with the
reason**.

**Then, BELOW the traceability table, under a new `###` heading** — because everything between
`**REQ-21**` and `### Traceability` is asserted unchanged — record the two measurement notes, in
Phase 3.1's format, original quoted and unedited:

1. **REQ-21's sentence *"the page presents both as current"* is one direction off.** Measured: the
   stale row is not presented **at all** — 3 of 13 at 08:25:10, 8 of 13 at 09:24:54, from an
   unchanged config. That is not better, and Dan's question — *"so they are out of stock as of
   when?"* — cannot be answered by a row that is missing either. **The requirement is not edited**,
   and the shape of the fix that landed (a row per configured watch) is what the note explains.
2. **The registry arithmetic**, so a reader of this file meets it once: 26 at phase start, seven
   added, 33 at close, `M21`-`M24` a deliberate Phase 6 gap.

### 3c — `.planning/STATE.md`

**Read hazard 1 in `<context>` first.** This file's `milestone:` frontmatter line is machine-read.

- Update the frontmatter `status`, `stopped_at`, `last_updated`, `last_activity` and `progress` —
  **read the block and compute the numbers from what is there** (v0.3 has one phase and six plans),
  and keep the existing comment explaining why `percent` is phase-based. That comment has been
  stripped by tooling eight times; if `gsd-tools update-progress` strips it again, restore it.
- **Do not change `milestone: v0.3`.** It is machine-read against `pyproject.toml`'s `0.3.0`
  component-wise, and v0.3 is the milestone Phase 7 sits in, not one it supersedes.
- Rewrite the Status block: Phase 7 complete with its verdict split, the next command, **and the
  fact that this phase's code is not on the wire until `sudo systemctl restart boty`** — measured in
  Task 1c, not assumed.
- Add the phase's decisions in the established `- [Phase 7]: <decision> — <reason>` form. At minimum:
  `read_at` is a fact about whether a page was read and is derivable from neither `refused` nor
  `availability`; staleness touches neither `Availability` nor `alertable`, because a `Result` is
  always fresh at construction so the term could never fire; **no `stale` flag is computed at write
  time** — raw facts published, staleness derived at render time in each consumer; a bare string in
  `state.json` loads as *availability with unknown age*, never as `now`; `boty check`'s `Pacer` is
  **load-only** and is never passed to `run_once`; the two staleness rules on the dashboard are
  **not** unified because they answer different questions; and **the M21-M24 gap plus the seven new
  idents ending at 33**, which a future reader needs more than any of the others.
- Record the **corrections** section: the three documents wrong about the registry, the 13-not-14
  denominator, the ~97-minute figure against a 21 600 s cap, and the three near-miss anchors with
  their measured counts.
- Record the **fourth leaked-markup instance** with Task 1e's re-measured sweep output, and that
  `.planning/` is still covered by no gate this repo ships.
- Replace the `make verify` live line with Task 1f's verdict, classes separated.
- Record **both of Dan's answers verbatim with the date**. On `note-it`, add the dashboard affordance
  as a decided, scoped item for a later plan and open a `QUESTIONS.md` entry for it. On `pin` or
  `defer`, update `QUESTIONS.md` § 0f (below). **Build nothing.**

**Then re-run the version binding**, because you have just edited a file a gate reads:
`.venv/bin/python -m pytest tests/test_packaging_metadata.py -q`. If it is red, your STATE.md edit
moved something it should not have — fix the edit, not the gate.

### 3d — `QUESTIONS.md`

Only if Dan's answer moves it. Append to **§ 0f** a dated entry carrying his verbatim answer:
`defer` is recorded as the **third** deferral on this question with what stays true as a consequence
(Walmart's GO Plus + row publishing `out_of_stock` with an UNKNOWN age indefinitely); `pin` is
recorded as answered with the action being his, and § 0f is closed **only** once the pin is actually
set, which this plan does not verify by reading it. On `note-it`, open a new entry for the dashboard
affordance. **No store number appears anywhere in this file, in any form.** Do not touch § 0e.

### 3e — `docs/retailer-evidence.md` — the closing record

Append a `## Phase 7 closing record (<date>) — …` section **beside** the Phase 6 record, not over it,
following that file's own stated convention. Mirror Phase 5's and Phase 6's numbered subsections:

1. **Where the phase landed against the ROADMAP's five criteria** — the four-column shape
   `| # | Criterion | Verdict | What settles it |`.
2. **The gates, and the ident arithmetic.** `make verify-offline` verbatim with the rise shown
   through this phase's five intermediate counts from **778 / 26-26** to what it reads now; the full
   ident list; each of M31-M37 CAUGHT by ident; and **M21-M24 as Phase 6's deliberate gap with
   06-03's and 06-04's own reasons quoted**. State the arithmetic once: 26 + 7 = 33.
3. **The two consequences put to Dan, and his answers verbatim with the date** — the row-count
   measurements that made the first one a measurement rather than an opinion, and the frozen Walmart
   entry with the code path that freezes it. **No store number, in any form.**
4. **The live `make verify` verdict, verbatim**, classes separated, any class this phase caused named
   as ours. **Do not write a bare verdict-shaped line** (`**Verdict: REFUSED**` and its siblings) —
   `scripts/evidence_check.py` matches those on whole lines.
5. **What waves 1-5 corrected, one short subsection each**, because each is a lesson rather than a
   status line. Quote the SUMMARY that made it. At minimum:
   - **Three documents were wrong about the mutation registry, in three different ways** — CONTEXT's
     set (which counted to 24 while claiming 26, and sent new idents to M29 when M29/M30 existed),
     PATTERNS' count of the set it had just corrected (28 for M1-M20 ∪ M25-M30), and the outline's
     six-not-seven. CONTEXT was auto-generated under `workflow.skip_discuss`, so its errors are
     **drafting errors and nothing Dan decided is being re-opened** — say that.
   - **The denominator is 13, not 14**, and the fourteenth `grep` match is a comment. A count taken
     off a grep is the same self-invalidating class as counting mutation idents with `grep -c`, in
     the same phase, twice.
   - **"~97-minute cadence" is a remaining-time figure, not a cadence.** `min(300 · 2^7, 21600)` is
     the 6-hour cap; target and walmart sit on **21 600 s**, and a 1800 s threshold would have
     painted every one of their rows stale while they behaved exactly as the politeness rule
     requires. M37's `breaks=` rests on the corrected number.
   - **The absent-row finding, which reframed the phase** — a paced-out watch has no row, so
     "presented as stale" needed a row before it could mean anything, and criterion 3's `boty check`
     half would otherwise have been unreachable code. **REQ-21's own sentence is one direction off
     and is recorded beside its unedited text.**
   - **Three near-miss mutation anchors, each measured before it was walked into**, with their
     numbers: M33's single-line candidate would have mutated a **comment** in `pacing.py` and
     **SURVIVED**; M34's would have hit a **retailer** row rather than a watch row and stood in the
     registry gating a rule it was not written for; and the same first-occurrence trap was
     pre-counted a third and fourth time in 07-05. A gate that cannot go red is this phase's own
     subject wearing the fix's clothes.
   - **The leaked-markup class, fourth instance**, with each of the four instances' own evidence,
     Task 1e's re-measured pattern and count, and the standing fact that **no gate this repo ships
     covers `.planning/`**.
6. **What this phase did NOT establish**, in its own subsection. At minimum: **none of this is on the
   wire until a restart** (with Task 1c's measurement); Walmart's GO Plus + row will publish an
   UNKNOWN age indefinitely until a store is pinned, and § 0f is now on its third answer; the live
   `make verify` classes are recorded and not diagnosed; no retailer was probed and every red-watch
   in this phase was offline by construction; `boty check`'s stale form is not reachable in
   production (it re-reads every watch, so its rows are fresh by construction) and is reachable only
   from a test, which is a property of the surface rather than a hole; and anything else the five
   SUMMARYs flagged as not established.

### 3f — Commit, once

Check `git config user.email` **before committing** — author identity has silently gone wrong in this
repo before. Run `.venv/bin/python scripts/identity_check.py --all` over the whole tracked tree after
the record is written and before the commit; the pre-commit hook runs it again, but running it first
means the failure is diagnosed rather than merely refused. If it fails, **find and remove the value —
do not add anything to `allowed`.**

Then re-run `make verify-offline` **once more**, because 3c and 3e both edited files that gates now
read (`.planning/STATE.md` through the version binding, `docs/retailer-evidence.md` through
`scripts/evidence_check.py` under `make test`). A closing record that reddened its own tree and
committed anyway would be this milestone's defect in its purest form.

One commit. The message states the verdict split and says in its body that **no criterion text was
reworded, shortened, merged or amended**, that the registry rose **from 26 to 33 with seven idents**,
and that **M21-M24 remain a deliberate gap**.
  </action>

  <verify>
    <automated>make verify-offline &amp;&amp; .venv/bin/python scripts/identity_check.py --all</automated>
    <automated>.venv/bin/python -m pytest tests/test_packaging_metadata.py tests/test_evidence_check.py -q</automated>
    <automated>.venv/bin/python -c '
import re, subprocess
P = ".planning/ROADMAP.md"
def show(ref):
    return subprocess.run(["git", "show", ref + ":" + P], capture_output=True, text=True, check=True).stdout
CRIT = re.compile(r"^\s+\d+\.\s+(.*\S)\s*$")
def crits(src):
    return {m.group(1) for m in (CRIT.match(l) for l in src.splitlines()) if m}
new = open(P, encoding="utf-8").read()
base = "HEAD" if show("HEAD") != new else "HEAD~1"
o, n = crits(show(base)), crits(new)
print("baseline:", base, "| criterion lines at baseline:", len(o), "| now:", len(n))
for g in sorted(o - n): print("REWORDED, SHORTENED, MERGED OR DELETED:", g)
assert not (o - n), "a criterion text no longer appears verbatim - this plan may not touch one"
print("every criterion text at " + base + " is still present verbatim; additions:", len(n - o))
'</automated>
    <automated>.venv/bin/python -c '
import subprocess
P = ".planning/REQUIREMENTS.md"
def show(ref):
    return subprocess.run(["git", "show", ref + ":" + P], capture_output=True, text=True, check=True).stdout
def body(src):
    seg = src.split("**REQ-21**", 1)[1].split("### Traceability", 1)[0]
    return " ".join(seg.split())
new = open(P, encoding="utf-8").read()
base = "HEAD" if show("HEAD") != new else "HEAD~1"
a, b = body(show(base)), body(new)
print("baseline:", base, "| REQ-21 body:", len(b), "chars")
assert a == b, "REQ-21 BODY CHANGED - the requirement is the record of why the work was done"
print("REQ-21 body byte-identical after normalisation; the checkbox, the traceability row and a")
print("section BELOW the table are the only permitted moves")
'</automated>
    <automated>.venv/bin/python -c '
import subprocess
def run(*a):
    return subprocess.run(list(a), capture_output=True, text=True, check=True).stdout
P = ".planning/ROADMAP.md"
new = open(P, encoding="utf-8").read()
base = "HEAD" if run("git", "show", "HEAD:" + P) != new else "HEAD~1"
changed = [l for l in run("git", "diff", "--name-only", base).splitlines() if l.strip()]
allowed = {
    ".planning/ROADMAP.md", ".planning/REQUIREMENTS.md", ".planning/STATE.md",
    "docs/retailer-evidence.md", "QUESTIONS.md",
    ".planning/phases/07-a-reading-has-an-age/07-06-SUMMARY.md",
}
print("baseline:", base, "| files changed:", changed)
stray = sorted(f for f in changed if f not in allowed)
assert not stray, "this plan writes NO code and touches nothing else: " + repr(stray)
print("no code, no archive, no planning document edited - only the closing set")
'</automated>
    <automated>.venv/bin/python -c '
import re
q = chr(34)
src = open("scripts/mutation_check.py", encoding="utf-8").read()
lines = [l for l in src.splitlines() if not l.lstrip().startswith("#")]
idents = re.findall("ident=" + q + "(M[0-9]+)" + q, chr(10).join(lines))
nums = sorted(int(i[1:]) for i in idents)
assert nums == list(range(1, 21)) + list(range(25, 38)), "registry is not M1-M20 + M25-M37"
doc = open("docs/retailer-evidence.md", encoding="utf-8").read()
sec = doc.split("## Phase 7 closing record", 1)
assert len(sec) == 2, "docs/retailer-evidence.md carries no Phase 7 closing record"
body = sec[1]
for token in ["26", "33", "M21", "M24", "M31", "M37"]:
    assert token in body, "the closing record never states " + token
print("registry 33; closing record present and states the rise from 26 to 33 and the M21-M24 gap")
'</automated>
    <automated>grep -c 'Phase 7 closing record' docs/retailer-evidence.md; grep -c '^milestone: v0.3$' .planning/STATE.md; grep -c '07-06' .planning/ROADMAP.md; git config user.email</automated>
  </verify>

  <done>
    - Five rows, five verdicts, each with a measurement or a reason, in the ROADMAP's three-column
      shape beneath the Phase 7 criteria. **Any criterion not fully met is recorded UNMET or MET IN
      PART with its date and reason, and no criterion text anywhere in the ROADMAP was reworded,
      shortened, merged or amended** — asserted by the extract-and-compare command in this task's
      `<verify>`, which exits non-zero naming what moved and falls back to `HEAD~1` so it cannot pass
      vacuously after the commit.
    - **Criterion 3's `status.json` third was argued explicitly and not rounded up:** the record
      states that no staleness key was added, why (a flag computed at write time is the bound that
      cannot bind), what the join test proves instead, and the verdict actually reached.
    - The phase gate paragraph carries `make verify-offline` verbatim with the rise **from 26/26 to
      33/33**, the full ident list, **M21-M24 as a deliberate gap**, and `make verify`'s live verdict
      **verbatim including its FAIL** with classes separated.
    - The Phase 7 checkbox is ticked, the `Plans:` list is ticked against SUMMARYs on disk without its
      objective text being rewritten, and **one line beneath it records the 13-not-14 correction
      beside the unedited lines**.
    - REQ-21 is flipped with an evidence-carrying traceability cell, or left Pending with the reason.
      **Its text is byte-identical** — asserted — and the two measurement notes sit **below** the
      traceability table.
    - STATE.md carries the phase's real position, its decisions including the ident arithmetic and
      the M21-M24 gap, the corrections, the fourth leaked-markup instance with a re-measured sweep,
      the new live `make verify` verdict and **both of Dan's answers verbatim with the date**.
      **`milestone: v0.3` is unchanged** and `tests/test_packaging_metadata.py` was re-run green after
      the edit.
    - `QUESTIONS.md` § 0f carries Dan's answer, dated, with **no store number in any form**; § 0e is
      untouched.
    - `docs/retailer-evidence.md` carries `## Phase 7 closing record` beside the Phase 6 record, with
      the four-column table, the gates and the arithmetic, the two consequences and Dan's answers, the
      live verdict, every correction as its own subsection quoting the SUMMARY that made it, and a
      *what this phase did not establish* subsection naming that **none of it is on the wire until a
      restart**.
    - `identity_check.py --all` PASSED after the record was written; `tests/test_evidence_check.py`
      and `tests/test_packaging_metadata.py` are green; `make verify-offline` exits 0 **after** the
      record was written, not only before.
    - One commit, `git config user.email` checked first, and a message saying no criterion was
      reworded, that the registry rose from 26 to 33 with seven idents, and that M21-M24 are
      deliberate. **No code was written** — the file-set assertion in `<verify>` proves it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the five criterion verdicts → `ROADMAP.md`, `REQUIREMENTS.md`, `docs/retailer-evidence.md` | The project's own audit trail. **A verdict table is a claim about when something was established, in the phase that exists because claims had no date on them.** A record is worth what its worst row is worth. |
| a measured consequence (a dashboard that changes daily use; a row frozen until a store is pinned) → a decision only Dan can take | Consequence egress. The failure mode is Claude absorbing a consequence the user never saw. |
| the Walmart store number → a checkpoint that asks about it | **The sharpest boundary in this plan.** A question whose answer is a host-identifying value is asked without the value ever entering a process Claude runs, reads or writes. |
| `.planning/STATE.md`'s `milestone` line → `tests/test_packaging_metadata.py` | A planning file is an input to an executable gate; the closing plan is the writer most likely to move it. |
| `docs/retailer-evidence.md` → `scripts/evidence_check.py` → `make test` | The closing record is itself parsed by a gate this project ships. |
| `make verify`'s live control stage → six retailers | Politeness, treated as a hard constraint rather than a preference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-60 | Repudiation | **the closing record itself — a table of verdicts, in the phase about undated claims** | mitigate | Six controls. **(a)** Every row carries a **measurement or a reason**, and where the measurement is a transcript it is **quoted from the SUMMARY**, never paraphrased. **(b)** The mutation idents are **read from the registry with comment lines filtered** rather than counted, because a bare `grep -c` counts prose — the exact class that produced *both* the 28-mutation figure and the 14-watch figure this phase had to correct. **(c)** The live `make verify` verdict is recorded **verbatim including its FAIL**, with any self-caused class named as ours. **(d)** Unmet is recorded unmet with the date and the reason, and **no criterion text is touched**; two `<verify>` commands assert it. **(e)** Stale figures are **re-measured, not repeated** — the row count, the watch count, the sweep count. **(f)** `make verify-offline` is re-run **after** the record is written, so the record cannot redden the tree it certifies. Residual, named: the record cites five SUMMARYs written by the plans they describe; the independent checks are `make verify-offline`'s exit code and the harness's own CAUGHT lines, both re-run here. Tasks 1, 3. |
| T-07-61 | Repudiation | a criterion quietly reworded so the table reads five-for-five | mitigate | The specific failure this project keeps catching in itself, and a closing plan is where the pressure is highest. **Criterion 3 is the pressure point**, because its `status.json` third landed as three raw facts plus a join test rather than as a `stale` key, and *"presented as stale in `status.json`"* is exactly the phrase a rounding-up plan would quietly re-read. Task 3a requires the argument in both directions and the verdict reached, with **MET IN PART** as the answer if it cannot be settled. Four written precedents are cited rather than invoked: 3.1's declined rewrite, Phase 4's two UNMET, Phase 5's NOT OBTAINED rows, Phase 6's MET IN PART. **Criterion text is out of scope for every edit**, and the `<verify>` command extracts every criterion body from the baseline and from the working copy, strips the leading numeral, and **exits non-zero naming any that no longer appears verbatim** — additions allowed, rewordings not — with a `HEAD~1` fallback so it cannot pass vacuously after this plan's own commit. A second command asserts the same for REQ-21's body. Task 3. |
| T-07-62 | Repudiation | a user-visible consequence absorbed by the agent instead of shown to the user | mitigate | **The reason this plan is `autonomous: false`.** Two consequences reach Dan's screen and neither is Claude's to accept: a dashboard that goes from a varying 3-8 rows to a constant 13, and a row that will read `out_of_stock · age ? · not checked` indefinitely. Controls: both are presented as **measurements** taken in Task 1 rather than as this plan's expectations; **no option is offered that would remove rows**, because that is the criterion rather than a preference; the answers are recorded **verbatim including a third `defer`**; and the five verdicts are decided by waves 1-5's measurements under every answer, so the checkpoint cannot become a vote on a verdict. Task 2. |
| T-07-63 | Information disclosure | **a real store number reaching a process, a log, a card or a tracked file** | mitigate | **The highest-severity threat in this plan, because the checkpoint asks a question whose answer is that value.** Controls, each stated as a prohibition rather than a preference: the number is never read, derived, inferred, requested or printed; presence is measured **as a count and never as a value**; nothing runs under the service's `EnvironmentFile` (05-04's recorded departure, whose reason still binds); on `pin`, **Dan enters it himself** through the existing hidden-prompt tool and Claude records only that he said so; `QUESTIONS.md` § 0f is appended to with no number in any form; and `scripts/identity_check.py --all` runs over the whole tracked tree **at the checkpoint and again before the commit** — a guard that has already rejected a commit in this repo over a four-digit literal in a test comment. Tasks 1c, 2, 3d, 3f. |
| T-07-64 | Tampering | the closing plan reddening a gate the phase just built | mitigate | Two gates read files this plan writes: 06-05's version binding reads `.planning/STATE.md`'s `milestone` line, and `scripts/evidence_check.py` reads `docs/retailer-evidence.md` under `make test`, matching verdict vocabulary on whole lines. Controls: `milestone: v0.3` is explicitly not edited; `tests/test_packaging_metadata.py` and `tests/test_evidence_check.py` are re-run immediately after the edits; no bare verdict-shaped line is written into the new section; and `make verify-offline` is re-run in full **after** the record is written and before the commit. Task 3. |
| T-07-65 | Information disclosure | the closing record becoming the **fifth** leaked-markup instance | mitigate | The class recurred in this phase's own planning — `07-01-PLAN.md`, caught before commit, the fourth instance and the second produced by an agent writing a PLAN into `.planning/`, which **no gate this repo ships covers**. **This plan and the record it produces name the tag shapes and never reproduce them**, exactly as the outline and the five plans did, and Task 1e re-runs the sweep at close so the record's claim about the sweep is measured rather than inherited — with the standing rule that the pattern is **not** adjusted until its count matches a remembered one. Tasks 1e, 3c, 3e. |
| T-07-66 | Denial of service | six retailers, via a live pass overlapping the daemon's own cycle | mitigate | `make verify`'s live control stage and `boty.service`'s cycle hit the same six retailers, three of which were refusing us this morning. Controls: `make verify-offline` runs **first**, so a red tree never costs a live pass; the live pass starts only after checking `served/boty/status.json`'s `updated`; and it is run **once**, never re-run until green. Tasks 1a, 1f. |
| T-07-67 | Tampering | the closing plan writing code to make a row read MET | mitigate | 05-04's recorded decision governs — *"a closing plan that implemented its way to a green table would be a phase measuring work it did in the act of measuring."* **No code is written.** The `<objective>` names every directory that is off limits; Task 2's `<verify>` asserts `git status --porcelain` is clean at the checkpoint; Task 3's `<verify>` asserts the changed-file set against an explicit allow-list and **exits non-zero naming any stray path**, which also protects `.planning/milestones/` and the three planning documents this plan is forbidden to edit; and Task 1's instruction on a red offline gate is to **stop and report**, not to repair. Tasks 1-3. |
| T-07-68 | Repudiation | four mutation idents read as lost, and three documents disagreeing about the count | mitigate | `M21`-`M24` are Phase 6's deliberate gap in an otherwise dense sequence, and **three planning documents in this phase state the registry's size three different ways** (24-called-26, 28, and six-new-not-seven). A future reader meeting `… M20, M25 …` goes looking for four deleted gates and concludes the harness was weakened; a reader meeting three counts trusts none. Controls: the idents are **read** rather than assumed; the `<verify>` command asserts the exact set `M1`-`M20` ∪ `M25`-`M37` and **fails naming the count if it is anything else**; the arithmetic **26 + 7 = 33** is stated once in the SUMMARY, once in STATE.md, once in REQUIREMENTS.md's note and once in the closing record, always with the gap and its reason; and each wrong document is **flagged, never edited**. Tasks 1b, 3b, 3c, 3e. |
| T-07-69 | Repudiation | a gate counted as watched-red that never was | mitigate | Criterion 5 says *"every gate this phase adds has been watched going red"* — **not every mutation**. Seven CAUGHT idents are the easy part and would be an incomplete answer: the phase also adds static AST completeness gates, producer/consumer pairs, a cross-surface equality test and a join test. Task 3a requires the gates each SUMMARY records to be **enumerated**, with a per-gate statement of whether it was observed failing first, and any gate that was never observed failing named as such. Rounding this row up on the strength of the mutation ratio is the same act as rewording a criterion, one level in. Task 3a. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | **This plan installs nothing and changes no code.** Every command is `make`, `git`, `grep`, `date`, `systemctl show`, `pytest` or an existing script in this repo. Research is disabled for this project, so there is no `## Package Legitimacy Audit` and the fallback policy stands: if the executor finds it needs a package, **stop** — every package is `[ASSUMED]`, and a blocking human-verify checkpoint (gate `blocking-human`) verifying it on `pypi.org/project` must precede any install. Not auto-approvable; `workflow.auto_advance` does not apply. |

**Threats owned by earlier plans in this phase, listed so this register is not read as complete:**
T-07-01 (a reading claiming an age nobody recorded) spans 07-01/02/04; T-07-02 (a stamp read back off
disk) and T-07-08 (an existing `state.json` broken by the shape change) are 07-02's; T-07-03/03b
(two writers to one `pacer-state.json`) and T-07-09 (M33's anchor hitting a comment) are 07-03's;
T-07-05, T-07-10, T-07-11, T-07-12 and T-07-13 (a widened published file, `alertable` on a memory,
M34's anchor hitting a retailer row, the payload size, a `null` reaching a consumer) are 07-04's;
T-07-04, T-07-15 and T-07-16 (an interpolated value at the `innerHTML` sink, a warn tag false in
register, a real age judged against a threshold nobody paces by) are 07-05's. T-07-06 (the mutation
harness's runtime cost) was accepted at outline time and re-priced by 07-04 and 07-05 with the
snapshot count.
</threat_model>

<verification>
Run from the repo root, in this order. **Offline before live, always** — a red tree must never cost a
live retailer pass.

1. `make verify-offline` → **exit 0**, verdict line, `N passed` and mutation `caught/total` recorded
   verbatim beside the phase-start **778 passed, 26/26** and the three earlier baselines. Allow it
   minutes; do not kill it.
2. The mutation idents, read with comment lines filtered → asserted to be exactly `M1`-`M20` ∪
   `M25`-`M37`, **33 total**, with M31-M37 each observed **CAUGHT by ident**, an empty survivor list,
   and the **M21-M24 gap** recorded as deliberate with 06-03's and 06-04's reasons quoted.
3. The configured watch count from **`Config.load`** → **13**, with the grep figure of 14 and the
   comment at `config/products.yaml:309` recorded beside it. The live `status.json` row count and
   `updated` stamp read as a third data point. `systemctl show boty` recorded. **No store number read,
   derived or printed; nothing run under the service's `EnvironmentFile`.**
4. `make verify` → run **once**, live, after checking the daemon's `updated`, recorded **verbatim
   including its FAIL**, classes separated.
5. The leaked-markup sweep → re-run, pattern and count both recorded as measured, the four instances
   stated with individual evidence.
6. The record written; `.venv/bin/python -m pytest tests/test_packaging_metadata.py
   tests/test_evidence_check.py -q` green after the STATE.md and evidence-doc edits.
7. The three assertion commands in Task 3's `<verify>` → criterion bodies unchanged, REQ-21's body
   unchanged, and the changed-file set inside the allow-list. Each **exits non-zero naming what
   moved**; none is a print.
8. `.venv/bin/python scripts/identity_check.py --all` → PASS over the whole tracked tree, after the
   record is written and before the commit.
9. `make verify-offline` → **exit 0 again**, after the record is written.
10. `git status --porcelain` → clean of anything outside the closing set before the commit.

**What is deliberately NOT done, and why each is a decision rather than an omission:**

- **No code is written.** A criterion unmet at this point is **recorded** unmet. 05-04's decision
  governs and is quoted in the objective.
- **`make verify` is not re-run until it is green.** It has read FAIL since 2026-08-06 for reasons
  this phase did not cause, and one of its three classes is Phase 5's guard **working**. Re-running
  until the number looks better is the omission this project keeps catching in itself.
- **`make verify` is not run under the service's `EnvironmentFile`.** It would pull a real store
  number into a process Claude launched and reads. The pinless run is what gets quoted, precisely
  because its output names a key rather than a number.
- **No retailer is probed and no fixture is re-captured.** Every red-watch in this phase was offline
  by design: constructed `Result`s, constructed persisted documents, the real pre-07 `state.json`
  shape, and mutations inside the harness's own sandbox.
- **The daemon is not restarted and no store is pinned.** Both are Dan's actions; this plan measures
  and records which side of them he is on.
- **No criterion is amended and no requirement text is edited**, including REQ-21's sentence that
  this phase measured one direction off. It is flagged below the traceability table, which is that
  note's job.
- **`07-CONTEXT.md`, `07-PATTERNS.md` and `07-PLAN-OUTLINE.md` are not edited**, though all three are
  wrong about something. A planning document is the record of what was believed when the work was
  scoped, and correcting it in place destroys the evidence that the measurement mattered.
- **`.planning/milestones/` is not touched.** v0.2's two outcome tables are the format this plan
  writes in, not documents it edits.
- **The milestone is not closed, tagged or rolled.** v0.3 has one phase in it so far.
</verification>

<success_criteria>
- [ ] `make verify-offline` exits 0, and its verdict line, test count and mutation ratio are recorded
      **verbatim** beside **778 / 26-26** at phase start and the three earlier baselines, so the rise
      is shown rather than claimed. It was allowed to finish.
- [ ] The idents were **read** from the registry with comment lines filtered; the set is asserted to
      be `M1`-`M20` ∪ `M25`-`M37` at **33**; **M31 through M37 were each observed CAUGHT by ident**
      with an empty survivor list; and the arithmetic **26 + 7 = 33** with the **deliberate M21-M24
      gap** is recorded in the SUMMARY, in STATE.md, in REQUIREMENTS.md's note and in the closing
      record.
- [ ] The configured watch count was measured through **`Config.load` and is 13**, and every number
      on the checkpoint card came from a measurement rather than from a planning document.
- [ ] Dan was shown both consequences at a **blocking** checkpoint, no option was offered that would
      remove rows from `status.json`, and **both answers are recorded verbatim with the date** —
      including a third `defer`. Nothing was built and nothing was restarted on any answer.
- [ ] **No store number was read, derived, inferred, requested or printed anywhere**, presence was
      measured as a count, and `identity_check.py --all` passed at the checkpoint and again before
      the commit.
- [ ] `make verify` was run **once**, live, and recorded **verbatim including its FAIL**, with the
      pre-existing classes named as pre-existing and any new class named as this phase's.
- [ ] Five criterion verdicts stand in `ROADMAP.md` in the established three-column shape, each with
      a measurement or a reason. **Any criterion not fully met is recorded UNMET or MET IN PART with
      the date and the reason, and nothing anywhere was reworded to pass** — asserted by command.
- [ ] **Criterion 3's `status.json` third was argued explicitly**, with what landed stated, both
      readings given, and the verdict reached rather than rounded up. **Criterion 5 enumerates the
      gates**, not only the seven mutations.
- [ ] The Phase 7 checkbox is ticked, the `Plans:` list is ticked against SUMMARYs on disk with its
      objective text unrewritten, and the **13-not-14** correction is recorded beside it.
- [ ] REQ-21 is flipped with an evidence-carrying traceability cell, or left Pending with the reason.
      **Its text is byte-identical** — asserted — and its two measurement notes sit **below** the
      traceability table.
- [ ] STATE.md carries the real position, the phase's decisions, the corrections from waves 1-5, the
      fourth leaked-markup instance with a **re-measured** sweep, the new live `make verify` verdict,
      and both of Dan's answers — with **`milestone: v0.3` unchanged** and
      `tests/test_packaging_metadata.py` re-run green after the edit.
- [ ] `docs/retailer-evidence.md` carries `## Phase 7 closing record` beside the Phase 6 record, with
      the four-column table, the gates and the arithmetic, the two consequences and Dan's answers,
      the live verdict, **every correction from waves 1-5 as its own subsection quoting the SUMMARY
      that made it**, and a *what this phase did not establish* subsection stating that none of it is
      on the wire until a restart.
- [ ] `QUESTIONS.md` § 0f carries Dan's answer, dated, with no store number in any form; § 0e is
      untouched.
- [ ] `identity_check.py --all` PASSED after the record was written, and `make verify-offline` exits
      0 **again** after it was written.
- [ ] One commit, `git config user.email` checked first, and a message saying no criterion was
      reworded, that the registry rose from 26 to 33 with seven idents, and that M21-M24 are
      deliberate.
- [ ] **No code was written.** The changed-file assertion in Task 3's `<verify>` exits non-zero
      naming any stray path, which also proves `.planning/milestones/` and the three planning
      documents are untouched.

**Coverage this plan owns** (the full four-source audit is in `07-PLAN-OUTLINE.md` and is not
restated here):

| Source | Item | Where in this plan |
|---|---|---|
| GOAL | *"Every reading says when it was taken… or says it does not know"* — recorded for all five criteria | Task 1b, Task 3a, Task 3e |
| REQ | REQ-21 — closed by measuring what landed, with its own sentence's one-direction error flagged and not edited | Tasks 1d, 3b |
| CONTEXT | Never amend a success criterion to make it meetable | Task 3a — and the ROADMAP diff, asserted rather than eyeballed, is the proof |
| CONTEXT | A gate must be watched going red before it is trusted | Task 1b — seven idents observed CAUGHT by ident, plus Task 3a's per-gate enumeration for criterion 5 |
| CONTEXT | Every criterion verified by something executable | Tasks 1a/1f — `make verify-offline`, `make verify`, the mutation harness |
| CONTEXT | UNKNOWN is never a verdict; where no time was recorded the age is UNKNOWN, never "now" | Task 3a rows 2 and 4, Task 3e — including Walmart's row publishing an UNKNOWN age indefinitely as the honest output |
| CONTEXT | Never write a real store number or host identity | Task 1c, Task 2, Task 3d, Task 3f — T-07-63 |
| CONTEXT | Politeness is a hard constraint — no live probing to test this | Task 1f — one live pass, after a daemon-cycle check, never re-run |
| CONTEXT | Deferred: `QUESTIONS.md` § 0f, § 0e, the live `make verify` classes, the `.planning/` contents gate | Surfaced at Task 2 and recorded at Task 3; **none is closed by this phase** |
| OUTLINE | 07-06 sketch — the blocking checkpoint, the gates measured, the five verdicts | Tasks 1-3 |
| OUTLINE | Evidence constraint — no acceptance depends on a live retailer read | Task 1a is the gate; Task 1f's live run is recorded, never required to be green |
</success_criteria>

<output>
Create `.planning/phases/07-a-reading-has-an-age/07-06-SUMMARY.md` when done.

It must carry, beyond the template:

- **The `make verify-offline` verdict**, verbatim, with its test count and mutation `caught/total`
  beside the phase-start **778 / 26-26**, Phase 6's **768→769 / 24-24**, Phase 5's **667 / 16-16** and
  the pre-milestone **531 / 8-8**, plus each of this phase's five intermediate counts.
- **The full mutation ident list as read from the registry**, with each of M31-M37's CAUGHT line, the
  empty survivor list, the arithmetic **26 + 7 = 33** stated once, and the **M21-M24 gap stated as
  deliberate** with 06-03's and 06-04's reasons quoted.
- **The measured numbers the checkpoint quoted** — the configured watch count from `Config.load`, the
  live row count and `updated` stamp, and `systemctl show boty`'s `MainPID` and
  `ActiveEnterTimestamp` — so the card can be checked against what it was built from.
- **Dan's two answers verbatim with the date**, including a `defer`, and including exactly what was
  therefore not decided.
- **The live `make verify` verdict, verbatim including its FAIL**, with the classes separated and any
  class this phase caused named as ours.
- **The leaked-markup sweep's pattern and count as measured at close**, with the four instances and
  their individual evidence, and any disagreement with a remembered figure stated rather than
  reconciled away.
- **The five verdicts as written into the ROADMAP**, reproduced, so the SUMMARY and the ROADMAP can be
  diffed against each other — including the criterion 3 argument in full.
- **Every correction from waves 1-5**, quoted from the SUMMARY that made it — the three documents
  wrong about the registry, the 13-not-14 denominator, the 21 600 s cap against the ~97-minute
  remaining-time figure, the absent-row finding and REQ-21's one-direction error, and the three
  near-miss anchors with their measured counts.
- **What this phase did not establish**, as its own section: none of it is on the wire until a
  restart; Walmart's row publishes an UNKNOWN age indefinitely until a store is pinned; the live
  `make verify` classes are recorded and not diagnosed; `boty check`'s stale form is not reachable in
  production; and anything the five SUMMARYs flagged.

Nothing in this summary is reworded to pass. If a criterion is unmet, it is unmet here in the same
words it is unmet in the ROADMAP. **Name the tool-call tag shapes; never reproduce them** — this file
must not become the fifth instance in a list this plan wrote.

**Expect `gsd-tools state advance-plan` to misfire** (it has, eight times, for the reason recorded in
STATE.md): correct `status`, `stopped_at` and `percent` by hand, restore the comment recording that
`percent` is phase-based, and do not trust the tool's summary line. **Do not edit STATE.md's
`milestone:` frontmatter key** — it is machine-read against `pyproject.toml` by
`tests/test_packaging_metadata.py`.
</output>
