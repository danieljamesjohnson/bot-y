# Milestone v0.3: Say When You Measured It

**Status:** ✅ COMPLETE IN THE TREE 2026-08-19 — **NOT deployed, NOT tagged, NOT published**
**Phases:** 7 (one phase) · **Requirements:** REQ-21 (one requirement)
**Total Plans:** 7 (six planned; 07-07 appended after the phase closed)
**Scoped:** 2026-08-13 (`d837676`) · **Closed:** 2026-08-17 · **Reopened and re-closed:** 2026-08-19 · **Audited:** 2026-08-19
**Audit:** [`v0.3-MILESTONE-AUDIT.md`](v0.3-MILESTONE-AUDIT.md) — status `tech_debt`; no blockers,
no unsatisfied requirements, six residuals carried forward

**"Shipped" is deliberately not the word, and this time it is not even close.** Everything this
milestone built is in the tree and **none of it is running** — the daemon watching for restocks
holds code that is about fifteen hours *older than this milestone's first commit*. See *The five
facts* below. No git tag exists, anywhere, and none was created at close: `git tag -l` → 0. That
decision is being handled separately and this archival did not touch it.

**This is the narrowest milestone this project has run: one phase, one requirement.** It is
recorded at that size deliberately. Nothing here is inflated to look like v0.2.

## Overview

Scoped 2026-08-13 from a question the system could not answer. Dan asked of the Amazon and
Walmart GO Plus + watches: *"so they are out of stock as of when?"* — and there was no recorded
answer. Not hard to find; **absent**.

v0.2 was *say only what you measured*. This is its unfinished half: **a reading with no age is a
claim about the past presented as the present.** Same shape as *"the detector is probably
broken"*, except the unestablished thing is *when*.

It has teeth rather than being tidiness. A retailer that refuses us backs off to multi-hour
intervals, so **the rows least likely to be current are exactly the ones that look identical to
the fresh ones.** And the measurement that opened the milestone found something sharper than the
requirement's own wording: a paced-out watch did not have a *stale* row in `status.json` — **it
had no row at all**, measured at 3 rows published for 13 configured watches. A staleness rule
applied only to the rows that exist would have been a bound that cannot bind, so the row-per-watch
fix (07-04) had to land before any staleness rule could mean anything.

## Measured stats

| | |
|---|---|
| Base commit | `b10df8c` — *docs(07): plan outline — six plans, and a paced-out watch has no row at all*, 2026-08-13. The milestone was **scoped** three commits earlier at `d837676` |
| Commits | **72** (`b10df8c..6c3761b`) |
| Diff | **41 files changed, +18,146 / −104** |
| Timeline | 2026-08-13 → 2026-08-19. Commits by day: 23 / 23 / 13 / 8 / 5 |
| Files outside `.planning/` | **21 modified, 0 added, 0 deleted.** Nine of them are shipped code — seven under `boty/`, plus `served/boty/index.html` and `scripts/mutation_check.py`; the rest are eight test modules, `Makefile`, `.gitignore`, `docs/retailer-evidence.md` and `QUESTIONS.md` |
| Phases / plans / SUMMARYs | 1 / 7 / 7 |
| Gate at close | `make verify-offline` **EXIT=0** — identity PASS over **225 files**, **884 passed / 0 skipped**, mypy clean over **18 source files**, **34/34 mutations caught, survivors 0** (re-measured by the audit at `b832643`) |
| Gate at milestone start | **778 passed, 26/26** (`dbc9d49`) |
| Mutation registry | **26 → 34.** M31–M37 from the phase itself, each watched red by hand before registration; **M38** from the post-close code review. `M21`–`M24` remain Phase 6's deliberate gap. **`M39` is free** |
| Git tags | **0.** Deliberate, and unchanged by this archival |

## The five facts this archive exists to keep

**1. THE HEADLINE: none of this is running, and it is older than that sounds.** `boty watch`
`MainPID=547119`, `ActiveEnterTimestamp=Wed 2026-08-12 17:28:29 CDT` — re-measured at this
archival and unchanged. That is roughly **fifteen hours before this milestone's first commit**, so
the deployed daemon has never at any moment held a line of this milestone's code. Measured on the
live documents rather than argued: `served/boty/status.json` carries **none** of the four keys the
milestone added (`read_at`, `checked`, `current_interval_seconds`, and the row-per-watch shape — it
publishes **10** rows for 13 configured watches), and `state.json` still holds **13 bare pre-07
strings** with no version field. The restart is deferred by Dan's decision, recorded **twice** in
`QUESTIONS.md` § 0f, the second answer verbatim `keep defer`. **The dates are measured here rather
than inherited:** the audit's `deployment.reason` gives them as *"2026-08-14 and 2026-08-17"*, and
§ 0f's own heading and body give **2026-08-10** (05-04's checkpoint, the section created that day
at `e55f733`) and **2026-08-17** (07-06's checkpoint). 2026-08-14 is a date on which the pin's
absence was *measured*, not a date on which Dan answered. § 0f also counts a **third** deferral by
any means — v0.2's milestone close on 2026-08-11, which carried no new answer from him. `boty` is
an editable install, so the phase reaches the dashboard at the next `sudo systemctl restart boty`;
that is **his action, neither performed nor recommended here.**

**2. That restart is verified SAFE BY EXECUTION, not by argument.** The integration checker ran
the new code against copies of all three live pre-phase documents: **13/13 availabilities
survive, 0 ages invented**, the pacer document round-trips, and the real `config/products.yaml`
does **not** trip the new WR-02 config guard. Rollback-after-restart cost is bounded at **2
duplicate restock pushes**, re-derived from the live ledger rather than estimated. So the deferral
is a choice about timing and not a risk that is being carried — which is worth stating precisely
because v0.2's deferral was priced wrong once already.

**3. Criterion 3 is MET IN PART by Dan's explicit decision, 2026-08-17 — not by oversight.**
`status.json` publishes the **ingredients** of a staleness verdict — `read_at`, `checked`,
`current_interval_seconds` — and **no verdict**, no `stale` key. A join test proves those three
jointly sufficient, and both rendering consumers do present staleness. But the criterion says
*presented as stale in `status.json`*, and *sufficient to derive* and *presented as* are different
sentences — which is the difference this whole milestone exists because of. **It is not a TODO,
not an oversight, and not work queued for a later plan;** 07-07 was fenced against touching it and
did not. **No key was added to make the row read MET.**

**4. Criterion 5's qualification is NOT discharged, and the archive says so in the verdict cell
itself.** 07-05's join test passed the moment it was written — on the RED commit, before any
implementation existed — and was therefore never observed failing. On 2026-08-19 Dan chose to
settle its non-vacuity **by measurement rather than by the override `07-VERIFICATION.md` had
drafted**, and it was watched going red **5 out of 5** against five separate breaks of the
behaviour it joins. That moves the verdict to **MET, qualified**. The qualification is not a
formality: **this red was watched after the fact, against an implementation that already existed.
The original RED was never observed and TDD ordering was NOT followed for that test.** *The gate
bites* and *the gate came first* are two different claims and **only the first is established.**

**5. A code review ran after the close and found 1 Critical + 8 Warnings; all nine were fixed.**
The Critical was a **reproduced** XSS sink: `served/boty/index.html` interpolated `w.availability`
into a `class="dot ${...}"` attribute unescaped, reachable because
`monitor._remembered_availability` deliberately returns any string from `state.json` and 07-04
publishes it. **The gate that should have caught it listed `w.read_at` as untrusted and omitted
`w.availability`, which arrives from the same file by the same route.** Both the sink and the gate
were fixed. That the phase shipped a bound which read like one and was not one, for the span
between 07-04 opening the sink and the fix, is carried in criterion 5's qualification rather than
erased.

---

## Milestone v0.3 — Say When You Measured It (Phase Details)

**Scoped 2026-08-13, from a question the system could not answer.** Dan asked of the
Amazon and Walmart GO Plus + watches: *"so they are out of stock as of when?"* — and
there was no recorded answer. Not hard to find; **absent**.

v0.2 was *say only what you measured*. This is its unfinished half: **a reading with
no age is a claim about the past presented as the present.** Same shape as *"the
detector is probably broken"*, except the unestablished thing is *when*.

It has teeth rather than being tidiness. A retailer that refuses us backs off to
multi-hour intervals, so **the rows least likely to be current are exactly the ones
that look identical to the fresh ones.** Detail and the reconstruction in
`.planning/seeds/a-reading-does-not-carry-its-age.md`.

### Phase 7: A Reading Has an Age

**Goal**: Every reading says when it was taken, and a reading too old to trust is shown as stale rather than as fact — or says it does not know.
**Depends on**: Phase 6
**Requirements**: REQ-21
**Success Criteria** (what must be TRUE):

  1. Every `Result` records when it was read, and that time is published per watch in `status.json`
  2. A reading with no recorded time is shown as UNKNOWN age, never as current — watched going red
  3. A reading older than its retailer's current interval is presented as stale in `status.json`, `boty check` and the dashboard, and the staleness is derived from the retailer's own pacing rather than a fixed clock
  4. The age survives a service restart, so a restart cannot make a two-day-old reading look fresh
  5. `make verify-offline` exits 0, and every gate this phase adds has been watched going red

**Outcome, recorded 2026-08-17 by 07-06 — three of five MET as written; criteria 3 and 5 MET IN
PART, each with the half that is met and the half that is not named separately. No criterion text
anywhere in this document was reworded, shortened, merged or amended, and that is asserted by
command rather than left to the eye.** The two partial rows are the two this phase had the most
pressure to round up, and the reasons are recorded rather than absorbed: criterion 3's `status.json`
third landed as three raw facts plus a join test rather than as a staleness verdict in the file, and
criterion 5's *"every gate"* is broader than *"every mutation"* — one gate this phase adds was never
observed failing, by construction, and is named below. **And as in Phases 5 and 6, none of this is
confirmed on the deployed daemon:** `boty watch` `MainPID=547119`, `ActiveEnterTimestamp=Wed
2026-08-12 17:28:29 CDT`, re-measured 2026-08-17 and unchanged — it predates 07-01 by ~15 hours and
the file it writes every cycle carries none of this phase's four keys. Full working in
`docs/retailer-evidence.md` § *Phase 7 closing record*.

**Post-review addendum, recorded 2026-08-18 by 07-07 — five figures in the table below are
superseded, and they are recorded here rather than edited.** A code review ran after the
phase closed and found one Critical and eight Warnings; all nine were fixed (nine `fix(07):`
commits, each carrying a `Watched going red:` count except `bc23a02`, a docstring rewrite
that adds no gate), and 07-07 then made the dashboard parse gate execute inside
`make verify-offline` for the first time. Re-measured 2026-08-18 at HEAD `85b337d`:

| Figure below | Claimed (2026-08-17, pre-review) | Measured 2026-08-18 |
|---|---|---|
| `Result(` construction sites | `all 20` | **21** (WR-07 added an arm) |
| read / non-read partition | `11 / 9` | **12 / 9** |
| offline suite | `865 passed` | **884 passed, 0 skipped** |
| mutation ratio | `33/33` | **34/34**, survivors 0 (M38 registered by the WR-01 fix) |
| identity check | `220 file(s)` | **224 file(s)** |

**No verdict changes.** Criterion 3 stays MET IN PART — `status.json` still publishes the
ingredients of a staleness verdict and no verdict, by Dan's explicit decision rather than by
oversight. Criterion 5 stays MET IN PART: its `every gate` half has **three** named reasons
and 07-07 closes the **third** only (a gate that skipped in the environment the project's own
gate runs in); the join test never observed failing and CR-01's gate that could not bite both
**stand**. Closing one of three reasons is not closing the criterion. No criterion text below
was reworded, shortened, merged or amended, and the `Plans:` list is left as written. The
mutation registry is untouched at `M1`-`M20` ∪ `M25`-`M38`, with **M39 still free**. Full
working, including why a superseded measurement is recorded beside while a description of
evidence that never existed is corrected in place, in `docs/retailer-evidence.md` §
*Phase 7 post-review addendum*.

**Non-vacuity measurement, recorded 2026-08-19 — criterion 5's verdict MOVES, and this is the
paragraph a reader meets before the row that carries the superseded one.** The 2026-08-17 and
2026-08-18 texts above are left exactly as written; nothing in them was edited. On 2026-08-19 Dan
chose to establish the join test's non-vacuity **by measurement rather than by the override
`07-VERIFICATION.md` had drafted**, and the measurement was taken: 07-05's
`test_status_json_carries_everything_a_consumer_needs_to_judge_staleness` was watched going red on
**five** separate breaks of the behaviour it joins, in a throw-away worktree at HEAD `bc31030`,
each applied alone against a baseline of 884 passed and each reverted to an empty
`git status --porcelain` — 3 failed / 881 passed (the cadence the join fetches, `EXIT=1`), 3 failed
/ 881 passed (the age), 4 failed / 880 passed (the provenance), and 2 failed / 882 passed twice for
the two forms of publishing the `stale` key the design refuses. **Criterion 5 moves from MET IN
PART to MET, qualified.** The qualification is not optional and is repeated in the row itself:
**this red was watched on 2026-08-19, after the fact, against an implementation that already
existed. The original RED was never observed and TDD ordering was NOT followed for this test.**
*The gate bites* and *the gate came first* are two different claims and **only the first is
established.** Criterion 3 is untouched and stays MET IN PART by Dan's explicit earlier decision;
no key was added to `status.json` and the tree ends the measurement byte-identical to `bc31030`.
The registry is untouched at `M1`-`M20` ∪ `M25`-`M38`, **`M39` still free** and deliberately not
spent on this. Full working, including the one unflattering finding — the test never goes red
*alone*, because every fact it joins carries its own gate — in `docs/retailer-evidence.md`
§ *Phase 7 non-vacuity measurement*.

| # | Verdict | Measurement or reason |
|---|---|---|
| 1 | **MET in the tree — NOT ON THE WIRE** | `Result.read_at` is stated at **all 20** `Result(` construction sites in `boty/retailers.py` — not inherited from a default — and completeness is proved by a **static AST gate over the source** (`20/20 sites name read_at`) rather than by covering arms one at a time, so an arm added later cannot silently omit it. The read/non-read partition is **11 / 9**, and the two Best Buy arms the naive rule gets wrong (`bad api json`, `sku not found`) are stamped **as reads**, because Best Buy answered. An arm that read nothing carries **`null`, never `0`** — epoch 1970 renders as maximally stale, which is the same lie one direction over. `status.json` publishes `read_at` per watch row, with `tests/test_status.py`'s exact-keyset assertion **updated by enumeration rather than loosened**. `CAUGHT M31 boty/retailers.py: 2 test(s) failed` — the mutation stamps a refusal that never received a page, which is the 2026-08-12 defect REQ-21 exists to fix, rebuilt inside the fix. Quoted from `07-01-SUMMARY.md`. **Live confirmation NOT OBTAINED, 2026-08-17:** the daemon's rows carry no `read_at` key at all |
| 2 | **MET in the tree — NOT ON THE WIRE. Spans two plans, and that is structure rather than dilution** | The criterion has a datum half and a rendering half, and the ROADMAP says so immediately below this table. **Datum, 07-01:** an unstamped reading is `None`, never `now` — red-watched by **M31**. **Rendering, 07-05:** `[age ?]` on `boty check` and an amber warn tag on the dashboard, both appended **unconditionally**, because if fresh rows carried no tag then an absent tag would *mean* fresh and the implicit claim would survive the fix. Red-watched by `CAUGHT M36 boty/cli.py: 1 test(s) failed — test_report_says_unknown_for_a_reading_nobody_dated`. **Measured on this host rather than argued:** the real payload renders **13 of 13 rows as `AGE ?`**, because `state.json` holds 13 availabilities and **0** stamps — re-confirmed 2026-08-17, still 13 bare pre-07 strings. The page was **looked at** with headless Chromium against a scratch copy, not inferred from source. Quoted from `07-01-SUMMARY.md` and `07-05-SUMMARY.md` |
| 3 | **MET IN PART — the `boty check` and dashboard thirds MET; the `status.json` third NOT SETTLED, recorded 2026-08-17** | **What is MET.** The threshold is the retailer's own current cadence, not a fixed clock: `Pacer.current_interval` is one number, `record` computes its own wait *through* it so the number shown and the schedule kept cannot drift apart, and `tests/test_pacing.py`'s schedule characterisation asserts **literal expected seconds** so it cannot pass by re-deriving them through the code under test (`CAUGHT M33 boty/pacing.py: 11 test(s) failed`). A row that *can* be old exists at all only because of 07-04 — **13 rows published while 3 were fetched**, `checked: false`, `alertable: false` stated (`CAUGHT M34`, `CAUGHT M35`). Both renderings judge against that published per-retailer number and never against `index.html`'s 30-minute banner constant, which was left exactly where it is (`CAUGHT M37 served/boty/index.html`). **What is NOT settled, and why it is not rounded up.** 07-05 deliberately added **no staleness flag and no new key** to `status.json`: a `stale` boolean computed at write time is written `false` and keeps saying `false` for exactly the interval during which it becomes true — `pacing.py:196-199`'s own recorded lesson, one file over — so it is *a bound that cannot bind, which is worse than no bound because it reads like one in the file*. Instead the file publishes three raw facts (`read_at`, `checked`, `current_interval_seconds`) and a join test derives the verdict from those three **and nothing else**, passing **on the RED commit before any implementation existed** — the strongest form that sufficiency claim can take. **The argument that this MEETS the criterion:** everything needed to present staleness is published, proved jointly sufficient by execution, and both consumers do present it. **The argument that it does not:** the criterion says *presented as stale in `status.json`*, and the file presents the **ingredients** of the verdict, not the verdict — a reader opening it sees three numbers and must subtract against their own clock. Those are different sentences, and this is the milestone that exists because a difference of exactly that size was published as if it were none. **Verdict reached: MET IN PART.** The criterion's text is **not edited**, and no key was added to make the row read MET |
| 4 | **MET in the tree — NOT ON THE WIRE, and the restart is the whole of what is missing** | `monitor.State` becomes a dated per-watch ledger, and the migration was measured against **the real pre-07 document on this disk** rather than a synthetic one: 13 bare strings loaded **without exception**, every availability preserved, every age **unknown**, and **alert behaviour byte-for-byte unchanged** — so nothing re-alerts on the migration itself. A stamp read back that is in the future, or a string, or a `True`, is **discarded rather than believed** (both ends of the bound validated). The age was observed surviving a restart **modelled as two `watch_loop` calls sharing one `state_path`**, each building its own `State` and its own `Pacer` so that **only the file crosses between them**, with **both ends asserted** — the bytes on disk and what a fresh `State.load` makes of them. **No process boundary was crossed, and no test in this phase crosses one** (corrected in place 2026-08-18; see the addendum above). `CAUGHT M32 boty/monitor.py: 7 test(s) failed` — the mutation defaults a missing stamp to `now`, which on this host's document dates all 13 entries at the instant the daemon starts and makes a two-day-old reading look four seconds old; that is this criterion's failure in one line. Quoted from `07-02-SUMMARY.md`. **NOT confirmed on the deployed daemon:** re-measured 2026-08-17, the running process is still the pre-phase one, so no service restart has yet exercised this path in production |
| 5 | **MET IN PART — the gate half MET; *"every gate"* NOT fully MET, recorded 2026-08-17** | **`make verify-offline` exits 0** — re-run at close on 2026-08-17 and **identical** to the 2026-08-14 run on every count: `identity check: PASS — 220 file(s)`, `865 passed`, `Success: no issues found in 18 source files`, `mutation check: 33/33 mutations caught`, `VERIFY: PASS (OFFLINE …)`, `EXIT=0`. Survivor list **empty**. **All seven new mutations were watched going red BY HAND, alone, before the harness was ever asked** — M31, M32, M33, M34, M35, M36, M37 — each reverted to an empty `git status --porcelain` before the next was applied, and in every case the by-hand killer list and the harness's list agree by name, which is the only thing that makes CAUGHT mean anything. **But *"every gate"* is broader than *"every mutation"*, and rounding this row up on the strength of 33/33 would be rewording a criterion one level in.** Enumerated: every TDD gate in the phase was observed failing first at a recorded RED count — 07-01 (3 failed / 33 passed, then 5 failed, then 4 failed), 07-02 (14 failed / 29 passed, then 2 failed / 45 passed), 07-03 (5 failed / 68 passed, then 5 failed / 25 passed), 07-04 (10 failed / 29 passed, then 3 failed / 39 passed), 07-05 (20 failed / 30 passed, then 6 failed / 9 passed) — **with exactly one named exception**: 07-05's join test, `test_status_json_carries_everything_a_consumer_needs_to_judge_staleness`, **passed the moment it was written, on the RED commit, and was therefore never observed failing.** That is not a weak test — it is the measurement that the three published facts were *already* jointly sufficient, and 07-05 recorded it as such rather than manufacturing a red — but the criterion says *every* gate, and this one was not watched going red. **Verdict reached: MET IN PART**, with the exception named rather than absorbed. **SUPERSEDED 2026-08-19 — see the next paragraph of this cell; the text above is the 2026-08-17 reading and is left unedited because it was true when written.** **UPDATED 2026-08-19 → MET, QUALIFIED.** The named exception is closed by measurement: the join test was watched going red on **five** separate breaks of the behaviour it joins, each applied alone in a throw-away worktree at HEAD `bc31030` and each reverted to an empty `git status --porcelain` — **3 failed / 881 passed** with `EXIT=1` (null the cadence the join fetches), **3 failed / 881 passed** (null the age), **4 failed / 880 passed** (invert the provenance), **2 failed / 882 passed** (publish the forbidden `stale` key on the remembered row) and **2 failed / 882 passed** (publish it on both row comprehensions, as somebody would actually ship it). Worktree baseline 884 passed; 884 passed again after the last revert. **THE QUALIFICATION, WHICH IS NOT OPTIONAL AND IS NOT A FORMALITY: this red was watched on 2026-08-19, after the fact, against an implementation that already existed. The original RED was never observed. TDD ordering was NOT followed for this test, and nothing here claims it was.** *The gate bites* and *the gate was written before the code it binds* are two different sentences and **only the first is now established** — which is the whole reason this row says MET **qualified** rather than MET. The verdict moves because the criterion's own text asks that a gate *have been watched going red* and does not say *first*, and because this repository's dominant instrument for that question is after-the-fact by construction — `mutation_check.py` breaks working code 34 times on every run and the phase calls those gates proven, and the 2026-08-17 verification re-applied three already-fixed defects by hand under the heading *"Gates re-watched going red"*. Reason (b), CR-01's escaping gate that could not bite, is also closed — `b1a3b88` widened `UNTRUSTED` to `w.availability` and the 2026-08-17 pass watched it red at 1 failed / 15 passed — but closing it does not erase that the phase shipped a bound that read like one and was not one for the span between 07-04 opening the sink and the fix, which is recorded in the qualification rather than in the verdict. **One finding recorded against interest: the join test never goes red alone.** All five breaks red at least one other test, because each of the three facts it joins carries its own gate; the test's contribution is the assertion that they *compose*, and a composition is reachable only through its parts. That is redundancy, not vacuity — a vacuous gate is one that cannot fail, and this one failed five times out of five. **No ident was registered and `M39` stays free**, because every break is already caught by a second test, so an ident would raise the denominator without defending anything new. Full working in `docs/retailer-evidence.md` § *Phase 7 non-vacuity measurement* |

**The phase gate, recorded 2026-08-17.** `make verify-offline`, run once at close and allowed to
finish, **exit 0**:

```
identity check: PASS — 220 file(s), no host identity found
All checks passed!
865 passed in 10.95s
Success: no issues found in 18 source files
control check: SKIPPED (--offline) — no live retailer request made.
mutation check: 33 mutation(s), sandboxed (the working tree is never touched)
  baseline  unmutated sandbox passes (836 passed, 29 skipped in 11.27s)
mutation check: 33/33 mutations caught
VERIFY: PASS (OFFLINE — live controls were NOT run, so nothing here says the retailers still work)
EXIT=0
```

**The ratio rose from 26/26 to 33/33**, and the arithmetic is stated once: **26 at phase start + 7
this phase = 33**. Read from the registry with comment lines filtered rather than counted with a bare
`grep -c`, which counts comment prose and is the self-invalidating class this phase had to correct
twice. The full ident list, in file order: `M1`–`M20`, `M25`–`M37`. **`M21`–`M24` ARE A DELIBERATE
GAP left by Phase 6 and are not four lost mutations** — 06-03: *"NO MUTATION REGISTERED, and M21-M22
left deliberately unallocated: `apply_mutation` cannot add a file, so the defect is outside the
harness by construction"*; 06-04: *"M23-M24 left unallocated, joining 06-03's M21-M22, so the
sequence carries a deliberate gap at M21-M24"*. The rise is **shown** rather than claimed: 531 / 8-8
(Phase 4 close) → 667 / 16-16 (Phase 5) → 768→769 / 24-24 (Phase 6) → **778 / 26-26 (Phase 7 start,
`dbc9d49`)** → 798 / 27-27 → 821 / 28-28 → 835 / 29-29 → 848 / 31-31 → 865 / 33-33.

**And the gate as it stands at the milestone's close, re-measured by the audit at `b832643` —
which is the figure a later reader should quote, not the 2026-08-17 block above:**

```
identity check: PASS — 225 file(s), no host identity found
884 passed in 10.95s          (0 skipped)
Success: no issues found in 18 source files
mutation check: 34/34 mutations caught, survivors 0
VERIFY: PASS (OFFLINE — live controls were NOT run)
EXIT=0
```

Registry **26 → 34** across the milestone: seven idents from the phase (M31–M37) plus **M38**,
registered by the WR-01 fix during the post-close review. **`M39` is free.**

**The live gate, run ONCE on 2026-08-14 and NOT repeated — its verdict is a 2026-08-14 observation
and is not restated as current.** Politeness is a hard constraint here and this close was budgeted
exactly one live pass, spent on 08-14 immediately after a daemon write so the two were not in flight
against the same six retailers. Verbatim, including the FAIL:

```
control check: FAIL — 2/6 control(s) not reading IN_STOCK
    walmart/CONTROL — Great Value whole milk: unknown — no store_id pinned for this watch
    amazon/CONTROL — Amazon Basics AA batteries (20-pack): unknown — blocked: challenge page
      matched 'to discuss automated access to amazon data' (HTTP 200)
control check: 2/6 control(s) could not run on THIS HOST
    bestbuy/… : fetch failed: no Chrome/Chromium binary found
    target/… : fetch failed: no Chrome/Chromium binary found
VERIFY: FAIL (live controls)
EXIT=2
```

The classes, separated, with **none of them this phase's**: (1) **2/6 cannot run on this host** —
Best Buy and Target, no Chrome/Chromium binary, pre-existing since 2026-08-06, and the tool itself
says this *"says nothing about the DETECTOR"*; (2) **the intermittent challenge class DID manifest on
2026-08-14**, on Amazon — it was absent on both 2026-08-10 and 2026-08-11, so the record now reads
present-absent-absent-present and *intermittent* remains the supported reading; (3) **Walmart through
Phase 5's config-gap guard**, because `make verify` runs in a shell with no `WALMART_STORE_ID` — that
one is Phase 5's and it is **correct**. The baseline's *"1/6 not reading IN_STOCK"* reads **2/6** here
because class 2 manifested, not because a new class appeared. **No control's verdict moved in a way
attributable to this phase**: GameStop and Nintendo both read `in_stock` as before, and no plan in
this phase touched a retailer, an extractor, a transport or a control. Recorded, not diagnosed, and
not re-run until green.

**Why the interval and not a fixed clock.** A retailer in backoff is *legitimately*
checked less often — Walmart at seven refusals is on a multi-hour interval and that
is the politeness rule working. What is dishonest is not the age; it is presenting
an age nobody recorded. So "stale" means *older than this retailer's own current
cadence*, which is a fact the `Pacer` already holds.

**The measurement that opened it**, reconstructed from refusal history because
nothing recorded it directly: Amazon's `out_of_stock` was from early 2026-08-13
(before ~06:37 — a refusal streak began then and the counter had reset, which only
happens on a successful read). **Walmart's could not be established at all**: no
later than 2026-08-12 16:49, plausibly 2026-08-11. A service restart at 16:49:57
zeroed the refusal counter and destroyed the evidence that would have settled it.

**Follow the `store` field's path.** It is the worked precedent four times over —
`rung`, `extraction`, the widened `degraded`, and `store` — and the groove is:
declare the field last with a default, write down what it is deliberately NOT folded
into, thread it through every `check_*` return including the error arms, publish it
in `status.write`, render it in `cli._report` and the dashboard, pin it with a
mutation anchored on behaviour rather than prose.

**Not to be confused with two things that already exist.** `status.json`'s
top-level `updated` is when the *cycle* ran — it is fresh even when every row in it
is stale. And `make verify`'s `fixtures` stage ages *captured test pages*, not live
readings.

**Plans**: 6 plans, in 6 waves — every wave serial, and for the plainest reason this
project has had: **no two plans own disjoint file sets.** The table is in
`.planning/phases/07-a-reading-has-an-age/07-PLAN-OUTLINE.md` § *Why every wave is serial*,
which is this phase's contract. Plans are written one at a time; a line below whose
`PLAN.md` does not exist yet is a plan scheduled, not a plan claimed.

Plans:

- [x] 07-01: A reading carries the moment it was taken, and a non-reading carries none — `Result.read_at`, all 20 construction sites, published as `null` never `0` *(wave 1)*
- [x] 07-02: The age survives the restart, and the file that survives it changes shape without breaking on Dan's disk *(wave 2, blocked on 07-01)*
- [x] 07-03: The retailer's current interval becomes one readable number, and both surfaces read the same one *(wave 3, blocked on 07-02)*
- [x] 07-04: Every configured watch has a row, and a remembered reading says it is remembered — 3 rows for 14 watches today *(wave 4, blocked on 07-03)*
- [x] 07-05: The three surfaces say the age out loud, and an absent one says UNKNOWN *(wave 5, blocked on 07-04)*
- [x] 07-06: Close — no code; the gates measured, the count observed rising from 26, five verdicts *(wave 6, blocked on 07-01 … 07-05, `autonomous: false`)*
- [x] 07-07: **APPENDED AFTER THE PHASE CLOSED, 2026-08-18** — three of the four verification gaps closed and the fourth deliberately fenced: five superseded figures recorded *beside* the closing record, the never-true "real two-process restart" clause corrected *in place* in all four documents that carried it, and the dashboard parse gate made to execute inside `make verify-offline` for the first time in its life *(wave 7)*

**The `Plans:` list above is reproduced exactly as `ROADMAP.md` carried it, which is why the
header says six and the list carries seven.** The 2026-08-18 addendum states in as many words that
*"the `Plans:` list is left as written"*, so 07-06's six-line list was never re-headed; 07-07 is
recorded here as the seventh so the archive agrees with the disk and with the audit, both of which
say **7/7**. Plan numbers are creation order; 07-07 is also wave order, because it ran after the
close rather than being inserted into it — which is the opposite of v0.2's 06-07, and worth the
sentence. **The 2026-08-19 work — the non-vacuity measurement and the phase-completion record —
carries no plan number at all:** it was direct work at Dan's direction (`5a083c7`, `0bd8ecb`,
`824ee1a`, `b832643`), and calling it an eighth plan would be inventing a planning artefact that
does not exist.

**Correction recorded beside the two lines above, which are not edited (2026-08-17):** the 07-04
entry and the paragraph below this list both say *"14 configured watches"*. Measured through the
loader that actually builds them — `Config.load('config/products.yaml')` — the count is **13**
(gamestop 5, walmart 2, nintendo 2, amazon 2, bestbuy 1, target 1; 6 of them controls). The
fourteenth `grep -c "retailer:"` match is a **comment** at `config/products.yaml:309` — *"There is no
`retailer: pokemoncenter` entry and that is a finding, not a gap"* — a sentence about an **absent**
watch counted as a present one. First measured by 07-04. The lines stay as written on Phase 3.1's
precedent: a planning document is the record of what was believed when the work was scoped, and the
"3 rows" half of both sentences is a real measurement that still stands.

**Criteria 2 and 3 each span more than one plan, and that is structure rather than
dilution.** Criterion 2 splits across 07-01 (the datum: an absent stamp is `None`, never
`now`) and 07-05 (the rendering: it shows as UNKNOWN). Criterion 3 splits across 07-03
(the interval), 07-04 (a row that can be old) and 07-05 (the three renderings) — because
a paced-out watch does not have a *stale* row in `status.json` today; **it has no row at
all**, measured at 3 rows for 14 configured watches, so a staleness rule applied only to
the rows that exist would be a bound that cannot bind.

### The code review that ran after the close, and the one number in the addendum that has moved

A standard-depth review of the phase's **17 changed files** ran on 2026-08-17 and returned
**1 Critical, 8 Warnings and 3 Info — 12 findings, status `issues_found`**. Its summary is worth
keeping because it is a hostile read that failed: *"The phase's central claim — no unknown age is
ever presented as a known one — holds on every path I could trace. I attacked it specifically and
could not break it."* The defects it did find are all one level out from that claim.

**All nine of the Critical and Warning findings were fixed.** The addendum above says *"nine
`fix(07):` commits"*; **measured at this archival there are ten**, because CR-01 needed a
follow-up — `9e7d302`, *"the CR-01 comment stopped the page parsing; gate it"* — where the comment
added by the escaping fix broke the dashboard's own parse. Nine findings, ten commits. The
addendum's sentence is **not edited**: it was a count of findings-as-fixed and it is right about
that; this is the count of commits and it is recorded beside, on the same rule the addendum itself
invokes.

**The three Info findings were recorded and NOT fixed, and that is stated rather than left to
inference** — re-measured at this archival: `state.json` still accumulates entries for watches
that no longer exist (IN-01, no prune in `monitor.save`); the strict boundary still renders
`[age 5m > 5m]` and the suite still asserts that exact string (IN-02, deliberate); and the
dashboard's `tick()` still reads `d.updated`, `d.retailers`, `d.watches` and `w.price.toFixed(2)`
with no shape guard outside its `try` (IN-03 — the only `catch` in the file is the fetch path's,
at `index.html:229-232`). IN-03 is the one worth a future plan: a malformed payload throws
mid-render and the list keeps whatever it last held, which is a **silently frozen dashboard** —
the failure class this project exists to prevent, one level up.

---

## Milestone Summary

**Phase checklist, as it stood in `ROADMAP.md` at close:**

- [x] **Phase 7: A Reading Has an Age** - Stamp it, publish it, and show a stale reading as stale rather than as fact (completed 2026-08-17 — three of five MET as written, criteria 3 and 5 MET IN PART, no criterion text amended)

**That checkbox line is reproduced as written and is one revision behind the record above it.**
It was written by 07-06 on 2026-08-17; criterion 5 moved to **MET, qualified** on 2026-08-19. Left
unedited on the same rule everything else here follows — a closing record is corrected beside
itself, not over itself — and the verdict table is the authority.

**Decimal phases:** none. **Inserted plans:** none — 07-07 was *appended after the close* rather
than inserted into the wave sequence, which is a different act from v0.2's 06-07.

**Verification status at close:** Phase 7 `passed`, 5/5. `07-VERIFICATION.md` first ran on
2026-08-17 and found **four gaps** at 3/5; 07-07 closed three on 2026-08-18; the fourth is
criterion 3's `status.json` third and is **open by Dan's explicit decision**. Re-verified at
`bc31030` on 2026-08-18 at 4/5, and criterion 5 moved on 2026-08-19 by measurement rather than by
the override the verification had drafted.

**Milestone audit:** status `tech_debt` at HEAD `b832643` — requirements 1/1, phases 1/1,
integration 5/5 wired, flows 1/1 complete, **zero blockers, zero unsatisfied requirements, zero
orphans**. Six residuals carried forward, listed under *Technical Debt Incurred* below.

### Key Decisions

| Decision | Rationale | Outcome |
|---|---|---|
| Staleness is measured against **the retailer's own current cadence**, never a fixed clock | A retailer in backoff is *legitimately* checked less often — that is the politeness rule working. What is dishonest is not the age but presenting an age nobody recorded | ✓ Good |
| An unstamped reading carries **`null`, never `0`** | Epoch 1970 renders as maximally stale, which is the same lie one direction over | ✓ Good |
| The stamp is **stated at every construction site**, never inherited from a default, and completeness is proved by a **static AST gate over the source** | Covering arms one at a time leaves an arm added later free to omit it silently. The gate counts sites, so a new arm fails the gate rather than the reader | ✓ Good |
| Two Best Buy arms the naive read/non-read rule gets wrong (`bad api json`, `sku not found`) are stamped **as reads** | Best Buy answered. The partition is about whether a page was received, not whether it was useful | ✓ Good |
| An age tag is appended **unconditionally**, including to fresh rows | If only stale rows carried a tag, an absent tag would *mean* fresh — and the implicit claim would survive the fix that was supposed to remove it | ✓ Good |
| **No `stale` key is computed at write time** in `status.json` | A boolean written `false` keeps saying `false` for exactly the interval during which it becomes true — `pacing.py`'s own recorded lesson, one file over. A bound that cannot bind is worse than no bound, because it reads like one | ✓ Good — and it is why criterion 3 is MET IN PART rather than MET |
| **One row per CONFIGURED watch**, before any staleness rule | A paced-out watch had **no row at all** — measured at 3 published for 13 configured — so a staleness rule over the rows that exist would have been a bound that cannot bind | ✓ Good |
| A remembered row states `checked: false` **and** `alertable: false` | A memory published without provenance is an observation nobody made. The disjointness was verified structurally *and* by execution: `_remembered_rows` selects by *absence* from `results`, so a remembered row is never a `Result` and cannot fire an alert | ✓ Good |
| A stamp read back that is **in the future, or a string, or a `True`, is discarded rather than believed** | Both ends of the bound validated. A migration that trusts its input is not a migration | ✓ Good |
| The migration was measured against **the real pre-07 document on this disk**, not a synthetic one | 13 bare strings, loaded without exception, every availability preserved, alert behaviour byte-for-byte unchanged — so nothing re-alerts on the migration itself | ✓ Good |
| `record` computes its own wait **through** `current_interval` | The number published and the schedule actually kept are then one expression and cannot drift apart | ✓ Good |
| The schedule characterisation asserts **literal expected seconds** | Re-deriving the expected value through the code under test is a test that cannot fail | ✓ Good |
| Both renderings judge against the **published per-retailer cadence**, never against `index.html`'s 30-minute banner constant | Two clocks is how the surfaces come to disagree. The constant was left exactly where it is, and M37 pins it | ✓ Good |
| Criterion 3 left **MET IN PART** rather than adding a key to make it read MET (Dan, 2026-08-17) | *Sufficient to derive* and *presented as* are different sentences — and this is the milestone that exists because a difference of exactly that size was published as if it were none | ✓ Good — an accepted partial, not an oversight |
| Criterion 5's non-vacuity established **by measurement rather than by the drafted override** (Dan, 2026-08-19) | An override is a signature; five reds are a measurement. It cost one throw-away worktree and produced a finding against interest (the test never goes red *alone*) | ✓ Good |
| The qualification on criterion 5 is carried **inside the verdict cell**, in all three documents | A qualifier in a footnote is a qualifier nobody reads. *The gate bites* and *the gate came first* are two claims and only the first is established | ✓ Good |
| **No ident registered** for the join test; `M39` stays free | Every one of the five breaks is already caught by a second test, so an ident would raise the denominator without defending anything new | ✓ Good |
| Superseded figures are recorded **beside**; a description of evidence that never existed is corrected **in place** | A measurement does not become false, it becomes superseded — but the "real two-process restart" clause described a test that never existed, and leaving that beside itself would preserve a claim rather than a measurement | ✓ Good — the sharpest distinction this milestone drew |
| The close writes **no code** (07-06) | Kept from 05-04 and 06-06. A closing plan that implemented its way to a green table would be a phase measuring work it did in the act of measuring | ✓ Good |
| **No `gsd-tools` state or phase WRITE is invoked on this repo** | Fourteen misfires across three subcommands, the last of which invented content. Read-only queries have been reliable throughout | ✓ Good — forced, and recorded in `STATE.md`'s own frontmatter |

### Issues Resolved

- **A reading did not carry its age at all.** `Result.read_at` is now stated at every `Result(`
  construction site in `boty/retailers.py` — **20 at the close, 21 after WR-07 added an arm** —
  with completeness proved by a static AST gate over the source rather than arm by arm.
  **M31** stamps a refusal that never received a page, which is the 2026-08-12 defect rebuilt
  inside the fix.
- **The age evaporated on restart.** `monitor.State` is a dated per-watch ledger that loads the
  real pre-07 document — 13 bare strings, no version field — without exception, preserving every
  availability, inventing no age, and leaving alert behaviour byte-for-byte unchanged. **M32**
  defaults a missing stamp to `now`, which on this host's document dates all 13 entries at the
  instant the daemon starts.
- **The cadence was two numbers that could disagree.** `Pacer.current_interval` is one number and
  `record` computes its own wait *through* it, so the number shown and the schedule kept are one
  expression. **M33**, killed by 11 tests.
- **A paced-out watch had no row at all** — measured at **3 rows published for 13 configured
  watches**, and moving 5 → 10 between two cycles eleven minutes apart on an unchanged
  configuration. `status.json` now publishes one row per configured watch, with a remembered row
  carrying the ledger's availability, the ledger's stamp (possibly `null`), `null` for every fact
  about the act of reading, and a stated `checked: false` / `alertable: false`. **M34/M35.**
- **Neither surface said the age out loud.** `cli._age_tag` and the dashboard's `ageTag` render
  the same four forms — `[age 4s]`, `[age 7h > 6h]`, `[age 3h, cadence ?]`, `[age ?]` — each
  judged against the retailer's own published cadence. **M36/M37**, the latter this registry's
  first mutation under `served/`.
- **A ledger string reached `innerHTML` unescaped (CR-01, Critical).** `served/boty/index.html`
  interpolated `w.availability` into a `class="dot ${...}"` attribute with no escaping, reachable
  because `_remembered_availability` deliberately returns any string from `state.json` and 07-04
  publishes it. **The gate that should have caught it listed `w.read_at` as untrusted and omitted
  `w.availability`, which arrives from the same file by the same route.** Both fixed (`b1a3b88`),
  and the gate watched red at 1 failed / 15 passed.
- **A backoff could make a refusal ask *more* often (WR-01).** A standing interval above the
  backoff cap inverted the recorded staleness direction. Clamped so a backoff may only ever widen
  the wait — and that clamp is what makes `pacer-state.json`'s non-atomic-write failure direction
  hold *universally* rather than usually. **M38.**
- **Seven more warnings, each fixed with its own red-watch:** a per-retailer override below
  `interval_seconds` is now refused (WR-02); `boty check` no longer renders a negative age (WR-03);
  `current_interval` no longer creates the record it reads (WR-04); `status.write` stages through
  a per-process temp file (WR-05); `boty check` no longer consumes a restock transition and sends
  nothing (WR-06); `check_bestbuy_api` proves the body is an object and coerces the price
  (WR-07); and `_remembered_availability`'s one-consumer premise, which had become false, was
  withdrawn rather than left for a future reader to trust (WR-08).
- **The dashboard parse gate had never once fired inside `make verify-offline`** (`85b337d`). It
  was the suite's **only** skip, for the whole of its life: 883 passed + 1 skipped became **884
  passed + 0 skipped**.
- **The closing record misstated the tree on five measurable numbers**, and a sixth clause
  described a test that never existed. The five are recorded **beside** the record in a dated
  addendum, not one edited; the "real two-process restart" clause was corrected **in place** in
  all four documents that carried it, because it was not a superseded measurement but a
  description of evidence that never existed. The behavioural claim is not weakened by one word
  and M32 stays CAUGHT with 7 tests failing.
- **Criterion 5's named exception, closed by measurement rather than by signature** — the join
  test watched red 5/5 against five separate breaks, each applied alone and each reverted to an
  empty `git status --porcelain`. With the qualification that it was watched **after the fact**,
  carried inside the verdict cell in all three documents.

### Issues Deferred

- **THE RESTART. This is the whole of what remains.** `sudo systemctl restart boty` is what makes
  any of this milestone real: it migrates `state.json` to its dated shape, starts publishing
  `read_at` / `checked` / `current_interval_seconds`, makes the dashboard show 13 rows instead of a
  varying 3–10, and makes the age visible at all. Deferred by Dan's decision recorded **twice** in
  `QUESTIONS.md` § 0f — **2026-08-10 and 2026-08-17**, the second verbatim `keep defer`.
  **Verified safe by execution** — see fact 2. Neither performed nor recommended here.
- **`QUESTIONS.md` § 0f — the Walmart store number.** Open as an *action*, closed as a *blocker*:
  the file's own header now says § 0f *"is no longer blocking anything — the pin and the restart
  remain available as actions, and Phase 5 and Phase 7 both closed without them, on the record."*
  Asked twice and answered `defer` twice (2026-08-10, 2026-08-17); deferred **three** times by any
  means, counting v0.2's close on 2026-08-11, which carried no new answer from him. § 0f records
  both readings and edits neither, having measured that two of its own SUMMARYs called the second
  answer *"the third"*. Measured as a count and never as a value:
  `grep -c 'WALMART_STORE_ID' "$HOME/.config/boty/env"` → **0**, on 2026-08-14 and again on
  2026-08-17. **A consequence worth carrying:** after the restart, that Walmart GO Plus + row
  publishes `out_of_stock · age ? · not checked` **indefinitely**, until a store is pinned —
  because Phase 5's store-gap guard returns UNKNOWN and `State.transitioned_to_stock` returns
  before it writes. That is the honest output, not a defect.
- **`QUESTIONS.md` § 0e — the fix is EXECUTED; what stays open is narrower and different from what
  v0.2's archive recorded, and it is re-read here rather than copied.** v0.2's close listed § 0e
  as *"Open, untouched by v0.2. The working tree is clean; the history is not."* Read at this
  archival, § 0e is headed **ANSWERED 2026-08-03, EXECUTED 2026-08-04** and its body is marked
  *"RESOLVED — nothing here needs Dan"*: `git filter-repo` over all 170 commits in two passes,
  **verified against a fresh clone from GitHub** rather than the local copy, force-pushed, every
  SHA changed, with prevention shipped the same day — `scripts/identity_check.py` now scans every
  tracked file and runs at commit time via a tracked `hooks/pre-commit`. **What is still open is
  three residues, all named inside § 0e or in the file's own opening block, and none of them is an
  unexecuted fix:** (i) the only files still naming the leaked values are § 0e itself and the three
  records that document the leak — *"its own question, flagged there rather than settled by
  scrubbing"*, which is what the file's opening means when it still calls the pushed public
  history unclean; (ii) the pre-filter bundle at
  `~/CodeProjects/bot-y-prefilter-20260803-1745.bundle` is *"the only remaining copy of the old
  values"*, deliberately local and outside the repo, and is Dan's to delete; (iii) GitHub keeps
  unreferenced objects reachable by old SHA until they GC, which is a support request nobody has
  filed. Untouched by v0.3.
- **The three Info findings from the code review — recorded and NOT fixed.** IN-01 (`state.json`
  accumulates entries for watches that no longer exist), IN-02 (the strict boundary renders
  `[age 5m > 5m]`, deliberate and asserted by the suite), IN-03 (the dashboard trusts the shape of
  `status.json` outside its `try`). Re-measured at this archival: all three still stand. IN-03 is
  the one that deserves a plan.
- **The live `make verify` failure classes still predate this milestone and are still unowned.**
  Two controls cannot run on this host at all (no Chrome/Chromium binary — Best Buy and Target),
  Walmart reads UNKNOWN for want of a pin, and the Amazon challenge class is intermittent
  (present-absent-absent-present across four passes). Carried from v0.2 unchanged; still needs its
  own plan.
- **v1.0.0 remains open, untagged and NOT archived.** Its definition of done includes *"Dan has
  successfully bought a Pokémon GO Plus +"* — a market condition, not a work item — and its own
  audit recommended against tagging it shipped. Its phases (1–4, 3.1) and their details stay in
  `.planning/ROADMAP.md`, untouched by this archival, exactly as v0.2's close left them.

### Technical Debt Incurred

The audit's `tech_debt` block, carried forward verbatim in substance:

- **W1 — `Pacer.due`'s tolerance is `default_interval * 0.5` regardless of any per-retailer
  override**, so GameStop is actually asked ~750 s while **900** is published and judged against.
  Under-reports staleness by ≤150 s. Recorded as a **deliberate residual** in `config.py`'s
  docstring; it is the one place the one-cadence invariant is not literally true.
- **W3 — `boty check` writes the live served `status.json` the running daemon owns**, and makes
  live retailer requests. Self-healing, measured in both directions — but operationally noisy
  while the deploy stays deferred.
- **`pacer-state.json` is written non-atomically with two readers.** The failure direction was
  confirmed **empirically** as *over*-reporting staleness (truncation drops the window
  21600 → 300, judging more harshly). WR-01's clamp is what makes that hold **universally** rather
  than usually.
- **Criterion 3's accepted partial** — `status.json` publishes the ingredients of a staleness
  verdict and no `stale` key. Recorded here so a later reader does not re-open it as a gap: it is
  an accepted decision with a date and a name on it.
- **Criterion 5's qualification is not discharged** and is carried in the verdict cell of all
  three documents. A future phase that wants it discharged has to write a test before the code it
  binds; nothing after the fact can do it.
- **`M21`–`M24` remain Phase 6's deliberate unallocated gap and are NOT debt.** Recorded here
  only so a future reader does not re-open them as four lost mutations. **`M39` is free.**

And one this milestone contributed to a list v0.2 opened:

- **`gsd-tools`'s state/phase WRITE subcommands are unusable on this repo — fourteen recorded
  misfires across three subcommands** (`state.advance-plan`, `state.begin-phase`,
  `phase.complete`), documented in five dated notes in `STATE.md`'s own frontmatter. The
  fourteenth, 2026-08-19, was the first to **invent content** rather than only delete or regress:
  it deleted all 57 comment lines and the `percent` block, regressed `stopped_at` to a stale
  `Completed 06-05-PLAN.md` for the fourth time, replaced a five-sentence `status:` with the
  string `Milestone complete`, rewrote an unrelated phase line to the incoherent `**Phase:** 07 of
  5 (Open Source Ready)` with `**Plan:** Not started`, and reported `roadmap_updated: true` and
  `requirements_updated: true` while changing neither file. **On this repo, `STATE.md` is edited
  by hand and a `cp` is taken first.** Read-only queries and `gsd-tools query commit` have been
  reliable throughout.

---

## No tag was created, and that decision remains separate

Recorded here in the same words v0.2's close used, because nothing about it has changed:
**NO GIT TAG WAS CREATED, and none exists.** `git tag -l` → **0**, re-measured at this archival.
That decision is being handled separately and this archival did not touch it. Nothing was pushed.

**And it is a recorded decision rather than an omission**, which v0.2's archive could not yet say:
at the v0.2 deploy on 2026-08-12 (`4609d95`) **Dan chose no tag, explicitly, matching v1.0.0** —
*"no tag exists: Dan chose no tag on 2026-08-12, matching v1.0.0. `git tag -l` → 0, remote → 0
refs. Nothing is published."* Creating the first tag in this repository's history is his call and
was not taken here.

**"Archived" is not "shipped", and that distinction is again the whole of what remains.** v0.2's
close said this about a daemon running eight-day-old code; v0.3's says it about a daemon whose
code is *older than the milestone's first commit*. Every one of the five criteria this milestone
met, it met **in the tree**. On the wire, the count is **zero**.

---

_v1.0.0 is open, untagged and NOT archived. Its phases and details remain in
`.planning/ROADMAP.md`. For current project status see `.planning/ROADMAP.md` and
`.planning/PROJECT.md`._
